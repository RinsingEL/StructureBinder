# AGENTS

## 仓库定位

当前双仓库：

- 实现仓库：`E:\Mod_Dev\StructureBinder`
  - Java、MCP、运行时实现、测试代码、调试产物和 `dev_docs/`。
- 文档仓库：`E:\Mod_Dev\designer_territoryMod`
  - 当前方案、契约、代码导览、测试入口、验收口径、影响面和工作流规则。

涉及当前方案或工作流时以文档仓库为真值。

## 开局路由

非纯粹小补丁或简单查询时，只先读取：

- `E:\Mod_Dev\designer_territoryMod\docs\00_nav\README.md`
- `E:\Mod_Dev\designer_territoryMod\docs\00_nav\task_router.md`

随后必须：

1. 声明本轮唯一活动角色。
2. 从 `docs\00_nav\domain_map.md` 进入目标 system / tool README。
3. 只读取该角色对应的一份 `docs\01_workflows\` 流程。
4. 再按任务读取目标领域的方案、契约、代码导览或测试入口。

无法判断领域时用 `rg` 搜索文档仓库核心名词，再回到命中文件所属 README；不要只靠记忆猜测。

## 角色边界

角色权限以文档仓库 `docs\02_standards\AI角色与权限.md` 为准。

- 验收执行员只能运行既有入口、复用产物并展示结果，不能修改代码、契约、配置或测试。
- 验收失败后停止报告，未经用户明确授权不得自动进入诊断或修复。
- 故障诊断员只定位根因和修改面，不修改实现。
- 开发或修复完成后结束可写角色，再按独立验收流程运行。
- 发布归档阶段不得改变功能行为。

## 领域真值

- 功能方案、阶段职责和边界：目标领域 `10_product/`。
- 字段、schema、输入输出、配置和 MCP 参数：`20_contracts/`。
- 代码入口、类职责和调用链：`30_code_guide/代码导览.md`。
- 测试、验收、影响范围和失败口径：`40_tests/`。

若文档真值和实现冲突，先明确冲突点。改变方案、契约、导览、测试入口、影响面或工作流时必须同步文档仓库；纯实现修补可以只更新实现仓库。

## dev_docs

`dev_docs/` 只记录过程、联调、诊断、故障复盘和提交归档依据，不作为当前功能真值。

默认不为每个任务创建记录。跨多轮交接、双仓库联动、契约或阶段变化、反复排查、用户明确要求或提交确需归档时，按以下位置创建：

- 系统：`dev_docs\systems\<系统>\active\YYYYMMDD_中文任务名\任务记录.md`
- 工具：`dev_docs\tools\<工具>\active\YYYYMMDD_中文任务名\任务记录.md`
- 工作流：`dev_docs\workflows\<主题>\active\YYYYMMDD_中文任务名\任务记录.md`

详细规则见文档仓库 `docs\02_standards\dev_docs组织规则.md`。

## 工作区与提交

- 修改前检查相关仓库 `git status --short`，不要回滚未知改动。
- 只提交当前任务相关文件；双仓库改动分别检查、验证和提交。
- 提交前查看目标仓库近期 `git log --oneline -n 10`。
- 提交标题和正文必须全中文并沿用近期版本号风格。
- 详细规则见文档仓库 `docs\02_standards\Git提交规则.md`。

## 收尾说明

说明本轮属于哪个仓库、读取了哪些真值入口、是否更新方案或契约等文档层、执行了哪些验证及剩余风险。若创建 active 任务记录，验收前必须显式读取。
