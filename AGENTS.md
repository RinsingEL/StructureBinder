# AGENTS

## 核心原则

- 最新一次经用户确认的原话、图片和示意图是需求最高真值。既有文档与其冲突时先指出冲突，再按最新确认结果工作。
- 不为简单需求主动增加通用化、未来扩展、兼容方案、fallback 或大段技术策划案。
- “看看、分析、为什么”默认只读；“改、实现、修复”允许修改并完成聚焦验证；提交、推送和重要删除需要用户明确授权。

## 双仓库

- 实现仓库：`E:\Mod_Dev\StructureBinder`，保存 Java、MCP、运行时、测试、调试产物和 `dev_docs/`。
- 文档仓库：`E:\Mod_Dev\designer_territoryMod`，保存当前需求案、稳定契约、必要代码入口、测试入口和项目规则。

需要判断领域时读取文档仓库 `docs/00_nav/domain_map.md`，再进入目标 system / tool README。只读取当前任务直接需要的文档，不默认加载产品、契约、代码导览和测试四层。

## 三项项目规则

- 编写或修改需求案时读取 `docs/01_workflows/需求案规则.md`。需求案只保留用户原话、示意图和 `*xxxx*：xxxxx` 形式的 AI 总结。
- 通过 MCP 做真实游玩测试时读取 `docs/01_workflows/真实游玩AgentLoop规则.md`。保持工具使用者视角，不读源码诊断；调用错误根据返回持续修正，不能直接判定功能失败。
- 修改实现、配置、测试、MCP 或项目工作流时读取 `docs/01_workflows/开发与Bug留档规则.md`，创建或更新一份简短开发记录；发现 Bug 必须登记故障索引。

这些规则的完整路径均位于 `E:\Mod_Dev\designer_territoryMod`。

## 开发方式

- 优先完成能直接观察结果的最小完整切片，尽早提供 preview 或目标阶段直达入口。
- 已废弃逻辑不自动成为兼容约束。它与最新需求冲突时优先删除、隔离或绕开，不在其上继续叠加兜底。
- 只有真正改变外部字段、配置、存档、MCP 输入输出或阶段结构化数据时，才同步对应契约。
- subagent 不负责解释需求或决定整体设计，只用于代码搜索、边界明确的局部实现、测试或 diff 检查。交接时必须携带用户原话和必要图片，主 Agent 对最终行为负责。

## 留档

- 开发记录位于 `dev_docs/systems|tools|workflows/<领域>/active/YYYYMMDD_中文任务名/任务记录.md`。
- Bug 发现时先登记；未定位可标记“待定位”，修复后补齐根因、修复、验证和提交。
- `dev_docs/` 不作为当前需求或契约真值，普通任务不默认读取历史记录。

## 工作区与提交

- 修改前检查相关仓库 `git status --short`，保护未知改动，不回滚用户现有工作。
- 只修改和提交当前任务相关文件；双仓库分别检查、验证和提交。
- 提交前查看目标仓库近期 `git log --oneline -n 10`，标题和正文使用中文并沿用近期版本风格。
- 收尾只说明实际修改、验证结果和剩余问题。
