# AGENTS

## 本文件定位

本文件只做任务路由和触发检索，不承载完整方案、契约、测试细则或开发流程。

当前双仓库：

- 实现仓库：`E:\Mod_Dev\StructureBinder`
  - Java / MCP / 运行时实现、工具代码、接口实现、调试记录、`dev_docs/`
- 文档仓库 / 策划案仓库：`E:\Mod_Dev\designer_territoryMod`
  - 当前有效的产品方案、数据契约、代码导览、测试入口、验收口径、影响面、工作流规则

开始任务时先判断本轮属于实现仓库、文档仓库，还是双仓库联动。涉及当前方案、契约、导览、测试、影响面或工作流规则时，真值以文档仓库为准。

## 开局检索

非纯粹小补丁或简单命令查询时，先读文档仓库导航：

- `E:\Mod_Dev\designer_territoryMod\docs\README.md`
- `E:\Mod_Dev\designer_territoryMod\docs\00_nav\repo_map.md`
- `E:\Mod_Dev\designer_territoryMod\docs\00_nav\function_map.md`
- `E:\Mod_Dev\designer_territoryMod\docs\00_nav\AI语义检索规则.md`

定位到具体系统或工具后，再读对应入口：

- 系统：`docs\systems\<系统>\README.md`
- 工具：`docs\tools\<工具>\README.md`

如果无法凭 `function_map.md` 判断归属，用 `rg` 在文档仓库搜索用户提到的核心名词，再从命中的系统 / 工具 README 回到对应真值文档。不要只靠记忆猜测。

## 语义触发

按任务语义选择文档类型，不在本文件维护大关键词表：

- 新系统 / 新工具 / 从 0 到 1 / 大重构 / 开发计划：
  - 先读 `docs\00_nav\从0到1设计开发流程.md`
  - 再进入目标系统或工具的 `10_product/`、`20_contracts/`、`40_tests/`
- bug / 异常 / 测试失败 / 行为不符 / 多轮不收敛：
  - 先读 `docs\00_nav\Bug修复流程.md`
  - 再读目标系统或工具的 `40_tests/测试入口.md`
  - 同时查实现仓库 `dev_docs\<systems|tools>\<名称>\故障索引.md`
- 数据结构 / 字段 / schema / 契约 / 输入输出 / 配置 / MCP 参数 / 返回值：
  - 读目标系统或工具的 `20_contracts/`
- 功能方案 / 阶段职责 / 主流程 / 边界 / 执行层：
  - 读目标系统或工具的 `10_product/`
- 代码入口 / 类职责 / 模块映射 / 从实现找文档：
  - 读目标系统或工具的 `30_code_guide/代码导览.md`
- 测试 / 自测 / 验收 / 回归 / 影响范围：
  - 读目标系统或工具的 `40_tests/`

若文档仓库真值和实现代码冲突，先明确冲突点。若本轮会改变方案、契约、导览、测试入口、影响面或工作流规则，必须同步更新文档仓库；纯实现修补可只更新实现仓库。

## dev_docs

实现仓库 `dev_docs/` 只记录过程、联调、诊断、故障复盘和提交归档依据，不作为当前功能真值。

新过程记录按领域存放：

- 系统：`dev_docs\systems\<系统>\active\YYYYMMDD_中文任务名\任务记录.md`
- 工具：`dev_docs\tools\<工具>\active\YYYYMMDD_中文任务名\任务记录.md`
- 跨系统或规则工作：`dev_docs\workflows\<主题>\active\YYYYMMDD_中文任务名\任务记录.md`

故障经验优先沉淀到：

- `dev_docs\systems\<系统>\故障索引.md`
- `dev_docs\tools\<工具>\故障索引.md`

默认不要为每个任务创建过程记录。只有跨多轮、需要交接、双仓库联动、接口或契约变化、反复排查失败、用户要求记录，或提交确实需要归档说明时才创建。不要新建 `dev_docs/current_*`、`最终版.md`、`最终版2.md`、`修改记录2.md` 等低语义文件。

## 工作区与提交

- 开始改动前检查相关仓库 `git status --short`，不要回滚未知改动。
- 只提交当前任务相关文件；双仓库改动必须分别检查、分别说明边界。
- 提交前必须先看目标仓库近期 `git log --oneline -n 10`，沿用既有标题风格和版本号口径。
- 提交标题和提交正文必须全中文，不使用纯英文标题、纯英文正文或 `fix`、`update`、`change` 这类模糊标题。
- 实现仓库标题优先沿用带版本号风格，例如 `国度系统-v0.1.2版本-核心主题`。
- 文档仓库标题优先沿用带版本号风格，例如 `文档仓库-v0.1.0版本-核心主题` 或近期日志中的同类格式。
- 提交正文必须详细说明：改了哪些模块 / 文档、新增哪些能力或规则、影响哪些阶段 / 接口 / 使用方式、执行了哪些验证、是否涉及 `dev_docs` 归档。
- 双仓库都需要提交时，分别提交、分别写清各自承载内容，不把实现改动和文档真值混成一个含糊提交。

## 验收说明

收尾时说明：

- 本轮属于实现仓库、文档仓库，还是双仓库联动。
- 触发读取了哪些文档仓库真值入口；若未读取，说明这是纯小补丁或简单查询。
- 是否更新了方案、契约、导览、测试入口、影响面或工作流规则。
- 执行了哪些验证。
- 若创建了 `dev_docs/.../active/.../任务记录.md`，验收前必须显式读取并据此说明完成情况和风险。
