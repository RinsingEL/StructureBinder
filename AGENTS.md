# AGENTS

## 双仓库协作规则

当前工作区已经拆分为两个长期并行维护的仓库：

- 实现仓库：`E:\Mod_Dev\StructureBinder`
  - 主要承载 Java / MCP / 运行时实现、工具代码、接口与调试过程记录
- 文档仓库 / 策划案仓库：`E:\Mod_Dev\designer_territoryMod`
  - 主要承载当前有效的产品方案、数据契约、代码导览、测试与验收入口

agent 在开始处理任务前，`MUST` 先判断本次改动属于哪个仓库：

1. 改代码、接口、运行时逻辑、MCP 工具、调试记录时，优先在实现仓库工作。
2. 改功能方案、测试入口、契约、跨仓映射、代码导览时，优先在文档仓库 / 策划案仓库工作。
3. 涉及双仓库联动时，`MUST` 明确哪些内容属于实现仓库，哪些内容属于文档仓库，避免把文档真值再写回实现仓库。

## 文档真值来源规则

从当前阶段开始：

- 实现仓库中的 `dev_docs/` `MUST` 保留，继续作为开发过程记录、联调细节、阶段讨论和提交归档依据。
- 实现仓库中的旧 `workFlow`、旧 whitepaper、旧总览型方案文档，不再作为当前功能真值来源。
- 当前有效的功能方案、数据契约、代码导览、测试入口，`MUST` 以文档仓库为准：
  - `E:\Mod_Dev\designer_territoryMod\docs\10_product`
  - `E:\Mod_Dev\designer_territoryMod\docs\20_contracts`
  - `E:\Mod_Dev\designer_territoryMod\docs\30_code_guide`
  - `E:\Mod_Dev\designer_territoryMod\docs\40_tests`

agent `MUST NOT` 再把新的主方案持续写回实现仓库旧文档体系中。

## 双仓库更新规则

当任务涉及以下内容时，agent `MUST` 同步判断是否需要更新文档仓库：

- 阶段职责变化
- MCP 输入输出变化
- C7 / C8 / C9 数据契约变化
- 次级建筑、道路接口、执行层边界变化
- 测试方式、验收标准或代码导览入口变化

当任务只涉及以下内容时，可以只更新实现仓库：

- 纯代码实现
- 调试日志
- 临时联调过程记录
- 提交过程中的 `dev_docs/` 归档

## Git 提交规则

本项目中的 agent 在执行 git 提交时，必须遵守以下规则：

1. `MUST` 使用全中文提交信息。
2. `MUST` 优先使用项目既有的带版本号提交标题风格，例如：
   - `国度系统-v0.1.2版本-...`
3. `MUST` 在提交标题中概括本次改动的核心主题，不得使用模糊标题，例如“更新”“修改”“fix”。
4. `MUST` 在提交描述正文里用中文详细说明本次改动，至少写清：
   - 做了哪些模块或流程改造
   - 新增了哪些能力或文档
   - 影响了哪些阶段、接口或使用方式
   - 做了哪些验证
5. `NEVER` 使用纯英文提交标题或正文。
6. `NEVER` 只写一行标题而没有详细中文描述，除非用户明确要求单行提交。
7. `NEVER` 提交与当前任务无关、未经确认的额外改动。

## 提交前检查

提交前，agent `MUST`：

- 检查工作区变更范围，避免误提无关文件。
- 确认本次提交内容和当前任务目标一致。
- 如有新增工作流、skill、协议或阶段文档，确保提交描述中明确点出。

## AI 工作流文档规则

本项目继续使用 `dev_docs/` 作为 AI 工作流文档根目录。

但需要额外遵守：

- `dev_docs/` 的定位是过程记录、讨论沉淀、测试与归档依据，不是当前功能真值目录。
- 若本轮任务同时影响当前方案或契约，`MUST` 将最终有效结论同步到文档仓库，而不是只留在 `dev_docs/`。
- `dev_docs/` 中允许保留更细粒度的思路、试验、收口过程，这部分不要求与文档仓库逐字一致，但最终结论必须能在文档仓库找到正式版本。

### 过程记录触发规则

默认 `MUST NOT` 为每个任务创建 `dev_docs/current_*` 或时间戳流水账目录。

只有满足以下任一条件时，agent 才应创建过程记录：

- 任务预计跨多轮对话、跨 compact 或需要多 agent 接力。
- 涉及双仓库联动，并且需要保留实现决策与文档真值同步依据。
- 涉及 MCP 输入输出、C7 / C8 / C9 数据契约、执行层边界或阶段职责变化。
- 排查过程出现失败验证、反复试验或需要保留关键诊断证据。
- 用户明确要求记录过程。
- 准备提交且本次改动需要额外归档说明。

过程记录目录命名 `MUST` 使用可读语义：

- `dev_docs/active/YYYYMMDD_中文任务名/`

目录内默认只创建一份：

- `任务记录.md`

`任务记录.md` 只记录关键内容：

- 目标与边界
- 当前计划
- 关键决策
- 重要修改
- 验证结果
- 未解决风险

小修改、纯代码补丁、简单查阅、单次命令验证，默认不创建过程记录。

`NEVER` 新建以下低语义过程目录或文件：

- `dev_docs/current_*`
- `修改记录2.md`
- `最终版.md`
- `最终版2.md`
- 其它只靠时间戳区分、无法从名称判断任务内容的目录

### 验收前

进入验收前，agent `MUST` 根据本轮任务类型检查：

- 若本轮创建了 `dev_docs/active/.../任务记录.md`，必须显式读取它，并据此说明完成情况和剩余风险。
- 若本轮影响方案、契约、导览、测试入口或影响面，必须显式检查文档仓库对应真值目录。
- 若本轮只是小修改且没有创建过程记录，验收说明中直接说明未创建过程记录的原因。

### 提交后归档

提交成功后，若本轮存在 `dev_docs/active/YYYYMMDD_中文任务名/`，agent `MUST` 将该目录重命名为：

- `dev_docs/archive/<git短提交号>_vX.Y.Z_中文标题`

归档时必须遵守：

1. 目录命名固定为 `git短提交号_版本号_中文标题`
2. 标题来源于本次提交标题中的核心中文主题
3. 保持中文标题，不转拼音
4. 默认一个提交对应一个独立归档目录
5. `NEVER` 在同一个目录中混放多个提交版本
6. 新任务如需过程记录，重新创建新的 `dev_docs/active/YYYYMMDD_中文任务名/`

若本轮未创建过程记录，则提交后无需补建归档目录。

### 定期清理规则

agent 在遇到 `dev_docs/current_*`、未归档 `dev_docs/active/*`、过大日志或重复过程文档时，`SHOULD` 主动建议清理。

清理时必须遵守：

1. 已归档、已提交、且不再作为当前任务依据的 `dev_docs/current_*` 可以删除。
2. 未提交或包含唯一诊断证据的目录，删除前必须先说明影响。
3. 大型 `.log`、临时响应、运行输出优先归档到明确任务目录，或在确认无追溯价值后删除。
4. 清理操作不得影响文档仓库当前真值。

## 提交边界补充规则

agent 在执行提交前，`MUST` 明确本次提交属于哪一个仓库：

- 若只提交实现仓库，提交内容中不得混入文档仓库的未确认调整。
- 若只提交文档仓库，提交内容中不得误带实现仓库代码改动。
- 若双仓库都需要提交，`MUST` 分别检查各自工作区，并在说明中明确两边提交分别承载什么内容。

agent `MUST` 优先保证：

1. 实现仓库提交说明清楚实现改造、接口影响、验证与 `dev_docs` 归档。
2. 文档仓库提交说明清楚方案更新、契约变化、导览更新、测试入口调整与历史归档关系。
