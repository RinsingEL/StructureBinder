# Geomantia 项目结构设计任务记录

## 目标与边界

- 将当前最小 Forge mod 名称从 `Structure Binder` 改为 `Geomantia`。
- 建立下一代地貌驱动主线的最小 Java 包结构。
- 只补项目结构骨架，不迁移 legacy 复杂实现，不恢复旧 C 链职责。
- 同步文档仓库中的代码导览、测试入口和影响面，确保当前真值指向 Geomantia 空壳与新包边界。

## 当前计划

1. 保留 Forge 1.20.1 / Java 17 最小可构建框架。
2. 将 `mod_id`、展示名、包名、主类名统一为 Geomantia。
3. 设计新包结构，先用 `package-info.java` 固定边界：
   - `core`
   - `platform`
   - `world/atlas`
   - `world/realm`
   - `city/terrain`
   - `city/zoning`
   - `city/roads`
   - `city/anchors`
   - `city/constraints`
   - `structure/pools`
   - `structure/materialize`
   - `runtime`
   - `debug`
4. 运行 `.\gradlew.bat build` 验证最小框架。

## 关键决策

- 新 mod 名称：`Geomantia`。
- 新 `mod_id`：`geomantia`。
- 新基础包：`com.rinsing.geomantia`。
- 新主线不沿用 `com/user/terra_script` 包路径，也不在 legacy 旧类上继续叠职责。
- 本轮只建立结构边界，不引入 GIS、MCP、阶段编排或结构池具体实现。

## 重要修改

- 实现仓库：
  - 将 `mod_id` 改为 `geomantia`，展示名改为 `Geomantia`，group 改为 `com.rinsing.geomantia`。
  - 将主类改为 `src/main/java/com/rinsing/geomantia/GeomantiaMod.java`。
  - 删除临时 `com.rinsing.structurebinder.StructureBinderMod`。
  - 新增下一代主线包结构的 `package-info.java` 边界说明。
- 文档仓库：
  - 更新 `docs/30_code_guide/city/c_refactor/代码导览.md`，记录 Geomantia 当前主类、mod 元数据和包结构。
  - 更新 `docs/40_tests/city/c_refactor/测试入口.md`，将当前验收口径改为 Geomantia 最小框架构建与包边界检查。
  - 更新 `docs/40_tests/city/c_refactor/影响面.md`，记录最小框架与包结构重置影响面。

## 验证结果

- 已执行 `.\gradlew.bat build`。
- 构建结果：成功。
- 当前产物：`build/libs/geomantia-0.1.0.jar`。
- 已检查 `Structure Binder`、`structure_binder`、`StructureBinderMod`、`com.rinsing.structurebinder` 不再出现在新主源码、资源配置和本轮同步的文档入口中。

## 未解决风险

- 文档仓库和实现仓库都已有大量未提交历史清理改动，本轮只处理 Geomantia 主线相关文件。
- legacy 代码仍需后续单独决定保留、归档或删除策略。
- 当前包结构只有边界说明，尚未实现 GIS、地貌区域、城市选址、功能区或结构池逻辑。
