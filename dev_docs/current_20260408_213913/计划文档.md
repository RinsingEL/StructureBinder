# 本次任务标题
快速启动 client 并默认进入新的世界 (2)

# 最终计划摘要
确认开发环境实际读取的游戏目录与存档位置，采用 Minecraft quick play 启动参数为 dev client 增加默认单机存档直达能力，并记录本轮调整与验证结果。

# 实施步骤
1. 检查 `runClient` 的工作目录、现有存档目录与可用世界名。
2. 验证当前 Minecraft/Forge 开发启动是否支持 `--quickPlaySingleplayer`。
3. 在启动配置中加入默认目标世界 `新的世界 (2)`。
4. 记录使用方式、影响范围与验证结论。

# 验收标准
- `runClient` 启动后默认尝试进入 `run/saves/新的世界 (2)`。
- 目标世界名在仓库内有明确配置入口，后续可快速修改。
- 本轮修改与验证过程已写入 `修改记录.md`。

# 关键假设
- 用户当前主要通过 IntelliJ 的 `runClient` 或 Gradle `runClient` 启动开发客户端。
- 目标存档长期位于 `E:\Mod_Dev\StructureBinder\run\saves\新的世界 (2)`。
