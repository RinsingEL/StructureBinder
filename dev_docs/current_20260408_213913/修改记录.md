# 修改记录

## 2026-04-08 21:39:13
- 开始处理“快速启动 client 并进入新的世界 (2)”需求。
- 原因：
  - 需要先确认开发客户端实际使用的游戏目录与存档位置，避免把根目录 `saves/` 误认为启动目录。
- 涉及模块或文档：
  - `build.gradle`
  - `gradle.properties`
  - `.idea/runConfigurations/runClient.xml`
  - `dev_docs/current_20260408_213913/*`
- 已做验证：
  - 确认 `runClient` 工作目录为 `E:\Mod_Dev\StructureBinder\run`
  - 确认目标存档存在于 `E:\Mod_Dev\StructureBinder\run\saves\新的世界 (2)`
  - 确认本地缓存与映射信息显示客户端支持 `--quickPlaySingleplayer`
- 当前还剩问题：
  - 待完成启动配置落地并进行参数层面的最终复核。

## 2026-04-08 21:45:00
- 已将 dev client 的默认 quick play 世界抽到 `gradle.properties`，并接入 `build.gradle` 的 `client` 运行配置。
- 同步更新 IntelliJ 本地 `runClient.xml`，让当前工作区直接点击运行也会带上目标世界参数。
- 原因：
  - 需要让 Gradle 与 IDE 两条常用启动路径都默认直达 `run/saves/新的世界 (2)`。
  - 需要把世界名收敛到单一配置点，后续切换目标存档时不用到处改。
- 涉及模块或文档：
  - `gradle.properties`
  - `build.gradle`
  - `.idea/runConfigurations/runClient.xml`
- 已做验证：
  - 复核 `build.gradle` 已在 `client` 配置内追加 `--quickPlaySingleplayer`
  - 复核 `gradle.properties` 已新增 `dev_quick_play_singleplayer_world=新的世界 (2)`
  - 复核 IntelliJ `PROGRAM_PARAMETERS` 已带上带引号的目标世界名
- 当前还剩问题：
  - 尚未实际完整拉起图形客户端做真机启动验证，本轮完成的是参数配置与目标存档定位校验。
