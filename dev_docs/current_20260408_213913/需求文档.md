# 用户本次需求摘要
希望把开发环境里的 client 启动流程配置得更快，做到开游戏后直接进入 `新的世界 (2)`。

# 用户特别强调的约束
- 目标是快速启动 client。
- 希望开游戏就直接进指定世界。

# 不做的事情
- 不假装已有“重启 Minecraft 并自动回到世界”的完整桌面自动化。
- 不修改文档真值仓库。
- 不引入与本次启动体验无关的运行时逻辑改造。

# 当前决定的工作边界
- 仅在实现仓库处理开发启动配置。
- 优先配置 `runClient` 的 quick play 单机直达能力。
- 仅补充本轮 `dev_docs/` 过程文档，不扩写旧方案文档。
