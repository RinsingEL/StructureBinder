# 装饰与 LandUse 故障索引

| ID | 状态 | 现象与根因摘要 | 记录 |
| --- | --- | --- | --- |
| `CITY-DL-20260807-01` | 规划与激活复验通过，待用户确认 TP | 原现场中 11 个 anchor 让三类 Landscape 展开为 95 个 source，竞争后小 Area 触发 `RELAY_GROWTH_MASK_TOO_SMALL_FOR_STAGES`。Catalog v0.7 现锁定 Landscape/Group 总预算；core 先竞争、fill 对冻结 claims 逐个准入。续验另修复 Parcel 中心距遗漏双方预计半径、不同 Landscape 未共享占用表、UNIFORM 无法为区域接力提供材料槽、D7 waiting 状态未提升四个问题。真实 run 最终冻结 1 Foundation + 16 景观 Area、66 个非空接力 region，D5 激活后 11 个结构正确处于 `waiting_for_worldgen`；目标 region 仅有 0 字节查询占位，未 TP。 | 真实 run `landscape_relay_playtest_20260807_v3`；`CityOutdoorBlueprintCompilerTest`；`CityStructureTemplateMaterializationPlannerTest`；[区域接力任务记录](../active/20260807_区域接力景观填充/任务记录.md) |
| `CITY-DL-20260806-02` | 已修复，待新区块实机验收 | 正式户外编译曾把建筑入口、组内最小连接树、跨组关系线和事后 residual 吸收共同当作地板几何，LandUse 实际承担了道路职责，造成建筑周围圆石环、双层栅栏夹圆石和大面积原群系孔洞。现破坏性升级为 CityBlueprint v0.7 / reference catalog v0.4：D6 后用可配置 FoundationProfile 生成唯一连续且有最小颈宽的城市基底，required/fill 建筑按 ParcelStyle 生成独立景观地块；正式路径固定关闭 corridor、auto-connect 和 residual resolver，RoadWeaver 保持唯一道路权威。 | [本轮任务记录](../active/20260806_统一城市基底与景观地块配置/任务记录.md)；`CityFoundationPlannerTest`、`CityOutdoorBlueprintCompilerTest`、`LandUsePlanningServiceTest` |
| `CITY-DL-20260806-01` | 已修复，RTF 真实规划复验通过 | 附着在同一建筑组上的 `structure_ground` 与 landscape 会复用外围种子；稳定扩张器原先让排序靠后的来源在共享种子竞争失败后直接退出，导致必需农田虽然有合法容量却认领 0 blocks。现只允许竞争失败的零成本种子继续发出自身 frontier，后续节点仍不得穿过已有 claim；反序输入保持同一结果。真实 RTF 城市的 `capital_farmland` 从 0 增长到上限 2,304 blocks，公共绿地 308，unknown residual 0。 | `StableLandUseExpanderTest.overlappingSeedsLaunchEveryFrontierWithoutMakingResultOrderDependent`；真实 run `rtf0805_newsave_w32768_20260805_1840` LandUse 产物 |
| `CITY-DL-20260724-05` | 已修复，LandUse 实机规划通过 | 文档契约明确支持 `military`，但领域默认目录、bundled `default_v0_1` 与本次运行 profile 均漏配该规则，导致瞭望锻坊的正式 v0.3 intent 返回 `LAND_USE_RULE_REF_UNKNOWN: military`。现三处使用同一军事规则定义，并增加 `function.guard_tower` 语义回归；运行配置热加载后原请求通过，4 个 seed group 编译为 6 个 area、认领 13,792 blocks、warnings 0。 | `LandUseRuleCatalogTest.commonTerraSenseTermsMapToDefaultProfiles`；真实城市 `city_skybell_firwatch_20260724_01` LandUse 产物 |
| `CITY-DL-20260723-03` | 已修复，真实跨区块 worldgen 验证通过 | 根因是通用写入在落块前对 wheat 调用 `Block.updateFromNeighbourShapes`，把请求状态解析为 air 后仍按解析结果校验成功。现对非连接方块直接写请求状态，并要求写后 `BlockState` 精确一致；不一致则 owner 失败且不进 ledger。本轮真实证据使用 x=1008..1027：两个 owner ledger 合计 BASE/CROP 各 20，`chunk_save` 观测确认 farmland 20、wheat 20。后续 GameTest 改用 UUID nonce 派生的新远区 seam 和唯一 cityId，避免复用该保存态。旧区块仍不回填。 | [本轮任务记录](../active/20260723_LandUse作物与栅栏WorldGenRegion修复/任务记录.md)；`run/world/geomantia_city_masks/` |
| `CITY-DL-20260723-04` | 已修复，真实跨区块栅栏验证通过 | 根因是逐块落地时立即求连接状态，`WorldGenRegion` 又不执行常规邻居通知，导致先写侧长期保留单边状态。现先 raw 写完整 owner boundary，再对本批边界及四邻连接方块统一计算、预检、写入和稳定性校验；后生成 owner 会把前一 owner 的接缝邻居纳入 finalize，失败则恢复连接快照并回滚当前 owner。finalize 使用独立 observation rollback token，失败不会留下派生连接假证据。本轮真实证据的 20 格 oak fence 共 19 对全部双向，x=1023/1024 两格 east/west 均为 true；后续 GameTest 使用 nonce 动态 seam。通用 `CrossCollisionBlock` 路径同样覆盖石墙连接；现场约 193 对不同高度墙属于坡地连续性，不在本单连接修复内。 | [本轮任务记录](../active/20260723_LandUse作物与栅栏WorldGenRegion修复/任务记录.md)；`run/world/geomantia_city_masks/worldgen_block_observations/` |
| `CITY-DL-20260722-01` | 已定位，待修复 | LandUse 当前仍把 owner 内 BASE/CROP/BOUNDARY 作为同一事务，任一局部 block 操作不可落地都可能回滚其他正常操作。需按 block 或依赖小组继续缩小事务，并区分局部内容冲突与系统错误。 | `CityLandUseChunkCompilerTest`、`CityLandUseChunkExecutorTest`、`CityLandUseWorldgenRegistryTest`；[故障修复案](../../../../../designer_territoryMod/docs/systems/city/40_tests/故障修复案/20260722_LandUseOwner事务失败粒度过大.md) |
| `CITY-DL-20260723-01` | 已修复，待本次新区块重规划复验 | `StableLandUseExpander` 以 `groupId` 汇总并封顶全部 claims；阵列内多个建筑及其不连通碎片共同消耗一份 `maxAreaBlocks`，使功能组到达总上限后仍无法完成近邻连接。现由每个 D6 anchor 按自身 locked footprint 创建 growth region，独立执行 `min/preferred/max`，group 只汇总额度并保留竞争、融合语义。 | `LandUseAutoConnectionPlannerTest`、`LandUsePlanningServiceTest`；[本次任务记录](../active/20260722_新存档河谷大城镇落地/任务记录.md) |
| `CITY-DL-20260723-02` | 已修复，待本次新区块实机复验 | `CONTOUR_BANDS` 将水槽连接器视为全局唯一占位，多个对角缺口复用同一连接格时会把后续 WATER 错误降级为 CROP，造成预览中水槽碎裂。现允许拓扑必要的连接格复用，并以最终四邻接连通分量决定 fallback。 | `ContourBandSurfaceClassifierTest`、`LandUsePlanningServiceTest`；[本次任务记录](../active/20260722_新存档河谷大城镇落地/任务记录.md) |
| `CITY-DL-20260720-01` | 已修复，待重启后新区块实机复验 | Decoration 连续 prefab 的 lined straight `3×1` 与 lined end-cap `3×2` 原本无法建立 terrain-drop fallback，loader 只接受 `1×1 -> 1×1`。现允许保持一个水平横截面、只沿 run 轴加长的 fallback。该结论不适用于已改为直接 block mask 的 LandUse。 | `CityDecorationContentCatalogLoaderTest`、`CityDecorationTerrainRunCompilerTest`、`CityDecorationChunkCompilerTest`；[本案任务记录](../active/20260720_W结果大城镇设计落地/任务记录.md) |
| `CITY-DL-20260719-02` | 修复未收敛，转 `CITY-DL-20260723-04` | 第一次修复只在写当前格前求邻居状态并使用 `UPDATE_ALL`；第二次改为显式重算当前格和四个水平连接邻居。该方案通过 FakeWorld / ServerLevel 测试，但最新真实 `WorldGenRegion` 保存态仍系统性单向连接，原结论已由 `CITY-DL-20260723-04` 推翻。 | `CityLandUseChunkExecutorTest` 的无通知 / x=15..16 / 失败恢复回归、`CityLandUseGameTests` 的 ServerLevel 冒烟；[本轮任务记录](../active/20260720_LandUse栅栏WorldGenRegion连接修复/任务记录.md) |
| `CITY-DL-20260719-01` | 待定位 | 新港口样城冻结的坡地农田中，43 个 slot 在 worldgen ledger 以 `CITY_DECORATION_FOUNDATION_NOT_MATERIALIZED` 跳过；其余农田 / 水渠仍应用 229 个，不能把规划槽位数直接当成实际装饰数。 | [生活感样城任务记录](../active/20260719_建筑群生活感设计/任务记录.md) |
| `CITY-DL-20260713-01` | 已修复，待新区块实机复验 | D5 曾把 D3 patch 外包框当作装饰投影范围，造成过宽禁植且未保护普通 / 模组结构。 | `CityPlanningEndpointHandlerTest`、`CityDecorationWorldgenRegistryTest`；[历史明细](./历史明细.md) |
| `CITY-DL-20260713-02` | 已修复 | 旧 Decoration 水渠 tile 未检查相邻坡降会外流，作物会受随机刻与漏水影响；后续增加坡降 fallback。该路径不是当前 LandUse 水槽实现。 | `CityDecorationChunkCompilerTest`；[历史明细](./历史明细.md) |
| `CITY-DL-20260713-03` | 已修复 | 嵌套 configured feature 可在同一 owner 重复投影完整 field，导致传送等待超时。 | `CityDecorationWorldgenRegistryTest`；[历史明细](./历史明细.md) |
| `CITY-DL-20260713-04` | 已修复，待实机复验 | 每个 fragment 都重写完整 JSON ledger，Windows 原子替换失败会中断 worldgen。 | `CityDecorationWorldgenRegistryTest`；[历史明细](./历史明细.md) |
| `CITY-DL-20260713-05` | 已修复，待实机复验 | `cross_section_repeat` 错用世界相位，修正后又跨 `WorldGenRegion` 写相邻 chunk。 | 装饰四项核心测试；[历史明细](./历史明细.md) |
| `CITY-DL-20260718-01` | 已修复，待新区块实机复验 | bundled 与运行期 LandUse profile 漏配 TerraSense `function.矿业`，矿业锚点被当作未知语义跳过。补充 `industry` 规则及 canonical term，重跑 LandUse 后应产出工业 area。 | `LandUseRuleCatalogLoaderTest`、落地测试矿业城 workflow |
