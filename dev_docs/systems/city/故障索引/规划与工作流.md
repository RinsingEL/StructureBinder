# 规划与工作流故障索引

| ID | 状态 | 现象与根因摘要 | 记录 |
| --- | --- | --- | --- |
| `CITY-PW-20260825-14` | 已修复，自动回归通过，待续跑真实 D4 | 正式 60×43 市政厅加住宅镜像环的真实需求超过 SMALL 96 格，但 `formationSpanBlocks` 被 extent 最大跨度硬截断；父槽因此只容纳中心建筑，首对 fill 全灭。现按中心模板与外围模板分别推导镜像半径，并允许真实阵列跨度超过 extent 软目标。 | [任务记录](../active/20260824_主从路网与景观层/任务记录.md)；`parentSlotExpandsPastExtentSoftTargetForLargeCenterSymmetricTemplate` |
| `CITY-PW-20260825-13` | 已修复，真实 LandUse 通过 | 林地保留容量足够，但 per-block 地形与 owner footprint 裁剪形成瓶颈拓扑，五个语义阶段无法保持顺序接力、精确份额和剩余连通三者同时成立。SurfacePrint 现保持角色原顺序逐级收缩阶段数、重新归一化实际份额，直到完整覆盖合法景观 mask；小型分叉回归和真实林地规划均通过。 | [任务记录](../active/20260824_主从路网与景观层/任务记录.md)；`reducesRelayStagesWhenTerrainLeavesABranchedMaskThatCannotBeSequentiallyPartitioned` |
| `CITY-PW-20260825-12` | 已修复，真实 D4 通过 | LINEAR 被通用二维 group minimum 和长轴 capacity 规则误判，exact slot 也只能取单一坐标；现 CITY/SMALL 最小 2 栋，容量沿入口垂直街轴计算，候选只沿同一街轴做稳定错位。真实市场完成 2/2。 | [任务记录](../active/20260824_主从路网与景观层/任务记录.md)；`linearFallbackGuidesOnlyStaggerAlongTheStreetAxis` |
| `CITY-PW-20260825-11` | 已修复，真实 D4-D7 通过 | required 景观容量旧有精确面积、首块紧贴 owner、全部实例原子满足三项约束已拆除；搜索允许少选/降面积/脱离 owner 邻接，Patch 只作软偏好，零格输出 warning 而不阻断整城。真实 D4 农田 384、林地 512、warnings 0，D7 全部落地。 | [任务记录](../active/20260824_主从路网与景观层/任务记录.md) |
| `CITY-PW-20260825-10` | 已修复，真实 D4 通过 | CENTER_SYMMETRIC 中轴路固定按 required core 中心画南北轴；真实 mirror pair 可占据该轴。现从南北/东西两组轴中选择不与非 core collision 相交者。 | [任务记录](../active/20260824_主从路网与景观层/任务记录.md) |
| `CITY-PW-20260825-09` | 已修复，自动回归通过 | COURTYARD/GRID/LINEAR 等硬骨架子阵列仍经过通用 `GROUP_CONNECTIVITY_GAP_EXCEEDED`，导致合法精确槽在道路形成前被拒。现硬骨架依赖正式道路连接，不再执行建筑近邻 gap 门禁。 | [任务记录](../active/20260824_主从路网与景观层/任务记录.md)；`CityBlueprintCompilerServiceTest` |
| `CITY-PW-20260825-08` | 已修复，真实 D4 通过 | 建筑阵列均合法后仍返回 `PARENT_GRAPH_DISCONNECTED`；根因是 connector 把窄区内路合法端点本身按更宽 transition 横截面重复检查并误拒。 | [任务记录](../active/20260824_主从路网与景观层/任务记录.md) |
| `CITY-PW-20260825-07` | 已修复，自动回归通过 | LINEAR state band 先后出现首栋 collision 共边、跟随建筑一侧重叠；根因是理论 anchor 不是真实包络中心。现首段从首栋轴向后缘外一格开始，最终横向轴由两排真实 collision 空隙求中点。 | [任务记录](../active/20260824_主从路网与景观层/任务记录.md)；`linearStreetAxisIsPerpendicularToPrimaryEntrance` |
| `CITY-PW-20260825-06` | 已修复，自动回归通过 | LINEAR 首栋提交时把入口 NORTH/EAST/SOUTH/WEST 直接写成街轴，入口沿街而非垂直朝街。现 N/S 入口对应 E/W 街轴，E/W 入口对应 N/S 街轴。 | [任务记录](../active/20260824_主从路网与景观层/任务记录.md)；`linearStreetAxisIsPerpendicularToPrimaryEntrance` |
| `CITY-PW-20260825-05` | 已修复，自动回归通过 | required 首栋在 preferred Patch 无解、从相邻 Patch 成功后，`formationPatches()` 只返回 claimed Patch，原 preferred Patch 上的其余庭院/阵列槽消失。现返回 preferred 与 claimed 的有序并集。 | [任务记录](../active/20260824_主从路网与景观层/任务记录.md)；`firstRequiredStructureFallsBackFromUnbuildablePreferredPatchWithinD3Grid` |
| `CITY-PW-20260825-04` | 已修复，自动回归通过 | 精确槽 cursor 跨不同 required/fill 模板累积；大模板跳过的槽不会给后续小模板复查。现每栋顶层建筑从槽 0 重扫，已占槽仍由 collision 门禁排除。 | [任务记录](../active/20260824_主从路网与景观层/任务记录.md)；固定 WEST COMPACT 回归 |
| `CITY-PW-20260825-03` | 已修复，自动回归通过 | COMPACT 固定只给 N/S 临路面，COURTYARD 也固定世界框架；正式 Trek `NONE+WEST` 入口模板始终报 `INTERNAL_FRONTAGE_UNAVAILABLE`。现保持槽语义，枚举四个 90°局部框架并选择可合法朝路者。 | [任务记录](../active/20260824_主从路网与景观层/任务记录.md)；`compactReorientsLocalLaneFrameForWestLockedEntrance`、`courtyardReorientsLocalRingFrameForWestLockedEntrance` |
| `CITY-PW-20260825-02` | 已修复，自动回归通过 | 同一远端 preferred Patch 的多个父阵列成员只复用唯一中心 origin；首组 reserve 后第二组失败。现按 zone 目标从中心向外稳定枚举全部 member cells。 | [任务记录](../active/20260824_主从路网与景观层/任务记录.md)；`parentGridPlacesTwoPatchConstrainedChildrenInsideTheirSharedRemotePatch` |
| `CITY-PW-20260825-01` | 已修复，启动与全量回归通过 | `CityInternalStreetPlanner` 的 `new LaneTarget(...)` 漏闭合括号，真实 `runClient` 在 compileJava 阶段中止。补齐语法后 JVM 全量通过，客户端成功进入 RTF0810。 | [任务记录](../active/20260824_主从路网与景观层/任务记录.md)；[调用记录](../active/20260824_主从路网与景观层/真实游玩调用记录.md) |
| `CITY-PW-20260824-02` | 代码与自动回归已修复，真实流程越过原故障 | 父 GRID 只围绕 center Group 产生 guides，无法在远端 preferred Patch 为 child Group 预留槽。现 guides 失败后进入该 child 的 preferred Patch 全格搜索，同时保留 composition/slot 身份；真实 D4 已越过原 `test_market_linear` 错误。 | [任务记录](../active/20260824_主从路网与景观层/任务记录.md)；[调用记录](../active/20260824_主从路网与景观层/真实游玩调用记录.md) |
| `CITY-PW-20260824-01` | 代码与自动回归已修复，待最终真实全链复验 | GRID 按 theoretical anchor 中点定路，但真实模板 anchor 可相对 collision 几何中心偏移；现场 theoretical X 为 -18/8，中点 -5，而左栋 collision 已到 -5。现按两侧真实 collision 边缘求自由间隙和路中心，间隙不足明确失败，视觉门禁检查 road/body 零重叠。 | [任务记录](../active/20260824_主从路网与景观层/任务记录.md)；`CityPlanningEndpointHandlerTest` |
| `CITY-PW-20260823-01` | 待方案（设计张力，非实现缺陷） | v0.1.88 硬骨架化后，真实地形六组团蓝图 28 次 D4 编译 27 次硬拒绝（9 required 候选穷举 / 6 district 容量 1/4 / 6 最小规模 1/8 / 5 跨区无连通槽 / 1 视觉门禁拦 COURTYARD）。编译行为符合设计，但报错只给"差多少"不给"怎么改"，调用方盲试 30 版；required 结构遇硬骨架槽位被地形全灭时无降级路径；D4 为区际连通自动生成 connectivity_* 建筑致锚点膨胀至 53；蓝图侧六区六算法一次塞入且漏声明 landscapes。最终删 COURTYARD/ORGANIC 两组后通过，53 anchors、quality 100。 | [任务记录](../active/20260823_硬骨架编译缺口与降级路径排查/任务记录.md)；[GPT 演变记录](../active/20260823_硬骨架编译缺口与降级路径排查/GPT蓝图30版演变记录.md) |
| `CITY-PW-20260822-01` | 已修复，真实六阵列 D4 复验通过 | 根因是规则阵列逐模板改变 spacing、随机旋转并混入 member-cell fallback，COURTYARD slot 0 占中心且最低仅 4，COMPACT/ORGANIC 共用近似螺旋，仅 LINEAR 冻结道路，quality 不检查最终几何。现父子规则阵列固定 pitch/轴/精确槽，COURTYARD 空心五槽，COMPACT 弯巷，ORGANIC 1–3 block gap；21 段正式区内道路进入 D6/Foundation，`arrayVisualQuality` 六组全过。 | `CityBlueprintCompilerServiceTest`、`CityBlueprintGroupLayoutPlannerTest`、`CityInternalStreetPlannerTest`、`CityArrayVisualQualityGateTest`；[任务记录](../active/20260822_阵列视觉骨架与区内道路实现/任务记录.md)；[调用记录](../active/20260822_阵列视觉骨架与区内道路实现/真实游玩调用记录.md) |
| `CITY-PW-20260821-04` | 已修复，真实六组父阵列 D4 复验通过 | 一个父阵列编排六个完整子 Group 时有三项实现缺口：CENTER_SYMMETRIC 的 formation span 未包含中心到最外镜像 pair 的半径；pair 请求未冻结 `exactCandidateOriginsOnly`，候选器会混入普通 cluster 点；普通结构 claimed area 先总和再除 D3 cell，四个独立占位可被压成三格。现分别按镜像 ring 推导跨度、冻结精确 pair guides 并让 CENTER/LINEAR 共用硬骨架规则、逐结构离散容量。真实 `RTF0805` 父 GRID 同时形成六种内部阵列，33 anchors、quality 100、无 warning。 | `CityBlueprintCompilerServiceTest.parentGridCanArrangeAllSixChildArrayAlgorithms`；[任务记录](../archive/de6fbc3_v0.1.87_阵列主从骨架与街带完整实现/任务记录.md)；[调用记录](../archive/de6fbc3_v0.1.87_阵列主从骨架与街带完整实现/全阵列真实游玩调用记录.md) |
| `CITY-PW-20260821-03` | 已修复，真实 D4 复验通过 | LINEAR 精确槽位命中 D3 Patch 空洞时，编译器曾把“该槽留空”误当 required 结构整体失败。现用独立 slot cursor 记录 `skipped_illegal_slot/LINEAR_SLOT_ILLEGAL_LEFT_EMPTY` 并继续后续 rank，有限搜索无解才失败。真实市场跳过 slot 2 后完成 4/4 栋。 | [任务记录](../archive/de6fbc3_v0.1.87_阵列主从骨架与街带完整实现/任务记录.md)；[调用记录](../archive/de6fbc3_v0.1.87_阵列主从骨架与街带完整实现/真实游玩调用记录.md) |
| `CITY-PW-20260821-02` | 已修复，真实 D4-D6/LandUse 复验通过 | D4 现冻结 `city_internal_street_band.v0.1` 的 straight bounds 与 `LOCAL_HARD_SKELETON platformBounds`，AnchorMap/D6 原样携带；Outdoor compiler 把 platform 纳入 Foundation。真实 street 宽 7、长 83，台基 83×43，LandUse quality pass。 | [任务记录](../archive/de6fbc3_v0.1.87_阵列主从骨架与街带完整实现/任务记录.md)；[调用记录](../archive/de6fbc3_v0.1.87_阵列主从骨架与街带完整实现/真实游玩调用记录.md) |
| `CITY-PW-20260821-01` | 已修复，真实 D4 预览复验通过 | LINEAR required/fill 现锁定同一轴、只用精确 guides，通用 legal-region helper 不再追加 cell centers；父槽按主入口方向从轴端单向延伸。真实市场四栋沿 WEST 直轴落位，左排 NORTH、右排 SOUTH，朝街 score 全 1。 | [任务记录](../archive/de6fbc3_v0.1.87_阵列主从骨架与街带完整实现/任务记录.md)；[调用记录](../archive/de6fbc3_v0.1.87_阵列主从骨架与街带完整实现/真实游玩调用记录.md) |
| `CITY-PW-20260820-04` | 已修复，真实 D4 预览复验通过 | 66 格父槽与 32 格 D3 网格不对齐时，legal region 曾要求整格完全位于槽内，把 `capital_core` 四个已预留功能区格二次裁成一个格，首栋后续必选结构只能重叠。现有 district reservation 时，真实预留 cells 直接作为建筑硬合法区，父槽只负责播种与编排。粗网格回归通过；真实同 Blueprint 编译成功，14 anchors、quality 100、无 hard block/warning。 | [任务记录](../archive/de6fbc3_v0.1.87_父阵列槽位间距虚胖/任务记录.md)；[调用记录](../archive/de6fbc3_v0.1.87_父阵列槽位间距虚胖/真实游玩调用记录.md) |
| `CITY-PW-20260820-03` | 已修复，真实 D4 复验通过 | Trek 正式 importer 已授权四向旋转；Blueprint compiler 现调用 `CityTemplateOrientationSolver`，只在模板白名单内选择朝街 transform，无合法朝向则明确拒绝。真实左/右排入口为 NORTH/SOUTH，alignment score=1。 | [任务记录](../archive/de6fbc3_v0.1.87_阵列主从骨架与街带完整实现/任务记录.md)；[调用记录](../archive/de6fbc3_v0.1.87_阵列主从骨架与街带完整实现/真实游玩调用记录.md) |
| `CITY-PW-20260820-02` | 已修复，真实 D4 预览复验通过 | CENTER_SYMMETRIC 父阵列原把 `extentMaxSpan(extentClass)` 上限常量当槽位边长，salt_kingdom 三个 SMALL 组因此各占 96×96、对称半径约 104。现按 required/fill 模板 transformed footprint、最低结构数、子算法、密度和区内街带推导 66/79/72 格槽位；真实对称半径约 80.65 格，三组 5/5/4 栋，quality 100。功能区与建筑可沿真实预留 cells 越出播种槽位，不再被槽框裁回矩形。 | [任务记录](../archive/de6fbc3_v0.1.87_父阵列槽位间距虚胖/任务记录.md)；[调用记录](../archive/de6fbc3_v0.1.87_父阵列槽位间距虚胖/真实游玩调用记录.md) |
| `CITY-PW-20260820-01` | 已修复，真实 D4 预览复验通过 | D4 已保存功能区 `reservationSpans` 的扩张格形状，但预览用 `districtEnvelope` 矩形填充和虚线作为主边界，只把真实 spans 画成弱色小框；L 形等功能区被误读为矩形。现合并 spans 并直接绘制真实外轮廓，局部图取景也包含功能区完整范围；没有 spans 时不再画假功能区。真实复用 `SLOPE-01 / p.302` 重编 D4，行政区 5 格折形边界可见，矩形外包框消失。 | [任务记录](../active/20260820_D4功能区真实轮廓预览/任务记录.md)；[调用记录](../active/20260820_D4功能区真实轮廓预览/真实游玩调用记录.md) |
| `CITY-PW-20260819-03` | 部分修复，模板地形约束待补 | 功能区最低/目标/上限容量现已使用 required/fill 模板 transformed footprint、最低结构数、阵列密度间距、嵌套槽位和 LINEAR 区内街带推导，固定 RoadWeaver 道路预算归零；STANDARD/PERIPHERAL 可按真实需求小于 extent 设计目标。尚未完成：把每个模板的 `terrainModes`、`terrainPosePolicy`、`supportPolicy` 联合成容量扩张地形门禁与代价。 | [任务记录](../active/20260819_功能区预分配与受约束扩张/任务记录.md)；[本轮记录](../archive/de6fbc3_v0.1.87_父阵列槽位间距虚胖/任务记录.md) |
| `CITY-PW-20260819-02` | 已修复，自动回归通过，待真实预览 | `CityDistrictCapacityPlanner` 先构造含水体的候选格，再生成 `landCells`；只有当陆地数量已达到最低格数时才把候选集合替换为 `landCells`。陆地不足时仍用原候选集合执行 BFS，水体可能被计入功能区容量。现已在候选入口直接排除水体、缺失和未采样格；整个域无可用陆地时记录 `NO_USABLE_TERRAIN`，将具体结构失败留给 terrain gate，避免水体进入容量面积。JVM 全量测试通过，真实预览待复验。 | [任务记录](../active/20260819_阵列拓扑分类与功能约束/任务记录.md) |
| `CITY-PW-20260819-01` | 待定位 | 真实启动 `RTF0810` 后，复用既有 D3 上游调用正式 `city_compile_d4_blueprint`，功能区容量预留返回 `admin_core reached 10 district cells but requires at least 11`，D4 未生成预览。根因待定位；当前不能把既有 D3 上游的容量不足、连通筛选或最低容量策略中的任一项当作结论。 | [任务记录](../active/20260819_功能区预分配与受约束扩张/任务记录.md)；[真实游玩调用记录](../active/20260819_功能区预分配与受约束扩张/真实游玩调用记录.md) |
| `CITY-PW-20260817-03` | 已修复，真实 D4 预览复验通过 | 初版 12 格 Group 硬缓冲没有理解显式 ADJACENCY、BETWEEN_GROUPS 和父阵列整体关系，`residential_connector` 被行政翼与商业街排斥到南侧水地，250 秒后只能形成 5/6 栋。现只隔离无直接构图关系的 Group；关系涉及父阵列时向全部成员展开一次，但不沿普通关系链传递。真实同 Blueprint 编译成功，43 anchors、居民簇 10 栋，无关系 Group 最近 collision 间距最小约 27.66 格。 | `CityBlueprintCompilerServiceTest`；[任务记录](../active/20260817_街区包络与组间缓冲/任务记录.md)；[调用记录](../active/20260817_街区包络与组间缓冲/真实游玩调用记录.md) |
| `CITY-PW-20260817-02` | 待定位 | Blueprint 提交两个 `FREE_STANDING`、`instanceCount=1` 的可选农田/林场后，D4 容量预留返回 `status=reserved`，但 `instances=[]`、`failures=[]`，预览为 `landscapes=0`；契约又要求 FREE_STANDING 必须 `required=false`，Agent 无法表达必须保留的独立景观。根因待定位。 | [任务记录](../active/20260816_嵌套阵列与关系位置/任务记录.md)；[调用记录](../active/20260816_嵌套阵列与关系位置/真实游玩调用记录.md) |
| `CITY-PW-20260817-01` | 待定位 | CENTER_SYMMETRIC 父阵列已为 `admin_core` 分配 `p.357` 内槽位，但市政厅槽内候选失败后通过 `d3_terrain_fallback` 落到 `p.692`；最终 Group 同时 claim 两个 Patch，父阵列不再严格安排完整子 Group。根因待定位。 | [任务记录](../active/20260816_嵌套阵列与关系位置/任务记录.md)；[调用记录](../active/20260816_嵌套阵列与关系位置/真实游玩调用记录.md) |
| `CITY-PW-20260815-01` | 代码与自动回归已修复，待真实预览 | D3 虽按城市 `cellStepBlocks` 重新采样和分类，但原先每个 GIS region 单独计算指标并合并 Patch；现对全部刷新 region 统一重算邻域指标并全局四邻接合并，连续平地跨 512 格接缝仍为同一 Patch。 | [任务记录](../../realm_planning/active/20260815_T与D分尺度Patch重算/任务记录.md) |
| `CITY-PW-20260812-06` | 代码与自动回归已修复，待真实新区块验收 | D4 现按 `CONFORM/BALANCED/ASSERTIVE` 对完整 transformed collision footprint 执行坡度、`localRelief` 和高程范围门禁；候选生成逐点跳过非法位置，首选 patch 无解时回退同一 D3 review grid，后续围绕 claimed patch 成形。 | `CityStructureTerrainGateTest`、`CityBlueprintCompilerServiceTest`；[任务记录](../active/20260812_城市台基与地形适应/任务记录.md) |
| `CITY-PW-20260812-05` | 待定位 | D5 已成功激活且新区块完成 FEATURES 后，以 `skipExisting=true` 续跑正式 workflow 仍再次调用 `city_execute_d5`，被 `CITY_LAND_USE_CHUNK_ALREADY_AT_FEATURES` 保护性拒绝；直接调用 `city_execute_d7` 可正常收齐 22/22 worldgen ledger。续跑阶段为何未识别已完成的 D5 激活尚待定位。 | [任务记录](../archive/be74ad2_v0.1.81_中心对称阵列/任务记录.md) |
| `CITY-PW-20260812-04` | 已修复，真实 D4 预览复验通过 | D4 预览渲染器只收到结构 anchor map，未收到景观容量预留计划，因此图中无法看到林场、田地、牧场和教堂花园的实际面积。现按 `reservationSpans[]` 精确叠图并在局部图中使用四类独立颜色与图例。 | [任务记录](../archive/be74ad2_v0.1.81_中心对称阵列/任务记录.md) |
| `CITY-PW-20260812-03` | 已修复，真实 D4 编译复验通过 | 中心对称 Group 原先要等所有 Group 的 required 核心播种后才补 fill，邻组会先抢占中心轴线；同时首锚 legal region 被限制在 preferred cell 的 64 格局部窗口，无法改选同一 preferred patch 内的合法位置。现中心主体后立即原子预留最低对称对，首锚允许搜索完整 preferred patch。 | [任务记录](../archive/be74ad2_v0.1.81_中心对称阵列/任务记录.md) |
| `CITY-PW-20260812-02` | 已修复，真实 D4 编译复验通过 | `LandscapeProfile` 已提供按 `extentClass` 解析 SMALL/MEDIUM/LARGE 面积的方法，但 D4 容量预留固定读取 `baseAreaMedium()`。现按 owner Group 的 `extentClass` 取面积；真实 SMALL 乡村组牧场、农田均为每 Parcel 192 格。 | [任务记录](../archive/be74ad2_v0.1.81_中心对称阵列/任务记录.md) |
| `CITY-PW-20260812-01` | 已修复，真实 D4 编译复验通过 | D4 必需景观联合搜索的单景观候选只依赖主体、地形、结构占用与 seed，却曾在组合递归的每个节点重新生成。现按 Subject 在单次 plan 内预计算一次候选，再只回溯候选间占地冲突；真实四景观联合搜索 10,610 节点并在约 4.3 秒内连同预览完成。 | [任务记录](../archive/be74ad2_v0.1.81_中心对称阵列/任务记录.md) |
| `CITY-PW-20260811-01` | D4 skip 真实复验通过，后续问题转 05 | Blueprint D4 现以 accepted contextId 与 Blueprint hash 识别当前产物并遵守 `skipExisting`；D5/D6 保持完整 anchor map 来源绑定，D6 单一 preflight 失败原因提升至 workflow manifest。 | [任务记录](../archive/be74ad2_v0.1.81_D6续跑误重跑/任务记录.md)；文档仓库故障修复案 |
| `CITY-PW-20260806-01` | 已修复，RTF 完整工作流复验通过 | Blueprint 编译器只把 `cityBlueprintCompileProvenance` 写进 anchor plan，通用 D4 最终化生成 anchor map 时漏掉该字段；D5 因而误判为 legacy LandUse，并用 v0.1 严格读取器拒绝自产的 v0.2 完成标记。现最终 anchor map 保留 Blueprint provenance，D5 正确选择 v0.2 身份绑定校验。真实 run 已成功完成 D4、D5、D6、LandUse 激活和 D7，40 个结构全部进入 `WAITING_FOR_WORLDGEN`，0 失败。 | `CityBlueprintCompilerServiceTest` provenance 回归；真实 run `rtf0805_newsave_w32768_20260805_1840` workflow 产物 |
| `CITY-PW-20260714-01` | 已修复，待实机复验 | 连续 outward 的混合尺寸簇只以首个 item 计算前沿，较大成员回压父簇；现已用整组 body 并集确定前沿。 | `CityStructureArrayLayoutLoopPlannerTest`；[历史明细](./历史明细.md) |
| `CITY-PW-20260714-02` | 已修复，待实机复验 | 东向 `guide_line_dual_side` 复用横向 offset，且 gap 只读首个 item，导致近圈距离被错误放大。 | `CityStructureArrayLayoutLoopPlannerTest`；[历史明细](./历史明细.md) |
| `CITY-PW-20260626-01` | 已修复 | 旧 D7 `variable_area` 只限制起点，jigsaw child piece 可越界或压占；现已做 piece 级约束与地形探针。 | `CityStructureD6D7Test`；[历史明细](./历史明细.md) |
| `CITY-PW-20260626-02` | 已修复 | D7 trace 重复写入大量 bounded trace，HTTP 返回 OOM、磁盘文件膨胀；现已压缩展示字段。 | `CityStructureD6D7Test`；[历史明细](./历史明细.md) |
| `CITY-PW-20260709-01` | 已修复 | profile cache hit 时 workflow 仍强制 profiling，破坏 artifact 级跳过语义。 | `CityPlanningEndpointHandlerTest`；[历史明细](./历史明细.md) |
| `CITY-PW-20260715-01` | 已修复 | 装饰计划测试夹具未携带 template transformed road entrance，正确的 bridge 校验因缺 `planned_road_corridor` 失败。 | 完整 JVM 377 项；[历史明细](./历史明细.md) |
| `CITY-PW-20260719-01` | 历史修复，已被固定主线替代 | 当时 D6 已为 `structure_start_beard_thin` 冻结 generator-base datum，但激活预检只接受 direct-template policy；该双 policy 兼容修复后来由 2026-07-25 固定 NBT 唯一主线整体替代，当前所有 catalog 模板只允许 beard-thin policy。 | `CityPlanningEndpointHandlerTest`；capital 完整城 D6 artifact |
| `CITY-PW-20260719-02` | 已修复，待实机复验 | LandUse 可把同一逻辑 group 拆为多个共享 `areaId` 的不连通 `areas[]` 组件，但 Decoration resolver 只读取首项；首项被结构障碍完全扣除时误报整个 target 为空。现先合并全部同 ID 组件，再执行 inset 与硬障碍扣除。 | `LandUseAreaDecorationProgramContextResolverTest`；capital 完整城 LandUse artifact |
| `CITY-PW-20260724-01` | 已修复，D4 两层实机复验通过 | 首轮根因是 Explorer 在 hard occupied 后仍合并 free components，resolver 也只注入 patch ref；已改为独立组件 + `candidateLegalRegion` 完整 collision 硬边界。后续修复 candidate session 全文误入 source identity、点级组件越界重复升级 hard block、主选择图 bbox 遮挡，以及 array 把城市局部 cell 与 D3 世界 cell 比较导致全部 `POINT_OUTSIDE_PATCH`。现在 D4 identity 只绑定 D3 + committed occupied，anchor/array/final anchor 按 member cell block range 判断，主图隐藏 bbox。聚焦 163 项通过；真实 `PLAIN-01` 同 ref 的 anchor 返回 5 个合法候选、quality 100，array 原 `arrayCount=1` 请求 584 ms 返回三组候选，三组 collision 越界 cell 均为 0。 | `PatchExplorerServiceTest`、`CityStructureLandingFlowTest`、`CityStructureLandingPreviewRendererTest`、`CityStructureArrayLayoutLoopPlannerTest`、`CityPlanningEndpointHandlerTest`；任务记录；selection `psel_453649c80ce199fe8c5a05956150591237cb6f0e8272fcd5bb4ca5bba52e0185` |
| `CITY-PW-20260724-02` | 历史修复，已被固定主线替代 | 新城 D4 曾把 Stubbs NBT 画像误当 configured identity；中间修复会从 TerraSense 同目录 companion catalog 自动补 provenance。2026-07-25 后该隐式补全也被删除，D4 必须显式传 `templateCatalogSource`，TerraSense 只提供语义标签。 | 历史实机城市 `city_skybell_firwatch_20260724_01` D6 重放产物 |
| `CITY-PW-20260725-01` | 已修复，待固定模板实机验收 | 预览 bbox 大于真实建筑的根因是 configured/Jigsaw 的 sampled envelope 与 `StructureStart` bbox 重新进入 D4-D7。现已删除 profiling、configured materialization、late paste 和 direct-template 双轨；body 只由 transformed NBT footprint 决定，collision/mask 独立分层，D6/D7 复核 owner chunks，所有 catalog 模板只生成 City single-piece beard-thin start。 | `CityStructureLandingFlowTest`、`CityStructureTemplateMaterializationPlannerTest`、`CityTemplateRuntimeTransformTest`、本轮双仓库任务记录 |
| `CITY-PW-20260801-01` | 已修复，三 Group 远 patch 正常 runClient 复验通过 | D4 用固定 64 格限制后续 Group 首栋，导致同城远 patch required 直接失败；现改为各 Group 独立播种、关系图驱动双方持续阵列连接。真实 296 格初始 gap 以 7 个工坊连接结构降到 7 格，D3 review.grid 仍是硬边界。 | `CityBlueprintCompilerServiceTest`；[任务记录](../active/20260731_D4单次城市决策/任务记录.md) |
| `CITY-PW-20260802-01` | 已修复，五方位三 Group 正常 runClient 复验通过 | connectivity 曾把靠近结构前沿的 D3 cell 中心当作上一栋结构中心，布局器又前进一个完整 spacing，导致首个连接槽位被双重外推，虽生成 5 个合法候选却全部命中 `GROUP_CONNECTIVITY_GAP_EXCEEDED`。现改从离目标最近的本组实际结构中心推进，D3 cell 仅作为 legal region。真实 EAST/SOUTH/WEST 样例两条边由 228.65/173.93 格收敛到 0 格。 | `CityBlueprintCompilerServiceTest.distantThreeGroupTopologyGrowsContinuousArraysInsidePlanningGrid`；[任务记录](../active/20260731_D4单次城市决策/任务记录.md) |
| `CITY-PW-20260802-02` | 已修复，远距地形三 Group 正常 runClient 复验通过 | 连接施工已按 resolved SPARSE handoff 40 输出 `HANDOFF_READY`，但 HARD 关系复验又读取 Group 内部 DENSE/BALANCED handoff 16/24，导致同一条边以 33 格先成功后误报 `CITY_BLUEPRINT_HARD_RELATION_UNSATISFIED`。现统一由双方解析后的 connection handoff 严格最小值复验。 | `CityBlueprintCompilerServiceTest.distantThreeGroupTopologyGrowsContinuousArraysInsidePlanningGrid`；[任务记录](../active/20260731_D4单次城市决策/任务记录.md) |
| `CITY-PW-20260804-01` | 已修复，真实四 Group D4 复验通过 | 本次真实 D4 先后暴露四层 L2 求解问题：合法 body-gap 区间内没有 16 格倍数时仍选到区间外，使 15 上限漂到 17；规划尺寸与旋转/混合模板的真实 `plannedFootprint` 不一致，使初步合法的 gap 再次漂移；旧 Agent Loop 的 `minCandidateCount>=2` 把唯一完整合法候选误判为失败；固定 4/6/8 栋批量在 handoff 尾端没有空间时直接中止。现把网格对齐降为区间内偏好，构建后按真实 footprint 复算并平移原点重建；破坏性删除 `minCandidateCount`，一组完整合法候选即可；连接批量按 `N..1` 确定性递减，收尾单栋仅放宽目标 edge gap，不放宽 terrain/collision/grid、组内最大 gap 或必须缩短目标 gap。定向 JVM 测试强制重跑通过；真实 run `trek_stubbs_newworld_20260804_120411` HTTP 约 1.48 秒编译 106 anchors，三条边由 57/536.97/627.48 收敛到 1/5/0，AI/manual candidate selection 均为 0。 | `CityBlueprintCompilerServiceTest`、`CityStructureArrayLayoutLoopPlannerTest`、`CityPlanningEndpointHandlerTest`；真实 compile trace 与 `structure_anchor_preview.png` |

## 2026-08-24 至 25 主从路网与景观层详细记录

### CITY-PW-20260825-14

*现象*：正式三区 capital Blueprint 使用 `CENTER_SYMMETRIC` 行政组和官方 60×43 市政厅；中心建筑成功，但首对住宅在父槽内无合法位置。改 MEDIUM 后可进入更外环，证明不是模板或算法输入错误。

*首次偏离*：空间需求已算出对称阵列真实半径，随后又把 `formationSpanBlocks`、宽和长按 SMALL=96 硬截断。

*根因*：`extentClass` 软目标被误作父槽硬边界；同时外围 `memberSpan` 读取全组最大模板，把中心市政厅尺寸重复用于住宅环。

*修复*：外围跨度仅统计中心之后的 planned refs；取消 formation span/width/length 的 extent 硬截断，D3 规划边界仍是硬边界。

*验证*：60×43 市政厅 + 小住宅镜像环的 SMALL 父槽回归通过，`plannedSpanBlocks > 96` 且最低成形量完成；待同一 D3 续跑真实 D4。

*记录*：[任务记录](../active/20260824_主从路网与景观层/任务记录.md)；未提交。

### CITY-PW-20260825-11

*现象*：RTF0810 三区用例四组 required 建筑已可放置，但 required 农田与林场容量无解，D4 返回 `CITY_BLUEPRINT_REQUIRED_LANDSCAPE_LAYOUT_UNSATISFIED`，没有正式三区预览。

*首次偏离*：`CityLandscapeCapacityReservationPlanner` 把每个 required Landscape 实例的所有 Parcel 作为精确面积、贴 owner、全有或全无的联合搜索 Subject；搜索为空后 `CityBlueprintCompilerService` 继续换建筑组合，最终把景观无解升级为整城失败。

*根因*：旧 required、ATTACHED 与 Patch/地形候选语义服务规划完整性，不服务最新确认的美观与工整目标；与 `City景观约束松绑-v0.1` 冲突。

*修复*：进行中。目标为只保留地形门禁；owner 仅提供外向生长种子；允许离开 preferred Patch；实际面积随可用好地缩减；完全零格写警告且 D4 继续。

*验证*：待自动回归与未生成区块真实三区 Agent Loop。

*记录*：[任务记录](../active/20260824_主从路网与景观层/任务记录.md)；未提交。

### CITY-PW-20260825-10

*现象*：两个 CENTER_SYMMETRIC Group 的 NORTH/SOUTH 中轴段分别压入镜像 fill 建筑。

*首次偏离*：`CityInternalStreetPlanner.centerAxis()` 固定以 core center X 生成南北轴。

*根因*：中轴方向没有读取实际镜像 collision；pair 恰位于轴上时仍无条件出路。

*修复*：生成南北轴与东西轴两组候选，按 `STAIR_SLAB_STAIR` 完整碰撞宽度检查；选择不与非 core body 相交的一组。

*验证*：`CityInternalStreetPlannerTest`、CENTER_SYMMETRIC 与主路聚焦回归通过；最新正式客户端 D4 `ok=true`，原 `CENTER_AXIS_STREET ROAD_OVERLAPS_STRUCTURE` 消失。

*记录*：[任务记录](../active/20260824_主从路网与景观层/任务记录.md)；未提交。

### CITY-PW-20260824-01

*现象*：GRID 正式道路与建筑 collision 重叠。

*首次偏离*：区内道路从 theoretical anchor 中点生成时。

*根因*：模板 origin 偏心，理论中点不等于两侧真实 collision 自由间隙中心。

*修复*：按两侧 collision edge 计算 gap/中心；间隙不足失败；视觉门禁检查 road/body 重叠。

*验证*：相关 GRID、视觉门禁与 JVM 全量曾通过；最终真实全链待本案收口。

*记录*：[任务记录](../active/20260824_主从路网与景观层/任务记录.md)；未提交。

### CITY-PW-20260824-02

*现象*：远端 preferred Patch 子 Group 返回 `ARRAY_COMPOSITION_SLOT_UNAVAILABLE`。

*首次偏离*：父阵列 child slot 只检查 center 周围 guides。

*根因*：父布局提案没有进入 child 自身 preferred Patch 搜索。

*修复*：guide 失败后在 preferred Patch 内稳定枚举合法 origin，保留 composition/slot 身份。

*验证*：远 Patch 回归通过；真实流程已越过原失败。

*记录*：[任务记录](../active/20260824_主从路网与景观层/任务记录.md)；[调用记录](../active/20260824_主从路网与景观层/真实游玩调用记录.md)；未提交。

### CITY-PW-20260825-01

*现象*：`runClient` 在 compileJava 报缺少 `)`。

*首次偏离*：整理 `LaneTarget` 构造时产生语法错误。

*根因*：构造表达式漏闭合括号。

*修复*：补齐括号。

*验证*：JVM 全量通过，客户端成功进入 RTF0810。

*记录*：[调用记录](../active/20260824_主从路网与景观层/真实游玩调用记录.md)；未提交。

### CITY-PW-20260825-02

*现象*：同一远端 Patch 的第二个子 Group 无槽。

*首次偏离*：多个 child 都取得同一个 preferred zone 中心 cell。

*根因*：Patch placement 只返回单个 origin，不做候选枚举。

*修复*：按 zone 距离排序全部 member cells，逐个检查 bounds/占用。

*验证*：`parentGridPlacesTwoPatchConstrainedChildrenInsideTheirSharedRemotePatch` 通过。

*记录*：[任务记录](../active/20260824_主从路网与景观层/任务记录.md)；未提交。

### CITY-PW-20260825-03

*现象*：`NONE+WEST` 模板在 COMPACT/COURTYARD 报内部 frontage 不可用。

*首次偏离*：局部巷线/庭院 frame 固定后再做模板朝向。

*根因*：布局只提供部分基数临路面，没有在不改变槽语义的前提下旋转局部 frame。

*修复*：枚举四个 90°局部 frame，选择模板白名单内 alignment 合法者。

*验证*：COMPACT 与 COURTYARD 固定 WEST 回归通过。

*记录*：[任务记录](../active/20260824_主从路网与景观层/任务记录.md)；未提交。

### CITY-PW-20260825-04

*现象*：大模板跳过槽 0/1 后，小模板从槽 3 开始，无法利用早期空洞。

*首次偏离*：`exactSlotCursor` 跨顶层建筑请求累积。

*根因*：槽搜索游标同时承担单次搜索与全组全局状态。

*修复*：每栋新建筑从槽 0 重扫；已占槽由 collision 门禁排除。

*验证*：固定入口 COMPACT 与父阵列聚焦回归通过。

*记录*：[任务记录](../active/20260824_主从路网与景观层/任务记录.md)；未提交。

### CITY-PW-20260825-05

*现象*：首栋 fallback 到相邻 Patch 后，后续庭院/阵列只在 claimed Patch 搜索并全部失败。

*首次偏离*：`formationPatches()` 在 claimed 非空时替换 preferred 集合。

*根因*：claimed 被误作唯一合法 Patch，而非新增可用 Patch。

*修复*：返回 preferred + claimed 有序并集。

*验证*：`firstRequiredStructureFallsBackFromUnbuildablePreferredPatchWithinD3Grid` 通过。

*记录*：[任务记录](../active/20260824_主从路网与景观层/任务记录.md)；未提交。

### CITY-PW-20260825-06

*现象*：LINEAR 后续建筑不能朝街，固定 WEST 模板持续失败。

*首次偏离*：首栋提交后初始化 `layoutFrame` 街轴。

*根因*：入口方向被直接当作街轴，而建筑入口应垂直朝街。

*修复*：N/S 入口映射 E/W 街轴，E/W 入口映射 N/S 街轴。

*验证*：`linearStreetAxisIsPerpendicularToPrimaryEntrance` 通过。

*记录*：[任务记录](../active/20260824_主从路网与景观层/任务记录.md)；未提交。

### CITY-PW-20260825-07

*现象*：LINEAR 路先压首栋，修正后又压一侧跟随建筑。

*首次偏离*：state band 按入口/理论 frame center 冻结中心线。

*根因*：模板 origin 与 collision 中心不一致，两排真实包络不关于理论轴对称。

*修复*：主街从首栋轴向后缘外开始；提交完两排后按左侧 max edge 与右侧 min edge 求真实 gap center。

*验证*：LINEAR 聚焦回归通过，road/body overlap=0。

*记录*：[任务记录](../active/20260824_主从路网与景观层/任务记录.md)；未提交。

### CITY-PW-20260825-08

*现象*：两个父槽都合法，但主路返回 `PARENT_GRAPH_DISCONNECTED`。

*首次偏离*：`CityMainRoadPlanner.roadConnector()` 从窄区内道路端点向外构造加宽连接段时。

*根因*：端点已由区内道路碰撞门禁证明合法，却又在 transition 第 0 步按更宽横截面重复检查；端点邻近建筑时 connector 被误判为 null，父图无法生成 gateway。

*修复*：非建筑 connector 从端点外第一格开始搜索，transition 也从第 1 步检查渐宽段，不重复否决合法窄端点。首个 `reservesInterGroupRoad` 错误修改保持撤回。

*验证*：新增 `narrowInternalEndpointCanEscapeBeforeMainRoadWidens` 并通过全部 `CityMainRoadPlannerTest`；最新正式客户端 D4 `ok=true`，原 `PARENT_GRAPH_DISCONNECTED` 消失。

*记录*：[任务记录](../active/20260824_主从路网与景观层/任务记录.md)；未提交。

### CITY-PW-20260825-09

*现象*：COURTYARD 合法基础槽被 `GROUP_CONNECTIVITY_GAP_EXCEEDED` 跳过，最终环不完整。

*首次偏离*：候选 terrain/collision 通过后进入通用建筑近邻 gap 门禁。

*根因*：通用簇连通规则没有区分由正式道路连接的硬骨架阵列。

*修复*：GRID/COURTYARD/LINEAR/CENTER_SYMMETRIC 不执行建筑近邻 gap 拒绝；仍保留 bounds、collision、terrain 与正式道路质量门禁。

*验证*：COURTYARD、LINEAR 聚焦回归通过。

*记录*：[任务记录](../active/20260824_主从路网与景观层/任务记录.md)；未提交。
