# 城市基面与真实路网

*需求入口*：`E:\Mod_Dev\designer_territoryMod\docs\systems\city\10_product\案子\City城市基面与路网形态-v0.1\README.md`。严格以该当前案为需求源；真实游玩使用新区块，功能区只作结构占位分组，不以功能属性验收。

*修改内容*：城区 Foundation 与陆地道路统一进入邻域主高程平台整地，保留 Landscape，并闭合没有显式水景意图的封闭小水坑与干坑；结构 footprint 继续作为铺面排除，但建筑 datum 改为读取所属 Foundation 的最终台地高度。台地目标按 4 格一级量化，相同高度连通区合并，小于 64 格的独立台地继续并入共享边界最多、其次高度最近的相邻台地。道路不再被错误排除出整地；所有真实道路相邻格都会扫描台地跃迁，不再只处理整条路的单一长轴，并在至少 4 格高差处主动生成直梯或左右分流梯。城市主路只消费显式 `CONNECTION` 并使用真实模板入口或区内街端点，关闭 HIERARCHY、父阵列与距离 MST 自动连路。跨水连接改由 City 生成独立 spruce 桥面、护栏和稳定间距水中石砖桥墩。主路、普通街、COMPACT 巷使用独立分级材质。RoadWeaver v0.3 不再自行求组间 MST，只等待规划域外显式交通意图。

*验证结果*：`gradlew test` 全量 757 项通过，99 suites、0 failure/error/skip；新增回归覆盖小高地并台、建筑尺度高地保留、结构读取合并后台地 datum、方形/转角道路主动楼梯，以及既有孔洞闭合、直梯、分流梯、桥面和路材质能力。真实游玩通过正式 MCP Agent Loop 在 `rtf_terrain_stairs_20260828_01` 选择新区块 `SLOPE-03`，D3 接受 `p.194`（连续坡地 24,257 格）；D4 使用三组紧凑阵列形成 10 栋 Trek 建筑，行政/商业/住宅占位分别为 5/2/3。D5 激活、实际 TP 生成后 D7 为 10/10，建筑 datum 只剩 85、89 两级，未再逐栋复制自然坡面的连续整数高程。8 个代表 owner chunk 共核对 7,844 个观测块；仅 5 格草方块因遮挡自然转为泥土，其余期望状态一致。`(123696..123711, 86..88, 128128..128138)` 实际生成 106 格深板岩主动步梯，其中 14 格 stairs、92 格 slabs，且同时出现 SOUTH/EAST 两个方向，证明转角道路不再被单轴判断漏掉。

*真实产物*：`run/realm_debug/rtf_terrain_stairs_20260828_01/city_test_runs/city_realm_salt_kingdom_0_capital/steps/d7/placed_structure_preview.png`；`steps/land_use/land_use_preview.png`；`steps/d7/placed_structure_ledger.json`；游戏保持开启，`Dev` 位于主动步梯 `(123706,90,128133)` 旁。

*当前追加*：2026-08-28 “合并台地 + 独立成台地门槛 + 主动楼梯”已经实现并完成自动化与新区块真实游玩验收；建筑同高、封闭孔洞、直梯和左右分流梯保持有效。本轮第一次复用旧城市身份时，运行账本正确将旧 anchor 位置判为 footprint/transform drift；最终通过全新结构身份与全新坡面完成无污染验收。另一次 `SLOPE-02` 的 GRID 内街净空仅 0、低于 5 格要求，D4 正确拒绝，未绕过门禁。

*剩余问题*：真实地形仍未命中短跨水桥，后续需在合适新区块补桥面、护栏与桥墩实机验收；本轮 5 格 grass→dirt 属原版遮挡退化，不影响台地与楼梯验收。
