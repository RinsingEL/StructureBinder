# 作者无固定正面声明

*提交*：实现 a0a2f89，契约 f6ff800；下方“未提交”为当时回合状态。恢复入口另开任务修复。

*需求入口*：用户先要求提交已有批次，并确认“无固定正面”作者标记建议；作者明确授权后，程序可从已经标注的入口择优朝向道路，不猜门、不替换必需建筑。

*修改内容*：模板目录增加可选 frontagePolicy=FIXED_FRONT（默认）/ANY_AUTHORED_ENTRANCE（作者授权无固定正面）。保留显式 frontageEntranceId、命名主入口 front 优先；仅从作者已有入口择优，每个允许旋转保留一个结果，同分稳定排序。非法类型/枚举及无入口授权拒绝。现有完整目录 snapshot 原样冻结字段，参与 Context hash，无运行时猜门或蓝图覆盖。契约同步；测试覆盖正式 prepare/submit/compile、缺省拒绝、授权通过、显式主入口、非法入口、受限旋转和镜像。

开发素材池 run/config/structureTemplate/terrasense/pcl_validation_02/template_catalog.json 仅为 geomantia:city/trek/landmark/plains_fountain_01 新增该授权；content pack catalogSha256 更新为 ee70dc1fcb13b1ded41f85ec8e07ad943af953e39fd8134a2c5c07b12cee6f37，四个门的位置、NBT 内容及其 hash 均不变。配置处于 gitignored run 目录，已把具体授权和四向入口断言加入受版本控制的 verify_validation_02_entrances.py，避免提交后丢失变更说明。TerraSense 无新增 UI/导出字段，本轮作者通过 Binder 模板目录配置即可生效。

*验证结果*：定向 JVM 13 tests 全通过；完整 JVM 794 tests、0 failures/errors、2 optional skips，耗时1m41s（Forge 47.4.23）。verify_validation_02_entrances.py 验证目录 hash、65 项 NBT 源 hash、此前10处入口修正和喷泉唯一授权均通过；双仓 diff --check 通过。前序检查点：实现 ada3aab / 留档 6529423，契约 4efbedc。

*剩余问题*：新增内容未提交。后续02:27实机复验被 blocked_by_program 下的 Context 准备门禁阻断，GLM 调用0，详见下方回合记录；原蓝图与历史失败预算未修改。港口已接受蓝图 hash 为 4db26a33083c4fcd3e6ff72b77523fddee0255f7f21b7e2d492fb81efc26cfec，Provider 已恢复 disabled；未覆盖 PCL 测试端。本轮只证明多入口声明能力，未声称新城实际落地或整体速度达标。

## 2026-09-06 02:27–02:28 GLM 复验（测试受阻，回合已结束）

- 用户请求“复验一下 GLM”。开启原 glm-5.3-flash/Hermes 配置，启动开发 validation_02，新素材目录安装生效；PCL 未覆盖。
- 启动准备时一次读取使用 MCP 子目录下相对路径，任务记录/配置读取失败，随后改用仓库 cwd；无状态影响。MCP listTools 成功，读取 queue、prepare、retry、save 的正式 schema。
- MCP city_design_queue_status(runId=provider_ce6a3c351fa2667b_r8192)：completedCount=1，当前 port 为 blocked_by_program，nextAction=city_post_d4_auto_compile_retry。
- MCP city_post_d4_auto_compile_status(同 run, citySeedId=city_realm_sunmarch_port)：attempt=2，active=false，原 FRONTAGE_ENTRANCE_AMBIGUOUS，历史 failureCount=2；返回 artifacts 为该城 test_run_manifest.json、steps/d4/city_generation_compile_trace.json、steps/blueprint/city_blueprint_failure_budget.json。
- 因作者目录确已改变，需要重新冻结 Context，正式调用 city_prepare_d4_blueprint_context(同 run/city)，返回 CITY_DESIGN_QUEUE_CURRENT_NOT_ACCEPTING_D4: status=blocked_by_program。没有新 Context。不能以原快照不变的 retry 掩盖配置仍未载入，也没有修改状态文件或代写蓝图绕过门禁。
- 本轮有效准备调用被状态门禁拒绝，接口没有提供允许加载新目录的修正参数，判为“复验受阻”，不是 GLM 设计失败。Hermes agent.log 最后模型事件仍为01:55:35，本轮模型调用0，尚未产生新的设计耗时或落地结果。
- MCP realm_debug_command(command=help geomantia, confirmCommandExecution=true, sourceMode=server, saveAfter=true) 返回 executed/worldSaveRequested=true；02:28:37 日志确认 All dimensions are saved，随后关闭本轮客户端，恢复 Provider disabled。
- 恢复入口尚需处理“作者元数据已修改，但 blocked_by_program 不允许正式重建 Context”的场景。此次仅复验，未修改该门禁实现。正式产物根：run/saves/RTF_beta_validation_02/realm_debug/provider_ce6a3c351fa2667b_r8192/city_test_runs/city_realm_sunmarch_port。
