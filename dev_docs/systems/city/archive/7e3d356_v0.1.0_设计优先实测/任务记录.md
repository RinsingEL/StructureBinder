# 设计优先真实 MCP 测试

*需求入口*：用户要求先提交，再验证真实 D4 预览和后续编译。

*修改内容*：测试前代码提交65d7dc5、归档93c10f6，契约f7481b5。准备独立开发存档副本；新场景阵列尚未接入正式蓝图，不纳入本次通过声明。

*验证结果*：真实 MCP 测试失败，未生成新的 D4 预览，未进入新 revision 的后续编译。不以旧 D4/D5/D6 产物充当通过。纯自动化测试此前为 885 tests / 0 failures / 7 skipped，不能替代本次实测。

*剩余问题*：商业区 organic_compact 必需花店的候选搜索耗尽。需要独立开发排查；本轮到此结束，不读源码后继续冒充同一真实 Agent Loop，不删除必需建筑或缩减功能区来制造通过。

## 环境与调用记录

- 代码为提交65d7dc5；保存副本 `run/saves/RTF_designfirst_20260907`，来自 `RTF_revision_loop_20260906`。没有改 PCL 用户存档。
- 当前代码通过 runClient 启动并自动进副本；MCP实际连接127.0.0.1:5000，非mock。
- `01` tools/list；`02` gis_status；`03` realm_status，连接成功。
- `04` ferry prepare：两套作者源歧义。文件哈希确认StructureProfile与PCL对应文件一致。
- `05` debug stop：单人游戏没有stop命令，失败。随后仅对测试游戏窗口CloseMainWindow，日志确认所有维度保存完成并退出，未强杀。
- `06` 以短源名重启后prepare：路径无效；正常关闭后改用完整启动参数 `-PgeomantiaDevPlanningSourceDir=E:/Mod_Dev/StructureBinder/run/config/structureTemplate/terrasense/pcl_validation_02`。
- `07` ferry prepare：队列要求capital先行，按返回信息纠正城市，没有绕开队列。
- `08` capital prepare成功；使用工具返回testRunPackage中的原蓝图作回放输入。保留seed、建筑、分组、关系与占比，只让宿主重绑定D3/catalog引用。
- `09` submit拒绝：旧 `fill:relay_dry_farmland` 已从当前作者目录撤下。按返回context中的作者配置，只迁移该项为 `fill:relay_irrigated_farmland` 与其示例roleShares，保持作物权重。
- `10` submit（autoAdvanceAfterD4=false）耗时1537ms，拒绝 `CITY_BLUEPRINT_DESIGN_GEOMETRY_INVALID` / `CITY_BLUEPRINT_REQUIRED_STRUCTURE_NO_LEGAL_PLACEMENT`。failureCount仍为0，未进入后续编译。
- 返回几何trace：核心建筑与农业必需建筑已能找到候选；`g_market_district` 第一栋general_store后，第二栋 `flower_shop_01` 从序号13至156持续无候选。早期记录 `OCCUPIED_ENVELOPE_OVERLAP`，最终为 `D4_ARRAY_COUNT_UNSATISFIED`，有限搜索耗尽。不能仅凭该记录认定一定是地形或模型错误。

每个编号的JSON保存完整实际参数、返回与stderr；10号记录包括耗时。几何失败trace另存本目录，避免后续测试覆盖。当前结论仅为“真实D4提交复验未过”，没有新预览/后续编译/落地验收通过声明。
