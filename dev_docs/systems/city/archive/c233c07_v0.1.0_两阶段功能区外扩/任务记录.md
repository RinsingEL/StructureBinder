# 两阶段功能区外扩

*需求入口*：`City单次递归蓝图与自愈编译-v0.1`；用户确认“两两连接本身也需要补向外阵列”，并要求连接性外扩与连接后的占比补足一起开发，完成后进行真实三区游玩测试。

*修改内容*：D4 固定为“初始形成 → 显式关系双方相向阵列至 handoff → 规划并冻结道路骨架 → 冻结唯一最高优先级区实际面积 → 各区沿自身算法补占比”。`HIERARCHICAL` 主路不再跳过连接阶段；LINEAR/COMPACT 等在第二阶段延续原阵列槽位，连接建筑参与冻结面积。带必需地景的功能区由地景承接建筑停止后的全部剩余目标面积，“固定建筑、剩下全部农田”不再被 `landscapeShare` 截断。大地景移除逐格整片边界重扫，并把占比阶段从 32 套完整预生成候选收成四个方向的有界尝试。

*验证结果*：编译器、地景容量和主路聚焦回归通过；最终 `gradlew test --rerun-tasks` 全量通过（1 分 52 秒）。正常启动 `runClient`、自动进入 `RTF0810` 后，经正式接口重编 `normal_three_zone_20260826_0935 / city_realm_salt_kingdom_0_capital`：13.4 秒返回 `ok=true`，正式 trace `compilationAcceptance.passed=true`、硬阻断 0、视觉门通过。行政—农业 39→0、农业—商业 27→5，均在 24 格 handoff 内且各提交 1 个连接建筑；关系阶段共 2 个连接建筑，随后主路规划 2 条。行政 24524/24524；农业由三栋固定建筑 4105 格加 4×7149 农田达到 32701/32699，缺口 0；商业沿 LINEAR 继续提交 5 栋后因水面/缓冲/占用与方向范围耗尽，14954/24524，正式记录缺口 9570。

*剩余问题*：本任务范围内无。商业区的占比缺口为本次 LINEAR 方向在真实地形中的可解释耗尽；“其他功能区改向可用方向继续阵列”已由用户明确暂不纳入本轮。
