# 落地生成隔离

实现提交：`1d647ad`。落地层专项 92 项通过，GameTest 8/8 通过，重混淆打包通过。

*需求入口*：用户要求只修落地层 mask，阻止外部自然植被和结构侵入；不修改城市设计、阵列或台面高度。

*修改内容*：移除 Feature 名称枚举和外部结构起始区块判断；新增按维度、区块索引的施工列保护，覆盖冻结铺装、道路、景观、CLEAR 区和 D5 建筑掩码，保护范围从基面下 1 格向上延伸。无冻结标高的列按生成器采样一次缓存，不请求真实区块。完整外部结构包围盒在注册及放置时检查；实际 Feature/Structure 写入在 WorldGenRegion 和 ServerLevel 拦截。显式 City 上下文放行自身模板及 LandUse，普通玩家修改不受影响。D5 mask 持久化维度，旧数据默认主世界。未修改 D4、阵列、台面标高。

*验证结果*：首次全量 JUnit：925 项，914 通过、10 跳过、1 个景观候选复用耗时断言失败（8.683 秒超过 8 秒）；随后 mask、上下文、注册表 14 项均通过，景观耗时单独复验仍失败，未更改其算法或测试门槛。隔离 GameTest 使用 `run-mask-validation-20260908`，关闭 StructurePacks/VisualPreview 开发依赖后，8/8 通过（23:12:07，日志 `run-mask-validation-20260908/logs/latest.log`）。验证了无名称特征的城外 Feature 向保护列高空写树叶被拦、城外写入保留，ServerLevel/WorldGenRegion 两个真实入口均通过；真实外部 StructureStart 被取消，CityTemplateTerrainStructure 可以放置；City 嵌套上下文和普通写入放行。首次 GameTest 被 Library Ferret 专用服务端加载错误挡住；首次测试用例采用模板相对高度未通过，改为明确位于合法世界高度内的高空位置后通过。未宣称完整整合包新城 GLM/GUI 验收。

*剩余问题*：绕过标准 WorldGenLevel/Level 写入、直接修改 ChunkAccess 或自行异步写世界的第三方 Mod 不在已验证覆盖内；不会自动清理旧区块中已生成的侵入物。全量测试的景观搜索耗时失败另属既有搜索路径，本次未修改。最终 `test --tests '*world.landuse.*' build` 成功，已生成重混淆 jar；未覆盖或重启用户运行中的游戏。GameTest 使用仓库默认 Forge 47.4.0，不冒称已复验整合包 Forge 47.4.23。
