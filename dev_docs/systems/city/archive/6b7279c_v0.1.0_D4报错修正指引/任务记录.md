# D4 报错修正指引

*需求入口*：用户要求统一批量修改报错，让模型知道怎样修正，优先保留设计意图。

*修改内容*：批量完善 CityBlueprintValidator、Codec 和必要建筑无落位报错；数量/份额提供实际值、范围与增减方向，模式冲突优先给保留设计的局部修正。提交封装新增 instruction 指引，覆盖 FINAL、补丁、hash、份额、路径与锁定身份；错误码、校验条件和失败预算不变。修正 connectionPlan.structurePools 引用错误的 fieldPath，避免模型改错字段。同步 D4 接口契约。

*验证结果*：`gradlew test build` 成功，912 项通过、10 项跳过，0 失败。回归覆盖按反馈修正后保留中心对称算法通过校验、枚举列出可执行值、封装 instruction 经模型呈现层保留，以及原有 10 次格式预算。新包 build/libs/geomantia-0.1.0.jar；未替换游戏实例。

*剩余问题*：尚未进行新一轮 GLM 实测，不能据自动化回归断言模型格式失败次数已下降；不涉及已生成区块补建。
