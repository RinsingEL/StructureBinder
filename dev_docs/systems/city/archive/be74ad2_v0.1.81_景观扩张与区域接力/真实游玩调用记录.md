# 真实游玩调用记录

## 测试环境

- 测试时间：2026-08-11。
- 存档：`RTF0810`，维度 `minecraft:overworld`，玩家 `Dev`。
- run：`rtf0810_medium_city_20260810_01`。
- 游戏由 `gradlew runClient` 启动，Geomantia GIS API 监听 `127.0.0.1:5000`。

## MCP 调用

| 顺序 | 工具 | 关键参数 | 返回摘要 | 纠正 / 结果 |
| --- | --- | --- | --- | --- |
| 1 | `tools/list` | 查询 City workflow、LandUse、D5-D7、worldgen observation 与 debug command 公开 schema | 工具可用 | 只使用公开工具描述与 schema |
| 2 | `city_run_workflow` | `city_rtf0810_tradefields_03`，`skipExisting=true`，`confirmWorldMutation=false` | D3-D6 复用；LandUse 失败 `CITY_FOUNDATION_THIN_BRIDGE:requiredWidth=17:maxJoin=64`，attempt 3 | 旧产物可能来自旧进程，改为完整重编 |
| 3 | `city_run_workflow` | 同城，`skipExisting=false`，`confirmWorldMutation=false` | D3 42.962s、D4 0.886s、D5 0.142s、D6 0.133s 全部成功；LandUse 0.881s 后仍返回 `CITY_FOUNDATION_THIN_BRIDGE`，attempt 4 | 证明不是旧产物或 schema 漂移；未激活、未写世界 |
| 4 | `city_run_workflow` | `city_rtf0810_greenfield_01`，`skipExisting=true`，`confirmWorldMutation=false` | D4/D5 成功；D6 失败 `STRUCTURE_CHUNK_ALREADY_GENERATED` | 该城旧区块已生成，不能作为新规划样本 |
| 5 | `city_plan_land_use` | `city_rtf0810_greenfield_01` | `CITY_BLUEPRINT_LAND_USE_D6_NOT_LOCKED` | D6 失败态产物不能绕过 locked 约束 |
| 6 | `city_prepare_d4_blueprint_context` | `city_rtf0810_tradefields_02` | 先提示 `blueprintReferenceCatalog object is required`；补入同 run v0.8 reference catalog 后返回 `CITY_BLUEPRINT_CITY_SEED_NOT_FOUND` | 该测试包是已从当前 registry 移除的遗留包，停止该路径 |
| 7 | `city_plan_land_use` | `city_rtf0810_tradefields_03`，请求排除无关 `market_core` group | `CITY_BLUEPRINT_WORKFLOW_LAND_USE_OVERRIDE_FORBIDDEN` | 按返回确认 Blueprint 是唯一户外设计权威，不再用请求级覆写绕过 |
| 8 | `city_prepare_d4_blueprint_context` | `city_rtf0810_tradefields_03`，尝试使用现有 snapshot 重准备 context | 依次返回 `schemaVersion is required`、`CITY_BLUEPRINT_CATALOG_SOURCE_REQUIRED`、`sourceType is required` | 公开 schema 未提供可复用的内部 source 包装实例，停止猜测调用 |
| 9 | `realm_debug_command` | `/tp Dev -1460 120 -900`，`confirmCommandExecution=true` | `DEBUG_COMMAND_EXECUTED`，result 1 | 玩家已到 `greenfield_01` 风车田上空 |
| 10 | `city_query_worldgen_observations` | chunk `-92,-57`，初次 `phase=all` | 返回 phase 只允许 `post_features|post_retry_tick|chunk_save` | 改为分阶段查询 |
| 11 | `city_query_worldgen_observations` | chunk `-92,-57`，三阶段，最终 `includeBlocks=false` | `post_features` 5 条；`post_retry_tick` 0 条；`chunk_save` 1 条，301 个 watched、297 匹配、4 不匹配、0 写后不匹配、0 回滚 | 现有景观确有 `land_use_direct` / `land_use_neighbor_reconcile` 真实 BlockState 写入；该记录来自 2026-08-10 已生成区块，不作为本次新规划通过证据 |

## 产物与结论

- 目标 Blueprint：`city_blueprint.v0.10`，含 `windmill_field_system` 一个实例、10 个 Parcel；第一块以 owner actual footprint 为起点，后续 Parcel 冻结父 Parcel boundary proof。
- 当前失败包：`run/realm_debug/rtf0810_medium_city_20260810_01/city_test_runs/city_rtf0810_tradefields_03`。
- 现有世界预览：`run/realm_debug/rtf0810_medium_city_20260810_01/city_test_runs/city_rtf0810_greenfield_01/steps/land_use/land_use_preview.png`。
- 规划阻塞已在 `CITY-DL-20260811-02` 登记；本轮完整重编复现同一失败，未进入 D5 激活与新区块 worldgen。
- 游戏保持运行，玩家停留在风车田上空。
