# Qadir 区块预检卡主线程

*需求入口*：用户报告关闭游戏卡住，怀疑线程卡死。

*修改内容*：用户随后要求代码修复。CityLandUseChunkCompiler 的 areaOwners/sharedContactCells 改为owner加一格边界裁切，保持跨chunk共享边界单writer，不再逐owner展开全城。CityLandUseChunkStatusPreflight 分离 PreparedOwners 与实时检查；准备结果绑定city/areaHash/surfaceHash，拒绝漂移；补纳入无Area但有道路feature/共享边界写入的owner。自动workflow及HTTP city_execute_d5均在worker准备owner，再派回主线程读取最新区块状态并激活。ServerThreadDispatch统一两处server dispatch，等待中轮询服务器生命周期，退出或中断时取消尚未执行的排队任务，保留中断标记；纯编译/预检循环支持协作取消。不跳过FEATURES安全检查，不改游戏进程或存档。

*验证结果*：2026-09-06 14:27–14:28，PCL Minecraft PID24020（14:24:28启动）。正式 manifest 已记录 city_plan_land_use success、21792ms，completion 文件14:25:44写入，证明上一轮区域算法优化已在游戏越过。当前两次 Server thread 采样均为 RUNNABLE，CPU114593.75→159343.75ms、elapsed131.96→178.13秒，在 CityLandUseChunkCompiler.compileInternal:156 构建 areaOwners；调用链 ownerChunks → preflightChunkStatus → handleExecuteD5，经 callOnServerThread 整体派到主线程。Post-D4 worker 等 CompletableFuture，Render thread 在退世界等待循环，并非采样到锁死循环。正式 area plan 共590824个member cell，仅member spans产生2378个候选owner chunk。代码确认每个owner的compilePrepared再次遍历全城memberSpans构建areaOwners，理论仅此一环完整执行约14亿次put，还未计其他编译工作。未确认世界已完成安全保存。

*剩余问题*：未在本轮实际重启PCL游戏验证退出及后续落地。最终世界状态探针仍在主线程进行，含必要的磁盘NBT状态读取；本轮未把其异步化以免引入状态证据过期的安全窗口。取消不能热修已经在旧Jar主线程上执行的任务。

## 验证

- QadirOwnerPreflightReplayTest 只读实测落盘的area/surface plan，无Minecraft实例、无世界写入；owner准备1.6069157秒完成，2574个owner（原member-only候选2378，新增用地外feature覆盖）。模拟探针逐一读取所有owner，任意最后owner达到FEATURES时仍拒绝；这不代表真实区块均符合激活条件。
- 第一轮聚焦测试：CityLandUseChunkCompilerTest、CityLandUseChunkStatusPreflightTest、CityPlanningEndpointHandlerTest、ServerThreadDispatchTest及上述实测输入回放均通过。服务器停止、中断取消、排队任务不复活的4个dispatch测试通过；已有跨chunk共享边界回归通过。
- 新增prepared hash/city漂移、实时状态变化、feature-only owner安全覆盖、中断标记保留测试，随全量构建验证。
- 最终补充：实时Minecraft探针每个owner检查server生命周期，正式激活前再次检查；PRESERVE不准备旧LandUse产物。最终全量构建2分22秒成功，825项、0失败、0错误、3跳过。预检7项、dispatch4项通过；真实数据回放再次1.6298218秒准备2574个owner，模拟状态变化仍拒绝激活，未冒充真实世界eligibility验收。
- 包：build/libs/geomantia-0.1.0.jar；SHA256 DA5B04DF0CA0236E1869C23614C7A748D9985896233D215B0A7D294F5FD4D7BF。未自动复制Jar/配置到PCL，未改存档，未提交。
