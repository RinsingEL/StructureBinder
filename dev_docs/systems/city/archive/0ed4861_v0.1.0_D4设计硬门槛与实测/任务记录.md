# D4设计硬门槛与实测

*需求入口*：用户要求按设计优先的硬门槛完全开发并真实测试。

*修改内容*：D4 推导槽位/跨度/最大间距改为偏好，取消默认组间 12 格硬隔离；保留规划边界、实体碰撞、通行间隙和显式硬关系。相反 HARD 方向给出具体冲突字段；缓存版本更新，避免旧几何冒充新验证。MCP submit 请求预算跟随几何求解改为 600 秒。真实 D7 暴露并修复自然树干/蘑菇误判，保留木材保护并补全施工失败坐标日志。

*验证结果*：

- 全量 Java 测试 895 项，0 failures/errors，8 skipped；MCP 11/11 通过。
- `gradlew build` 成功，包含最新 MCP bundle；正常关闭客户端后全部维度 chunks saved。
- 原五功能区完整蓝图离线回放 41.96 秒通过，没有删花店、换算法或减少内容。
- 正常 runClient 自动进入隔离存档 `RTF_designgate_20260907`，真实 MCP 提交同一设计，修复超时后约 43 秒返回 accepted/designGeometryValidated=true。
- D4 输出新预览与 203 anchors；D5 162ms、D6 85ms，户外编译 103244ms，完整后半段 111509ms 进入 worldgen。D5/D6/户外计划是新执行，不是跳过旧产物。
- 最终 `09_worldgen_progress.result.json`：203/203 structures，0 waiting/failure；3401/3401 LandUse owners completed，failures=[]。
- `10_observe_road.result.json`：原失败 owner 130,341 的 post_retry_tick 和 chunk_save 均为 452/452 实际 BlockState 匹配，0 mismatch/rollback。
- 所有真实请求、返回、超时和诊断阶段分别保存；测试的是正常游戏中的 MCP/实际 worldgen，不是 GLM/Hermes 自主新设计，也未做人走遍全城的审美验收。

*剩余问题*：本次硬阻断回归已通过；D4 仍有巷道/入口连接质量警告、布局疏密问题，未伪称审美验收通过。203 建筑的大方案户外编译约 103 秒，性能仍可后续优化；自然植被识别采取保守规则，不放开任意木材/建筑替换。测试只修改开发副本，未替换玩家 PCL jar 或原存档。
