# 真实 MCP 阶段记录

隔离存档：RTF_designgate_20260907。旧五功能区请求保持不变，未删花店或减少内容。

第一轮：01_prepare 成功；02_submit 在客户端 10 秒超时，未立即重复提交；03_compile 确认服务端已接受，生成 203 anchors 和真实 PNG。04_workflow 执行新的 D5/D6/LandUse/D5 激活/D7，111509ms，进入 waiting_for_worldgen。05_worldgen：529/3401 地面 owner，25 结构，暂无失败。此轮编译验证结束，转开发诊断超时。

超时修复复验：再次用原 02_submit 请求，由新 MCP handler 正常返回 accepted、designGeometryValidated=true、failureCount=0，完整结果覆盖前已保存 02_submit_timeout.result.json。

继续生成的日志发现 chunk=130,341 的 CITY_ROAD_FROZEN_GRADE_TERRAIN_CONFLICT，rollbackComplete=true（22:02:33）。这属于真实落地失败，本轮不能记作整城通过，转执行层诊断。此前报告的暂无失败仅对应 05_worldgen 当时快照。

Agent 调用错误：一次误写 dev_docs/sity 路径，ENOENT 未发送到 MCP，不属于产品失败。

第二轮诊断重启：同一 D7 job 在 owner 130,341 重现；新日志明确为 (2090,5461) spruce_log，表面80/设计76。结束该轮后加入受建筑占地保护的天然树干识别。

第三轮：树干检查通过，但 owner 130,341 报 MICRO_FILL_TARGET_OCCUPIED；进一步日志在第四轮明确为 (2087,74,5456) brown_mushroom。不是方案错误或实体建筑碰撞。结束测试轮后扩展小型植物可清理规则，不允许任意实体方块替换。

最终轮：08 启动修复后 D7，09 查询完成。203 structures、3401 owners 全部完成，无失败。10 查询原失败 owner 的实际方块观测，post_retry_tick 与 chunk_save 均 452/452 匹配。该轮正常结束，随后正常关闭测试客户端保存世界。
