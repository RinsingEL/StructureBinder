# 阵列主从骨架与街带完整实现

*需求入口*：用户要求按 `City阵列主从骨架与街带-v0.1` 与 `City阵列拓扑分类与功能约束-v0.1` 完成开发和真实游玩，不再把父槽间距修复当作整案完成。

*修改内容*：LINEAR required/fill 锁轴并只使用精确 guide，不再回退 cell center；非法槽位留空后继续后续 rank。父槽按主入口方向从轴端单向延伸，次建筑在模板允许旋转内由 OrientationSolver 朝街。D4 冻结 street bounds 与硬骨架 platform bounds，AnchorMap/D6/预览保留，Outdoor compiler 将台基纳入 Foundation。Foundation 改为一个 owner 下多个有限局部平台，细桥不再强接全城。Landscape fill 含 GROUND/BANK corridor 时父子 Parcel 留一格地形路隙，并冻结新 origin kind。补齐父阵列内 CENTER_SYMMETRIC 子组的真实镜像跨度、精确 pair origins 和粗 district cell 绕开规则；功能区容量按每栋结构 claimed area 分别向上取整到 D3 cell，避免四栋离散占位只预留三格。同步 City/Blueprint/LandUse 契约和代码导览。

*验证结果*：`./gradlew.bat test --no-daemon` 全量 683 项通过，0 failure/error/skipped；`country_designer_mcp npm test` 7/7 通过；双仓 `git diff --check` 通过。真实 `RTF0810` D4 成功：18 anchors、quality 100；市场 4/4 栋，WEST 直轴，左排 NORTH、右排 SOUTH，朝街 score 全 1；street 7×83，局部台基 43×83。D5/D6 成功，D6 locked=true；LandUse quality pass、无 warning，Foundation 使用局部平台组件而非全城细桥。另用真实 D3 上游编译 optional FARMLAND 三 Parcel，面积 823/956/371，两处 child 均为 `PARENT_PARCEL_ROAD_GAP`，中间格未被 Parcel claim。最终在真实 `RTF0805` 平原以一个父 GRID 同时编排 COMPACT / GRID / LINEAR / COURTYARD / ORGANIC_COMPACT / CENTER_SYMMETRIC 六个完整子 Group；D4 共 33 anchors，六组内部数量 5/5/7/4/6/5，quality 100，0 hard block/warning/review，正式同城 preview 非空。全程未执行 D5 激活、未改世界。

*剩余问题*：无。
