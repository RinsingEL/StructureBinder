# 阵列主从骨架与街带完整实现：真实游玩调用记录

*目标*：真实启动 `RTF0810`。结构样例重新 prepare/submit/compile，验证 LINEAR 连续街带、次建筑合法朝街、单向父槽与 Foundation 交接；景观样例复用最高有效上游，验证田野/林场父子 Parcel 保留一格地形路隙。全过程记录实际 MCP 请求与返回，不以旧 preview 充当新结果。

| 序列 | 工具与关键参数 | 返回摘要 | 后续 |
| --- | --- | --- | --- |
| 1 | `gis_status` | 成功；`Dev` 位于 overworld `(-4189,79,-416)`。 | 世界正确。 |
| 2 | `city_prepare_d4_blueprint_context`；复用原结构画像与 Reference Catalog，读取刷新旋转授权后的 template catalog | 成功；新 `contextId=sha256:067163...`，AI 调用数 0。 | 提交同一设计。 |
| 3 | `city_submit_d4_blueprint`；只替换新 Context 的 source refs 与 generation seed | 成功；valid=true、issues=[]、submission count=1。 | 编译 D4。 |
| 4 | `city_compile_d4_blueprint`；`realm_w_mszypml0_fd99c6f2 / city_realm_salt_kingdom_0_capital` | 有效业务失败；`CITY_BLUEPRINT_REQUIRED_STRUCTURE_NO_LEGAL_PLACEMENT`。核心 4 栋完成，市场 2 栋且已形成 WEST 直轴；市场第三栋 `plains_small_house_1` 返回 `D4_ARRAY_COUNT_UNSATISFIED`，本轮未生成新 preview。 | 工具无可修正参数，结束本轮 AgentLoop，登记 `CITY-PW-20260821-03`。 |
| 5 | `gis_status` | 成功；玩家位置未变。 | 未 TP、未改世界。 |

*首次结果*：暴露非法槽位未跳过；修复后继续如下。

## 修复后结构链

| 序列 | 工具与关键参数 | 返回摘要 | 修正/结论 |
| --- | --- | --- | --- |
| 6 | `city_compile_d4_blueprint`；修复 illegal-slot cursor 后 | 成功；18 anchors、quality 100；market 4/4。slot 2 等非法槽位记录 `skipped_illegal_slot`，未移动到 cell center。 | 检查正式 preview、朝向和 street artifact。 |
| 7 | 读取 D4 正式产物 | market 主楼 `NONE/WEST`；左排 `CLOCKWISE_90/NORTH`，右排 `COUNTERCLOCKWISE_90/SOUTH`，alignment 全 1；street bounds `7×83`。 | 门脸朝街、连续直轴成立。 |
| 8 | `city_run_workflow skipExisting=false` | `awaiting_site_review`；误触发重建 D3。 | 改用 `skipExisting=true`；仍因评审 identity 失效停住。 |
| 9 | `city_review_d3_site` | 接受当前同点位；发现误重建 D3 hash 改变。 | 从备份 Context 精确恢复原 D3 raw package，hash 回到 `3b5d6b...`，再次 review。 |
| 10 | 多次 `city_prepare_d4_blueprint_context` / `city_submit_d4_blueprint` | 修正 stale Context 与已消费 claim；通过增加未使用目录版本标识得到新 Context，固定使用已成功 generation seed。 | 最终 Context/Blueprint accepted。 |
| 11 | `city_run_workflow skipExisting=true` | D5、D6 成功；LandUse 依次暴露 debug Foundation `maxJoin=16`、全城细桥和 SurfacePrint disabled。 | 按返回修目录 maxJoin/SurfacePrint；实现 Foundation 局部多组件和 LINEAR platform bounds。 |
| 12 | 最终 `city_compile_d4_blueprint` + `city_run_workflow skipExisting=true` | D4 18 anchors/100；D6 locked，18 structures，source AnchorMap 含 street/platform；LandUse quality pass、无 warning；workflow `waiting_for_confirmation`，`worldSaveRequested=false`。 | 结构、街带、局部台基正式链通过；未执行 D5 激活。 |

## 景观路隙链

| 序列 | 工具与关键参数 | 返回摘要 | 修正/结论 |
| --- | --- | --- | --- |
| 13 | `city_plan_land_use`；既有 `city_rtf0810_tradefields_03` | 旧 Blueprint authority 非当前 accepted v0.6。 | 改用 workflow 刷新 authority。 |
| 14 | `city_run_workflow skipExisting=true`；tradefields | 旧 `city_blueprint.v0.10` 缺 `arrayCompositions`，迁移 v0.11 后可接受；新 D4 又被旧 commercial capacity 2/22 阻断。 | 不继续改旧结构样例，改用已通过全链的 salt 上游构造 current-schema FARMLAND 验收。 |
| 15 | `city_prepare_d4_blueprint_context` / `city_submit_d4_blueprint`；新增测试用 FARMLAND profile 与 `CULTIVATED PATCH + GROUND_PATH CORRIDOR`，3 Parcel | Context、Blueprint accepted；D4 18 anchors/100。 | workflow 首次到 SurfacePrint 后提示缺 `channelBankBlockId`。 |
| 16 | 新 Context 补 `channelBankBlockId=minecraft:dirt`；再 submit/compile/workflow | workflow `waiting_for_confirmation`，LandUse quality pass、无 warning；Parcel 面积 823/956/371。 | parcel_02/03 均为 `PARENT_PARCEL_ROAD_GAP`；父格到起点距离 2。 |
| 17 | 检查 AreaPlan owner | 路隙中间格 `(-3174,-843)`、`(-3160,-857)` 均未被 Parcel claim。 | 原地表自然路隙成立；未改世界。 |
| 18 | `gis_status` | `Dev` 仍在 overworld `(-4189,78,-416)`。 | 全程未 TP；Y 自然变化 1 格；未确认世界修改。 |

*正式产物*：

- `realm_w_mszypml0_fd99c6f2/city_test_runs/city_realm_salt_kingdom_0_capital/steps/d4/structure_anchor_cluster_preview.png`
- `.../steps/d4/structure_anchor_plan.json`
- `.../steps/d6/structure_materialization_plan.json`
- `.../steps/land_use/land_use_plan_trace.json`
- `.../steps/land_use/land_use_preview.png`

*最终结果*：整案结构街带、合法朝向、非法槽留空、局部台基与地形景观路隙均取得正式可判断结果。未执行 `confirmWorldMutation=true`，没有 D5 激活或方块写入。
