# 多池与规整小组合外扩

*需求入口*：City逐区设计与地形保护落地-v0.1 的最新外扩修订。用户要求同功能区配置多个池后 roll；算法按代码固定规整优先级，GRID 常见、COMPACT 靠后；外扩小组合自带局部空间关系。

*修改内容*：fillPools/structurePools 加权配置及单池兼容、严格互斥与正数权重校验；初始填充逐次选池，连接和比例外扩按组合选池。固定 GRID、LINEAR、COURTYARD、COMPACT、ORGANIC_COMPACT 顺序；组合原子提交，内部道路和真实门口接续同时验证、接入既有道路并预留给后续扩张。模板数量上限沿用选中池规则，计数跨全组阶段累计。新增组合不搬动旧建筑，不继承原 organic 间距去错误拒绝 GRID。同步案子、契约、MCP/Provider schema 和模型指令。

*验证结果*：多池 Codec、权重可复现与频率、GRID 小组合巷道、既有道路接入、堵门整组拒绝及编译集成针对性回归通过；MCP 11 项通过。全量 Java 918 项：908 通过、10 条件跳过、0 失败；test/build 成功。最终首都副本重放与正式预览渲染通过：共 327 栋，外扩 35 个组合/194 栋，其中 GRID 188、COMPACT 2、ORGANIC_COMPACT 4；4 主路全通、无外扩道路越界、无道路与建筑碰撞；剩余 2 个原有入口告警。两仓 diff --check 通过。

*剩余问题*：未运行新的 GLM 设计或完整 RTF 实景验收；首都副本仍有 2 个原有连接扩张建筑的入口告警，未冒充完整城市质量通过。旧 live save 未改动。

证据：`java-test-summary.json`、`full-verification.log`、`mcp-tests.log`、`capital-replay-summary.json`、`capital-replay.log`、`capital-preview.log`、`首都外扩预览.png`。新构建产物 `build/libs/geomantia-0.1.0.jar`。
