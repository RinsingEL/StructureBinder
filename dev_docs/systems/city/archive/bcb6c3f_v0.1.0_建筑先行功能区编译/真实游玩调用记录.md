# 三区真实游玩调用记录

*运行环境*：2026-08-26 13:54 后 `runClient` 正常启动并自动载入 `RTF0810`，`127.0.0.1:5000` 服务可用。复用当天正常 W/T 选城流程产生的 `runId=normal_three_zone_20260826_0935`、`citySeedId=city_realm_salt_kingdom_0_capital` 和身份仍匹配的冻结 D3。

| 顺序 | 工具与关键参数 | 返回摘要 | 修正理由 / 结果 |
| --- | --- | --- | --- |
| 1 | `realm_status` | `ok=true`、`serviceReady=true`、`gisReady=true` | 确认真游戏与调试服务已就绪。 |
| 2 | `city_compile_d4_blueprint`，复用已接受的三区 Blueprint | `ok=false`、`previewCompiled=true`、`compilationAcceptance.passed=false` | 正式失败，不进入 D5/D6，不得宣称验收通过。 |
| 3 | 检查正式 D4 trace | 行政区 7 栋、商业区 2 栋，required 均齐；农业区仅风车 1 栋，两栋 required household 缺失；三区功能区均由已提交建筑/景观 claim 形成 | 建筑先行的功能区形成顺序已在真实输出中生效，但农业 required 落位仍失败。 |
| 4 | 检查 selection 与视觉验收 | household 两组各尝试 11 个 exact slot，均 `D4_ARRAY_COUNT_UNSATISFIED`；slot 1 另有 `OCCUPIED_ENVELOPE_OVERLAP`，slot 2 另有 `GROUP_CONNECTIVITY_GAP_EXCEEDED`；视觉 hard block 为行政 courtyard 基础环不完整、农业 compact 巷道缺失/断开 | 当前真实阻断不是“面积不足”，而是 compact required 候选/阵列合法性没有产出可提交位置，并连带导致巷道验收失败。 |

*正式产物*：`run/realm_debug/normal_three_zone_20260826_0935/city_test_runs/city_realm_salt_kingdom_0_capital/steps/d4/city_generation_compile_trace.json`；本轮时间为 2026-08-26 14:00:59。

## 2026-08-27 COMPACT 修复复验

| 顺序 | 工具与关键参数 | 返回摘要 | 修正理由 / 结果 |
| --- | --- | --- | --- |
| 1 | 重启 `runClient`，自动载入 `RTF0810` | `127.0.0.1:5000` 服务就绪 | 确保真实运行加载本次 COMPACT 实现。 |
| 2 | `city_compile_d4_blueprint`，`runId=normal_three_zone_20260826_0935`、`citySeedId=city_realm_salt_kingdom_0_capital` | `ok=false`、`CITY_BLUEPRINT_COMPILED_ANCHOR_FINALIZATION_FAILED`；总览及三个 Group 局部图路径均返回 | 复用最高有效 Blueprint/D3，只重跑 D4；当前失败仅来自行政庭院，不代表农业修复失败。 |
| 3 | 检查正式 anchor plan 与 compile trace | 农业 windmill、household_01、household_02 共 3 栋全部 committed；方向 `FORWARD/RIGHT/BACK_LEFT`；`allRequiredStructuresCommitted=true` | 水中方向失败后成功换角，没有进入远端 S 形槽。 |
| 4 | 检查正式 quality 与农业局部图 | 农业 `alleySegmentCount=5`、`alleyConnected=true`、`frontagePassed=true`；商业街带存在且连通；唯一 hard block 为行政 `COURTYARD_BASE_RING_INCOMPLETE` | 农业 COMPACT 原阻断真实复验通过，整城仍因行政独立问题停在 D4。 |

*正式产物*：`run/realm_debug/normal_three_zone_20260826_0935/city_test_runs/city_realm_salt_kingdom_0_capital/steps/d4/structure_anchor_group_agriculture.png`；同目录包含总览、行政和商业局部图。
## 2026-08-26 16:58 三区真实复验：Patch 仅作起点、功能区范围渲染

### 调用与纠正

| 序号 | 动作 | 关键参数 | 返回 / 纠正 |
|---|---|---|---|
| 1 | 启动真实客户端 | `./gradlew.bat runClient` | 客户端完成加载，HTTP 服务监听 `127.0.0.1:5000`。 |
| 2 | 查询 Realm 状态 | 初次误用 `POST /realm/status`、`POST /realm/realm_status` | 分别返回 405、404；纠正为 `GET /realm/status` 后返回 `ok=true`、`serviceReady=true`、`gisReady=true`。这两次属于调用路径纠正，不计为产品失败。 |
| 3 | 编译同一三区案例 | `POST /realm/city/compile_d4_blueprint`；`runId=normal_three_zone_20260826_0935`；`citySeedId=city_realm_salt_kingdom_0_capital` | `ok=true`；正式 trace 的 `compilationAcceptance.passed=true`、`previewCompiled=true`；质量报告 `passed=true`、`score=100`、`hardBlocks=[]`。 |
| 4 | 核对三区正式产物 | 读取本次 D4 trace、quality report 与局部预览 | 农业区 `3/3`、行政区 `8/1`、商业区 `4/1`，三个功能区的必需建筑均已落地。 |
| 5 | 核对行政区跨 Patch 连续生长 | 行政首栋与后续提交记录 | 首栋 `admin_required_001` 落在蓝图首选 `p.367`；后续均标记 `continuous_growth_from_selected_patch`，其中 `admin_fill_004`、`admin_fill_007` 已落入相邻 `p.419`。证明 Patch 只约束首栋起点，不再作为后续功能区硬边界。 |
| 6 | 核对预览 | 总览及 agriculture/admin/commercial 局部图 | 总览和三区局部图均生成；图例出现 F1/F2/F3 功能区，功能区范围以半透明着色和虚线边界绘制。失败尝试位置也保留；商业区局部图包含 245 个失败尝试标记，信息完整但视觉较拥挤。 |

### 正式产物

- 目录：`E:\Mod_Dev\StructureBinder\run\realm_debug\normal_three_zone_20260826_0935\city_test_runs\city_realm_salt_kingdom_0_capital\steps\d4`
- 总览：`structure_anchor_preview.png`
- 行政区：`structure_anchor_group_admin.png`
- 农业区：`structure_anchor_group_agriculture.png`
- 商业区：`structure_anchor_group_commercial.png`
- 编译轨迹：`city_generation_compile_trace.json`
- 质量报告：`quality_report.json`

### 本轮结论

- 真实客户端三区编译正式通过，质量分 100，无硬阻断。
- 行政区已验证能够从指定 Patch 的首栋起点向相邻 Patch 连续生长，旧的“指定 Patch 外拒绝落地”语义已消除。
- 功能区范围图层已随总览和局部图一并渲染。
- 商业区局部图的失败尝试数量较大，可读性问题作为本轮可视观察保留，未在真实游玩 Agent Loop 内进入源码诊断。
## 2026-08-26 18:09 LINEAR 单候选与街道前沿搜索真实复验

- 入口：`POST /realm/city/compile_d4_blueprint`
- 参数：`runId=normal_three_zone_20260826_0935`，`citySeedId=city_realm_salt_kingdom_0_capital`
- 返回：`ok=true`；墙钟耗时 `33.844s`；`compilationAcceptance.passed=true`；质量 100。
- 三区结果：行政 8、农业 3、商业 4，必需建筑完整。
- 商业优化前后：selection `82→10`；raw failed position records `122096→18`；唯一失败世界坐标 `85→6`；compile trace `63991603→348954` bytes。
- 商业局部图：`structure_anchor_group_commercial.png`，只绘制 6 个聚合失败位置；5 个为水体、1 个为阵列范围上限，功能区和四栋建筑清晰可读。
- 结论：精确槽位不再按 519 个 Patch 复制求值；每个 LINEAR 模板从当前街道前沿开始，理论位置失败后才微调，前沿附近均无解才停止。正式结果与质量未回退。
