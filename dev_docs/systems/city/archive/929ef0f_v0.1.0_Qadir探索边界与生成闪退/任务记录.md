# Qadir 探索边界与生成闪退

*需求入口*：用户实测 validation_03，向东找到城市后被提示不在可探索范围，并于 2026-09-06 15:09:33 闪退；本轮只读诊断，不修改游戏、实现或存档。

*修改内容*：仅登记诊断证据。

*验证结果*：
- 现场 run 为 provider_e88e62d43d1875ba_r8192，city_realm_qadir_capital。D5 于14:53:18到达 waiting_for_generation，真实执行4929ms。
- 原截图玩家(-3640,4712)，最近登记建筑(-3351,4995)，约404格；不能用该处村庄证明城市落地或判定自然生成来源。随后用户向东找到城区。
- 实际注册83栋，首次读取 placement ledger 有30栋 worldMutationApplied=true；不等于全城完成。
- PlanningAreaAccessPolicy.loadSnapshot 只采用 citySeed.anchorBlock=(-3472,4752)、planningRadiusCells=3、W cellStepBlocks=128，以384格圆作为 CityArea；没有读取编译后建筑/用地范围。83个建筑anchor中64个在圆外。该几何错位已确认；用户被拦的精确坐标没有记录，不能认定具体一次判定的 reasonCode。
- CityDesignQueue.normalize 将 waiting_for_generation 计入 completedCount，无当前项时队列标记 completed；地图“已完成1”是规划队列口径，不代表世界生成验收。
- crash-2026-09-06_15.09.33-server.txt：Exception generating new chunk，区块(-177,317)。直接异常 CITY_LAND_USE_LEDGER_PHASE_COUNTS_INVALID，CityLandUseWorldgenRegistry.phaseCounts:780，经 applyForChunk:370/318 逃逸到区块生成任务，导致服务端崩溃。
- phaseCounts 在执行器已返回 APPLIED 后重新推算 BASE/CROP/BOUNDARY，preparedBase<0 或 prepared!=applied 时直接抛异常。APPLIED 工厂让 prepared/applied 相等，因此现场对应 preparedBase<0。代码中 preservedFoundationColumns 会跳过显式boundary但不递增 occupiedBoundarySkipped，登记方却按原boundary总量倒推，存在可确认的口径缺口；现场具体哪些列触发仍需重放证明，不能把该候选机制写成唯一最终根因。
- 最新日志另有 CITY_LAND_USE_ACCESS_PLATFORM_MISSING、rollbackComplete=true；此前已有同类故障记录，这不是本次致命异常码。

*剩余问题*：探索范围应对齐实际已放行城市几何；计数应来自执行器真实准备/应用结果，不能事后猜测，且写后记账失败不得无保护地击穿worldgen；需专项重现与实机复验。未修改代码、未修复存档、未宣称全城验收通过。
