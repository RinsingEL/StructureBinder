# 城市编译可靠性修复

*提交*：实现 `eded826`；契约仓 `018454c`。新基面离线重编译已通过，实机落地验收仍待进行。

## 连续基面与整体定高（9月7日最新确认）

用户要求距离够的台面完整相连、每个大台面统一高度。现定位Foundation规划允许thin-bridge拆回逐栋，实档16个分量；后续按owner局部定高保留119格44低台。修改方向：近距建筑组生成完整建设包络，不再静默拆分；规划阶段按完整基面组件冻结高度，执行只裁切且保留现场安全检查。农田/显式景观覆盖关系保留。旧已施工块不覆盖冒充新效果；用户负责走动验收。

实现：删除原薄桥拆回逐栋的休眠算法；近距组外扩后凸包填满。SurfacePrint uniform recipe 新增可选 platformSpans，整域 BFS 定高、codec/hash 冻结、owner 裁切读高；主干道优先冻结基面高。执行不再以局部坑底重新投票；超安全土方不能静默 PRESERVE 留洞。旧 recipe 无字段保持兼容。

验证：全量 test/build 一轮成功，随后最后道路/codec 改动的 CityFoundation*、CityLandUse* 聚焦 test/build 成功。新增 44→68 跨负区块深坑、分离台面、输入顺序稳定、冻结高度 JSON/hash 回归。复制正式 GLM 冻结产物到 build/playtests/foundation-replay-20260907，以正式 Outdoor handler 重编译成功（3033ms）：foundationComponentCount 16→1，最终基面 70013 格，所有 platformSpans.targetY=68，显式农田/树林仍保留。景观容量轻微缩减仍为 warning，非硬阻断。此为离线正式输入重放，不是实机落地验收；原世界没有改写。测试启动过程中纠正本机默认 JDK11 和相对 debugRoot 的工具参数错误，未修改产品校验以绕过。

用户实走触发黑暗/弹回：上轮仅验证安全东入口，不足以保证城内通行。此次将未完成城市本体保留最高优先级；已连通城市activated footprint优先于邻城视距辅助buffer，域外仍受buffer保护。新增实测坐标沿路逐格回归及本体/域外反例。待实机沿路复验。

*需求入口*：用户授权修复道路避让、连接后核心冻结与占比补足、局部修订保留、作者素材重复控制，并要求真实游玩测试。

*修改内容*：内部骨架完整横截面有限绕行全部建筑，道路预留含路缘；核心跳过百分比补建筑，连接面积计入缺口，旧不停扩张开关不再抬高目标；景观不再吞掉建筑缺口和留空份额。新增作者池可选单结构单组上限及跨阶段最少使用优先，未配置仍不限，不按功能名称猜限额。近邻启动阈值可配置，显式 FAR 等关系优先，自动近邻扩张不改写作者关系。可选连接不足仅警告，显式 HARD 道路/关系及真实碰撞仍硬阻断。现有局部输入 patch 已存在，此前口头分析遗漏；此次新增精确请求/状态/占用依赖的成功候选检查点，变化失效、最终安全必查，不宣称整组完全锁定。原档未动。

*验证结果*：849 项全量测试 0 失败、7 跳过，之后补充可选连接语义与说明，聚焦回归收尾中。原新档冻结方案拷到 build/playtests/reliability-replay 重放：首轮 172.8 秒安全通过（60 anchors）；排查慢点是每候选检查点重复序列化完整 GIS 与 sourcePatches 二次复杂度去重，改用冻结 context 覆盖 GIS、HashSet 去重后复跑 35.2 秒，66 anchors、finalQuality=true/hardBlocks=[]。两轮之间还更新了景观面积算法，不能仅凭两次总耗时断言性能倍率。原方案完全没改，未激活世界。

打包测试环境在 build/playtests/packaged-20260906，与 PCL 同组 8 个模组，新的本地测试身份 RTFAcceptance、API 5001、GLM-5.3-flash/Hermes。独立新档 RTF_packaged_fresh_20260906，seed=690609062126；只从原档读取 worldgen/pack 设置并写出新的 level.dat，重置初始化、时间和玩家；没有复制区块、realm_debug、data、玩家数据。沿用 RTF preset datapack。测试前世界只有 level.dat 和 preset zip。配置复用开发端现有凭据（不输出），测试目录单独启用 Provider，Hermes profiles 隔离，runtime 使用开发端安装目录 junction。PCL 原配置和原世界、开发 provider.enabled 均未改。

准备阶段修正两项调用错误：Java classpath 文件是逐行列表，须 join 分号；单 zip Copy-Item 到未创建目录会把文件重命名为 datapacks，已在独立测试世界内恢复其正确目录/名称，原件未改。不是产品故障，不算游戏验收。

## 打包新档第一轮现场（2026-09-06 21:21—21:28）

正式 jar SHA256=49625D452D5D620F8FA9902888C5DA871B3A828C5DCB03B8AC60332CF2603287，Forge 47.4.23，实际进入独立新世界。W 扫描 11 秒。GLM/Hermes 自行生成 3 个国度，T2 完成 3/3，T3 已产出，当前国度 T4 登记 3 城市；未由测试 Agent 代提交设计。

21:25:09 宿主自动请求 city_plan_d3(runId=provider_a0cb8200ee_r8192, citySeedId=city_frostpine_pinewood, cellStepBlocks=16, preferGeneratorNativeTerrain=true)，21:25:10 推进到 patch_explorer_show_candidates，21:25:15 正式停止：PLANNING_HOST_BLOCKED: PATCH_EXPLORER_TYPE_NOT_IN_SCOPE: unknown。未到 D4，不能把本轮称为道路或城市编译验收失败。运行线程快照显示服务器与 Provider 空闲等待，非主线程死锁。本轮游玩结束并进入原授权范围内诊断，根因待定位。

实际 MCP 只读调用记录在 run/codex_logs/packaged-fresh-tools.json、packaged-fresh-status-01.json、packaged-fresh-queue-01.json；后者在城市队列建立前查询返回 CITY_DESIGN_QUEUE_REGISTRY_NOT_FOUND，未无变化重试，不记产品 Bug。截图 build/playtests/packaged-20260906/entered.png、map-progress.png、agent-progress.png；正式 D3 请求与产物索引在测试世界 realm_debug/provider_a0cb8200ee_r8192/city_test_runs/city_frostpine_pinewood/test_run_manifest.json。启动时 Quick Play 停在世界列表，通过界面选中已有独立新档进入，未要求用户另建档。

根因与修复：hostArguments 曾将 D3 周边所有 patch 类型合并；现场 scope 实际只含 cliff/plain/ridge/shore/slope/valley/water，而报告还含 unknown/terrace。新增 PatchExplorerService.initialPageRequest 从同一冻结 scope 的 typeCatalog 构建首页，宿主不再读取 D3 类型并集，不重开会话、不改筛选硬校验。对应 PatchExplorerServiceTest + PlayerProviderAgentRunnerTest 共 16 项通过，打包成功（44 秒）。客户端正常关闭，所有维度区块保存完成。正在全量回归，之后替换独立测试目录 jar，复用未受修改影响的 D3 上游重新进入游戏；该续跑不冒充又一次全新建档。

全量回归 test build（包含本次 D3 修复和前述全部城市改动）850 项，0 失败、0 错误、7 跳过，1 分 52 秒成功。21:35 左右仅替换隔离测试目录 jar 并启动客户端 PID=1896，继续同档最高有效 D3 上游进入第二轮 Agent Loop。

## 第二轮同档续跑（21:40—21:46）

新 jar SHA256=4E001E3CD5B5ED48966DE44A00829EA39DEF83B375A252643A096AE48D586023。启动经 ReTerraForged ResourcePackInfo 警告页进入主菜单再选同档；键盘焦点误打开 latest.log 记事本，已关闭这一个只读窗口并恢复游戏，未编辑日志。21:40:43 原 D3 会话自动完成 PATCH_REVIEW_COMPLETED，interestTypes 仅实际七类，未重开会话。21:41:00 起 GLM 自行 D4，先修正字段/未知目录引用/景观语义错误，再收到一次 required structure 无合法位置；第二版编译成功。测试 Agent 未代改蓝图。

MCP packaged-fresh-queue-02/03.json、packaged-fresh-auto-01.json 保存状态请求结果（runId=provider_a0cb8200ee_r8192，citySeedId=city_frostpine_pinewood）。最终有效返回：D4 3776ms 成功，D5 107ms 成功，D6 50ms 成功，city_plan_land_use 2561ms 失败：RELAY_GROWTH_CANDIDATE_RETRIES_EXHAUSTED:RELAY_GROWTH_BACKTRACK_BUDGET_EXHAUSTED:region-001-GREEN。队列 reasonCode=POST_D4_WORKFLOW_UNEXPECTED_STATUS/status=blocked_by_program。本轮已获得明确程序失败结果，结束 Agent Loop，按既有修复授权进入景观问题诊断。不能宣称已实际落地。

景观定位：复制冻结 run 到 build/playtests/outdoor-replay；OutdoorProbe 离线调用正式 outdoor handler，不碰活档。第一次 Java source launcher 因 package-private 的类加载器边界报 IllegalAccessError，改为 javac 输出 probe-classes 后按类名启动，不是产品 Bug。原版重放 4683ms 复现，SearchExhausted 携带不可变 Request 供导出重放。移除快速连通性缓存作对照仍失败，已还原缓存。实际为 64 格牧场、GREEN 56/GROUND 8 的顺序贪心搜索陷入死路。新增两角色反向小补集有限求解，再按原始 source 和原阶段顺序恢复真实邻接 trace；精确占比、mask 和双方连通不变。另修正末尾只剩 1 格的接力判断。修复后同蓝图整套 LandUse 5306ms 成功，skippedLandscapeCount=0；既有容量警告仍保留（牧场 127/128、农田 759/768），不是静默抹去质量问题。新增真实 mask 与单格末尾回归。第二客户端请求正常关闭以替换 jar，准备第三轮正式续跑。

景观相关 125 项聚焦回归全部通过、build 成功（1m15s）。第三轮 jar SHA256=5EA3EEEEAF9E1EC84A4795AE80F9747DEF136EDEB0CF16375F329C5896EFFB65，只部署隔离测试目录，启动 PID=33184。第二轮客户端所有维度保存成功；第三轮进入正式 Agent Loop，复用有效 D4/D5/D6，重新执行失败的 LandUse 下游，不替换 AI 设计。

## 第三轮正式后半段复验（22:03—22:08）

city_post_d4_auto_compile_retry 同 run/city 正式入队 attempt=3（packaged-fresh-retry-01.json），随后 city_post_d4_auto_compile_status 返回 waiting_for_generation / WAITING_FOR_GENERATION / active=false（packaged-fresh-auto-02.json）。GLM 蓝图未改，修复后完整后半段成功。

city_execute_d7(executeStructurePlacement=true, playerName=RTFAcceptance) 两次返回 waiting_for_worldgen、41 栋 WAITING_FOR_WORLDGEN、0 failures，237 个 LandUse owner queued、appliedAfterCount=0（packaged-fresh-d7-01/02.json）。正式调试切 spectator 并请求 tp -6260 160 3630；命令返回 executed 不能视为成功到城，日志随后显示区域未开放，gis_status 仍为原位置31,90,59（packaged-fresh-visit-01.json、packaged-fresh-paused-01.json）。

实地验收当前受环境暂停阻塞：两次 time query gametime 间隔至少12秒都为36335（packaged-fresh-paused-01/02.json），服务器日志有 Saving and pausing game；窗口前台验证持续失败，未继续盲发按键。已向用户请求点游戏回前台，暂无回复。本轮 Agent Loop 因需用户恢复游戏输入/解除暂停停止，不能称为实际落地验收通过。保留运行客户端 PID=33184 和现有世界，不关闭或更换原 PCL 游戏。后续恢复后先确认 gametime 推进，再 D7 轮询、TP 与地面截图；不得重复 GLM 设计或重跑有效上游。

## 用户恢复后的正式失败（22:09 起）

用户反馈已点击游戏但“已完成 1”仍无城市标记。实际 city_design_queue_status / city_execute_d7 / gis_status 请求返回保存在 run/codex_logs/packaged-fresh-resumed-01.json：首城 waiting_for_generation，第二城 needs_agent / D4_HUMAN_REVIEW_REQUIRED；D7 返回 CITY_LAND_USE_D7_OWNER_INCOMPLETE。正式 steps/d7/land_use_owner_completion.json 为 failed，237 owners 仅完成 4，失败 owner (-391,217) 原因为 CITY_LAND_USE_ACCESS_STAIR_UNRESOLVED，rollbackComplete=true。placed_structure_ledger.json 为空；日志存在 StructureStart 注入但不能据此宣称建筑完成。玩家仍在出生点附近。恢复后已有真实程序失败，不再归因于暂停。本轮 Agent Loop 保存有效调用与报告后结束，进入台阶生成和地图完成状态诊断；不重做 AI 蓝图。

源码诊断已确认两个独立展示因素：CityDesignQueue.normalize 直接将 waiting_for_generation 累加到 completedCount，地图原样使用，所以“已完成”不是实际生成完成；地图网络请求默认以玩家为中心半径4096，StatusReader过滤视窗外节点，首城(-6288,3600)在出生点默认视窗外。Debug图层仅绕过未揭示过滤，不取消服务端视窗裁切。用户当前缩放未取得截图，不能断言其实际视窗；节点不是在registry中被删掉（3个seed仍在）。台阶失败来自MicroGrader.platformAccess的直梯/折梯搜索，具体候选失败仍待现场还原。

用户追加要求配置自动进测试存档：launch_packaged.ps1 默认指向隔离测试目录与 RTF_packaged_fresh_20260906，验证 level.dat 存在及单目录名称。已有 --quickPlaySingleplayer，但 Forge completeModLoading 在警告页提前返回，跳过 QuickPlay；仅隔离测试 config/forge-client.toml 设置 showLoadWarnings=false，警告仍写日志、error 仍阻断，原 PCL 不变。待下次替包启动验证，不重复启动占用当前存档。

22:25 诊断包仅增加可选失败输入采样（不修改生成结果），编译成功；旧进程正常保存退出，PID9920 使用无参数 launch_packaged.ps1 实测自动进入原档，gis_status 验证在线。隔离 options pauseOnLostFocus=false，避免失焦自动暂停。city_execute_d7 入队后再次得到同一 owner incomplete，实际输入保存在 landuse-failure-captures/owner_-391_217.json；调用 packaged-stair-capture-01/02.json。有效失败已复现，本轮游玩结束，转为真实输入离线诊断。

真实采样离线 AccessProbe 稳定复现：当前 owner 193 列正常，周围 6 个 chunk 的694列全部为surfaceY=-65（空气），量化形成-64平台与68高台。这次具体根因是 D7只等待owner FULL，随后 ServerLevel.getHeight 对未FULL邻块返回minY，缺失数据被当成真实地形。修复为异步等待mask halo16+reference3所需2chunk半径邻域FULL（每tick仍最多一个owner动作，不同步join未完成future），ServerLevel采样另加邻块就绪检查，阻止假高度再次进入。CityLandUse聚焦test与build成功44秒；准备同档复验，尚不宣称解决真实落地。

22:32 修复包7E3C2CCA...同档复验：packaged-stair-fixed-01/02.json，D7 applied从4增到21，但(-391,217)仍失败，另有worldgen(-391,219)真实正常高度下ACCESS_STAIR_UNRESOLVED；新采样owner_-391_219.json。第一项缺失高度修复不足以使城市落地，本轮有效失败结束，继续新样本诊断，不跳过通路检查。

正常高度的新采样复现第二根因：1格64低台与981格68高台都借同一building footprint完整面积达到保留门槛，导致1格低台被强制保留且无法建4级台阶。现按每个高度组件实际接触格数分摊建筑面积，不重复全额支持，1格低台正常并入邻台。真实owner采样加入src/test/resources/city/landuse-access-owner-minus391-219.json，新增CityLandUseCapturedAccessTest断言无FAILED、1格台被合并、结果确定性。CityLandUse专项与build全部通过46秒。地图计数措辞改为“规划就绪/待规划”，不再称为已完成城市。

全量853项0失败7跳过，2m2s成功。22:39同档正式复验packaged-stair-weighted-01/02.json：D7推进到92/237后新失败(-395,223) ACCESS_PLATFORM_MISSING；采样只有1格无purpose台地被撤回，农田入口却被要求寻找foundation。另有(-393,223)、(-393,225)台阶失败采样。有效失败结果保存，本轮结束，继续诊断自然地面入口与台地入口边界。

进一步收敛台地面积规则：分摊建筑面积仍可能将49格狭窄低台保留为独立平台，而实际台阶只能写入grading格，不能写入已排除building footprint。因此容量仅计实际component格数；小台相邻则合并，孤立且有purpose者保留而不撤回。入口周围没有最终人工平台时明确记录NO_ARTIFICIAL_PLATFORM / NATURAL_TERRAIN_UNMODIFIED（不宣称自然入口通达性验收），避免为原本自然地面强制造台阶；实际存在平台的通路失败仍阻断。新增(-395,223)及(-393,225)实测输入回归，两项和原专项全部通过，build47秒。准备继续正式D7。

22:48 同档D7 packaged-stair-final-01/02.json推进142/237后(-395,225)仍STAIR_UNRESOLVED，另有新采样(-395,227)。保存有效失败结果，结束本轮继续诊断，不把局部进展当作验收通过。

## 用户确认单侧步梯与主路高侧下挖

按最新示意图实现：双侧步梯分支分别判定，只有一侧有空间时保留该侧；入口直梯/侧梯仍放不下时，在人工基面范围内尝试向高侧内移梯口，限制下挖12格且不进入建筑footprint。新增真实(-395,225)、(-395,227)输入回归，入口均返回ACTIVE_STAIR/ACCESS_CUT。

主路新增CityRoadGradeProfile整段纵断面求解，优先每3格升1格、空间不足尝试每2格升1格；端点/交叉口/入口附近固定于冻结地形高度，不改变路网拓扑。SurfacePrint可选targetSurfaceY参与哈希并贯穿owner裁切和执行，执行器按同一高度清空高侧、填实低侧；不再由各chunk独立估坡。不可行路段当前警告并保留旧处理，尚未实现自动绕行；入口固定值仍来自冻结D3采样，不等价于实测门槛高度。旧计划没有该字段，不会自动获得新主路坡度。

专项CityLandUse与CityRoadGradeProfile及build通过（41秒），包括145格跨区块主路冻结、坡长不足保护固定点、实际写入下挖/填充和5个实测owner输入。准备同档正式D7续验：此轮可验证新入口台阶，不能当作新主路坡度的全新规划实测。

23:25正式续验PID19804自动入档成功，packaged-high-cut-01/02.json：D7仍142/237，(-395,225)同类失败。旧采样离线ACTIVE_STAIR不足以代表当前活档输入；结束本轮游玩，继续重新采样诊断。报告中22栋planned、19栋waiting并非整城完成。23:27旧采样保留为.pre-cut.json，正常退出并同包重启PID33568，packaged-high-cut-capture-01/02.json复现同错误，成功取得新现场输入；本轮游玩结束，进入对比诊断。

新旧(-395,225)样本对比确认220列surfaceY改变，多列天然stone/andesite变为stone_bricks/stone_brick_wall（例如-6291,3611从55到69）。动态整地读取了已经施工的邻域成品，导致同一owner的台地分组和台阶可行性发生漂移；新样本离线稳定FAILED。需要将整地的设计输入与现场写入安全采样分离，不能仅加台阶候选解决。当前未完成这项更深层修复，也未对旧档假报成功。

本轮最终全量test/build成功（2m17s）：863项、0失败、7跳过；git diff --check通过。隔离游戏23:31正常退出并所有维度保存，原PCL未改。最新源码包额外加入固定高度只允许道路地表的字段校验；此校验包没有再次实机启动。无提交。全量绿灯不改变上述实机失败结论。

## 继续修复施工顺序反哺（用户要求彻底解决）

实际视距12chunk对应224格安全buffer，首都保护半径608，城市中心仍落在其中。新增从城市安全侧选择接入点，来源点也使用真实路由终点而非不可到达中心；224格回归与既有保护测试通过（42秒build）。受保护西侧仍禁入，不将全部城市强制释放。

23:45正式packaged-design-terrain-zero-01/02：237/237 owners完成，90个缺失owner全补齐；qualityReport plannedCount41/waiting0/failure0。随后正式tp -6260,110,3630在packaged-design-terrain-visit-01中被探索权限弹回出生区域，不能称为可游玩验收通过；日志明确“目标区域尚未开放”，本轮结束，进入已知城市可进入性诊断，不关闭保护绕过验收。已分离未完成城市保护本体与寻路辅助膨胀域，新增安全末端接入；真实坐标回归及原有不可越过预留墙测试均通过，build43秒，准备正式入城复验。

现场高度与设计高度相等时重算mode=FILL但无需填方，旧shouldFill仍要求目标必须空气，误拒绝天然地表；修正为仅正delta要求填方，专项含天然实心地面固定道路高度测试。另加诊断包占用坐标/实际状态日志。CityLandUse专项/build44秒通过，准备再次同档续验。23:51 packaged-design-terrain-visit-02正式TP仍被弹回；基于默认视距的回归不足，结束本轮核对实际访问配置和运行时视距。

分离ExecutionWorld设计采样与现场安全采样；整地/结构datum共用生成器地形源，不读已施工方块。采样缓存有界262144列，RTF每512格区域校准控制位置固定，避免首次请求的建筑footprint决定后续地形值。现场占用保护不放开，实际填挖方向按现场高度重算但目标不变，超界明确报错。邻区块天然地面变高墙后的整地诊断和写入一致回归通过；CityLandUse与结构放置专项及build通过46秒。开始同档D7续验，不将旧已施工区块当作新实现重建结果。23:41正式packaged-design-terrain-01/02：原(-395,225)台阶阻塞越过，147/237后(-381,226)SURFACE_TARGET_OCCUPIED。RTF校准日志offset=0/residual=0，未观察到逐列getBaseHeight回退；本轮游玩结束，进入新实际占用诊断。

*剩余问题*：实际生成仍待完整通过；邻域已施工地形反哺整地计算导致结果漂移；自然入口未做独立通达性验收；远城地图定位尚未改；第二城要求人工审阅尚未解决。主路新纵断面尚待重新规划后的实地验证。
