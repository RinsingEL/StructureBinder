# 港城入口歧义阻塞诊断与反馈修正

*需求入口*：用户 2026-09-09 港城 D4 宿主阻塞截图及“也行，再改改吧”。

*修改内容*：主工作树修正入口歧义反馈，列出模板、入口 ID/方向及明确宿主修正方式；清除该类错误无效的布局参数建议。必选建筑所有候选均入口歧义时停止换槽位和前缀组合重搜，仍有其他模板候选时保留搜索。通用无合法落点消息改为要求使用具体失败证据，去除笼统扩大范围/保留算法的修正处方。沿用原报错码、宿主故障归属、重试预算和作者入口授权规则。

*验证结果*：gradlew test build 成功（2 分 10 秒），统计 {'tests': 926, 'failures': 0, 'errors': 0, 'skipped': 10}。回归覆盖具体入口信息穿透 submit 响应、入口歧义不换槽位、作者允许任选入口时仍能编译、混合候选仍保留搜索机会。git diff --check 通过。包位于 E:/Mod_Dev/StructureBinder/build/libs/geomantia-0.1.0.jar。

*剩余问题*：本次只修反馈和无效搜索，未改 plains_big_house_1 入口元数据或冻结存档，旧港城仍需解决入口声明才能继续；未部署游戏实例、未提交 Git。原始诊断：两个 WEST 入口，无明确主入口/任选策略，8 次位置尝试均 INTERNAL_FRONTAGE_UNAVAILABLE，格式 4/10、几何 0/5，宿主主动停止。
