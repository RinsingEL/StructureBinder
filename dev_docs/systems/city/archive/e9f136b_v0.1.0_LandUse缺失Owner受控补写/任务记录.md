# 2026-08-28 LandUse 缺失 Owner 受控补写

## 需求入口

- 城市：`city_realm_salt_kingdom_0_capital`
- 现场坐标：`141652,142169`
- 该点属于 URBAN member spans、不在 exclusion，并冻结 `ROAD_SLAB`。
- 当前 SurfacePrintPlan 规划 85 个 owner chunk，成功 ledger 只有 66 个；19 个缺失 owner 中包含多块完整 16×16 城市基面，形成现场裸露原地貌。

## 修改内容

- 普通旧 chunk 继续拒绝回填。
- 只有 `city_execute_d7` 显式世界修改可对当前 exact plan identity 的 ledger 缺失 owner 执行 `CONTROLLED_D7_BACKFILL`。
- 已成功 owner 幂等跳过；失败保留 chunk、reasonCode 与 rollbackComplete。
- 写 `steps/d7/land_use_owner_completion.json`；计划 owner 未全部成功时以 `CITY_LAND_USE_D7_OWNER_INCOMPLETE` 拒绝验收。
- Feature 预检会读取同一事务前置 base mutation；只有同一坐标最后一个前置 mutation 是 `MICRO_CUT→minecraft:air` 时，才允许穿过当前旧地形占用。
- 其他真实占用仍拒绝；replaceable 冲突按 `FEATURE`、`SURFACE`、`CROP`、`MICRO_FILL` 阶段输出独立错误码。

## 验证结果

- `CityLandUseChunkExecutorTest`
- `CityLandUseWorldgenRegistryTest`
- `CityPlanningEndpointHandlerTest`
- 2026-08-29 已重启游戏，在原 run `building_parcel_greenery_20260828_01` 对原城市执行真实 `city_execute_d7`。
- D7 正确以 `CITY_LAND_USE_D7_OWNER_INCOMPLETE` 拒绝通过：官方完成报告为 `plannedOwnerCount=78`、`appliedBeforeCount=59`、`backfilledOwnerCount=0`、`appliedAfterCount=59`、`missingOwners=19`。
- 19 个缺失 owner 全部返回 `CITY_LAND_USE_MICRO_FILL_TARGET_OCCUPIED`，且全部 `rollbackComplete=true`；完整性闸门有效，但受控补写未完成现场修复。
- 新增自动回归：前置 CUT 从 Y=65 降到 Y=64 后，同列 GREEN_PLANT 可在 Y=65 落地；没有 CUT 的同类占用仍以 `CITY_LAND_USE_FEATURE_TARGET_OCCUPIED` 拒绝且零写入。
- `CityLandUseChunkExecutorTest`、`CityLandUseWorldgenRegistryTest`、`CityPlanningEndpointHandlerTest` 专项通过。
- 2026-08-29 完整 `gradlew test` 通过。
- 2026-08-29 使用新代码重启游戏并复用原 run/城市执行真实 D7：`appliedBefore=59`、`backfilled=16`、`appliedAfter=75`、`missing=3`。原 19 个 Feature/CUT 占用失败已越过 16 个。
- 剩余 chunks `(8854,8882)`、`(8856,8883)`、`(8850,8886)` 均返回 `CITY_LAND_USE_BLOCK_WRITE_FAILED` 且 `rollbackComplete=false`，D7 正确保持 `complete=false`。
- 用户已现场查看原裸地所在 chunk `(8853,8885)` 的补写效果并确认“效果挺好的”；该视觉结果通过，不代表剩余三个 owner 或整城 D7 已通过。

## 剩余问题

- `CITY-DL-20260829-02`：定位余下三个 owner 的实际方块写入失败及不完整回滚；修复后继续在同一城市执行 D7。
- 最终验收条件仍为 `complete=true`、78/78、无 missing owner 及现场裸地区域闭合。
- 本轮实现提交：`e9f136b`；剩余故障由 `CITY-DL-20260829-02` 后续任务继续。
