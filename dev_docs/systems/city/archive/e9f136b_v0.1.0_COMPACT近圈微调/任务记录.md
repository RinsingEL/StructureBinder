# COMPACT 近圈微调

*需求入口*：真实建筑地块测试中，住宅 COMPACT 第一圈候选受水面、碰撞和功能区缓冲阻断，程序继续扫描外圈并产生大量 `GROUP_CONNECTIVITY_GAP_EXCEEDED`；用户确认保持半自动、不搬家、不放宽连接间隔，只在近处微调。

*修改内容*：COMPACT 每个理论槽位保留原点，并按确定顺序增加同半径左右近移、左右稍远移共四个切向候选；微调不改变 Patch、阵列核心、最大连接间隔或功能区位置。首栋周围第一圈若没有任何跟随建筑提交，编译器以 `CITY_BLUEPRINT_COMPACT_LOCAL_FRONTIER_EXHAUSTED` 停止本地生长，不再扫描必然断连的外圈。Blueprint trace 记录 `compactMicroAdjustmentEnabled` 和实际候选点，便于后续预览审计。

*验证结果*：`CityBlueprintGroupLayoutPlannerTest`、`CityBlueprintCompilerServiceTest`、`CityStructureLandingFlowTest` 聚焦回归通过；Java 全量 `gradlew test` 通过。测试覆盖八方向优先、每槽五个同环候选、外圈前沿门禁，并复用既有精确候选按序跳过失败点的流程测试。按用户要求未重启游戏，当前验收现场保持原版本。

*剩余问题*：尚未用新区块真实游玩验证本轮微调能否在该住宅极限样本中找到第二栋；等待后续新区块复测。实现提交：`e9f136b`。
