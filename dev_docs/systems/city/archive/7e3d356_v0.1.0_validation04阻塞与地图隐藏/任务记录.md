# validation_04 阻塞与地图隐藏（只读诊断）

## 需求入口

用户报告 RTF_beta_validation_04 再次程序阻塞；随后确认计数是已完成 1、剩余 2，但地图不显示已完成城市。要求检查，尚未要求本轮实施修复。

## 修改内容

用户后续授权修复道路接驳与复用失败验收，继续定位第二城组合槽问题。开发阶段使用冻结输入离线重放，不修改 PCL 原档、不执行激活；地图权限问题暂不纳入本次修改。

仅新增诊断记录和只读探针；没有修改生产代码、配置、存档，也没有执行重试或激活。存档位于 D:/PCL2-2.8.12/.minecraft/versions/1.20.1-Forge_47.4.23/saves/RTF_beta_validation_04，run 为 provider_22d55c3faaf1fd10_r8192。

## 验证结果

- 注册表仍有三城：Millfield Village `(2032,4016)` 为 waiting_for_generation；Ferry Cross `(2160,4144)` blocked_by_program；capital `(2288,5456)` pending。队列 completedCount 直接统计 waiting_for_generation，不是世界落地完成数。
- Millfield：16:55:30 开始编译，16:56:08 D4 终审失败，原因 `CITY_BLUEPRINT_COMPILED_ANCHOR_FINALIZATION_FAILED`；hardBlocks 是 `city_main_road_002::target_transition: ROAD_OVERLAPS_STRUCTURE:g_pasture_required_001`。16:58:36 重试却把 city_compile_d4_blueprint 标为 WORKFLOW_EXISTING_ARTIFACT，继续 D5、D6、LandUse 和激活，16:58:43 waiting_for_generation。D4 quality_report 至今 passed=false。当前 active registry 有该城 39 个 plannedStructures，但 placedStructures、templateFragments 和 LandUse appliedOwners 均为空。
- 首次偏离：workflowBlueprintAnchorMapCurrent 只验证 Blueprint context/hash、提交 accepted 和设计 schema validation.valid，未验证 D4 最终质量；失败终审已留下带 provenance 的 anchor map，于是 skipExisting 把失败中间产物当作可复用成功结果。已有产物不等于本阶段已通过。
- 地图：两城中心距离 sqrt(128²+128²)=181.019 格。未释放 Ferry 的预留区半径至少 `256 + 256 + 128 + ceil(128*sqrt(2)) = 822` 格。PlanningAreaAccessPolicy 先判未释放城 reservation，再判已释放城，因此 Millfield 中心被 Ferry 封锁；普通地图依据 revealedAt 隐藏节点。
- 只读执行当前编译的 PlanningAreaAccessPolicy / AdventurerMapStatusReader，使用实测端已核实配置 enabled=true、initialRadius=2048、firstCityMinimumDistance=3072。按本次登录位置约 `(-200,80)` 和默认 zoom=1 的视口 `(-128,128,4096)`，探针实际输出见 evidence/probe-result.txt：Millfield 的拒绝原因 UNRELEASED_CITY_RESERVED，责任城市 Ferry；快照包含 Millfield 节点，但 revealed=false。未取得用户当时的缩放/Debug 开关，因此不排除视口裁剪另有影响；预留区重叠与隐藏链已经复现。
- Ferry：17:01:05 首次失败 REQUIRED_STRUCTURE_NO_LEGAL_PLACEMENT，g_residential 无合法候选，正常回给 AI 修改。17:01:53 第二次 D4 在 60 ms 内抛出 `CITY_BLUEPRINT_ARRAY_COMPOSITION_SLOT_UNAVAILABLE: comp_ferry_core cannot reserve child Group g_farmstead.`。组合槽规划直接抛异常，绕过 CompilationResult 的结构化失败路由；WorkflowStepRunner 只保存 reasonCode/error，没有 nextAction，自动队列随后兜底为 POST_D4_WORKFLOW_UNEXPECTED_STATUS / program / retry。底层槽位失败究竟是设计约束过紧还是布局搜索不足，尚未做受控编译复现，不能单凭这条异常归责 AI。
- 游戏后来重开，17:55 的队列仍为 1 完成、2 剩余，Ferry job attempt=2 未改变；不是活跃编译线程一直运算。本轮未进行实机 MCP 或写世界测试。

## 开发与离线复验（用户后续授权）

- 道路：入口沿作者门向投影到实际 footprint 外侧，模板内部不写接驳；仅放行所属模板的外部 clearance，保留 body 和其他建筑障碍。过渡段从首格到末格按路面加两侧路缘检查。新增内凹马厩入口及四向入口回归，最终质量门禁保持不变。
- 重试：D4 最终质量报告绑定 `sourceAnchorMapHash`；复用必须同时满足质量通过、map 原文 hash 匹配及 accepted Blueprint 身份。旧版缺 hash、失败报告、缺报告、报告与 map 漂移均不跳过编译。未修改已激活原档计划。
- 第二城：原样冻结输入离线重放先复现 `comp_ferry_core / g_farmstead` 异常；增加有限候选联合回溯和结构化失败后，确认不是单纯前一成员抢位：农舍组旧预算 span=388，500 个候选中 234 越界、266 与已保留中心重叠，没有合法槽。进一步确认 COMPACT 布局已改每环八槽径向，但 `planGroupSpatialDemands` 仍按旧长条队列估算。预算现与实际径向算法对齐，并完整计入模板起始锚点的尺寸外伸；农舍预算变为 249，原组合槽分配已通过。搜索额度耗尽仍属于程序失败，不归责设计。
- 修改后重放首城原 Blueprint：编译 27,693 ms，含最终 StructureAnchorPlanner 复验共 27,724 ms，61 个 anchors，`finalQuality=true / hardBlocks=[]`。这是冻结输入的 D4 离线编译/终审，不是 GLM 新设计、D5/D6 激活或真实 worldgen。
- 修改后重放第二城：组合三组均有合法槽，约 1.4–1.7 秒返回结构化 `CITY_BLUEPRINT_REQUIRED_STRUCTURE_NO_LEGAL_PLACEMENT`，终止项是住宅组 `household_02`。候选拒绝含水域、组内连接距离、占用和组合槽越界；原方案仍未通过 D4，不能宣称第二城完成。此失败沿既有设计反馈路径处理，而不是无 nextAction 的异常。尚未运行 GLM 接收反馈后的真实修订回合，也未证明任意移动中心/槽位后的全局几何无解。
- 初轮全量测试 838 项：一项已有 8 秒耗时断言超 346 ms，当时并行重放城市；功能断言无其他失败。新增四向入口覆盖并关闭并行编译后全量复验：839 项，0 失败、0 错误、7 跳过，BUILD SUCCESSFUL（1m46s）。未放宽或修改该耗时断言。

## 剩余问题

## 18:52 实测再次停在 needs_agent（后续只读诊断）

用户随后授权修复并要求真实测试：修订身份纳入稳定的 post-D4 attempt、恢复 contextId、失败预算 contextId/failureCount；不以轮询更新时间作为新任务。先做重复轮询/新失败/快速失败竞态回归，再用独立存档副本运行真实 GLM/Hermes，原档不动。

实测环境首轮受阻：自动进档的既有 base64 参数优先于新世界名，日志证明启动到旧开发档 RTF_beta_validation_01，未进入复制的目标。开发端意外执行了一次 W 扫描（9 秒）并留下新 run；保留产物，不冒充目标验收。MCP helper 未把自定义端口环境传给子进程，首次 realm_status 报 ECONNREFUSED 5000（记录 run/codex_logs/revision-loop-status-01.json）；已修 helper 环境继承。对已确认属于本次启动的 5001 服务调用 /realm/debug/command stop 返回 DEBUG_COMMAND_NO_SUCCESS（集成服无 stop 命令），随后向唯一 forgeclientuserdev Java 窗口请求正常关闭。后续使用显式 base64 参数重开副本。

- 用户已部署最终 jar，PCL mods 中 SHA-256 与 `410DD03D9256373251BD22A08C82FCF0D3472C087C9D8E8061EC2741F06F1E85` 一致。
- 18:51:07 重试原方案，返回 REQUIRED_STRUCTURE_NO_LEGAL_PLACEMENT。GLM 随后提交新版本：住宅去掉 household_02，密度改 SPARSE；农舍从 MEDIUM/BALANCED 改 LARGE/SPARSE。18:52:02 的 attemptIndex=3 编译仅 96 ms，农舍 formationSpan=413，500 候选中 247 越界、253 与中心预留冲突，合法 0。新失败为 ARRAY_COMPOSITION_SLOT_UNAVAILABLE，并非旧长条预算修复未生效。
- post_d4 job attempt=4/status=needs_agent/failureOwner=design/nextAction=city_submit_d4_blueprint；failureCount=3/maximum=5/remaining=2。地图读此最新状态；自动规划 UI 仍显示提交后的 WAITING，未有下一次 stage。
- 查证 runner 的 `acceptSuccessfulTurnOnlyAfterStateProgress` 在提交进入 WAITING 时将 before.semanticIdentity 记为 lastCompletedIdentity；Discovery.step 没有把提交/编译轮次纳入 CITY identity。失败回到 needs_agent/submit 后与上一次修订身份相同，tick 去重直接 return。只读反射探针用同 run 的两份修订队列状态复现 identity 相同（仅变化失败计数和更新时间），输出 newFailureHasSameCompletedIdentity=true。
- 本轮没有修改生产代码或用户存档。后续修复应让新 D4 失败产生新的稳定修订身份，保持重复轮询不触发重复调用；回归必须覆盖 提交→WAITING→编译拒绝→再次 CITY→新修订，而不能只测单次编译。

### 修复后的真实 GLM/Hermes 测试

- 实测环境：独立 `run/saves/RTF_revision_loop_20260906`（从 PCL validation_04 复制，排除 session.lock）、端口 5001；明确 base64 自动进档；模型 glm-5.3-flash / Hermes，作者素材配置只读指向 PCL 的原 template_cfg。未修改原档或消耗原档失败额度；未手工设计/提交/编译测试城。
- 19:15:42 服务启动到正确副本，MCP realm_status 确认 debugRoot；19:16:10.607 GLM 开始第二城修订，19:16:34.018 提交（约 23.4 秒）。GLM 将住宅从 LARGE/SPARSE 调为 MEDIUM/SPARSE、农舍 LARGE/SPARSE 调为 SMALL/SPARSE，保留必需建筑和设计职责。
- 第二城 job attempt=5：D4 31,671 ms、D5 137 ms、D6 50 ms、LandUse 20,921 ms、激活 5,131 ms；19:17:32 进入 waiting_for_generation。正式 quality.passed=true/hardBlocks=[]、24 anchors、roadStructureOverlapCount=0。GLM 修订起至待生成约 81.5 秒；计启动/准备约 109 秒。不代表目标区块已实际落地，本轮未 TP 验证 worldgen。
- 自动转入首都。首都初始 GLM 多次 schema 提交拒绝（错误 Patch、关系字段、CORE 唯一性等），随后真实提交进入异步编译：attemptIndex=0 于 19:22:59.237 开始，19:23:00.193 以 REQUIRED_STRUCTURE_NO_LEGAL_PLACEMENT 失败（943 ms）；19:23:06.215 宿主自动开启修订会话 `geomantia-design-4cfa8a40-fafd-3236-bf84-97deb4b19cba`。
- GLM 在新会话修订并于 19:24:08.629 提交；attemptIndex=1 编译 1,178 ms，19:24:09.834 再次同类失败。19:24:15.690 自动开启下一修订会话 `geomantia-design-21942318-82be-3d71-ae1c-5fb1918a2dda`，19:24:16.331 已开始真实模型请求。证明运行中两次异步失败均可自动唤醒，第二次不是依靠重启/按钮。首都仍未通过设计；最后新会话未完成回复即停止测试，不记作首都完成。
- 19:24:18 请求关闭本次 forgeclientuserdev 窗口；19:24:19 日志确认各维度保存完毕。还原开发 provider enabled=false，与测试前备份 SHA 完全一致。原 PCL 配置/存档未动。
- 证据：`evidence/real_glm_tool_calls.json` 是 Hermes SQLite 只读导出的 20 条真实工具请求/返回（不导出模型 reasoning 或凭证）；`real_glm_agent.log` 保留模型名、会话和时间；`real_ferry_manifest.json`、`real_capital_manifest.json`、`real_queue_final_status.json` 保留正式结果。首次错误端口及纠正后的 MCP 调用另在 `run/codex_logs/revision-loop-*.json`。
- 先前“首都尚未进入编译”的中间更新是 19:20 左右的状态；收尾已核实随后实际进入两轮编译并成功再次唤醒，以以上正式时间线为最终结论。
- 自动验证：20 项聚焦 provider 测试通过。最终全量 842 项出现一项原有 8 秒性能断言超 31 ms（功能断言无其他失败、7 跳过），未放宽断言；聚焦性能及 provider 重跑 33 项全部通过（0 失败、0 错误、0 跳过），随后同一聚焦命令加 build 核对 BUILD SUCCESSFUL。不能将聚焦重跑写作全量通过。
- 本次调度修复最终产物：`build/libs/geomantia-0.1.0.jar`，SHA-256 `E61E1A0D91AEE04CEB4D3F58EC1EB76730780125B7FC985AA7022AADEBD7C010`，未部署到 PCL、未提交 Git。MCP helper 只显式转发 GEOMANTIA_MC_API_URL / MC_API_URL，其他环境保持 SDK 默认继承，node --check 通过。确认本次游戏进程已退出，开发 provider 配置与备份 hash 一致。

最终复查修正报告对象共享引用：外部质量 hash 写入 deep copy，避免序列化 map 后再改变其内嵌 quality；端点集成测试新增落盘 map 与响应 map 一致、质量 hash 匹配断言，聚焦回归与 build 再次通过。产物 `build/libs/geomantia-0.1.0.jar`，SHA-256 `410DD03D9256373251BD22A08C82FCF0D3472C087C9D8E8061EC2741F06F1E85`，未复制到 PCL、未提交 Git。

补充道路碰撞定位：马厩 horse_stall_02 的 actualFootprint 为 x=2696..2716、z=4488..4519，南向入口 (2704,4510) 位于模板矩形内部。target_transition 为 x=2704、z=4512..4529，路面与占地重叠 1×8 格；CityArrayVisualQualityGate 还向横向各扩 1 格检查。CityMainRoadPlanner.roadConnector 为入口接驳构造 transitionObstacles 时排除了入口所属建筑的整个 originObstacle，沿入口方向直线伸出到矩形外，因此允许接驳穿过所属模板范围；终审却对所有建筑 body 无条件判重叠，二者约束不一致。尚未检查该段模板实际方块，不能把矩形重叠称为已证实的穿墙或落地覆盖；本轮只读源码及保存定位记录。

仍待处理：第二城原方案后续住宅落位失败及 GLM 修订复验；区分设计完成/待生成/实际落地；相邻未释放城对已释放城的预留区遮挡。原档已激活失败方案的处置须单独校验，不直接删除产物或重置世界。本次未修改上述地图权限和原档状态，不应把源码修复等同于旧存档已经自动修复。
