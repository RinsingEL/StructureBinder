# City 系统故障索引

## 故障记录

| 日期 | 现象 | 根因 | 修复 | 验证 |
|---|---|---|---|---|
| 2026-06-26 | D7 `variable_area` 只能保证起点不越界，后续 jigsaw child piece 仍可能越出功能区、压占其他结构或落在不合适地形上。 | 旧路径缺少 piece 级规则决策；`ZoneInfo.covers` 用 clamp 做 cell 检查存在越界误判风险；MC bounded 后端 paste accepted pieces 前没有逐 piece 地形 / 流体 / 支撑探针和植被清理。 | `CityStructureD7Executor.ZoneInfo.covers` 改为先检查完整 bounds；`CityConstraintField` / `BoundedJigsawSolver` 输出 `ruleDecision` 与 `ruleResults[]`；新增 `CityTerrainProbe`；`MinecraftStructurePlacementBackend` 对每个 bounded piece 运行地形探针并在 paste 前清理软植被。 | `.\gradlew.bat test --tests "com.rinsing.geomantia.systems.city.*" --tests "com.rinsing.geomantia.platform.http.CityPlanningEndpointHandlerTest"` 通过；真实游玩待 MC/API 在线后继续验收。 |
| 2026-06-26 | D7 v2 真实执行已成功落地，但 HTTP 返回 `structure_generation_trace.json` 时触发 `OutOfMemoryError: Required length exceeds implementation limit`；磁盘 trace 一度达到 2GB。 | `boundedJigsawSamples[]`、selected attempt 和 `placedStructureMap` 重复嵌入完整 bounded trace；每个 sample 包含海量 rejectedPieces / stoppedBranches / terrainProbeSummary.probes 明细，导致展示层 JSON 膨胀。 | `CityStructureD7Executor` 写出 compact bounded trace：dry-run sample 只保留 `boundedJigsawTraceSummary`，selected / placed trace 限制 rejected / stopped 明细数量，`terrainProbeSummary` 压缩为 `probeSamples` + `omittedCount`，保留 accepted pieces、score、termination / rejection report 和 ledger 所需字段。 | `.\gradlew.bat test --tests "com.rinsing.geomantia.systems.city.CityStructureD6D7Test"` 通过；真实 `runClient` + `/realm/city/execute_d7 executeStructurePlacement=true` 使用 `codex_d7_start_expansion_true_20260626_v3` 成功返回，trace 缩至约 5.3MB，quality passed。 |
