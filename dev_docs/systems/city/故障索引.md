# City 系统故障索引

## 故障记录

| 日期 | 现象 | 根因 | 修复 | 验证 |
| --- | --- | --- | --- | --- |
| 2026-07-15 | 模板阵列实机验收中，D7 ledger 与 worldgen 日志均称 10 个 template 已落地，但地表逐点检查完全不可见；玩家进入地下后确认全部结构位于 `Y=-64`。 | D4/D6 模板落地计划只携带 `anchorBlock.x/z`、尺寸和变换；worldgen 在字段缺失时回落到世界最低高度。三个 NBT 本体均存在、非空且可由运行时读取，非维度或资源缺失问题。 | D6 对模板固定写入 `templateDatumPolicy=worldgen_surface_motion_blocking_no_leaves`，D5 active registry 原样保留；worldgen 仅在 owner chunk 生成时用 `MOTION_BLOCKING_NO_LEAVES(anchor.x, anchor.z)` 解算 `templateDatumY`。缺失 / 未知 policy 或不可用高度图均 hard fail，禁止回落 `minBuildHeight`；runtime ledger 写入实际 `templateDatumY`。同步产品、交接契约和测试入口。 | `city_template_datum_fix_20260715_01` / `city_realm_salt_kingdom_0_mining`：D6 policy 正确，D5 激活 1 个模板与 1 个真实入口；未生成 chunk 首次传送后，D7 返回 `TEMPLATE_MATERIALIZATION_RECORDED`，ledger 与日志均记录 `templateDatumY=148`、footprint `400640..400650 / 399440..399450`。 |
| 2026-07-15 | 两个模板锚点跨越 chunk 边界时，owner chunk hook 已写入局部 fragment，但 D7 保持 `WAITING_FOR_TEMPLATE_MATERIALIZATION`，没有完整 ledger。 | `MinecraftCityTemplateWorldgenPlacer` 的 owner fragment 写入后返回 `TEMPLATE_CHUNK_WRITE_WAITING`；active registry 只按 anchor owner chunk 调用，未为覆盖的其他 chunk 建立 fragment callback，也未聚合完整模板完成状态。 | 未修复，本轮不将其混入 datum 修复。后续需要按 template footprint 投影所有 owner chunk，并以 fragment ledger 聚合后才写完整 placed ledger。 | `city_template_datum_fix_20260715_01` / capital：两个跨 chunk 模板均触达 worldgen hook，D7 无 completed ledger；worldgen failure ledger 为空，说明没有被误报为已落地。 |

| 2026-07-14 | 连续 outward 的混合尺寸农庄中，首个农田满足 gap，较大的磨坊却回压父簇；整组最小 gap 为负并导致三圈都拒绝。 | `frontierReference` 只读取第一个 `DesiredItem` 的 D2 `plannedFootprint`，没有让同簇中方向外伸更大的成员参与前沿起点计算。 | 连续前沿参考改为汇总 `requiredItems`、`featuredItems` 与展开 `fillPool` 的全部 D2 body 并集；方向最大外伸成员决定首排退距，原有整组最小 gap 校验继续兜底。同步 v0.5 产品、接口、交接契约、代码导览与测试口径。 | `CityStructureArrayLayoutLoopPlannerTest` 强制重跑通过，新增小/大 body 混合 1×2 北向簇回归；待重启客户端后继续真实三轮 outward。 |
| 2026-07-14 | D4 v0.5 东向连续外扩的 `guide_line_dual_side` 请求 16..28 格 body gap，却稳定观测到 32 格以上，近圈因此无候选。 | 连续路径复用了普通双侧线的横向 cross offset；东向外扩首个 guide 点先向外移动一条侧排距离，且 gap 校验只读取 `items[0]`，不能表达整组最近实体。 | `CityStructureArrayLayoutLoopPlanner` 为连续双侧线固定内侧首排在 frontier，再将外侧排向外展开；`actualBodyGapBlocks` 改为遍历全组 `plannedFootprint` 取最小方向 gap。同步 v0.5 产品、接口、交接契约和测试口径。 | `CityStructureArrayLayoutLoopPlannerTest` 强制重跑通过，新增 37 格父 body、49 格子 body、4 结构双侧线回归；待重启客户端后用同一 D4 state 验收三轮 outward。 |
| 2026-07-13 | 新区块农田实测虽然大体正常，但一座普通/模组世界生成房屋落入田地并被装饰替换；同时 active mask 把 D3 patch `132608..133119 / 129536..130047` 整块标为禁植，而真实农田 slot 仅为 `132889..132959 / 129897..129915`。 | D5 `appendDecorationVegetationMasks` 错把 `CompiledDecorationProgram.targetMask().bounds()` 当作实际投影范围；该值是 D3 patch member bounds 的外包框。D5 只写 `noVegetationMask`，普通原版 / 模组结构起始没有相同保护。 | D5 改读并校验 `city_decoration_slot_projection.json`，再与 compiled plan 的确定性投影逐项比对；按每个有 slot 的 program 计算 slot min/max 窄包络，同时写 `maskType=decoration_projection` 的 `noVegetationMask` 与 `noVanillaStructureMask`，并返回两类计数。slot projection 缺失或不一致时拒绝 activation。 | `CityPlanningEndpointHandlerTest` 断言小矩形投影不退化为完整 patch，植被 / 结构 mask 数量和 bounds 一致；`CityDecorationWorldgenRegistryTest` 回归通过。必须在新的未生成区域实测普通/模组结构不再抢占。 |
| 2026-07-13 | 默认地形农田的水渠在陡坎处向低处外流，冲掉部分小麦；同一田块出现成熟、幼苗和空耕地混杂。 | `water_channel_tile` 是裸水源；`maxSlopeDelta=1` 只检查单个 prefab footprint，而水渠 tile 为 1x1，未比较相邻地表，192 个水源跨 Y=148..165 均被落地。`crop_tile` 固定为初始阶段小麦，之后受原版随机刻和漏水影响。 | content index 增加可选 `terrainDropFallbackContentRef`，仅接受同 catalog、同 placement 语义的 1x1 source/fallback。chunk compiler 在配置该字段的 tile 面向可通行下坡超过 Program 阈值时，`clip` 改放 fallback、`skip` 不落地；基础水渠配置 fallback 为 `crop_tile`，实现台地收尾而非裸水源。 | `CityDecorationChunkCompilerTest` 覆盖下坡水渠改为作物收尾；content loader 与默认 catalog 回归通过。旧世界已落地方块不会自动改写，须重新规划、激活并在未生成区域验收。 |
| 2026-07-13 | 默认世界未加载区块的装饰实测中，玩家传送等待超时；线程栈显示 `ConfiguredFeature.place -> RandomPatchFeature -> ConfiguredFeature.place` 重入，同一 1824-slot field 在一个 owner 内反复投影。 | `ConfiguredFeatureMaskMixin` 对每一层 configured feature 都调用装饰 apply，且同一 `WorldGenRegion` 后续顶层 feature 也没有 owner 去重；激活计划后无关 chunk 也会先投影全部 slot 才发现不属于 target。 | registry 使用线程局部 depth 仅允许 outermost configured feature 尝试 apply，以弱引用 `WorldGenRegion + dimension + owner chunk` 记录本轮已处理 owner；nested feature 跳过 apply 但仍执行 suppression。`Feature.place` redirect 在 `finally` 退出 guard，取消与 HEAD 异常显式退出；激活 / reload / reset 清 guard。投影前先以 target mask member bounds 与 owner chunk 做廉价相交判定。 | `CityDecorationWorldgenRegistryTest` 新增嵌套、同 owner、不同 owner / scope、reset 与并发 claim 覆盖；装饰四项核心测试强制重跑通过。重启客户端后向未加载目标区块执行一次 `tp`，4 秒内完成，ledger 写入 1,824 个 fragment / 14 个 owner，且无远区块、ledger、投影或 feature placement 异常。 |
| 2026-07-13 | owner-local 修复后的默认世界未加载区块实测中，玩家 `tp` 等待超过 45 秒；日志将 `CITY_DECORATION_LEDGER_WRITE_FAILED` 包装成 `Feature placement`，目标 chunk 生成失败。 | 一次 owner 回调可成功落下数百 tile fragment，但 `recordApplied` 每成功一个就重写完整 JSON ledger；2MB ledger 的固定 `.tmp` 文件在 Windows 原子替换时触发 `AccessDeniedException`，异常上抛到 worldgen。 | `recordApplied` 只更新内存幂等账本，`applyForChunk` 在 owner 回调结束时批量 flush 一次；使用同目录唯一临时文件，写失败只记录 pending / 延后重试并限频告警，绝不把 ledger 文件异常抛回 worldgen；ledger 访问统一使用 registry 锁。 | `CityDecorationWorldgenRegistryTest` 强制重跑 11 项通过：单 owner 多 fragment 只写一次、写失败不重放同会话方块且恢复后落盘、两个 owner 并发保留两条 ledger。真实复测待重启修复客户端。若进程在 pending ledger 成功写盘前崩溃，重启可能重放已落方块；要完全消除需后续引入随 chunk 持久化标记。 |
| 2026-07-13 | 超平坦实测农田本应为“栅栏、5 田地、水渠、5 田地、水渠、5 田地、栅栏”，却从水渠开始且只落下 L 形三块；随后在默认世界的未加载区块实测中，`forceload` 长时间不返回，退出卡住。 | `cross_section_repeat` 以 compiled coordinateFrame 原点取模，Shape 采用局部 `u=120` 起始时被错误前移 6 格；修正 L 形的中间方案又让一个 `WorldGenRegion` 回调预检并写入全部 owner fragment，触发 `Detected setBlock in a far chunk`。线程栈确认服务端主线程卡在 `ForceLoadCommand.changeForceLoad -> ServerChunkCache.getChunk`，worker 同时持续运行装饰投影。 | `DecorationPatternSupport` 为 rectangle / ellipse / ring / polygon / mask 提供局部 phase 起点，cross-section / rows / grid 全部相对 Shape 起点取模；worldgen 改为只编译、预检、写入当前 owner chunk，READY fragment 在当前 feature 继续前登记 vegetation mask，不可写 owner 单独延后并由自己的回调重试。禁止跨 `WorldGenRegion` 写入。 | `CityDecorationProgramPlannerTest`、`CityDecorationChunkCompilerTest`、`CityDecorationNbtPlacerTest`、`CityDecorationWorldgenRegistryTest` 通过；两 chunk 用例断言 chunk 0 仅写 x=15、chunk 1 不可写时只 mask/不落地、恢复可写后独立写 x=16。实机需用修复后的新 client 对未加载区块复测。 |
| 2026-07-05 | 启动时忘开 RoadWeaver 后，`roadProvider=auto` 会在 D7 生成旧 WorldEdit/debug gravel road；换 agent 或复跑 workflow 时容易把旧道路误当成正式 RoadWeaver 道路。 | `auto` 缺 RoadWeaver 时写入 fallback state，D7 按 `useWorldEditDebugFallback=true` 自动后处理；启动规范也未把 RoadWeaver 与结构包作为默认启用项固定下来。 | `CityRoadWeaverBridge` 改为 `auto` 缺 RoadWeaver 时 `skipped` + `ROADWEAVER_UNAVAILABLE`，`useWorldEditDebugFallback=false`；D7 只在显式 `roadProvider=worldedit_debug` 时生成旧调试路，缺失 road state 也不再默认 fallback；`gradle.properties` 默认开结构包、开 RoadWeaver，并移除 RTF / ReTerraForged 启动参数与运行依赖；同步 MCP 描述和文档仓库启动 / 契约 / 测试口径。 | `.\gradlew.bat test --tests "com.rinsing.geomantia.platform.http.CityPlanningEndpointHandlerTest.handleExecuteD7BuildsRoadPlanFromWorldgenLedgerActualFootprints" --tests "com.rinsing.geomantia.platform.http.CityPlanningEndpointHandlerTest.handleExecuteD7SkipsLegacyRoadWhenAutoRoadWeaverMissing" --tests "com.rinsing.geomantia.platform.http.CityPlanningEndpointHandlerTest.handleExecuteD5_roadweaverProviderHardFailsWhenModMissing"` 通过；`.\gradlew.bat test --tests "com.rinsing.geomantia.platform.http.CityPlanningEndpointHandlerTest"` 通过；`cd country_designer_mcp; .\node_modules\.bin\tsc.cmd --noEmit` 通过。 |
| 2026-06-26 | D7 `variable_area` 只能保证起点不越界，后续 jigsaw child piece 仍可能越出功能区、压占其他结构或落在不合适地形上。 | 旧路径缺少 piece 级规则决策；`ZoneInfo.covers` 用 clamp 做 cell 检查存在越界误判风险；MC bounded 后端 paste accepted pieces 前没有逐 piece 地形 / 流体 / 支撑探针和植被清理。 | `CityStructureD7Executor.ZoneInfo.covers` 改为先检查完整 bounds；`CityConstraintField` / `BoundedJigsawSolver` 输出 `ruleDecision` 与 `ruleResults[]`；新增 `CityTerrainProbe`；`MinecraftStructurePlacementBackend` 对每个 bounded piece 运行地形探针并在 paste 前清理软植被。 | `.\gradlew.bat test --tests "com.rinsing.geomantia.systems.city.*" --tests "com.rinsing.geomantia.platform.http.CityPlanningEndpointHandlerTest"` 通过；真实游玩待 MC/API 在线后继续验收。 |
| 2026-06-26 | D7 v2 真实执行已成功落地，但 HTTP 返回 `structure_generation_trace.json` 时触发 `OutOfMemoryError: Required length exceeds implementation limit`；磁盘 trace 一度达到 2GB。 | `boundedJigsawSamples[]`、selected attempt 和 `placedStructureMap` 重复嵌入完整 bounded trace；每个 sample 包含海量 rejectedPieces / stoppedBranches / terrainProbeSummary.probes 明细，导致展示层 JSON 膨胀。 | `CityStructureD7Executor` 写出 compact bounded trace：dry-run sample 只保留 `boundedJigsawTraceSummary`，selected / placed trace 限制 rejected / stopped 明细数量，`terrainProbeSummary` 压缩为 `probeSamples` + `omittedCount`，保留 accepted pieces、score、termination / rejection report 和 ledger 所需字段。 | `.\gradlew.bat test --tests "com.rinsing.geomantia.systems.city.CityStructureD6D7Test"` 通过；真实 `runClient` + `/realm/city/execute_d7 executeStructurePlacement=true` 使用 `codex_d7_start_expansion_true_20260626_v3` 成功返回，trace 缩至约 5.3MB，quality passed。 |
| 2026-07-03 | `wallVersion=v4` 实机中节点和墙体形成内外两圈：一圈独立柱子 / 节点在内侧，外面另有闭合城墙；门楼不明显或难以辨认。 | v4 水体退避会移动 `wallUnits[].blockBounds`，但 `wallNodes[]` / `nodeConnectorUnits[]` 仍使用退避前的 ring start 坐标；同时普通节点全部被标为 `beacon_tower` 并由执行层独立放置，导致 16 格一个柱子。 | `CityWallPlanner` 改为从最终 `V4UnitDraft.bounds` 推导节点 / connector 坐标，普通中间节点降级为 `junction` 且 `placementRole=graph_only`；`CityWallPlacementBackend` 对 `junction` / `beacon_tower` 不再独立放置实体节点，只保留相邻 wall / connector 表达；补 gatehouse 节点必须覆盖 skipped road unit 的测试。 | `.\gradlew.bat test --tests "com.rinsing.geomantia.systems.city.CityStructureLandingFlowTest.wallPlannerV4*"` 通过；完整 `.\gradlew.bat test --tests "com.rinsing.geomantia.systems.city.*" --tests "com.rinsing.geomantia.platform.http.CityPlanningEndpointHandlerTest"` 通过；`cd country_designer_mcp; .\node_modules\.bin\tsc.cmd --noEmit` 通过。 |
| 2026-07-03 | `wallVersion=v4` 预览右上角断开，部分墙线像是跑到未扫描 patch 外；修正后又一度出现整圈全为蓝色 `natural_boundary_gap`。 | 水体退避候选未限制在已知 patch 域内且未排除相邻 unit 重叠；同时 D5 `seedPatches[]` 丢失 D3 `memberCells[]`，v4 用大型 water patch envelope 做碰撞，把 512x512 扫描域当硬水体。 | v4 输出 `knownPatchBounds` 并要求退避候选仍在已知 patch 域内、不得与其他 unit 重叠；D5 `seedPatches[]` 保留 `geometryMode` / `cellStepBlocks` / `memberCells[]`；endpoint 对旧 D5 artifact 从 D3 做内存级 cell 补全；v4 水体判定优先用 member cell，`shore` 不再直接视为普通墙禁落点。 | 定向 `.\gradlew.bat test --tests "com.rinsing.geomantia.systems.city.CityStructureLandingFlowTest.wallReservationV4*" --tests "com.rinsing.geomantia.systems.city.CityStructureLandingFlowTest.wallPlannerV4*"` 通过；实机同城重跑预览 `outsideKnownPatchUnits=[]`、`ordinaryWallUnitsInWater=0`。 |
| 2026-07-03 | v4 实机复跑时北侧 / 右侧大量红色 gatehouse opening，右上角墙体被 road mask 打断。 | `CityRoadMaskScanner` 把 `stone_bricks` / `cobblestone` / `andesite` 等城墙或结构材料视作 road-like；复跑后旧墙材料进入 `actualRoadMask`，planner 将大量 unit 标为 `ROAD_MASK_GATEHOUSE_OPENING`。 | road-like 材料收窄为 `dirt_path` / `gravel` / `coarse_dirt` / `packed_mud` / `mud_bricks`；v4 planner 对旧 `actualRoadMask` 中明确 masonry blockId 做兼容过滤；补 `wallPlannerV4IgnoresMasonryRoadMaskFalsePositives`。 | 定向 v4 测试通过；实机同城重跑后 gatehouse 节点从 35 降到 5，右上 `wall_unit_23..28` 不再为 road opening。 |
| 2026-07-03 | v4 新区块压力测试中右侧墙体超出 patch 背景；`wallBounds.maxX` 大于 `knownPatchBounds.maxX`，有 23 个 unit 超出已知 patch。 | City D3 只刷新 `regionForBlock(center)` 单个 GIS region；城市 grid 和 v4 actualFootprint land ring 可跨到相邻 GIS region，但 D3 `landformPatches.memberCells` 只覆盖中心 region。 | `handlePlanD3` 新增默认 `patchScanPaddingBlocks=128`，按 city grid + padding 覆盖所有触达 GIS region 并合并 patches/memberCells；D3 package 写 `patchContextBounds` / `refreshedRegions[]`；v4 validation 增加 `outsideKnownPatchUnitCount` 和明细。 | `landformReviewBuilderIncludesPaddingCellsAcrossGisRegions` 通过；完整 City/HTTP 回归通过；MCP `tsc --noEmit` 通过。 |
| 2026-07-05 | `wallVersion=v5` 实机墙底边缘出现小悬空洞，尤其在低洼 / 坡坎贴墙处。 | v5 先铺水平坑底地板，再让 foundation 重新读取 `surfaceY()`；新铺地板被当作地表，导致原始低洼列没有从原 surface 填实到台基。 | v5 surface sample 固定保留原始 1-block 高度；`placePitFloor` 按原始 surface 填实到水平地板，`placeWallV5` 的 foundation 也使用原始 surface sample；补门楼 / 烽火台模板表现测试。 | 定向模板 / foundation 测试通过；完整 `.\gradlew.bat test --tests "com.rinsing.geomantia.systems.city.*" --tests "com.rinsing.geomantia.platform.http.CityPlanningEndpointHandlerTest"` 通过；MCP `tsc --noEmit` 通过。 |
| 2026-07-05 | `wallVersion=v5` 实机烽火台加高后，墙顶处被塔身挡住；南北向楼梯在南北墙上会占用横向通路，导致无法从一段墙穿过烽火台到另一段墙。 | v5 节点是 5x5 方形 bounds，若从节点自身宽高判断方向会失去墙段轴向；烽火台模板 / backend 只保留直梯和底层门洞，没有按墙轴打通墙顶走道。 | `CityWallPlanner` 从邻近 D5 `wallLine` 推断 `wallAxis`；`CityWallPlacementBackend` 按 X / Z 轴生成墙顶通道并把梯子放到非通道侧；`CityWallTemplateLibrary` 的 beacon 模板加入默认 X 向墙顶通道。 | 定向 `beaconTemplateKeepsWalkableCenterAndStraightClimbAccess` 与 `wallPlannerV5KeepsD5WallLineAndDeclaresNoRelineDowngradePolicies` 通过；完整回归待本次重启实机后复验。 |
| 2026-07-09 | D2 profile cache 首切片后，`skipExisting=true` 且已有 `structure_envelope_facts.json` 的 workflow 仍强制进入 profiling；无 MC runtime 的 HTTP workflow 测试失败。 | `CityPlanningEndpointHandler` 为让 cache 自判定 hit/stale，把 `city_profile_structure_envelopes` workflow step 的 `skipArtifact` 改成 `null`，破坏了 workflow artifact 级复跑跳过语义。 | workflow 先计算 `forceRefresh || cacheMode=rescan`：普通复跑传入既有 `structure_envelope_facts.json` 作为 skip artifact；强制刷新才传 `null` 并进入 profiler。补 `cacheMode=rescan` 即使已有 facts 也重写 profile facts 的测试，并同步接口契约 / 测试入口。 | `.\gradlew.bat test --tests "com.rinsing.geomantia.platform.http.CityPlanningEndpointHandlerTest"`、`.\gradlew.bat test --tests "com.rinsing.geomantia.systems.city.CityStructureLandingFlowTest"`、`cd country_designer_mcp && npm run build` 通过。 |
| 2026-07-10 | 单人游戏退出时 RoadWeaver 日志持续刷屏，游戏长期停在关服阶段；现场 `latest.log` / `debug.log` 各约 186MB，约 2 万轮 H2/MVStore 异常。 | RoadWeaver 2.2.2 的 `ServerStoppingEvent` 先取消道路 Future，但不等待 `RW-Idle` / `RW-Gen` 任务真实退出；随后 `CacheManager` flush 并关闭 H2 连接，最后才关闭通用线程池。仍在运行的 `Road.generateRoad -> RoadSqliteStorage.addRoad/queryRect` 因文件通道已关闭触发 `ClosedChannelException`，异常任务和回调继续访问数据库，形成关服刷屏。Geomantia 7 月 9 日提交未修改 `CityRoadWeaverBridge`；bridge 最近改动是 7 月 6 日和 7 月 1 日。本存档道路库创建于 7 月 6 日，历史日志从 7 月 1 日起已有零星 MVStore 关服异常，7 月 9 日首次放大为持续循环。 | 当前只完成定位，未修改 RoadWeaver 或 Geomantia 业务代码。建议 RoadWeaver 增加 stopping gate，先停止接单，再 `shutdownNow + awaitTermination` 等待 idle/generation worker 和完成回调退出，之后才 flush/close DB；数据库 executor 也应拒绝关服后的新任务，并对 ClosedChannel/DB closed 失败熔断，避免继续重试和写日志。临时规避是新建/备份后重置该存档 `data/roadweaver/minecraft_overworld/`，并在退出前关闭 idle generation 或等待道路任务完成。 | 分析 `run/logs/latest.log`、`roads.trace.db`、历史压缩日志、RoadWeaver 2.2.2 源码关服顺序和 Geomantia Git 历史；详见 `dev_docs/systems/city/active/20260710_RoadWeaver关服刷屏诊断/任务记录.md`。截图中的缺块未在日志中发现客户端渲染或区块删除异常，暂不能证明与数据库竞态为同一根因。 |
| 2026-07-12 | 单人游戏运行中和退出前出现 35 秒以上卡顿，服务器主线程在 RoadWeaver 道路回调内持续运行。 | 两次现场 `jcmd Thread.print` 显示 `RoadGenerationService.safeGenerate -> RoadSnapService.snapAroundConnection -> RoadSqliteStorage.queryRect -> H2/MVStore LobInputStream.read` 在服务器主线程执行；同一栈出现 `ThreadPoolExecutor.reject`，RoadWeaver 内部任务已被拒绝但回调仍读取道路库。 | 测试环境在 `run/config/roadweaver/roadweaver.json` 关闭 `structurePrediction.enabled`、`structurePrediction.structureAvoidanceEnabled`、`planning.dynamicPlanEnabled` 和 `roadAppearance.roadsEnabled`；保留 Mod 加载和 API 依赖。 | 重启前 JSON 解析确认四项均为 `false`；新客户端已进入 Forge 启动阶段。后续装饰实机验收必须使用干净超平坦存档，避免遗留 RoadWeaver 道路任务干扰。 |
| 2026-07-15 | 完整 JVM 测试中 `handlePlanCityDressingWritesV02ArtifactsAndAvoidsHardObstacles` 稳定失败，缺少 `planned_road_corridor`。 | RoadWeaver 已要求 transformed template road entrances，旧测试 helper 仍只复制 bbox / footprint；连接计划按新契约正确拒绝了伪造入口。 | 测试夹具为每个 planned structure 补合法 `templatePlacementPlan.transformed.roadEntrances[]`，生产代码继续禁止 bbox fallback。 | 完整 `./gradlew.bat test --no-daemon` 377 项通过；详见 `dev_docs/workflows/City与Realm框架清理/active/20260715_LandUse前置框架清理/任务记录.md`。 |
