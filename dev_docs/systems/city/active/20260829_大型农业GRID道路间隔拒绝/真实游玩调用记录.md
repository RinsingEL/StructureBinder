# 大型农业新区块真实游玩调用记录

- 目标：在新区块生成以大片田地为主的真实城市，观察大型农业区能否完成 D4-D7。
- W run：`realm_w_mte38ltl_f1964c5a`。
- 城市：`city_realm_green_court_2_capital`，第二个 T4 候选，anchor `(139152, 138800)`。
- D3：首选点因中心水体破碎被人工否决；第二候选接受。
- D4 Top Patch：农业选择 `SLOPE-01`，selection `psel_d1b05dd5966983c5b8f96c2852748110f9fccd0c91f83af2fad2ed7ffc51712a`；粗连续面积 73,728，确认预览最大连续可建设面积 19,456。配套选择相邻 `SHORE-03`，selection `psel_255d8398618ed4e8d5ea69c21bdd012dec6e1b1c5ebb3f49d55d8d768cd9eabe`。
- Blueprint：农业/行政/商业目标占比 `0.75/0.15/0.10`；农业 `LARGE + BALANCED + GRID`，风车 required、`debug_fill`、四块 attached FARMLAND Parcel。
- 提交修正：首次只读校验指出 CONNECTION 的 `distancePreference` 必须为 `NONE`，该次 `attemptConsumed=false`、计数 0；修正为 `NONE` 后正式接受，计数 1。
- 最终结果：`city_compile_d4_blueprint` 返回 `CITY_INTERNAL_STREET_WIDTH_EXCEEDS_COLLISION_GAP:agriculture_large:GRID_ROW_GAP:available=0:required=4`。状态仍为 `awaiting_d4_compile`；没有 D4 anchor/preview，没有世界变更。
- Blueprint：[city_blueprint.json](../../../../../run/realm_debug/realm_w_mte38ltl_f1964c5a/city_test_runs/city_realm_green_court_2_capital/steps/blueprint/city_blueprint.json)。

## 2026-08-30 修复复验

- 正常启动开发客户端并自动进入 `RTF0810`；复用现有运行包、D3 与正式接受的 Blueprint，没有重跑 W/T/D3，也没有重新提交设计。
- 第一次调用 `city_compile_d4_blueprint(runId=realm_w_mte38ltl_f1964c5a, citySeedId=city_realm_green_court_2_capital)` 返回 `ok=true`，原道路间隔异常消失；预览门禁发现 `agriculture_large: GRID_STREET_NETWORK_DISCONNECTED`。原因是本轮初版把每对行列道路裁到各自局部范围，交叉点被裁掉；据此收敛为只裁未占用外围网格、实际道路覆盖全组真实 GRID 占用范围。
- 重启客户端后使用完全相同参数第二次调用，返回 `ok=true`。最终农业 25 个 anchor，其中初始 GRID 12 个；8 条 GRID 道路连通，固定 pitch，最大格点误差 1，road/body overlap 0；compile trace 不含 `CITY_INTERNAL_STREET_WIDTH_EXCEEDS_COLLISION_GAP`。
- 将净空预留明确限制在正式 GRID/占比阶段、不作用于 `connectivity_growth` 后，再次重启客户端并以相同参数完成最终复编；上述数量与质量指标保持不变，证明最终代码与留档证据一致。
- 预览：[农业局部图](../../../../../run/realm_debug/realm_w_mte38ltl_f1964c5a/city_test_runs/city_realm_green_court_2_capital/steps/d4/structure_anchor_group_agriculture_large.png)；[总览](../../../../../run/realm_debug/realm_w_mte38ltl_f1964c5a/city_test_runs/city_realm_green_court_2_capital/steps/d4/structure_anchor_preview.png)。
- 正式产物：`steps/d4/structure_anchor_plan.json`、`quality_report.json`、`city_generation_compile_trace.json`。本次停在 D4，没有执行 D5/D7 世界写入。

## 2026-08-30 农业景观流线接管

- 按最新口径，同一已接受 Blueprint 中 `FUNCTIONAL` 农田的 `GROUND_PATH + CORRIDOR` 改由景观 Parcel 间隔承担农业内部流线，不重新提交设计。
- 重启客户端并正常调用相同 `city_compile_d4_blueprint`，返回 `ok=true`；农业 25 个 anchor 不变、正式 GRID 道路从 8 条降为 0，视觉门禁记录 `internalCirculationMode=LANDSCAPE_GAPS`。
- 4 块农田共 15,456 blocks；三个非根 Parcel 均保留宽 1 block 的分隔，彼此 shared boundary 为 0，最小 separated boundary 为 40。
- 最新农业局部图仍为：[农业局部图](../../../../../run/realm_debug/realm_w_mte38ltl_f1964c5a/city_test_runs/city_realm_green_court_2_capital/steps/d4/structure_anchor_group_agriculture_large.png)。本次仍停在 D4。
