# 嵌套阵列与关系位置

*需求入口*：文档仓库 `City嵌套阵列与关系位置-v0.1`，提交 `ff5bc96`；用户要求父阵列可排列子阵列，并支持 Patch/边界/建筑群关系位置与子阵列独立地形限制。

*修改内容*：CityBlueprint 升级到 v0.11、Context 升级到 v0.10；新增 Group `placementRelation` 与根级 `arrayCompositions[]` 严格契约。编译器支持 `BETWEEN_PATCHES`、`ALONG_PATCH_BOUNDARY`、`BETWEEN_GROUPS` 首个阵列定位，并以完整 Group extent 规划父阵列槽位；中心对称父阵列会旋转或交换成对子 Group，使各自槽位中心命中独立偏好 Patch，同时保留子 Group 自身算法、模板池和地形策略。compile trace v0.11 与 extent map v0.8 输出可审查槽位。同步 MCP schema、Java/MCP 测试及文档仓库契约、代码导览和测试入口。

*验证结果*：`./gradlew.bat test` 通过，674 个测试全部成功；`country_designer_mcp` 执行 `npm test` 通过，7 个测试全部成功。真实游玩复用 `rtf0810_medium_city_20260810_01 / city_rtf0810_platform_terrain_21` 的既有 D3 和 `top_patches_overview_313bac8c0c4b.png`，未传送、未扫描新区块、未执行 D5/D6。最终 Blueprint v0.11 编译通过，质量 100、warnings 0，共 42 anchors：中心对称行政三簇 5/4/4，坡间广场边缘 3，BETWEEN_GROUPS 居民连接 11，双侧线性商业街 12，连通岸线 ALONG_PATCH_BOUNDARY 市场 3。完整过程见 [真实游玩调用记录](./真实游玩调用记录.md)。

*剩余问题*：广场与码头 NBT 尚未由玩家录制，本次不伪造模板。真实编译发现父阵列 required structure 可从槽内失败后 fallback 到槽外 Patch，以及 FREE_STANDING 可选景观会被静默规划为 0 个，已登记 `CITY-PW-20260817-01/02`。
