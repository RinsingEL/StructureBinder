# D4 失败恢复路由

*需求入口*：用户发现新存档自动规划器在首城 D4 后停止，并要求修正 Agent Loop 相关代码。

*修改内容*：Post-D4 队列从失败 workflow step 保留恢复动作；`city_submit_d4_blueprint` 被标记为 `D4_BLUEPRINT_REVISION_REQUIRED`，城市队列将该动作传到顶层。Provider discovery 只对这类 `needs_agent` 重新开放 City Agent Loop，普通程序/环境失败仍保持非 actionable，继续依赖人工修复后重编。Post-D4 队列启动时重新发布已落盘终态，使当前旧现场能从嵌套 workflow step 推导出 Blueprint revision 动作并同步上层队列。GIS `PreviewExporter.Metric` 改为普通枚举加 switch，消除 Forge 后台线程首次加载时的四个匿名枚举子类。

*验证结果*：Post-D4、城市队列、Provider discovery 与 GIS 聚焦测试通过；`gradlew test jar --no-daemon` 全量 102 suites / 709 tests 通过，0 failure/error/skip。构建 JAR 仅含 `PreviewExporter$Metric.class`，不再含 `$Metric$1..$4`；`git diff --check` 无空白错误。

*实机部署*：当前工作区全部改动重新执行 `gradlew test jar --no-daemon` 后部署至 Forge 47.4.23 实例；构建包与安装包 SHA-256 均为 `DAA8EFD7BCF3736C4E766B8F96168066E2FD6A4E01112CF442C5F2A766789BCA`。旧包备份为 `geomantia-0.1.0.pre-20260904-agent-ui-recovery.jar.bak`。

*剩余问题*：重启后确认旧首城队列自动转为 `city_submit_d4_blueprint` 并由 DeepSeek 修订。
