# LandUse 树冠占用回填

*需求入口*：用户要求定位并修复同城 D7 单 owner 的 `CITY_LAND_USE_SURFACE_TARGET_OCCUPIED`，且优先复用当前城市，不默认删除区块。

*修改内容*：确认失败区块 `(8679,8699)` 的首个冲突来自跨入城市铺装成员格 `(138864,68,139192)` 的原版橡树叶。LandUse 使用 `MOTION_BLOCKING_NO_LEAVES` 采样树冠下地面，却在写入阶段拒绝叶块，前后口径不一致。`CityLandUseChunkExecutor.WorldGenExecutionWorld` 现将 leaves 纳入 LandUse 地表可替换目标；logs、石砖、容器等实体占用仍不放行。新增聚焦回归冻结该边界。

*验证结果*：`CityLandUseChunkExecutorTest` 通过；全量 `gradlew test` 通过。重启开发游戏后复用 `realm_w_mte38ltl_f1964c5a / city_realm_green_court_2_capital`，不删除任何区块，正常 `execute_d7` 仅补写 1 个缺失 owner：`appliedBefore=267`、`backfilled=1`、`appliedAfter=268`、`complete=true`、`missingOwners=[]`、`failures=[]`，33 个结构保持完成。玩家已传送至农业区验收点 `138950 90 139187`。

*剩余问题*：无。
