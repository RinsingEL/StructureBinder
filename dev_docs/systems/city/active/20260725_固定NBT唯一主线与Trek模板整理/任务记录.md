# 固定 NBT 唯一主线与 Trek 模板整理任务记录

## 范围

本轮为双仓库联动：

- 实现仓库 `E:\Mod_Dev\StructureBinder`：删除 configured/Jigsaw/envelope active path，收紧 D4-D7 与 worldgen，新增 Trek B0.6 离线导入器、manifest、测试、预览和目标存档输出。
- 文档仓库 `E:\Mod_Dev\designer_territoryMod`：更新固定模板方案、模板契约、MCP 契约、代码导览、测试入口、影响面和 City README。

## 读取的文档真值

- `docs/README.md`
- `docs/00_nav/repo_map.md`
- `docs/00_nav/function_map.md`
- `docs/00_nav/AI语义检索规则.md`
- `docs/00_nav/Bug修复流程.md`
- `docs/systems/city/README.md`
- `docs/systems/city/10_product/案子/00_跨阶段主线/City固定模板唯一落地主线-v0.1/README.md`
- `docs/systems/city/20_contracts/数据契约/City模板建筑主路径数据契约.md`
- `docs/systems/city/20_contracts/数据契约/结构落地交接契约.md`
- `docs/systems/city/20_contracts/接口契约/City调试MCP接口.md`
- `docs/systems/city/30_code_guide/代码导览.md`
- `docs/systems/city/40_tests/测试入口.md`
- `docs/systems/city/40_tests/影响面.md`

## 已完成实现

- D4 key、array、cluster、continuous 和 staged workflow 只接受显式 template catalog 与 `templateId/templateRef + variant`；删除 configured identity、envelope facts 和随机 template variant 选择。
- TerraSense catalog 的输入不再要求 footprint/profileType 等几何字段，active 输出收紧为 `city_semantic_profile_catalog.v0.1`；查询响应升级为 `city_structure_catalog_query.v0.2`，只返回 `semanticProfileId` 与 terms，不再返回 `structureId`、`hardFacts` 或任何 profile 几何。D4 不再按 template ID 回查 TerraSense identity；旧源文件 `structureId` 仅作为读取别名。
- D5 只按 exact footprint + clearance 生成 collision，再按 collision + maskMargin 生成 mask。
- D6 使用当前世界 NBT inspector 复核 hash、rawSize、rotation、mirror、footprint、collision、mask 和 owner chunks；D7 对同一 identity 与几何做完整 drift 校验。
- active schema 只保留 `city_template_catalog.v0.1`、`city_template_placement_plan.v0.1`、`city_active_template_placement_registry.v0.1`、`city_template_placement_ledger.v0.1`；piece boxes、start signature、bbox group 和 envelope sample 均退出 active path。
- `tryGenerateStructure` 只保留自然结构 mask；`createStructures` 只注入 City 自有单-piece fixed-template start；所有 catalog 模板冻结为 `structure_start_beard_thin`。
- 预览分层使用 template body、collision、mask；不使用 `StructureStart.getBoundingBox()` 表示建筑实体。
- 删除 envelope profiler/facts/sampler、configured materialization backend、direct FEATURES template placer、retry coordinator、`city_profile_structure_envelopes` 与 `debugLateMaterialize`。
- 旧 configured payload/artifact 在 active endpoint 返回 `CITY_CONFIGURED_STRUCTURE_FLOW_REMOVED`。

## Trek B0.6 输出

- 受版本控制的 `tools/city_templates/trek_fixed_manifest.json` 精确列出 20 个单根模板，source jar hash 为 `78c27ae931278eca9e2fd86e73b08f44ff5962cac4d1a3f701a26c257d6e32ef`。
- 导入器证明 start pool 只有一个根 element；结构性 target pool 以 `TREK_TEMPLATE_STRUCTURAL_JIGSAW_REJECTED` 拒绝。
- `trek:mobs/*` marker 替换为 final state，输出零 Jigsaw；实体列表清空；block entity 与 loot table 保留。
- `farm_pillager` crackify 与 `villager_castle` mossify 使用 importerVersion `trek_b0_6_fixed_v1`、source identity、processor rule index 和 block 坐标组成的 SHA-256 稳定 seed 烘焙。
- 目标存档 `run/saves/新的世界 (5)` 已输出 20 个 NBT、20 组俯视+四面预览及 `trek_fixed_import_report.json`，NBT 不进入 Git。
- 20 个入口已按固定 NBT、俯视图和 TerraSense 四视图逐项确认；manifest 全部为 `entranceConfirmed=true`，并以 `entranceEvidence` 冻结可复核的 NBT 方块证据。`farm_pillager` 修正为北门，`fort` 收回到南侧可走地面，`square_tower` 改为东侧地面梯口，`tower` 改为西侧地面开口。
- 当前没有运行中的 Minecraft metadata endpoint，因此报告为 `runtimeMetadataConfirmed=false`，未写 `template_catalog.json`。不得用离线文件 hash 伪装 runtime hash。

## Trek 固定模板语义包（2026-07-27）

- manifest 新增 20 项人工确认的 `functionTerms/styleTerms/placementTerms`、共用 Stubbs 单体模板 terms 和完整冻结词表；导出器只消费显式标签，不按结构 id 自动推断。
- 新 profile identity 为 `geomantia:city/trek/...`，统一使用 `single / structure_template_nbt / city_template_nbt / fixed_footprint`、`usage.single_structure` 与 `template_role.city_building`。
- `maison` 旧审核记录确实存在且为 `manual_override=true`，当时标作“磨坊 + 农业”；旧记录不改写，新固定模板画像纠正为 `function.residential`、沿街住宅。
- 四视图复核确认 `dark_oak_trade` 与 `small_red_trade` 均为船舶，已从错误的陆地沿街商铺修正为 `harbor_trade_ship`、`function.港口 + function.商业` 与 `placement.waterfront`。
- 新增 `--profile-dir` 语义包独立导出模式，不要求写入存档；输出 `StructureProfile.jsonl`、完整 `StructureVocabulary.snapshot.json` 与 `TerraSenseStructureProfileSource.official.json`。
- semantic profile 的 `approved_baseline` 不解除正式 catalog 的 runtime metadata 闸门；入口闸门现已通过带 NBT 证据的人工复核，但 runtime hash/rawSize 仍不得离线伪造。

## 潮汐王冠边界

- 旧 80-anchor artifact 依赖 13 个 Trek configured ID、sampled envelope 和 `StructureStart` bbox，已写 `FIXED_NBT_MAINLINE_INVALIDATED.json` 标记 D4-D7 作废。
- 13 个源 ID 均在本批 20 个固定模板内；入口已确认，仍必须等运行时 metadata 后用新 `geomantia:city/trek/...` ref 从 D4 重新计算，不能替换字符串沿用旧 bbox。
- 本轮未调用 `city_execute_d5`，未触发潮汐王冠世界写入。

## 已执行验证

- `gradlew test --rerun-tasks --no-daemon`：69 个 suite、474 个测试，failure/error/skipped 均为 0。
- `country_designer_mcp/npm test`：TypeScript build 通过，Node test 2/2 通过。
- `python tools/city_templates/test_import_trek_fixed_templates.py`：16/16 通过；除原导入覆盖外，新增 Stubbs 固定模板 profile 契约、词表完整性、official source 包、`maison` 旧错误画像隔离、20 项入口证据、证据漂移拒绝、三处入口修正和 runtime metadata 齐全时 20 项 catalog 构建测试。
- 对 `run/saves/新的世界 (5)` 执行显式 `--save-dir --dry-run`：20/20 target ref 解析通过，source jar hash 与 manifest 一致。
- 输出逐项离线复核：20 个 NBT 可读、Jigsaw 数为 0、entities 数为 0，报告 rawSize/hash 与文件一致；共生成 100 张俯视/四面预览。
- Forge GameTest 首次按默认开发依赖启动时分别被 RoadWeaver、ReTerraForged 的过期 mixin target 拦截；仅对本次命令关闭可选依赖，并临时隔离 `run/mods` 中唯一的 ReTerraForged jar 后，现有 5 个 required GameTest 全部通过。隔离 jar 已自动恢复原位。
- `git diff --check` 在实现仓库和文档仓库均无 whitespace error；未回滚或覆盖双仓库既有未知改动。

现有 Forge GameTest 只覆盖仓库原有 5 项，不包含本计划要求的普通 Trek、processor 模板和大型 `villager_castle` 三类专项落地，因此不能把 5/5 解释为这三项现场验收完成。

## 待现场验收

- 启动与 `run/saves/新的世界 (5)` 对应的服务端，调用 `city_query_template_metadata` 回读 20 个 NBT 的 runtime hash/rawSize。
- 用一次性新测试存档完成普通住宅、processor 模板和大型 `villager_castle` 的真实 single-piece `beard_thin` 落地、重复 hook 和重启幂等验收。
- runtime catalog 就绪后重算潮汐王冠 13 模板的 D4-D6 与预览；仍不得在本任务中执行 `execute_d5`。
