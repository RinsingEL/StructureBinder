# 当前素材配置检查

检查对象：`D:\PCL2-2.8.12\.minecraft\versions\1.20.1-Forge_47.4.23\config\structureTemplate\terrasense\template_cfg`。当前游戏仅发现这一套完整素材目录。

范围：65 个模板、65 个结构引用、65 条语义配置、42 个填充池。只读检查，未修改游戏配置或存档。

## 已确认的编译阻塞

`plains_big_house_1`：两个 WEST 入口 `(0,4)`、`(0,10)`，默认 FIXED_FRONT，无名为 front 的入口。默认调用未指定 frontageEntranceId 时无法定向。喷泉四入口已声明 ANY_AUTHORED_ENTRANCE，可正常解析。

## 需要复核的道路接口

下面 8 个接口位于模板内部。当前 legacy_catalog 模式允许内部坐标，因此不是 8 个必现编译错误。若迁移到 approved entrance sidecar，其边界朝外检查会拒绝这些坐标。射线抽查只记录原始方块，不等于 Minecraft 完整通行判定，也未验证绕行路径。

| 模板 | 接口与方向 | 发现 |
|---|---|---|
| farm_storage_house_01 | (9,13) SOUTH | 接口在内部；向南有木平台、第二道门和末端台阶，需要核对对外接路点。 |
| horse_stall_02 | (8,22) SOUTH | 向南直线有木柱、围墙；不能直接视作无障碍入口通道。 |
| guard_outpost_01 | (19,9) EAST | 向东模板外缘方向存在底部空气段，需要检查落地支撑及接路高度。 |
| motel_01 | (26,8) EAST | 门口位于较高层，外侧有台阶；需要核对道路接入高度。 |
| town_workshop_01 | (11,4) SOUTH | 向南直线有木桶、实体石块，需要确认真正对外通道。 |
| mangrove_watchtower_1 | (6,4) NORTH | 门高于底层，向北有高差和盆栽，需要核对通道。 |
| mangrove_watchtower_2 | (6,4) NORTH | 门高于底层，向北有高差和盆栽，需要核对通道。 |
| claw_pillager | (12,1) EAST | 接口在内部，向东底层存在未写方块，需确认是否确实需要道路连接。 |

## 本次检查未发现异常的项目

- 65 个内容文件均存在，文件校验值与清单一致；模板目录校验值与打包清单一致。
- NBT 可读取，尺寸与目录一致；方块坐标和 palette 索引未发现越界。
- 入口坐标均在模板范围内，入口 ID 无重复；旋转、镜像、frontagePolicy 枚举检查通过。
- 结构引用到模板变体、填充池到结构引用均未发现断链；引用结构均有 approved 语义及功能/风格项。
- 先前修正的 10 个 Stubbs 入口沿门到外缘的通道回归检查通过，喷泉任选入口配置仍保留。

## 后续修正顺序

先修大住宅入口声明；再复核马厩和城镇工坊的实际对外通道；随后检查其余内部接口的高差、支撑和接路点。不要批量将所有入口投影到边界，也不要批量授予任选入口策略。

限制：未逐栋进游戏验收，未对全部入口执行完整碰撞/寻路测试。目录检查通过不代表建筑视觉与通行质量全部合格。
