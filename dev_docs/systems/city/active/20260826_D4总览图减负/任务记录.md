# D4 总览图减负

*需求入口*：用户要求总览图不再密集标注，只保留各建筑包围盒、景观和最重要的功能区区域渲染。

*修改内容*：仅精简 `structure_anchor_preview.png`：关闭 Patch 名称、A/F/L 编号、道路文字与住宅溢出层、标题说明和右侧几何清单；保留无文字地形底图、网格、无文字道路几何、建筑 body/collision/mask 三层包围盒、精确景观容量和半透明功能区实际范围，并使用整张 1280×900 画布。总览按“地形 → 景观 → 功能区填色 → 道路 → 建筑包围盒 → 功能区边界复描”绘制，功能区使用约 41% 透明度与 3.2px 高饱和实线边界；行政/商业/农业固定为紫/青/绿，避免被景观覆盖或与蓝色 body、橙色 mask 混淆。功能区局部排错图继续保留失败位置、道路和原因。

*验证结果*：`CityStructureLandingPreviewRendererTest` 9 项通过；最终 `gradlew test --rerun-tasks` 全量通过（2 分 4 秒）。正常启动 `runClient`、进入 `RTF0810` 后正式重编 `normal_three_zone_20260826_0935 / city_realm_salt_kingdom_0_capital`，约 14 秒返回 `ok=true`；正式总览确认只剩目标几何层，紫/青/绿三类功能区清晰可辨，农业与商业局部图仍保留完整排错标注。

*剩余问题*：无。
