# 中心对称阵列

*需求入口*：用户原话“感觉可以用点那种中心对称的”“加一个吧”。

*修改内容*：为正式 CityBlueprint D4 增加 `CENTER_SYMMETRIC` 阵列；中心主体固定为唯一 required 结构，fill 建筑按中心对称的双结构批次原子搜索和提交，并在 trace 中保留 collision footprint 对称证明。中心主体提交后先锁定最低成形对数，每层两组正交轴线、下一层旋转 45 度；偶数通用最低栋数投影为不小于 3 的最近较小奇数。首个 required anchor 可在完整 preferred patch 内改选。同步修复景观联合搜索重复生成候选、owner Group extent 面积未生效的问题，并把四类景观的精确 reservation spans 画入 D4 局部预览。

*验证结果*：聚焦 JVM 回归已覆盖中心原子成对、轴线轮换、最低栋数投影、跨组播种顺序、景观候选缓存、SMALL extent 面积和预览像素。真实 MCP 在既有 D3 城市 `city_rtf0810_tradefields_03` 重新编译 D4 成功：行政组 5 栋，2 组对称证明全部通过；商业组 10 栋、教堂组 4 栋、乡村组 3 栋，所有 Group 达到最低成形数。四类必需景观均 `RESERVED`，联合搜索 10,610 节点，最终含预览编译约 4.3 秒。农田 3×192 格、林场 2×256 格、牧场 2×192 格、教堂花园 2×128 格。继续正式 workflow 后，D5/D6/LandUse/D7 注册成功；TP 加载新区块后直接 D7 收齐 22/22 结构、0 等待、0 失败。LandUse 覆盖 102 个 owner chunk，执行 22,600 项操作，其中 694 项作物、408 项边界。真实俯视截图为 `run/screenshots/2026-08-12_01.17.32.png`。

*剩余问题*：`CITY-PW-20260812-05` 待定位：新区块生成后以 `skipExisting=true` 续跑 workflow 会重复执行 D5 并被 FEATURES 保护拒绝；本次改用正式 `city_execute_d7` 直接收账成功。道路按 `roadProvider=none` 未生成，城墙未规划。
