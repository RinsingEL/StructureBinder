# 台地成立条件与可达性

*需求入口*：`E:/Mod_Dev/designer_territoryMod/docs/systems/city/10_product/案子/具体流程的案子/60_LandUse与城市基面/City台地成立条件与可达性-v0.1/README.md`。用户要求严格按案子开发，修复空台面被保留、只有道路跨台地才生成楼梯的问题。

*修改内容*：D6 已冻结的建筑实际占地与真实入口不再在 owner chunk 编译时丢失，现分别进入 `PlatformPurposeAnchor` 与 `PlatformAccessDemand`。独立台地必须同时满足容量与真实用途；无用途台地按共享边最长、再按高差最近合并，无法安全合并时退出 Foundation 铺装而不是原高程铺地。农业与其他非 Foundation 景观仍不进入微整地。可达性先复用正式道路楼梯；没有道路跨越时，从冻结入口所属台地向最近合法高差边界生成短接驳路与直梯/左右分流梯，同一占用台地默认共享一个主动主入口；无解返回明确失败码。

*诊断证据*：D7 owner 账本升级为 `city_land_use_worldgen_ledger.v0.4`，记录用途锚点数、入口需求数、台地 `RETAINED / MERGED / WITHDRAWN`、合并原因、用途 ID、接驳路径格数、楼梯格数以及入口采用既有道路楼梯/主动楼梯/共享主入口/失败。D7 完成报告透传 `foundationDiagnostics`。v0.3 账本在加载时原子迁移，历史 owner 补空诊断且不丢失。

*自动验证*：新增回归覆盖真实建筑用途与入口传递、大面积无用途台地合并、孤立无用途 Foundation 撤回、建筑用途台地保留、无道路主动入口梯、同台地稀疏主入口以及 v0.3→v0.4 账本迁移。全量 `gradlew test` 通过；随后相关 MicroGrader 与 WorldgenRegistry 回归再次通过。

*真实运行验证*：用新字节码两次启动 `runClient` 并自动进入 `RTF0810`，世界与 HTTP 服务正常加载。磁盘 LandUse 账本由 v0.3 一次性迁移为 v0.4，1554/1554 条历史 owner 均保留并补齐诊断结构。当前运行时 `knownRuns=[]`，没有可合法续跑的未生成区块城市，因此本轮没有把旧城市重复 D7 冒充新区块几何验收。

*后续修复*：真实运行暴露 Outdoor 分层路径把 D6 已冻结入口丢在 Foundation 之外，导致 `gateSlots=0`、`accessDemands=0`、`stairs=0`。现已让 Foundation 继承所有建筑的 transformed `roadEntrances`，并新增 D6 入口传至 Foundation 的回归测试。另已彻底删除旧 WorldEdit 沙砾道路提供器、D7 延迟后处理、MCP 参数枚举和对应测试路径；道路只允许 `auto / roadweaver / none`。

*剩余验证*：下一次正常城市规划进入未生成区块后，需用 D7 `foundationDiagnostics` 和实景共同验收空台地消失、占用台地主入口与主动楼梯观感，并确认不再产生旧沙砾道路。
