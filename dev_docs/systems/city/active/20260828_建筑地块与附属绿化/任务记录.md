# 建筑地块与附属绿化

*需求入口*：`E:\Mod_Dev\designer_territoryMod\docs\systems\city\10_product\案子\City建筑地块与附属绿化-v0.1\README.md`；用户确认开始按案开发测试。

*修改内容*：已按案完成 Blueprint `buildingGreeneryPolicy`、D4 每建筑 `buildingParcelPlan`、硬碰撞/可压缩地块分层、D6 户外空间冻结消费、SurfacePrint 绿化配方和 D4 局部预览。地块尺寸已纳入阵列空间需求，地块冻结后不重新参与建筑阵列求解；地板仍为最底层，景观和绿化特征可以覆盖。同步更新 MCP schema、数据契约、代码导览和测试入口。

*验证结果*：Java 全量 `gradlew test` 通过；MCP `npm test` 7/7 通过。新区块真实游玩 runId=`building_parcel_greenery_20260828_01`，城市 `city_realm_salt_kingdom_0_capital`，Blueprint hash=`sha256:197f8ee5c31825efa24f311f0f4b631271668768bbb4f00c9aa4f89dcee7f8ed`。首轮 D4 暴露“preferred parcel 从 footprint 外扩、随后又扣除自身 clearance，导致 28/28 建筑 usableGreenCells=0”的系统性错误；退出 AgentLoop 后修为“从硬碰撞外沿再扩 parcel margin”，并重新编译、重新真实游玩。修正版 D4 共 25 个建筑，25/25 具有可用绿化格（87..148），15 个按覆盖率选中附属绿化，9 个地块发生合法压缩。D7 attemptIndex=2 完成，25 个结构全部进入 placed ledger，质量门通过。SurfacePrint 实际产出 1,438 格 `GREEN_GROUND`、641 个 `GREEN_PLANT`、20 格 `GREEN_PATH`；世界生成账本按本轮 planHash 命中 66 个新区块，记录 3,344 次 feature 操作和 16,013 次总应用操作，证明绿化已落入世界生成而非只停留在蓝图。

*验收材料*：总入口 `E:\Mod_Dev\StructureBinder\run\realm_debug\building_parcel_greenery_20260828_01\city_test_runs\city_realm_salt_kingdom_0_capital`；D4 总览 `steps\d4\structure_anchor_preview.png`，各功能区局部图位于 `steps\d4\group_previews`，LandUse 计划位于 `steps\land_use\city_land_use_surface_print_plan.json`。玩家已传送到测试城市附近 `141650 160 142170`，客户端保留运行供人工游览。

*剩余问题*：本轮只有既有 warning：住宅 compact 巷道未形成，以及两条跨组连接因无合法地形路径按既定口径 `SKIPPED_NO_LEGAL_PATH`；均未阻断 D7，与本案建筑地块/附属绿化功能无关。无本案遗留硬阻断。
