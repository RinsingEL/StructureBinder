# Trek 混合大城规划真实游玩调用记录

*目标*：在 RTF0810 同一片未生成地形上，以 Trek 为主体、Stubbs 为补充，完成更大且建筑种类更丰富的城市 D4 正式规划；不进入 D5。

*可复用上游*：`runId=normal_three_zone_20260826_0935`，`citySeedId=city_realm_salt_kingdom_0_capital`，复用身份匹配的 D3、GIS 地形和正式 65 模板目录。

*实际调用与结果*：已停在 D4；没有进入 D5/D6，也没有写入世界区块。

- 正式 Blueprint：7 个 STRUCTURE Group、6 条显式关系、5 个 Landscape；Trek 为主，Stubbs 作为市政厅、酒馆、风车、仓库和工坊等识别锚点。
- 最终一次性 Blueprint Context：`sha256:9c038559fbb274e8048f0d6779666889f72f78b364a1f2a1947172a89e723e74`；Blueprint 校验 `ok=true`。
- D4 已实际执行，结果 `status=failed`，`reasonCode=CITY_BLUEPRINT_CONNECTIVITY_NO_LEGAL_PATH`。
- 唯一失败边为 `agriculture -> defense`：初始间距 `111.31` blocks，编译器放置 2 个连接建筑后仍余 `78.87` blocks，超过 `40` blocks 的双侧移交阈值。
- 失败发生前形成 47 个建筑候选：行政 8、广场 7、商业 5、住宅 12、农业 3、防御 9、矿业 3；未生成成功态总览/局部预览。
- 同一次真实流程还复现了提交门禁缺陷：一次 schema 校验失败的 trace 明示 `attemptConsumed=false`，同 Context 修正后重试却返回 `CITY_BLUEPRINT_AI_SUBMISSION_ALREADY_CONSUMED`。本次没有删除 claim 或绕过门禁。

## 水域连接桥梁化复测

- 实现后复用同一 accepted Blueprint 重跑 D4；建筑连接无候选不再提前截断全城，后续主路与预览继续执行。
- `cityMainRoadPlan.bridgeConnections=3`：`agriculture -> defense` 64 格、`agriculture -> residential` 96 格、`residential -> commercial` 96 格，均为 `DELEGATED_TO_ROADWEAVER / AUTO_BRIDGE_NO_GROUND_SURFACE_PRINT`。
- `connectivityPlan.bridgeDelegatedEdgeCount=3`、`skippedEdgeCount=0`；普通 `streetBands` 不包含这三条水上桥面，后续由 RoadWeaver 自动桥梁能力接管。
- D4 core trace 为 `compiled`，总览和 7 张 Group 局部图均已生成；最终接口因农业 required terrain/stable、商业 tavern、行政 COURTYARD 环四个独立质量 hard block 返回 `CITY_BLUEPRINT_COMPILED_ANCHOR_FINALIZATION_FAILED`。
- 聚焦自动回归覆盖 Compiler、MainRoad、RoadWeaver bridge plan 和 Preview Renderer，全部通过；未调用 D5/D6/D7。

## D4-D7 完整真实验收

- 用户确认马厩、酒馆、农业地形缺口和行政庭院基础环不完整只作为警告，不阻断整座城市。修改后重跑 `city_compile_d4_blueprint`，正式返回 `ok=true`；D4 质量 `passed=true / score=100 / hardBlocks=0`，形成 64 个结构 anchor、3 条桥梁委托，并保留 5 条可追踪警告。
- `city_plan_d5` 通过；`city_plan_d6` 通过并锁定 64 个 planned-worldgen 结构，waiting 0、failure 0。
- 首次 `city_plan_land_use` 暴露 D4 正式阶段 `percentage_growth` 未被户外编译器识别；补齐阶段契约并重启后，继续暴露 required Landscape Parcel 的父界面耗尽被升级为整城失败。按当前案子“预览范围或合法界面耗尽时停止扩张并记录面积缺口”口径，改为保留已形成景观、跳过失败 Parcel 及其依赖后继并输出警告。
- 修复后 `city_plan_land_use` 返回 `ok=true / status=warning`：23 个 seed group 形成 14 个实际区域，claimed 101,965 blocks；9 个 required Parcel 因空间不足跳过，below-minimum group/region 均为 0。
- `city_execute_d5(confirmWorldMutation=true, roadProvider=roadweaver)` 成功激活。世界变更后端为 `worldgen_time_registry_only`，即时 changedBlocks=0；64 个建筑入口完成注册，3 条跨水连接全部为桥梁 connection 且状态 `planned`，无 partial registration。
- 首次 `city_execute_d7` 在传送前返回 `waiting_for_worldgen`：64 waiting、0 failure。随后通过 `realm_debug_command` 把玩家传到城市中心上空 `(62650,220,56990)`，再传到农业风车附近 `(62646,150,56965)`，由正常区块加载触发世界生成。
- 客户端关闭后无法再次调用 HTTP D7 收账；这次连接拒绝只记为环境已关闭，不记为功能失败。存档权威 `worldgen_placement_ledger.json` 按本次 `runId + citySeedId` 精确过滤得到 64/64 个不同 anchor，全部为 `TEMPLATE_MATERIALIZATION_RECORDED`，failure 0；生成时间 `2026-08-26T18:40:38.957372Z` 至 `2026-08-26T18:41:41.470722900Z`。
- 用户完成实机巡检并确认：本次建筑均已生成，D7 功能表现可接受；其余观感问题留待后续单独提出。

## 最终产物

- D4 总览：`run/realm_debug/normal_three_zone_20260826_0935/city_test_runs/city_realm_salt_kingdom_0_capital/steps/d4/structure_anchor_preview.png`
- LandUse 预览：`run/realm_debug/normal_three_zone_20260826_0935/city_test_runs/city_realm_salt_kingdom_0_capital/steps/land_use/land_use_preview.png`
- D7 存档账本：`run/saves/RTF0810/geomantia_city_masks/worldgen_placement_ledger.json`
