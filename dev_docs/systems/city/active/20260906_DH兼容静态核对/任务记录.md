# DH 兼容静态核对

*需求入口*：用户要求查看未完成的 DH 兼容案，并提供本地 mods 目录。产品真值为策划仓 docs/systems/city/10_product/案子/具体流程的案子/00_跨阶段主线/City固定模板唯一落地主线-v0.1/DistantHorizons远景兼容.md。

*修改内容*：只做静态核对并登记兼容缺口，没有修改运行时代码、玩家模组或配置。

*验证结果*：
- 实际文件 D:/PCL2-2.8.12/.minecraft/versions/1.20.1-Forge_47.4.23/mods/DistantHorizons-2.3.6.jar 的 mods.toml 与 MANIFEST.MF 均标明 2.3.6-b，目标 Minecraft 1.20.1。
- javap 核对该 jar：Forge StepStructureStart.generateGroup 调用 ChunkGenerator.m_255037_（createStructures）；StepFeatures.generateGroup 调用 m_213609_（applyBiomeDecoration）。DhLitWorldGenRegion 继承 WorldGenRegion；不能仅以 instanceof WorldGenRegion 判断真实生成。
- ChunkGeneratorStructureMaskMixin 的结构 TAIL、FEATURES HEAD/TAIL 都没有 DH 来源隔离；MinecraftCityWorldgenStructurePlacer 查询已激活计划并注入模板 start；CityLandUseWorldgenRegistry.applyForChunk 在 FIRST_WORLDGEN_FEATURES 路径 claim owner，成功后 recordApplied。由此确认伪生成可进入 City 执行与账本路径，但未实机复现某个存档已发生污染。
- ConfiguredFeatureMaskMixin 的植被抑制也未区分 DH 来源，兼容时应一起核对自然远景是否受到城市遮罩影响。
- src 未找到 distanthorizons/DhApi 接入；build.gradle 仅有开发预览 runtimeOnly 依赖，不能作为兼容已实现的证明。
- 本地 jar 提供 BatchGenerationEnvironment.isCurrentThreadDistantGeneratorThread() 和 IDhApiTerrainDataRepo.overwriteChunkDataAsync(...)，可作为后续隔离与刷新接入的调查入口；尚未验证刷新参数、完成时机、并发与游戏内表现，不能据签名宣称可直接完成兼容。

*剩余问题*：伪生成隔离、真实城市完成后的远景刷新及专门兼容回归尚未完成。需要证明 DH 预生成期间 waiting/owner/账本不变，玩家真实生成后城市完整且远景更新，重复刷新与重进存档不重复落地。本轮未启动游戏、未测 CPU、未提交。
