# 新存档河谷大城镇落地任务记录

## 任务边界

- W 来源：`run/realm_debug/new_save_w_20260722_01`。
- 城市 ID：`city_new_save_rice_town_20260722_01`。
- D3 精扫中心：`(-7104, 7232)`；目标区块必须在激活完成前保持未生成。
- 目标：完成 D3-D6、LandUse、Decoration 与 D5 worldgen 激活，停在可安全 TP 触发首次生成的状态。
- 城区和农业所有可印刷 LandUse group 均保持 `autoConnect=true`；不得出现 `autoConnect=false`。

## 设计要求

- 农业区包含风车、谷仓、仓储、马厩和农舍；使用连续不规则 CULTIVATE 面域、`CONTOUR_BANDS` 田地/水槽条带、稀疏稻草人和石质矮墙。
- 中心广场以喷泉为核心并布置长椅；行政区包含市政厅和警卫厅。
- 商业功能建筑与 3-4 栋小屋形成建筑群，优先使用双侧阵列；居民区使用多种阵列组织大量住宅。
- 至少三座警卫塔覆盖不同入口或外围方向。
- 城区使用连续 PAVE 铺装并由 RoadWeaver 真实道路连接，不保留大块裸露群系地板。

## 当前状态

- W 已 sealed；目标宏观区域为平原、向日葵平原、花之森林与河流交界，寒冷群系占比为 0。
- 目标 region 文件在 D3 前不存在。
- D4/D6 已冻结 52 个模板建筑；D4 阵列 provenance 已修复。
- LandUse 已按 52 个 anchor 的独立 growth region 重规划：14 个 group、16 个 area、83,977 个 claim；18 对自动连接中 9 对已连、7 对经扩张连通、2 对远端塔楼未连，所有 group/region 均达到最小额度。
- 等高线田地已修复 WATER 连接器复用导致的碎裂；农业区保持单一连通 area。
- Decoration 已冻结 10 个 program、134 个投影 slot，包含喷泉、长椅、灯、稻草人及各功能区生活装饰，质量分 100。
- D5 已激活 52 个建筑、LandUse、Decoration 和 RoadWeaver；D7 激活前检查为 52 个等待首次 worldgen、0 个结构失败。

## 首次 TP 崩溃与恢复（2026-07-23）

- `/tp Dev -7237 140 7458` 首次触发新区块时客户端崩溃；报告为 `ClassMetadataNotFoundException: CityDecorationBeardifierAccess`。
- 根因是客户端运行期间执行 `--rerun-tasks`，Gradle 先清空松散 class 输出后因文件占用未完成重建；已在客户端退出后强制重编译，缺失 class 恢复。
- 同次日志暴露目标存档未携带本次 catalog 使用的 Stubbs NBT，建筑批量 `TEMPLATE_NOT_FOUND`；已从 catalog 同源的 `落地测试/generated/geomantia/structures/` 补齐，52 个锚点的 39 个唯一模板引用现均存在。
- 四个目标 region 仍为 0 字节，worldgen ledger 中 placed/fragment/datum 均为 0，本次崩溃没有把半成品区块写入存档；失败记录保留作诊断证据。
- 强制重建和 5 组定向回归通过。后续客户端运行期间禁止再执行会改写 `build/classes` 的 Gradle 任务。
- 当前客户端已重新启动到主菜单；进入存档后先做当前世界模板实读，再次放行 TP。

## 实机观感与道路问题

- 用户确认整体方向可接受，除细节外暂未发现其他结构被道路破坏。
- 唯一明确现场位于 `(-7232, 82, 7391)`，属于市政厅实际 footprint；市政厅楼内入口 `(-7228, 7410)` 同时连接警卫厅与中心广场。
- City 连接计划保留 `direction=NORTH`，但 RoadWeaver bridge 只传端点坐标和模板 ID，导致警卫厅至市政厅的首段道路直接穿过市政厅。该现场归入既有 `CITY-RW-20260717-01`，本轮只补证据，不修改算法。
