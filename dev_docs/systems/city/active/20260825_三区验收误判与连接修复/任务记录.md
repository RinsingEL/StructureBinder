# 三区验收误判与连接修复

*需求入口*：用户要求“让子智能体严格按照案子在这个基础上再开发、测试一版”；当前真值为 `E:/Mod_Dev/designer_territoryMod/docs/systems/city/10_product/案子/具体流程的案子/30_D4蓝图编译/City递归蓝图与失败预算编译-v0.1/README.md`，尤其是相关功能区距离远时双方相向生长直到相遇、按唯一最高优先级功能区实际所属面积冻结占比基准，以及 D4 自行消化运行问题。

*修改内容*：已登记 `CITY-PW-20260825-18`。编译结果新增独立 `compilationAcceptance`：多 Group 且允许关系连接时，未在 Blueprint 关系中出现的功能区直接形成 hard block；每条正式连接边必须达到 handoff gap。D4 finalizer 会把内层阵列视觉 hard block 提升到外层 quality，不再出现内层失败、外层 100 分。最高优先级功能区面积改为首轮真实已提交结构与连接占地，不再用旧最低面积、功能区保留面积或 bbox 抬高；真实面积可为 0。`compiled` 只表示预览产物已生成，不再代表通过验收。

*验证结果*：修复前 run 已确认盐城 `relations=[]`、连接 0 边、`structureGraphConnected=false`；三区锚点最远约 475 格，432 个精确槽留空；外层 quality 100 与内层 6 个视觉 hard block 冲突。子智能体完成局部实现和测试后，主 Agent 去除了“要求所有功能区全图连通”的越界条件，保持案子规定的“无关 Group 不兜底连线”。聚焦 Java 回归 139 项通过；完整 Java 回归 725 项通过，0 failure/error/skip；MCP `npm test` 7 项通过；两仓库 `git diff --check` 通过。2026-08-25 真实 runClient 复验中，旧 `relations=[]` 三区被正确判为 `compilationAcceptance.passed=false`，不再误报通过；随后三个一次性正式 Blueprint 场景均未形成可验收三区，详见《真实游玩调用记录》并新增 `CITY-PW-20260825-19`。

*剩余问题*：真实测试环境已恢复，但三区生成仍未验收通过。跨海远区与内陆远区分别在主路阶段返回 `CITY_BLUEPRINT_MAIN_ROAD_NO_LEGAL_PATH`；按 D3 邻接图选择的三 Patch 同陆块场景虽然编译出 preview 状态，仍出现农业区为空、正式关系边 0、COMPACT 巷道缺失/断开，`compilationAcceptance.passed=false`。当前只能确认“误验门已修复”，不能确认“三区功能可用”。
