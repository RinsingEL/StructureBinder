# 阵列拓扑分类与功能约束

*需求入口*：文档仓库 `City阵列拓扑分类与功能约束-v0.1`；用户要求根据案子开始开发。

*修改内容*：在 `CityBlueprintGroupLayoutPlanner` 中将现有算法统一映射为核心规则、轴线/廊道、有界簇团、地形跟随四种空间落地模式；在功能区容量预留中按模式调整扩张排序，轴线型偏向编排槽位长轴，地形跟随型提高坡度、局部起伏和粗糙度代价；候选入口直接排除水体、缺失和未采样格，整个域无可用陆地时记录 `NO_USABLE_TERRAIN` 并保留结构 terrain gate 的具体诊断；D4 `groupResults[]` 和 `districtCapacityPlan.groups[]` 记录派生 `placementMode`。同步更新 CityBlueprint 数据契约与拓扑分类需求案。

*验证结果*：`./gradlew.bat test --tests com.rinsing.geomantia.systems.city.application.CityBlueprintGroupLayoutPlannerTest --tests com.rinsing.geomantia.systems.city.application.CityBlueprintCompilerServiceTest` 通过；`./gradlew.bat test --no-daemon` 通过；`country_designer_mcp` 执行 `npm test`，7 个测试全部通过；`git diff --check` 通过。跨水 terrain gate 既有回归仍返回原有具体地形失败，不再被容量阶段提前吞掉。真实启动 `RTF0810` 后，复用 T4 选中的 `SLOPE-01` 与 D3 `p.302`，D4 最终编译成功：25 个锚点、质量分 100、无 hard block/warning；中心簇 17 栋、居民簇 4 栋、市场轴 4 栋，三组最低成形数均达到。未执行 D5/D6，未 TP，未改世界。详见同目录 `真实游玩调用记录.md`。

*剩余问题*：模板组合 footprint、supportPolicy 和阵列预期空间尚未纳入功能区最低面积计算；该项仍由 `CITY-PW-20260819-03` 跟踪。当前 SLOPE-01 的核心槽位在 `SPARSE/BALANCED` 下无法达到城市尺度最低 4 栋，改用核心 `DENSE`、`ASSERTIVE` 后成功；这说明地形与阵列密度仍会直接决定可成形数量，后续需将该预判前置到蓝图/容量提示。
