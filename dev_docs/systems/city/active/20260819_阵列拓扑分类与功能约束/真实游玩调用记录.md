# 阵列拓扑分类与功能约束：真实游玩调用记录

*测试目标*：启动 `RTF0810`，不传送、不重扫、不执行 D5/D6；复用 T4 已选 `SLOPE-01`、D3 `p.302`，验证 `CENTER_SYMMETRIC` 父阵列下的核心簇、线性市场轴、有界居民簇，以及 terrain gate、功能区容量和 D4 预览。

*运行环境*：Forge 1.20.1 / 47.4.0；世界 `RTF0810`；玩家 `Dev`；游戏坐标始终为 overworld `(-4189,79,-416)`。MCP `geomantia-gis-debug` 服务正常；测试全程没有 TP 或世界写入。

## 调用摘要

| 序列 | 工具/动作 | 结果 |
|---|---|---|
| 10-41 | GIS、W、T1-T4、D2-D3、站点评审、官方结构目录 | 成功；T4 选 `SLOPE-01`，D3 选 `p.302`；一次早期候选参数错误已按返回修正。 |
| 42-65 | D4 Context/Blueprint schema 校正 | 记录并修正 `catalogMode/path/catalogPath`、引用 hash、safe integer seed、数组组合字段等错误；最终接受过一版 Blueprint。 |
| 66-69 | D4 编译 | 父阵列槽位可预留；随后暴露中心对称内部填充无合法落点。 |
| 70-72 | D4 编译再试 | `SMALL/BALANCED` 下组最低成形数不可达：核心实际 3、最低 4；terrain gate 本身通过。 |
| 73-78 | 稀疏版 Context/Blueprint/Compile | 去除 `fillPoolRef` 被 schema 拒绝；补回后提交成功；仍因城市尺度最低 4 栋失败。 |
| 79-83 | 四栋必选模板试验 | 重复 `requiredStructureRefs` 被拒绝；改为唯一模板后提交成功，但核心第四栋被 `D4_ARRAY_COUNT_UNSATISFIED`、包络重叠/局部起伏拦截。 |
| 84-86 | 唯一小模板试验 | 提交成功；核心/市场/居民组合仍无完整合法组合，返回 `CITY_BLUEPRINT_REQUIRED_STRUCTURE_NO_LEGAL_PLACEMENT`。 |
| 87-90 | 核心 `DENSE` + 全组 `ASSERTIVE` | 提交、编译成功。核心簇 17 栋，居民簇 4 栋，市场轴 4 栋；三组最低 4 栋均达到。 |
| 91 | `gis_status` | 成功；`Dev` 仍在 `(-4189,79,-416)`。 |
| 92 | `realm_status` | 成功；run `realm_w_mszypml0_fd99c6f2` ready，无 warning/error。 |

## 最终 D4 结果

*阵列结果*：父级 `CENTER_SYMMETRIC` 生成三个稳定槽位；`capital_core` 落地为 `CLUSTER_BOUNDED` 17 栋，`residential_cluster` 为 `CLUSTER_BOUNDED` 4 栋，`market_axis` 为 `AXIS_ANCHORED` 4 栋。

*地形结果*：最终三组 terrain gate 均通过，编译评估范围为全部相交 terrain-field cells；没有水体穿透或缺失采样导致的假成功。

*质量结果*：`quality_report.json` 为 `passed=true`、`score=100`、`hardBlocks=[]`、`warnings=[]`、`acceptedAnchorCount=25`。可选景观本轮没有实例，不影响 D4 成功。

*预览*：

- `run/realm_debug/realm_w_mszypml0_fd99c6f2/city_test_runs/city_realm_salt_kingdom_0_capital/steps/d4/structure_anchor_cluster_preview.png`
- `run/realm_debug/realm_w_mszypml0_fd99c6f2/city_test_runs/city_realm_salt_kingdom_0_capital/steps/d4/structure_anchor_preview.png`
- `run/realm_debug/realm_w_mszypml0_fd99c6f2/city_test_runs/city_realm_salt_kingdom_0_capital/steps/d4/city_generation_compile_trace.json`
- `run/realm_debug/realm_w_mszypml0_fd99c6f2/city_test_runs/city_realm_salt_kingdom_0_capital/steps/d4/group_extent_map.json`

*范围*：本次止于 D4 结构/功能区预览；没有启动 D5 道路实体化、D6、D7 或真实方块落地。
