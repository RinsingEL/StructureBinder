# 真实游玩调用记录

## 2026-08-27 新区块城市基面与道路

- 客户端：正常 `runClient`，世界 `RTF0810`，玩家 `Dev`，正式 HTTP AgentLoop。
- 历史候选门禁：`city_rtf0810_platform_terrain_05` 与 `city_rtf0810_platform_terrain_21` 均在确认阶段返回 `CITY_LAND_USE_CHUNK_ALREADY_AT_FEATURES`，未强落、未计验收。
- 正常选城：打开 `realm_t4` Patch Explorer，选择 `SLOPE-09`，`patchSelectionRef=psel_0171d04229cd468f67ce2f5eec6a631a5078adde17e4b69dad837d7a06dfe3a9`；通过新 T4 规划会话保留原 6 城并新增 `city_rtf0810_terrain_road_22`，中心 `(-4432,-4592)`。
- D2/D3：成功；城市级 Patch Explorer 选择最大连续坡地 `SLOPE-01`，正式 Patch `minecraft:overworld:step.16:city.city_rtf0810_terrain_road_22:p.464`。
- D4：5 个带冻结 `roadEntrances` 的 Trek 结构仅作占位；质量 100。30 段道路：15 `CITY_MAIN_ROAD`、2 `CITY_MAIN_ROAD_TRANSITION`、6 `COMPACT_ALLEY`、6 庭院环/门、1 `LINEAR_STREET_BAND`。行政—农业主路成功；农业—商业因完整宽度路径不可用而警告跳过。
- D5：新区块门禁通过，确认激活成功。
- 世界加载：旁观模式依次 TP `(-4039,-4777)`、`(-4337,-4706)`、`(-4184,-4600)` 与主路中段 `(-4188,-4740)`，等待真实 chunk worldgen。
- D7：重跑工作流后 `completed`。worldgen ledger：5/5 结构；LandUse ledger：57 owner chunks、5816 applied operations，其中 SurfacePrint feature operations 4994。
- 道路材质：深板岩瓦 slab/stairs 3656 格；磨制安山岩 920 格；泥砖 418 格。本次地形未生成桥，不记真实桥验收。
- 预览：`steps/d7/placed_structure_preview.png`、`steps/land_use/land_use_preview.png`。游戏保持开启，玩家停在主路中段上空。
