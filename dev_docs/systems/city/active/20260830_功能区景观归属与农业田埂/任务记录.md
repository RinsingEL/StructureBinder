# 功能区景观归属与农业田埂

*需求入口*：用户确认农业田野不应使用城市 GRID 道路；农田由整个功能区景观处理，相邻田块自行留一格间隔，并进一步确认景观应区分建筑地块、功能区和道路三种归属。

*修改内容*：Blueprint `LandscapeOwner` 允许只写 `groupId` 表达功能区所有，旧 `requiredStructureRef` 保持兼容；D4 景观容量对 `FUNCTIONAL + GROUND_PATH/CORRIDOR` 的后续 Parcel 从父边界外第二格接力，并禁止任何后续田块吃掉中间一格。该策略同时把所属建筑组切换为 `LANDSCAPE_GAPS`，关闭 GRID 净空预留、正式区内 GRID 街网和相应街网门禁，但保留建筑阵列几何。D4 局部预览按 Parcel 交替着色并标注，便于直接观察田块与间隔。

*自动验证*：新增功能区 owner codec 回归、四块功能农田一格互斥回归和无正式街网的 GRID 视觉门禁回归；全量 `gradlew test` 通过。

*真实 D4 验证*：复用 `realm_w_mte38ltl_f1964c5a / city_realm_green_court_2_capital` 已接受 Blueprint 正常调用 `compile_d4_blueprint`，结果 `ok=true`、质量通过。农业区 25 个建筑 anchor 保持不变，正式区内路由 8 条降为 0；4 块农田共 15,456 blocks，所有非根田块 `separatorWidthBlocks=1`、最大 shared boundary 为 0、最小 separated boundary 为 40。该旧 Blueprint 仍报告 `STRUCTURE_SEEDED`，但相同 schema 已支持后续设计直接使用 `FUNCTION_AREA` owner。

*范围*：本轮验证到 D4 规划与预览，没有执行 D5/D7 世界写入；田埂当前是景观保留的一格原地表间隔，不是城市正式道路。

*后续真实 D7*：同一 run 已执行 D5-D7。首次 LandUse 为 267/268，唯一缺失 owner 因跨边界原版树冠触发占用保护；修复 `CITY-DL-20260831-01` 后未删除区块，受控回填 1 个 owner 至 268/268，33 个结构全部完成。玩家验收点为 `138950 90 139187`。
