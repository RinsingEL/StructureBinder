# Qadir 户外编译耗时诊断

*需求入口*：用户要求检查 validation_03 一直显示“D4 后自动编译中”的现场状态。

*修改内容*：用户随后授权优化。RelayRegionGrowthClassifier 改为增量候选排序、按需安全检查、精确 LIFO 回溯及原生 long 集合；SearchFrame 只保存已尝试位置，不再每格复制整个前沿。新增 GridRemovalConnectivity，用平面四邻接图的对偶面与可回滚并查集精确计算割点（删除非孤立顶点的连通分量增量为 degree - incidentFaceCount）；删除时缓存割点直到邻格变化，回溯时清除缓存。极稀疏大包围盒仅切换到精确多源图搜索，不截断 mask、不新增设计阻断。LandscapeParcelExpander 用有序候选队列，仅更新新 claim 周围的评分/路径，同时保留原 contested 计数时序。未修改面积、内容、配比、起点、种子、连通要求或世界数据。

*验证结果*：2026-09-06 13:44–13:46 读取 validation_03/provider_e88e62d43d1875ba_r8192 的 Qadir 首都 Post-D4 job，attempt=2、status=running，开始时间13:38:41。Minecraft PID27484。两次 jcmd Thread.print 的 Geomantia-City-Post-D4 均为 RUNNABLE，线程 elapsed369.75→403.06秒、CPU350796.88→382078.12毫秒，约94%单核；均在 RelayRegionGrowthClassifier.articulationPoints，经 safeCandidates/growRegion、SurfacePrintPlanner、handlePlanBlueprintOutdoor 调用。当前 land_use/city_outdoor_intent_plan.json 更新时间13:38:47，后续规划尚未完成。代码确认区域逐格扩张时 safeCandidates 可重算全剩余 mask 的割点；stageCanRelay 等路径也会全图检查。哈希树查找出现在两次栈顶，但其具体耗时占比未经性能分析，不能断言为唯一原因。当前不在 AI 调用，也不是线程锁等待。

*剩余问题*：游戏内后续激活、落地与可视化未在本轮验收，子步骤 UI 进度尚未修改。此前 BlueprintCompiler 独立回放不覆盖本次户外铺地路径。第一次口头更新误称尚未进入建筑编排，后续已按工作流顺序纠正：当前处于建筑规划之后的户外处理。

## 优化验证

- 现场容量并非小农田：farm_lands 请求 6 个 Parcel、冻结 4 个，actualAreaBlocks=614925；四块为388083/81918/912/144012。本轮未缩小该方案。
- 仅惰性检查、原生集合或多源 BFS 的中间版本仍有分钟级瓶颈；已终止相应开发端独立测试进程，不将中间版本称为通过，未终止游戏进程。
- 旧版 12 组区域扩张结果（包含逐格 provenance/失败原因）的 SHA-256 为 a86dbe78691cdfba9232126ff20edacfc580869444903df58072bf66321dc188，最终优化后相同。
- 旧版 8 组外轮廓、路径代价、父子来源与计数指纹为572ae8196458a003eadd822d013cc23c482da22c7d73c37a1bd295cf0f1f1db2，优化后相同。
- 穷举所有65535个非空4×4 mask的每个删除点（524288个删除判定），对偶面索引和精确搜索均与独立暴力连通分量计数一致；同时验证删除后的查询与恢复。
- 14:17专项25项通过。真实冻结 Blueprint、catalog、D5/D6、capacity 与 terrain 全部只读输入，QadirOutdoorReplayTest 在10.4981896秒完成 CityOutdoorBlueprintCompiler + LandUsePlanningService，areas=5；其中输入编译2.1088861秒。不是 GLM 回合或完整城市落地耗时。
- 最终全量 `gradlew.bat build '-Pforge_version=47.4.23'` 成功，817项、0失败、0错误、3跳过；显式设置 GEOMANTIA_QADIR_RUN，因此本次 Blueprint 与 Outdoor 两个真实输入回放均实际执行而非跳过。完整户外规划再次完成于9.7187396秒，5个Area，其中4个接力Parcel实际覆盖575630格；逐格验证 member减exclusion 完整覆盖、无重复/越界，span与region面积一致。预留614925格与户外实际575630格不是同一指标，不能声称预留全是作物。
- 非环境依赖65536格回归0.406秒完成，两区域各32768格；坐标Integer边界、盒装Set调用、连续删除与LIFO恢复、稀疏超大包围盒精确搜索均通过。
- 包：build/libs/geomantia-0.1.0.jar；SHA256 CF346FA47ECE92C30588D30B8381E5B2C99F0E4B2192ABC883F33F7904FA658E。未自动替换PCL实例Jar或配置，未提交。
