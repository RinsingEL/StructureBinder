# 连接参数 Schema 互斥

*需求入口*：真实 Provider Agent 在首城 D4 提交中把 `compound_cluster` 与 `guide_line_dual_side` 参数混填，连续校验拒绝后停止；用户要求修复。

*修改内容*：游戏内 Provider 与外部 MCP 的 `connectionPlan.parameters` schema 已改为两个严格分支：`compound_cluster` 仅公开 `clusterShape`，`guide_line_dual_side` 仅公开 `sideMode/stagger/widthClass`；同时在 Agent 指令、算法引用字段和提交工具说明中明确必须先解析 planner family，禁止跨族混填。

*验证结果*：`DeepSeekToolLoopClientTest` 针对性回归通过；`country_designer_mcp` 构建及 7 项测试通过；`gradlew test jar` 全量 709 项测试通过，0 failure、0 error；`git diff --check` 无空白错误（仅工作区既有 LF/CRLF 提示）。生成 JAR SHA-256 为 `46F3A4E2E7790146B4D1168432CD7B016623C0A55F9F9C7FDD0ED90EA6F86638`。

*部署*：已在确认无 Java 游戏进程后部署到 `D:\PCL2-2.8.12\.minecraft\versions\1.20.1-Forge_47.4.23\mods\geomantia-0.1.0.jar`，目标哈希与构建产物一致；旧包备份为同目录 `geomantia-0.1.0.pre-20260904-connection-schema.jar.bak`，哈希 `DAA8EFD7BCF3736C4E766B8F96168066E2FD6A4E01112CF442C5F2A766789BCA`。

*剩余问题*：需启动游戏后用真实 Provider 续跑当前 `D4_CONTEXT_PREPARED` 城市，确认模型按互斥分支重提并推进编译；若仍拒绝，应保留新的 Provider 响应作为独立兼容性证据。
