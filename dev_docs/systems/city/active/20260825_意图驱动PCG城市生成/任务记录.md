# 意图驱动 PCG 城市生成

*需求入口*：`E:/Mod_Dev/designer_territoryMod/docs/systems/city/10_product/案子/具体流程的案子/30_D4蓝图编译/City单次递归蓝图与自愈编译-v0.1/README.md`；用户要求严格完成案子，三区实例真实游玩，成功后生成预览图并推进至 D6。

*修改内容*：补齐 `targetAreaShare`、`spaceComposition`、`expansionPolicy` 的蓝图编解码与校验；冻结唯一 CORE 实际占地并按占比派生动态目标；显式关系相向扩张且取消无关最短路回退；按最终目标重算必需景观 Parcel 面积；普通坡度/起伏进入 foundation/platform 适配，特别陡成员可单项跳过；运行期容量/候选域/阵列视觉缺口记录并继续 D4，保留 D3 memberCells 供逐结构 terrain gate；无效输入写报告后允许同案重提且不消耗次数；同步 MCP Group 严格输入与数据契约；更新对应单测。

*验证结果*：目标单测已通过（75 tests）；`npm test` 已通过（7 tests）；全量 `./gradlew cleanTest test --no-daemon --console=plain` 已通过（99 classes / 724 tests，0 failures，0 errors）。正式 MCP 真实游玩 `rtf0825_city_single_recursive_20260825_02`：W/T1/T2/T3/T4 三区验收通过；盐/石/绿 D3 完成，D4 全部 `compiled`，D5 全部 `ok`，D6 全部 `ok` 且 `locked=true`，分别生成 structure materialization plan、ledger、trace、preview。

*剩余问题*：无；已完成双仓库 diff/契约检查，待提交。
