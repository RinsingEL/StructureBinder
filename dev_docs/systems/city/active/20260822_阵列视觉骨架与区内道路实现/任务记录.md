# 阵列视觉骨架与区内道路实现

*需求入口*：`City阵列主从骨架与街带-v0.2`；用户确认 GRID 行列误差最多 1 block、COURTYARD 最小 5 栋、GRID/COURTYARD/COMPACT 必须产区内道路、CENTER_SYMMETRIC 轴街可选、ORGANIC_COMPACT 不产正式道路，并补齐六种阵列视觉几何门禁。

*修改内容*：GRID、COURTYARD、LINEAR、CENTER_SYMMETRIC 及父规则阵列统一最大模板跨度、世界/入口轴与精确槽位，非法槽留空；父槽分离几何 `origin` 与 LINEAR 轴端 `placementOrigin`。COURTYARD 改为空心五槽和南门，COMPACT 改为弯巷两侧并按完整 pitch 分隔同侧建筑，ORGANIC_COMPACT 只在随机 guides 内形成 collision gap 1–3 blocks 的连通簇。新增 `CityInternalStreetPlanner`，把 GRID 街巷网、COURTYARD 环路/门洞、COMPACT 弯巷、LINEAR 街带和可选 CENTER 轴街冻结为多段 `streetBands[]`；新增 `CityArrayVisualQualityGate`，最终几何不达标即阻断 D4。Reference Catalog/MCP schema 增加仅 CENTER 可用的 `centerAxisStreetEnabled`。

*验证结果*：`./gradlew.bat test --no-daemon` 全量 695 项通过；`country_designer_mcp npm test` 7/7 通过；双仓 `git diff --check` 通过。真实 `RTF0805 / city_realm_salt_kingdom_8_capital / r.23.-12:p.1` 以一个父 GRID 同时形成六种子阵列，共 38 anchors；GRID 理论格点最大误差 0 block、COURTYARD 中心空且基础环围合率 1、COMPACT 弯巷/门脸通过、ORGANIC 1–3 block gap 图连通、CENTER 镜像与可选轴街通过。正式道路共 21 段，最终 quality 100，0 hard block/warning/review；未执行 D5 或世界修改。

*剩余问题*：无。
