# 落地底线与质量分层

*需求入口*：用户确认“安全与核心功能过关即可落地，局部质量问题告警”，要求调整并试验。

*修改内容*：指定建筑以同 Group 最终实际提交的同一 structureRef 和数量核对；保留 REQUIRED 阶段缺口。入口局部断路及阵列美学问题作为警告；资源、边界、碰撞、空功能区及必需关系仍阻断。道路安全检查包含 connectivity_growth 建筑；旧 arrayVisualGapRecorded 不再能降级碰撞。新增 allRequiredContentPresent/qualityFullySatisfied 和明确验收策略。MCP 仅将有正式引用的四类完整编译几何/搜索产物替换为摘要与文件引用，不修改原 artifact，不截断作者数据或质量报告。

*验证结果*：首轮聚焦 82 项中 1 项旧入口硬失败断言需同步；修正后全量 763 项仅未修改的景观缓存性能测试失败（8 秒阈值实际 8.814 秒），2 个环境测试跳过。新增精确 ref/数量、局部入口告警的完整编译→finalize、视觉警告、碰撞不可被 gap 标记降级、连接建筑碰撞、MCP 大产物摘要且完整质量保留测试均通过。正在单独重测性能项。开发存档 MCP 待复验；不重跑 GLM，不改原测试端存档。

*剩余问题*：用户确认后的局部调剂已实机通过，D4/D5/D6/户外编译和世界激活完成。灯柱连接修正后 D7 户外 owner 从 61/276 增至 182/276，停在 (139,308) `CITY_LAND_USE_ACCESS_PLATFORM_MISSING`，该新错误尚未定位或修改。D7 内联结果仍超限 391207 chars，展示修复未完全收敛。本阶段复用旧 GLM 蓝图，由 Codex 直连 MCP 验证，不能代表 GLM+Hermes 自主首城速度或完整落地通过。用户要求暂停后未继续修复；开发客户端仍开着，Provider disabled，无新 GLM 调用。全量性能项曾超 8 秒、单独能过，未改阈值。

## 验证进度与 MCP 调用

- 阶段提交前全量回归：`gradlew.bat test -Pforge_version=47.4.23 --console=plain`，1m52s BUILD SUCCESSFUL；772 tests、0 failures/errors、2 optional skips。本轮性能项亦通过，未放宽阈值。三仓 diff --check 通过；文档提交 ce98fcd、TerraSense 兼容说明提交 7f829d5，均未推送。

- 最后实机轮：realm_status 同开发副本；`city_execute_d7` 同 run/city/executeStructurePlacement=true 实际续队列，返回展示超限 391207 chars。读取日志发现新原因后，以同参数正式轮询刷新结果，返回 `CITY_LAND_USE_D7_OWNER_INCOMPLETE: plannedOwners=276, appliedOwners=182, missingOwners=94`；completion status=failed，(139,308) ACCESS_PLATFORM_MISSING、rollbackComplete=true。有效调用已获得失败结果，用户要求暂停；未继续修复、未重跑 GLM。会话 63601 保持运行。
- 用户确认阶段提交。提交前只做全量自动回归、差异检查、补全记录与归档，不处理新入口台地或 D7 展示缺口。后续主线应验证 GLM+Hermes 的实际设计/编译/落地耗时和调用次数，不能以 Codex 直连结果替代。调度表述明确为：全部 T2 完成→统一 T3→单国 T4 城市选址→该国城市流程，未改为单国提前 T3。

- 灯柱执行修正：连接方块（Fence/Wall）和 boundary 一样先写精确 raw 身份、整批结束后统一求连接状态；收尾位置覆盖 BASE/FEATURE/CROP/BOUNDARY 与邻居，不再只处理 boundary。连接快照回滚同样接受已恢复状态。新增无 boundary 的 ROAD_LAMP 仍纳入 finalize 回归，原跨 owner 双向 fence 和失败恢复回归保留。D7 `landUseOwnerCompletion` 纳入已批准的正式 artifact 摘要策略，complete/计数/failures 原样保留，不内联数十万字符的 foundation 诊断。三组聚焦测试通过，准备重启只续 D7。

- 写入诊断复验：realm_status 确认同开发端；`city_execute_d7` 同 run/city/executeStructurePlacement=true 实际入队，返回展示超限 467760 chars（未立即重试）；正式 D7 quality 5 planned/18 waiting，completion queued、61/276。20:32:37 日志精确定位 `(2285,79,4808)`、`city_main_road_001::segment_002::lamp_2285_4808`、FEATURE ROAD_LAMP：dark_oak_fence 写后 west=false，邻居求解要求 west=true。owner (142,300) 再失败但 rollbackComplete=true。本轮有效结果失败，结束游玩并保存关闭会话 65989。首次 worldgen 与 FULL 补写均可遇到连接方块没有显式写入 resolved shape；不再把其混同普通相同状态返回 false。接下来仅修连接写入与批次收尾，并把已有 artifact 的 D7 owner diagnostics 从工具内联转为正式引用，保持完整正式文件与错误信息。

- 写入层轻量检查确认 `writeExactBlockState` 把 setBlock 返回 false 一律当失败；Minecraft Level 对相同状态写入返回 false，回滚亦会遇到相同快照。已改为先验证可写、已有精确状态幂等成功、写后严格比对实际状态；拒绝错误状态/不可写不变。加入写入坐标/phase/请求状态/实际状态日志，不能据此提前断言 worldgen 首次失败同根因。Executor/Registry 聚焦回归通过。按 bug-layer-debugger 在执行层以正式 owner 报告、原版本地源码和状态回读为证据；将重启同存档仅续 D7，不动冻结计划或已有 owner ledger。

- 续轮实机：20:22:53 `city_run_workflow` 同 run/city、skipExisting=true、confirmWorldMutation=false、planWalls=false，8143ms 返回 waiting_for_confirmation；户外编译 7770ms 成功，正式 quality 精确记录同 CULTIVATED 区域 256→259、256→253，BANK/WATER 未改。
- 20:27:15 `city_run_workflow` 同参数改 confirmWorldMutation=true，2950ms 返回 waiting_for_worldgen；D5 激活成功，D7 正式启动 276 owner 异步队列。20:27:18–25 日志出现 23 个模板 StructureStart，但这不等同于全部方块已落地。
- 随后 `city_execute_d7` 同 run/city、executeStructurePlacement=true，返回 `CITY_LAND_USE_D7_OWNER_INCOMPLETE: plannedOwners=276, appliedOwners=61, missingOwners=215`。正式 `steps/d7/land_use_owner_completion.json` status=failed，chunk(142,300) `CITY_LAND_USE_BLOCK_WRITE_FAILED`、rollbackComplete=false。日志还含多个 worldgen owner 写入失败。有效调用已得到功能失败，结束本轮 Agent Loop，根因待定位；不重跑上游或把部分落地报成功。
- 20:28:18 `realm_debug_command {command:help geomantia, confirmCommandExecution:true, sourceMode:server, saveAfter:true}` executed；日志 All dimensions are saved，随后关闭自有客户端会话 6166，进入已授权写入层诊断。

- 续轮用户确认“总面积不变、功能角色保留”的局部比例微调。采用最多 ceil(地块面积×1%) 的连通性调剂，优先从同 role 区域借用且任何阶段至少保留 1 格；trace 保留原目标与实际，不删除角色或修改作者蓝图。实现和复验进行中。
- 已实现 root 连通最低配额预计算与确定性小额调剂、原目标/实际 trace、quality `LANDSCAPE_LOCAL_SHARE_ADJUSTED` 告警；SurfacePrint 不再删后继角色。625 格线形冻结 source=258 回归精确证明首区 256→259、同 FIELD 后区 -3、BANK/WATER 不变、完整覆盖与相邻 provenance。超出 1% 仍拒绝。codec 证明目标/实际差异保留并进入 hash。聚焦回归 43 项通过（首轮 42 项中的旧“删角色成功”断言已按用户新边界改为拒绝）。开始本轮实机复验，不重做 D3/D4/D5/D6。
- 全量 763 项中的唯一超时项单独复测通过（未放宽 8 秒阈值）；不能表述为全量一次全绿。
- 本轮目标：复用开发端 validation_02 有效 D3 和旧 GLM accepted Blueprint；更新的编译器重新产出 D4，随后按正式 workflow 激活与检查落地。不会 prepare 重置 Context，不改 failure budget。
- `tools/list`：已读取 compile/workflow/status/debug command/worldgen observation/D7 等正式说明与 schema。
- 通过 Gradle 启动 Forge 47.4.23 + RTF + 结构包，显式指定 validation_02 与已修入口素材池。
- `realm_status {}`：serviceReady=true，debugRoot=开发端 validation_02/realm_debug。
- `city_compile_d4_blueprint {runId:provider_ce6a3c351fa2667b_r8192, citySeedId:city_realm_sunmarch_caravan}`：17645ms，ok=true/compileStatus=compiled；23 anchors，4 功能区、3 required relation、allRequiredContentPresent=true、hardBlocks=[]。qualityFullySatisfied=false，19 入口/1 organic/3 REQUIRED 阶段缺口继续警告。上下文未改变，failureCount 保持 1，预算正式记 succeeded。MCP 完整返回，展示超限已在本城消除。
- `city_run_workflow` 同 run/city，skipExisting=true/confirmWorldMutation=false/planWalls=false：开始 D5/D6 预检，未提前传送或加载目标区块。
- 返回：828ms，D5 success、D6 success，city_plan_land_use 有效调用失败 `CITY_OUTDOOR_REQUIRED_LANDSCAPE_INSTANCE_DRIFT:ls_caravan_pasture::instance_01`；未激活/写世界。正式 manifest 位于同城 test_run_manifest.json。本轮 Agent Loop 结束，按既有修复授权进入 D4 capacity→D6 户外交接诊断，不绕过冻结一致性门禁。
- 诊断：D4 capacity 仅有 fields/meadow，无 pasture 且 warnings=[]；Blueprint 显式绑定马厩，马厩只在 connectivity/percentage 阶段存在。两端 owner 筛选均只认 REQUIRED。修改为同 Group 同 ref 全部实际实例按 REQUIRED 优先、anchorId 稳定排序；最终百分比容量重规划纳入后续主人，冻结 ID/footprint 检查保留。
- `realm_debug_command help geomantia + saveAfter=true` 返回 executed；19:55:57 All dimensions are saved 后结束自有 Gradle 客户端会话，准备重建复验。
- 主人修正后聚焦回归 79 项全部通过（Blueprint 45、capacity 13、outdoor 21），含后续阶段同 ref 主人、REQUIRED 优先、冻结 footprint 漂移仍拒绝。进入第二轮 Agent Loop，重新启动同一开发存档。
- 第二轮 `realm_status` 确认同开发存档；`city_compile_d4_blueprint` 17523ms，ok=true/hardBlocks=[]/failureCount=1；牧场实例现绑定 g3_trade_barns_connectivity_003，实际面积 4497，农田 4500、花地 936。
- `city_plan_d5`、`city_plan_d6` 同 run/city 显式刷新：接口展示分别超限 255006 / 406580 chars（未重复执行）。先前正式返回的 D5/D6 quality artifact 已在 20:00:31/20:00:58 更新且 passed=true。通过 workflow 的紧凑返回继续，不把展示错误当程序未执行。
- `city_run_workflow` 同 run/city，skipExisting=true/confirmWorldMutation=false/planWalls=false：3332ms，正式复用 D3/D4/D5/D6，户外编译再报 `RELAY_GROWTH_START_DISCONNECTS_REMAINDER:region-001-CULTIVATED`。第二轮游玩结束，未激活；继续诊断局部 region-growth 程序问题。
- 20:03:35 正式 help geomantia/saveAfter 保存，日志 All dimensions are saved，结束自有客户端会话。
- 接力算法修正：原算法在根 source 是割点时立即拒绝，即使第一角色预算足以吸收短枝也不尝试。现在保持精确 source，先以相邻逐格生长吸收较小分支，只留下单个连通 remainder；不改 mask/source/目标面积/角色数量，预算不足仍拒绝。新增可解/不可解线形瓶颈回归并验证 provenance、精确覆盖与预算；接力/SurfacePrint 聚焦测试通过。真实冻结 capacity 另发现 flower 第二 Parcel 有 1 格短枝；本次 CULTIVATED 的最终 area 仍需实机复验。
- D5/D6 已有正式 artifact 的大几何也纳入同一 MCP 展示摘要策略，质量报告和 locked 结论保留。
- 第三轮 `realm_status` 确认同开发存档；`city_run_workflow` 同参数 4546ms，D3-D6 正式复用，户外失败被精确定位为 `RELAY_GROWTH_START_DISCONNECTS_REMAINDER:region-001-CULTIVATED:minimumRootArea=259:target=256`。起点分支前缀修正不是本城完整修复，实际第一角色配额差 3 格；本轮停止扩大修改，比例松弛/角色缩减会改变景观配额语义，留给用户确认。不放宽冻结范围、碰撞或偷偷删角色。
- 最终收尾 `realm_debug_command help geomantia/saveAfter=true`：executed，20:09:36 All dimensions are saved，随后关闭自有 Gradle 客户端；TCP 5000 无监听。没有提交或推送，没有改 PCL 原存档，没有新调用 GLM。执行最终全量测试与双仓 git diff --check。
- 最终全量：Forge 47.4.23，767 项，1 项失败、2 项环境跳过，其余通过；唯一失败仍为 `CityLandscapeCapacityReservationPlannerTest.reusesInvariantCandidatesDuringMultiLandscapeJointSearch` 的 8 秒性能预算。新增验收、owner、连通前缀、D4/D5/D6 展示回归全部通过。双仓 diff --check 无空白错误（仅既有 LF/CRLF 提示）。
