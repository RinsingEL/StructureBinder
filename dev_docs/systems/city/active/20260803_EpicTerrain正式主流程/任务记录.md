# Epic Terrain 正式主流程任务记录

## 任务范围

- 复用已完成 W 的 `runId=epic_terrain_w_20260803_01`，不重扫 W。
- 走正式 T1-T4 与 City D3/site review/CityBlueprint/D4/D5/D6/LandUse/D7 路径。
- 城市目标为大城市，至少三个功能组，必须包含农业与 `contour_bands`，不使用 legacy/debug D4。
- 本轮没有修改实现代码或文档仓库真值，也没有提交。

## 国度阶段

### T1

- 墙钟耗时：438 ms。
- 生成三个国度：`realm_salt_kingdom_0`、`realm_stone_march_1`、`realm_green_court_2`。
- 产物根目录：`E:\Mod_Dev\StructureBinder\run\realm_debug\epic_terrain_w_20260803_01`。

### T2

- 盐之王国：`MINECRAFT_PLAINS-01`，grid `(21,7)`，block `(2688,896)`，提交 233 ms。
- 石之边境：`MINECRAFT_BADLANDS-01`，grid `(-22,-23)`，block `(-2816,-2944)`，提交 23 ms。
- 绿庭：`MINECRAFT_BIRCH_FOREST-01`，grid `(6,10)`，block `(768,1280)`，提交 22 ms。
- 三处均通过正式 Patch Explorer `open -> show -> select` 取得 `patchSelectionRef`，没有直接提交网格坐标。

### T3

- 墙钟耗时：314 ms。
- 三个国度均为单一连通分量，detached/hole 均为 0。
- owned 95.0196%，wild 1.1765%，contested 1.0196%，blocked 2.7843%。
- 产物：`realm_territory_map.json`、`territory_preview.png`、`t3_report.json`。

### T4

- 使用正式 patch planning、T4 Patch Explorer、capital selection 与 finalize。
- 盐之王国首都最终锚点 `(2752,960)`，`theoreticalScale=capital`；finalize 389 ms。
- 石之边境和绿庭首都均为 `large_city`；finalize 分别 247 ms、187 ms。
- City 主流程选择 `city_realm_salt_kingdom_0_capital`：滨海大平原适合行政、市场、住宅，西侧山谷承担农业。
- 一次脚本变量名误用 PowerShell 只读 `$PID`，只额外留下盐之王国 planning session `90512`；未改变最终 T4 registry。

## City 阶段

### D3 与现场复核

- D3 墙钟耗时：42,867 ms；`cellStepBlocks=16`，`patchScanPaddingBlocks=512`。
- 结果：192 patches、36 regions、2304 LandUse cells。
- 关键地形：北部 `平原42` 约 212,065 blocks；西侧 `山谷136` 约 122,177 blocks；东侧为水域。
- site review 接受耗时：223 ms。
- 复核图：`city_d3_city_realm_salt_kingdom_0_capital/landform_review_map.png`。

### CityBlueprint 与程序化 D4

- 正式 Blueprint context 准备：446 ms；恢复后重建同一确定性 context：428 ms。
- 第一次提交因调用脚本中的 `Group` 被 PowerShell 解析为 `Group-Object`，实际 `groups=[]`，服务端正确拒绝，耗时 250 ms。失败产物完整保存在：
  `city_blueprint_city_realm_salt_kingdom_0_capital_invalid_agent_scripting_20260803_012425`。
- 第二次提交：217 ms，通过 Blueprint v0.4 校验。
- Blueprint 含 5 个功能组：核心市场、东西住宅、工坊工业、西侧农业；使用 `preferredPatchZone`、`extentClass`、`densityClass`、`connectionPlan` 和显式 HIERARCHY/CONNECTION。
- 程序化 D4 编译：821 ms，生成 78 个锚点；农业 25、工业 13、核心 10、东住宅 10、西住宅 20。
- 5 条连接边 final gap 均为 0，`structureGraphConnected=true`，fallback edge 0，质量 100。
- 产物：`city_d4_city_realm_salt_kingdom_0_capital/structure_anchor_map.json`、`city_generation_compile_trace.json`、`group_extent_map.json`、`structure_anchor_cluster_preview.png`。

### D5/D6 与模板导入

- D5 预案：468 ms；78 anchors，植被/原版结构 mask 各 248，质量 100。
- D6 首次检查：293 ms，78/78 `TEMPLATE_NOT_FOUND`。原因是当前存档 `新的世界 (6)` 没有 generated Geomantia 模板，而不是 D4 几何或 hash 漂移。
- 从旧存档 `新的世界 (4)` 向当前存档导入本次使用的 7 个 Stubbs NBT；不覆盖任何现有文件。执行服务端 `/reload` 后，metadata 7/7 可读，尺寸和冻结 hash 全部一致。
- D6 重跑：550 ms；locked 78/78，失败 0。
- 结构 owner chunks 共 338 个，覆盖 block x `2384..3039`、z `192..607`。

### LandUse

- LandUse v0.3 墙钟耗时：240,370 ms。
- 显式规则：核心商业、东西住宅、工业、农业；农业强制 `contour_bands`。
- 冻结材料：`minecraft:farmland`、`minecraft:wheat`、`minecraft:dirt`、`minecraft:water`、`minecraft:oak_slab`；城区 PAVE 默认 `minecraft:gravel`。
- 结果：5 个 seed groups、11 areas、201,513 claimed blocks；农业 139,925 blocks。
- 自动地表连接 1 条且已完成，below minimum 为 0，warnings 为空。
- 产物：`city_land_use_city_realm_salt_kingdom_0_capital/city_land_use_area_plan.json`、`city_land_use_surface_print_plan.json`、`land_use_preview.png`、`city_land_use_planning_complete.json`。

### 激活与当前状态

- `city_execute_d5`：1,923 ms，成功激活 78 个 planned structures、LandUse 与 RoadWeaver；未提前生成区块，changedBlocks=0。
- RoadWeaver 已注册：5 个 placement groups、77 条连接，其中组内 73、组间 4，`generateImmediately=false`。
- `city_execute_d7` 首次检查：486 ms；78/78 为 `WAITING_FOR_WORLDGEN`，failure 0。
- 当前正式状态是等待目标 owner chunks 首次生成。没有强制传送玩家或强载区块，以免把等待态误当 late paste；区块生成后应再次调用 `city_execute_d7` 完成 ledger 与道路后处理。

## 验证与风险

- W 沿用 sealed artifact，没有重扫。
- T1-T4、D3 review、Blueprint validation、D4 quality、D5 quality、D6 runtime NBT/hash、LandUse quality 均通过。
- 当前唯一未完成项是 worldgen 自然触发后的 D7 completion；这不是规划失败。
- 当前存档新增 7 个 Stubbs NBT；实现仓库原有 `build.gradle`、`gradle.properties` 与运行日志改动均未回滚或覆盖。
