# D4 蓝图失败预算

*需求入口*：`docs/systems/city/10_product/案子/具体流程的案子/30_D4蓝图编译/City递归蓝图与失败预算编译-v0.1/README.md`；用户确认应删除 CityBlueprint 单次提交消费语义，改为与 Codex 类似的 5 次真实失败预算；Agent Loop 应依据工具返回信息修正，不以读取源码或项目文档作为恢复步骤。

*修改内容*：删除 Context 级单次 `.claim` 与 `aiCityDesignSubmissionCount` 门禁，新增独立 `city_blueprint_failure_budget.json`：D4 程序编译或终审明确失败时原子增加 `failureCount`，同一 Context 最多 5 次；提交参数/schema/引用拒绝不计数。第 1～4 次失败返回 `retryAllowed=true`、`nextAction=city_submit_d4_blueprint`，第 5 次转人工。拒绝 revision 写入独立 rejection artifacts，不覆盖当前 accepted Blueprint；active revision 发布只做短原子写入，不锁编译过程。MCP 与 workflow report 明确只允许依据工具响应和返回 artifacts 修正，禁止 Agent Loop 读取服务端源码、项目文档或未返回的原始 run 文件。同步更新文档仓 `CityBlueprint数据契约.md` 的旧单次提交边界与错误码。

*验证结果*：`CityBlueprintServiceTest` 覆盖合法重提、拒绝不覆盖 accepted、5 次耗尽和并发失败原子计数；`CityWorkflowStepRunnerTest` 覆盖自动 workflow 保留失败预算和黑盒恢复指引；相关 HTTP/队列聚焦回归通过。Java 全量 105 suites / 770 tests，0 failure、0 error、0 skipped；MCP `npm test` 7/7 通过；`git diff --check` 无空白错误。真实 Agent Loop 已续跑 `natural_3realm_3city_20260901_02`：队列由 3/9 推进到 9/9，全部城市为 `waiting_for_generation`。石境矿镇首版 D4 终审失败写入 `failureCount=1/5`，Agent 仅依据工具返回和 artifacts 在同一 Context 提交完整 revision 2，随后成功推进；参数/schema 拒绝未计失败。详见[真实游玩调用记录](./真实游玩调用记录.md)。

*剩余问题*：真实 attempt 1 同时出现 `test_run_manifest.json` 临时文件原子替换异常并把 workflow step `reasonCode` 截成 `E`，已登记 `CITY-PW-20260902-01` 待定位；队列已通过 revision 2 恢复并完成。
