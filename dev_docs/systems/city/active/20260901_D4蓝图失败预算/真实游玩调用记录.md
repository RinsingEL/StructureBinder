# D4 蓝图失败预算真实游玩调用记录

*运行目标*：`natural_3realm_3city_20260901_02`；把 3 国 9 城设计队列连续推进到全部 `waiting_for_generation`，并验证同一 D4 Context 在真实失败后可提交完整 Blueprint revision。

*公开调用入口*：本轮通过游戏内 HTTP 对应的正式 City / Patch Explorer MCP 入口调用；仅使用接口返回及其明确返回的 artifacts 恢复，没有读取实现源码。

## 调用序列

| 序号 | 工具 | 关键参数 | 返回摘要 | 修正理由 |
| --- | --- | --- | --- | --- |
| 1 | `city_design_queue_status` | 空请求 | `400 runId is required` | 补入由现有队列产物确认的 `runId`。 |
| 2-4 | `city_design_queue_status` | `natural_3realm_3city_20260901_02`、`natural_3realm_3city_20260901_01`、`beta_queue_real_20260831_2349` | 目标 run `_02` 为 3/9，当前盐境边堡 `needs_agent`；其余为旧队列 | 锁定上一任务明确要求的 3 国 9 城 run。 |
| 5 | `city_post_d4_auto_compile_status` | 盐境边堡 | D4 终审失败；`ridge_watch` 为空且缺少必需哨所；返回完整预览、质量报告、compile trace 和 manifest | 读取返回 artifacts，发现原 `p.297` 仅 17 个粗格。 |
| 6 | `city_submit_d4_blueprint` | 同 Context，误用字段 `blueprint` | `400 cityBlueprint object is required` | 改为公开请求字段 `cityBlueprint`；校验拒绝未计失败。 |
| 7 | `city_submit_d4_blueprint` | 同 Context；核心哨所由 `p.297` 改到缓坡 `p.285` | accepted；`failureCount=0/5`；post-D4 attempt 2 queued | 使用返回 D3 包中的更大连续缓坡。 |
| 8-9 | `city_post_d4_auto_compile_status` | 盐境边堡 | `running` → `waiting_for_generation` | 无设计修正。 |
| 10 | `city_design_queue_status` | 目标 run | 4/9；下一城翠庭边堡 | 继续队列。 |
| 11 | `city_plan_d3` | 翠庭边堡 | `api_worker`，276 patches，等待 Top Patch 复核 | 选择 `ridge/slope/plain`。 |
| 12 | `patch_explorer_show_candidates` | 翠庭边堡；`ridge/slope/plain`，pageSize 8 | review completed；返回候选页与同框总览 | 选相邻 `p.42` 缓坡、`p.24` 与 `p.59` 平原。 |
| 13-16 | `city_prepare_d4_blueprint_context` | 翠庭边堡 | 依次提示缺 `terrasenseProfileSource`、`templateCatalogSource`、`blueprintReferenceCatalog`，补齐后成功；Context `e67bd...`，0/5 | 按每次错误提供的新 schema 信息逐项补齐，无重复无变化请求。 |
| 17-18 | `city_submit_d4_blueprint` / `city_post_d4_auto_compile_status` | 翠庭边堡“坡上主堡、两翼平原”蓝图 | accepted，0/5；一次到 `waiting_for_generation` | 无修正。 |
| 19 | `city_design_queue_status` | 目标 run | 5/9；下一城盐境矿镇 | 继续队列。 |
| 20 | `city_plan_d3` | 盐境矿镇 | `api_worker`，164 patches | 复核 `valley/slope/plain`；另试 `basin`。 |
| 21 | `patch_explorer_show_candidates` | 含 `basin` | `400 PATCH_EXPLORER_TYPE_NOT_IN_SCOPE: basin` | 删除不在本会话 typeCatalog 的 `basin`。 |
| 22 | `patch_explorer_show_candidates` | `valley/slope/plain` | review completed | 选大缓坡 `p.80`、相邻平原 `p.217` 与短坡 `p.221`。 |
| 23-25 | `city_prepare_d4_blueprint_context` / `city_submit_d4_blueprint` / `city_post_d4_auto_compile_status` | 盐境矿镇“高坡工坊—平原市场—短坡住区” | Context `1c3484...`；accepted 0/5；一次到 `waiting_for_generation` | 无修正。 |
| 26 | `city_design_queue_status` | 目标 run | 6/9；下一城翠庭矿镇 | 继续队列。 |
| 27-31 | `city_plan_d3`、`patch_explorer_show_candidates`、`city_prepare_d4_blueprint_context`、`city_submit_d4_blueprint`、`city_post_d4_auto_compile_status` | 翠庭矿镇；复核 `valley/slope/plain/basin`；采用 `p.20` 长坡、`p.301` 谷地、`p.291` 平原 | D3 280 patches；Context `9df1c7...`；accepted 0/5；一次到 `waiting_for_generation` | 无修正。 |
| 32 | `city_design_queue_status` | 目标 run | 7/9；下一城石境边堡 | 继续队列。 |
| 33-37 | `city_plan_d3`、`patch_explorer_show_candidates`、`city_prepare_d4_blueprint_context`、`city_submit_d4_blueprint`、`city_post_d4_auto_compile_status` | 石境边堡；采用西北脊 `p.397`、北部平原 `p.377`、长坡 `p.183` 北侧 | D3 312 patches；Context `da6a7e...`；accepted 0/5；一次到 `waiting_for_generation` | 与相距 128 格的翠庭矿镇错开到规划区西北侧。 |
| 38 | `city_design_queue_status` | 目标 run | 8/9；下一城石境矿镇 | 继续队列。 |
| 39-42 | `city_plan_d3`、`patch_explorer_show_candidates`、`city_prepare_d4_blueprint_context`、`city_submit_d4_blueprint` | 石境矿镇；首版采用台地 `p.158` + 平原 `p.161` | D3 278 patches；Context `8b7ad5...`；accepted 0/5；attempt 1 queued | 等待真实编译。 |
| 43 | `city_post_d4_auto_compile_status` | 石境矿镇 attempt 1 | `needs_agent`；`failureCount=1/5`；质量报告显示两个平原组为空、必需 tavern/household 缺失；workflow 同时报告 manifest 临时文件原子替换异常 | 按返回质量报告改换更大连续地形；另把临时文件异常登记为待定位 Bug。 |
| 44 | `city_submit_d4_blueprint` | 同 Context revision 2；工坊/住区改用大缓坡 `p.15`，市场改用相邻 `p.32` | accepted；保留 `failureCount=1/5`；attempt 2 queued | `p.32` 连续矩形 `64×48`，大于失败的 `p.161` `64×32`。 |
| 45 | `city_post_d4_auto_compile_status` | 石境矿镇 attempt 2 | `waiting_for_generation` | 真实验证同 Context revision 重提成功。 |
| 46 | `city_design_queue_status` | 目标 run | `completed`，9/9，全部 `waiting_for_generation`，`remainingCount=0` | 达到停止条件。 |

## 最终结果与产物

- 队列状态：`natural_3realm_3city_20260901_02/automation/city_design_queue.json`，`status=completed`、`completedCount=9`、`remainingCount=0`。
- 本轮处理城市的正式包均位于 `natural_3realm_3city_20260901_02/city_test_runs/<citySeedId>/`，入口为各自 `test_run_manifest.json`。
- 失败预算真实覆盖：石境矿镇 attempt 1 写入 `failureCount=1/5`，同一 Context revision 2 被接受并推进成功；提交字段错误与 Patch 类型错误均未计入失败预算。
- 未执行 D7/worldgen；9 城均按本轮目标停在 `waiting_for_generation`。

## 2026-09-02 石境矿镇首次生成验收

*验收环境*：存档 `RTF_beta_validation_01`；城市 `city_realm_stone_march_1_mining`；本次加载的 LandUse identity 为 `areaPlanHash=944eda2a...`、`surfacePrintPlanHash=6e5091cb...`。重启后共 103 个 owner 于 `2026-09-02 01:14:27–01:17:49 +08:00` 完成落地，因此以下现象不是旧 JVM 未加载代码。

| 项目 | 玩家观察 | 现场证据与定位 | 后续方向 |
| --- | --- | --- | --- |
| 道路被台地分成两个高度 | 俯瞰仍是一条连续道路，立体上出现两段断头路 | 从保存态重建出 4569 个道路列；相邻道路中有 29 对完整 4 格断层，其中 27 对恰好跨 owner 区块边界。相邻 owner 各自在自己的 halo 内独立合并台地、只输出本 owner，边界两侧因 purpose 上下文不同分别冻结为 68/72/76 等不同目标；主动楼梯扫描看到的是各自局部一致的模型，无法看到最终拼接后的跨 owner 高差。另有 2 对同 owner 的 4 格断层，位于转折/窄路处，修复时仍需单独收口。登记 `CITY-DL-20260902-01`。 | 台地目标需要先形成跨 owner 一致的冻结真值，再由 owner 切片执行；随后对最终道路高程边统一生成直梯、分流梯或明确缓坡，不能只在局部模型内补楼梯。 |
| 道路半砖露出原始地块半截 | 道路顺应原地形没有问题，但半砖嵌入地表后露出相邻原始方块上半截 | 保存态中 2601 个 polished-andesite slab 与 595 个 mud-brick slab 全部为 `type=bottom`。执行器把道路写在 `surfaceY` 并强制 bottom slab，相当于用下半砖替换原完整地表块，道面天然比旁边完整地块低半格；这是稳定复现的执行语义，不是地形偶发。登记 `CITY-DL-20260902-02`。 | 保留逐地形铺设，但常规平路改用同材质完整方块/双层半砖；半砖只用于明确需要的半格细节，台地高差继续由楼梯或缓坡承担。 |
| 台地边缘石墙均不连接 | 症状与旧农业栅栏相似，农业栅栏已正常 | ledger 记录 126 个台地边缘全部准备并应用，其中护栏 119、绿化 7、占用跳过 0；保存态逐格确认 119/119 个 `stone_brick_wall` 的 north/east/south/west 仍全为 `none`。旧 `CITY-DL-20260723-04` 的 finalize 只以 `CrossCollisionBlock` 识别连接方块，能覆盖农业 `FenceBlock`，但 `WallBlock` 不属于该类型，石墙被整个跳过。旧 Bug 单中“同样覆盖石墙”的结论不成立，已纠正并登记 `CITY-DL-20260902-03`。 | 连接收尾应显式覆盖 wall 的四向 `WallSide`，并继续保持 raw 全量写入、四邻统一 finalize、失败回滚和跨 owner 接缝复算语义。 |
| 主路路灯 | 建议在主路旁增加栅栏灯柱、锁链和吊灯笼 | 已确认不再复用旧通用装饰链；该链会恢复独立规划与目录职责。 | 由道路 owner 从冻结的 `CITY_MAIN_ROAD` segment 路面格确定性派生栅栏灯柱、锁链和悬挂灯笼，不新增规划输入。 |
