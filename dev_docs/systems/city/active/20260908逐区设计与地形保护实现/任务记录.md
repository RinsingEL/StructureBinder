# 逐区设计与地形保护实现

*需求入口*：City逐区设计与地形保护落地-v0.1；用户要求严格按案完全开发。沿用本 Session 已明确跳过清理的授权；主线身份变化已登记，不执行清理。

*修改内容*：逐区 DRAFT 预览与 FINAL 提交；当前失败定位、有效底图保留与红框；格式十次独立于设计五次预算，外层续跑识别真实修订，stale 返回当前基底；Hermes 续轮携带新预览及未解决质量告警。阵列填充前保护主路接口，连接时遍历备用接口；次级通路接入城市主路、支持一格无路缘巷道、门口接续与纵坡。台面改为建筑附近局部铺地与标高，LANDSCAPE 保持原地形，默认超过 16 格采用稀疏黑石墙支撑。同 origin chunk 合并建筑 pieces，防止漏建；自然 feature 写入时保护冻结道路及表层。同步相关接口、数据契约与测试入口。

*验证结果*：Java 全量 913 项，903 通过、10 项按条件跳过、0 失败；MCP 11 项通过。最终独立 Minecraft GameTest 7 项全部通过，test/build/runGameTestServer 整体 BUILD SUCCESSFUL（3 分 19 秒）。两仓 git diff --check 通过。原首都副本重放保持旧 Blueprint，编译出 316 栋建筑、4 条主路连接、824 个道路分段；安全准入通过，主路无跳过、道路与建筑无碰撞。入口 312/319 已接通，7 个未接通告警如实保留。重放不是新 GLM 设计或真实 RTF 城市落地验收。

*剩余问题*：完整 GLM+RTF 新城实景验收未执行；旧首都设计仍有 7 个入口及阵列质量告警，需在逐区预览修订设计，不能宣称其观感合格。两个门外点被其他建筑占据，其余未在当前受限次级寻路内接通；没有为通过验收而移动建筑或隐藏告警。未部署到或改写用户旧存档。

证据：`final-verification.log`、`java-test-summary.json`、`mcp-test-final.log`、`capital-replay-final.log`、`capital-replay-summary.json`、`首都重放预览.png`。构建产物 `build/libs/geomantia-0.1.0.jar`，SHA-256 `2FD4A123FBFD101E4F477EEC9F561296B80E07C28663FAE6F41E8F4EF985A21E`。

验证中发现并修正：有效空草稿预览的不可变列表排序；GameTest 旧路宽夹具与随机栅栏高程干扰；窄巷验收器仍加路缘的误报；旧测试把主路失败与入口失败绑定的过时断言。主路硬约束没有取消。
