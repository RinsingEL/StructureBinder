# 城市台基与地形适应

*需求入口*：`designer_territoryMod/docs/systems/city/10_product/案子/City台基与地形适应-v0.1/README.md`

*修改内容*：D4 结构门禁已对完整占地执行坡度、起伏和高程范围上限，候选逐点跳过坏位置，首选 patch 无解时可回退同一 D3 grid；D4/D6 景观统一用相邻高程差阻止跨崖。固定模板使用完整占地 generator height 中位数作为 datum，`full_footprint_support` 获得矩形台基；外围 Foundation 不再因 relief 留下零星未铺列。

*验证结果*：上述七组聚焦回归通过；随后执行 `./gradlew.bat test --no-daemon`，完整 JVM 测试在 1 分 8 秒内通过。真实新区块仍待执行。

*剩余问题*：不同功能区边界按用户要求不在本次处理；Foundation 整体形态的 Group-aware polygon 重构仍保留在既有 `CITY-DL-20260811-02`，本轮不混入。真实新区块需复验建筑四角承托、外围硬质地表和农田/林场断崖边界。
