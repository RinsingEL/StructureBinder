# 区域接力景观填充

## 背景

上一版 `SINGLE_SOURCE_LAYERS` 使用单源八邻域距离和周期取模分配角色。它虽然把菱形距离改成近似圆形距离，但仍属于“从共同中心生成固定等距轮廓”，实际预览形成同心环，未满足自然景观诉求。本轮破坏性替换该默认算法，不保留固定图形 fallback。

## 已锁定硬契约

1. 扩张算法唯一：所有景观格必须由可追溯的逐格扩张产生；严禁菱形、圆形、矩形、同心环、预制 mask 或其他固定图形作为正式算法、快速路径或失败保底。无法完成扩张时 hard fail。
2. AI 负责多样性：Blueprint 只配置填充方案、选择权重、角色 / 类型、目标占比和内容权重等语义参数；AI 不提交坐标、逐格 mask、固定图形或自由算法脚本。
3. 区域接力生长：第一块区域从景观起点扩张；后续区域必须从上一块或明确父区域的局部边界选择新起点继续扩张。所有区域必须冻结 `regionId/parentRegionId/start/sourceFrontier/targetArea/actualArea` 等 provenance。

## 实现边界

- 保留上一版已经建立的 Blueprint 方案选择、角色占比、内容权重和稳定 seed 思路，但将其字段语义升级为区域接力，不再表达距离层宽和重复周期。
- SurfacePrint 继续冻结全局 role spans，owner chunk 只裁切执行，不能在 chunk 内重跑扩张。
- 农田首个验收序列为主题耕地 -> 田埂 -> 水渠 -> 田埂 -> 新耕地；每一步从父区域局部边界接力，不形成完整闭环。
- 花田以多个大小不同的花区和局部叶带接力；林场本轮至少形成树木主区、灌木区与石子地面区的非同心面域。树木 / 花种内容的真实多 prefab 散布仍遵守 Decoration 职责边界。

## 质量阻断

- 任一默认或 fallback 路径仍调用 `SingleSourceLayeredFillClassifier`。
- 使用到共同中心的全局距离、距离取模或固定几何公式决定角色。
- 后续区域没有父区域或不是从父区域局部边界邻接起步。
- 为完成覆盖而对断开分量静默重新播种，或用 bbox / 固定图形补齐残余。
- 相同 Blueprint 参数和 seed 不确定，或输入集合遍历顺序改变结果。
- 预览仍呈完整同心环、统一菱形 / 圆形模板，且 trace 无法解释每块区域如何接力生长。

## 当前状态

- 用户已锁定三条硬契约。
- Blueprint 已破坏性升级为 v0.9，Context v0.8、Reference Catalog v0.6、Snapshot v0.9；唯一算法为 `SINGLE_SOURCE_REGION_RELAY`，固定接力来源为 `PARENT_REGION_LOCAL_BOUNDARY`。
- `roleShares[]` 与目录示例均为有序区域 stage；同一 role 允许重复，每 occurrence 独立选择 `PATCH|CORRIDOR`，同 role 占比聚合后校验范围。
- `RelayRegionGrowthClassifier` 已替换旧单源距离分类器；使用 4 邻接 frontier、稳定 drift、连通保护和有界确定性回溯。CORRIDOR 先逐格生长稳定随机弯曲 spine，再从 spine 周边逐格增厚，避免同心环与一格宽迷宫。
- SurfacePrintPlan 已升为 v0.5，冻结带 region 身份的 spans 与 `parent/start/sourceFrontier/target/actual/growthForm` trace；chunk 只裁切冻结 spans。
- Preview 显示区域边界与接力界面；最终 synthetic review 图为 `build/reports/city-landscape-relay-growth-preview.png`，已人工检查不再呈同心圆或全域迷宫，当前剩余视觉风险是面域仍偏块状。
- 当前本地 `run/config/.../blueprint_reference_catalog.json` 已迁移到 v0.6 五套接力示例；该路径受 gitignore 管理，不进入提交 diff。
- 已通过区域接力核心、Blueprint outdoor、SurfacePrint codec/planner、chunk compiler 与 preview 共 34 项专项；MCP TypeScript/Node 7 项通过。
- 全量 Java 回归共 618 项，`failures=0`、`errors=0`、`skipped=0`；最终预览专项再次通过。
- 实现仓库与文档仓库均通过 `git diff --check`（仅有现有 LF/CRLF 提示）；旧字段、旧分类器和 `SINGLE_SOURCE_LAYERS` 仅保留在负向拒绝测试中，生产代码不存在旧算法或 fallback。

## 新区块前真实工作流复验

- 目标存档 `RTF08052`；选择未生成 region `r.26.-19`，对应新 W/T run 的矿业镇种子 `city_realm_salt_kingdom_0_mining`。三轮规划期间该 `.mca` 始终不存在，玩家未传送，未消耗首次 worldgen 条件。
- `landscape_relay_playtest_20260807`：Blueprint 有效，但 `SMALL + SPARSE + ORGANIC_COMPACT` 的第三个 required 模板触发 `GROUP_EXTENT_LIMIT_EXCEEDED`，属于测试 Blueprint 参数错误。
- `landscape_relay_playtest_20260807_v2`：组范围升为 `MEDIUM` 后 D4/D5/D6 通过，疏散布局的 Foundation 触发 `CITY_FOUNDATION_THIN_BRIDGE`，未进入世界激活。
- `landscape_relay_playtest_20260807_v3`：改为 `MEDIUM + BALANCED + COMPACT` 后 D4/D5/D6 与 Foundation 通过；正式户外意图把 11 个 anchor 的三类 Landscape 展开成 95 个 source，竞争 / 排除后至少一个 Area 的可填格数少于五个 stage，`city_plan_land_use` 触发 `RELAY_GROWTH_MASK_TOO_SMALL_FOR_STAGES`。
- 按 Bug 修复流程到达第三次未收敛后停止参数试错，没有执行 D5 世界变更、没有传送。故障登记为 `CITY-DL-20260807-01`；后续修复不得引入固定图形、geometry fallback、静默丢弃 required Parcel 或偷偷减少 AI 提交的有序 stage。
