# City 待修 Bug 集中修复

*需求入口*：用户要求“让子智能体修复一下这些bug吧，之后跑三区测试的时候，选城市走正常流程就行了”。最新确认“禁止搬家”覆盖现有案子中 Patch 仅作软偏好的冲突表述：显式选择 Patch 后只允许 Patch 内换点和处理小坑，放不下必须记录失败，不得换 Patch 落位。

*修改内容*：

- 删除 required 结构的跨 Patch `d3_terrain_fallback`；显式 Patch 内可换点，无法承载时保持空置。selection、Group 与 acceptance 逐级保留 terrain/surface 原因；局部小坑仍记录 `foundation_or_skip`。
- 空 fill pool 不再取模除零；仍只按显式 required 计算必需结构。
- 验收逐项对照 Blueprint `requiredStructureRefs[]`，只统计 REQUIRED 阶段提交；新增 `requiredStructureCounts/missingRequiredStructures/allRequiredStructuresCommitted` 与逐结构 `REQUIRED_STRUCTURE_MISSING` hard block。
- D5 active registry 新增来源 provenance；workflow 仅在当前 D5/D6、可选 LandUse/Decoration 和 road provider 全部匹配时跳过既有 D5。
- RoadWeaver bridge 保留模板入口点，实际 road gateway 按冻结方向投影到 locked footprint 外一格。
- 六份 `town_workshop_01.nbt` 的三株无支撑 grass 原位替换为 moss carpet，并同步两份正式 catalog 哈希。
- 旧意图 PCG 自动物化 15/16/17 已被当前显式 required/fill/composition 主线替代，不重新引入旧分类与混池路径；其余旧 RoadWeaver H2/同步查询故障由当前文件存储与异步查询主线替代。

*验证结果*：修改整合后聚焦 Java 三类测试通过；最终完整 Java 730 项通过，0 failure/error/skip；MCP 7/7 通过；Python importer/sanitizer 23 项与 NBT/catalog 6/6 哈希校验通过；两仓库 `git diff --check` 通过。正常 W→T4 选城真实 run `normal_three_zone_20260826_0935` 已从 W 扫描、T1-T4、D2/D3、D4 Top Patch、单次 Blueprint 提交跑到 D4 正式编译。空 fill pool 越过原除零；required 未跨 Patch；首次发现 required 漏验后修复并重启同一城市重编，真实输出两栋小屋的 `REQUIRED_STRUCTURE_MISSING`，`passed=false`。

*剩余问题*：该正常城市没有通过三区验收：D3 选址为 `border`；D4 农业 `SHORE-01` 精细可建率 31.37%、含水率 56.37%，COMPACT 只放下风车，两栋小屋留空；行政庭院和农业巷道视觉门禁失败。系统已正确停止在 D4，未执行 D5/D6。商业 fill 扩张会认领邻接农业 Patch，但显式 required 酒馆仍留在自己的 Patch；当前“禁止搬家”契约只冻结 required，若用户要求所有 fill 也不得越 Patch，需要另行确认并修改案子。
