# 正常城市三区真实游玩调用记录

*运行环境*：2026-08-26，`runClient`，正式 MCP Agent Loop；runId=`normal_three_zone_20260826_0935`。

*正常选城流程*：W 扫描半径 8192，完成 1089 tile、278784 micro sample；T1 创建一个沿海国度；T2 从 Top 3 正常选择 `SHORE-02` 国度锚点；T3 扩张为单一连通国境；T4 从都城候选榜选择面积最大的合法 `SHORE-01`，其城市级确认预览可建率 0.78125，并与 `SLOPE-01` 直接相邻。未使用特殊城市、规则夹具或手写城市坐标。

*D3 复核*：都城 `city_realm_salt_kingdom_0_capital` 正常进入 D2/D3。D3 输出 `territoryCheckResult=border`，提示 1536×1536 规划范围部分越出所属国度边界；保留告警并接受当前正常选址继续检查三区。

*D4 Top Patch*：选择紧密相邻的三块功能区 Patch：行政 `SLOPE-01=p.367`、农业 `SHORE-01=p.207`、商业 `SLOPE-03=p.179`。`SLOPE-01` 精细可建率 0.6133；`SHORE-01` 可建率 0.3137、含水率 0.5637；`SLOPE-03` 可建率 0.3594、含水率 0.4375。未因精细预览较差更换 Patch。

*Blueprint*：单次提交三区 Blueprint：行政 COURTYARD；农业 COMPACT，显式 required 为一个风车和两栋不同小屋，空 fill pool，剩余空间为农田；商业 LINEAR。行政—农业、农业—商业为 HARD CONNECTION。提交校验通过，空 fill pool 未再触发除零。

*编译结果*：`previewCompiled=true`，但 `compilationAcceptance.passed=false`，未进入 D5/D6。行政 required 与 fill 全部留在 `p.367`；农业风车留在 `p.207`，两栋 required 小屋因 `D4_ARRAY_COUNT_UNSATISFIED` / `OCCUPIED_ENVELOPE_OVERLAP` 留空；商业 required 酒馆留在 `p.179`。没有 required 结构跨 Patch 搬家。

*发现的新 Bug*：首次编译时农业组只有风车，未满足 Blueprint 的两栋 required 小屋，但 `allFunctionAreasFormed=true`，验收 hard block 只列 `COURTYARD_BASE_RING_INCOMPLETE`、`COMPACT_ALLEY_MISSING`、`COMPACT_ALLEY_DISCONNECTED`，没有明确列出必需结构缺失。登记为 `CITY-PW-20260826-02`。

*修复后真实重编*：重启包含修复代码的客户端，对同一已提交 Blueprint 再次调用 `city_compile_d4_blueprint`。结果保持 `previewCompiled=true/passed=false`，新增 `allRequiredStructuresCommitted=false`，并分别输出 `household_01 missingCount=1`、`household_02 missingCount=1` 两条 `REQUIRED_STRUCTURE_MISSING` hard block；三个 Group 的 required 结构仍未跨出显式 Patch。漏拦截真实复验通过。

*产物*：`run/realm_debug/normal_three_zone_20260826_0935/`；D4 trace 位于 `city_test_runs/city_realm_salt_kingdom_0_capital/steps/d4/city_generation_compile_trace.json`；Top Patch 图位于 `patch_explorer_pex_d99041aeb536429498b90a7f1ef748ad/top_patches_overview_7977d73340f0.png`。
