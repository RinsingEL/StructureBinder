# 设计优先与场景阵列

*需求入口*：用户确认 submit 接受布局/台面方案，细地形用于兑现地板、支撑和收边，不再否决设计；将广场、两类市集、Patch 交界阵列接入 Java。

*修改内容*：D4 submit 先真实编译几何并检查 compilationAcceptance，成功后发布 hash 绑定的 geometry commit；后续 D4 复用冻结结果。失败不覆盖旧 accepted 文件；编译器异常保留 proposal 并按程序问题处理。只有声明铺台面的功能区将起伏转换为工程要求，PRESERVE/未声明铺台面的区保留地形适配边界。冻结台面的深坑采用两层桥面，浅凹填土、凸起切除。修复16格 GIS 单元边缘误删合法建筑槽位。

Java 已新增 `CityPublicSpaceLayoutPlanner`（可空广场、内部市集、边缘市集）与 `CityPatchInterfaceLayoutPlanner`（真实 A/B 接壤、放置侧、朝向、偏移、区间）。只使用作者入口和模板真实尺寸，不恢复额外安全距离。**这两个类目前是纯几何核心，尚未进入正式 Blueprint 编译调用链。**

*验证结果*：Java 广场/市集4项、交界4项通过；真实编译器 submit 冻结/损坏拒绝集成通过；拒绝/异常保持旧 accepted 原字节测试通过；MicroGrader/区块执行器的深坑与高台测试通过。这里的执行器测试使用合成世界，不是真实游玩。HTTP submit 测试补齐真实 Patch member cells 后通过。基线 `b04219b` 独立工作树全量872项3失败8跳过；已定位一项生产几何错误和两项旧测试假设并修正。最终 `gradlew test` 全量成功（1分38秒，885项、0失败、7跳过），结果位于 `build/test-results/test`。`git diff --check` 通过。

*剩余问题*：任务未完成。还需正式 scene Blueprint 字段、目录/Agent 告知、场景占地与铺地板产物、D4 主链调用、与旧算法的混合城市测试。目前冻结的是D4，不是D5/D6/D7全链工程预验，台面标高也尚未前移为D4冻结设计字段。submit 同步编译的真实耗时/HTTP超时、局部修订与宿主重试需后续验证。船只水深/起重机工作端/靠泊侧没有作者数据时不得猜测。未启动 MC、未修改用户存档、未替换测试 jar、未声明已能新档真实跑通。新增 Java 源码与本轮改动尚未提交。基线工作树位于 `E:/Mod_Dev/StructureBinder_designfirst_baseline`，仅用于对照测试。
