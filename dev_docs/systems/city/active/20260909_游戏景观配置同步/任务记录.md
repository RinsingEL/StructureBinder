# 游戏景观配置同步

*需求入口*：用户要求同步游戏景观配置，提及上次出现旱地。

*修改内容*：以两套语义一致的开发目录为源，仅同步游戏 template_cfg/blueprint_reference_catalog.json 的 landscapeFillProfiles 与 surfaceRecipes。移除 fill:relay_dry_farmland，保留灌溉农田；农田 fieldBeforeBlocks/fieldAfterBlocks 从 5 改 3，渠岸覆盖由 grass_block 改 stone_brick_slab。原文件已备份，更新片段已留档。

*验证结果*：回读 JSON 与开发引用目录完全语义一致；仅两节发生变化；无残留旱地 profile 引用、无重复景观/地表配方 ID；住宅任选入口声明仍在，模板目录哈希一致。未改 Java，无需重新打包 JAR。

*剩余问题*：上次旱地与旧配置的实际因果未做存档核对，不能认定此次同步已证明该成因。已有冻结城市和已生成地表不自动更新；新建城市上下文读取新配置。未执行游戏内生成验收，未提交 Git。
