# 真实游玩调用记录

## 2026-08-24 正式客户端启动

*目标*：启动正式开发客户端，继续通过 MCP 验收 City 主从道路、建筑绿化和住宅外溢真实方块效果。

*实际调用*：`.\gradlew.bat runClient -PgeomantiaDevUseStructurePacks=true -PgeomantiaDevUseRoadWeaver=true`

*返回摘要*：客户端未启动；`compileJava` 报 `CityInternalStreetPlanner.java:120` 缺少 `)`。

*修正理由*：本轮整理 `LaneTarget` 构造时漏写闭合括号。退出 Agent Loop 后修正语法，再重新执行同一正式启动命令。

*最终结果*：修正后 JVM 全量测试通过；第一次重启进入 RTF0810 时，旧 `active_city_land_use_area_plans.json` 携带 SurfacePrint v0.6，被当前 v0.7 parser 正确拒绝。

*产物路径*：旧 active 与 ledger 已可恢复移动到 `run/saves/RTF0810/geomantia_city_masks/stale_v06_backup_20260824_2201/`。

## 2026-08-24 RTF0810 三区正式 D4

*目标*：复用冻结 D3，在 `SLOPE-01=p.302`、`SHORE-01=p.141`、`SHORE-06=p.317` 提交行政 COURTYARD、市场 LINEAR + 住宅 GRID、农业 COMPACT，并携带农田、林场、HIERARCHICAL 主路、建筑绿化 palette。

*实际调用*：`prepare_d4_blueprint_context` -> `submit_d4_blueprint` -> `compile_d4_blueprint` 对应 HTTP 正式路由；run=`rtf0810_medium_city_20260810_01`，city=`city_realm_salt_kingdom_0_capital`。

*修正过程*：首次 PowerShell 函数名 `Group` 被解析为 `Group-Object`，groups 为空；第二次 CONNECTION 错传 `distancePreference=NEAR`；每次均根据返回修正，并通过改变实际 palette 权重获得新 contextId，未重扫 D3。第三次提交校验通过，issues=0。

*最终结果*：D4 有效失败：`CITY_BLUEPRINT_ARRAY_COMPOSITION_SLOT_UNAVAILABLE: test_three_district_parent cannot reserve child Group test_market_linear.`。测试用例要求父阵列关系跨 SLOPE/SHORE Patch，当前父阵列 slot 无法落到远 Patch；已登记 `CITY-PW-20260824-02`，退出 Agent Loop 回开发修复。

*产物路径*：`run/realm_debug/rtf0810_medium_city_20260810_01/city_test_runs/city_realm_salt_kingdom_0_capital/steps/blueprint/`；test run manifest 同包根目录。

## 2026-08-24 至 25 RTF0810 连续 D4 Agent Loop

*目标*：复用最高有效 D3 与已冻结正式目录，取得可继续 D5/D6/LandUse 的 D4 正式结果；三区只作为测试范例。

*正式工具*：反复调用 `/realm/city/prepare_d4_blueprint_context`、`/realm/city/submit_d4_blueprint`、`/realm/city/compile_d4_blueprint`；每个 one-shot context 均通过实际 reference catalog palette 权重变化获得新 identity，没有删除 claim 或绕过一次提交约束。

*实现 Bug 与修正*：

- 远 Patch child slot 失败：退出 Loop，修复后原错误消失，登记 `CITY-PW-20260824-02`。
- 同 Patch 第二 child 无槽：枚举 preferred Patch member cells，登记 `CITY-PW-20260825-02`。
- 固定 WEST 入口无法适配 COMPACT/COURTYARD：四向局部 frame，登记 `CITY-PW-20260825-03`。
- 大模板跳过槽后小模板无法复用：每栋重扫 exact slots，登记 `CITY-PW-20260825-04`。
- terrain fallback 后只保留 claimed Patch：改 preferred+claimed 并集，登记 `CITY-PW-20260825-05`。
- LINEAR 入口方向误作街轴：改垂直轴，登记 `CITY-PW-20260825-06`。
- LINEAR state band 压首栋/一侧跟随建筑：按首栋轴向后缘起步，并按两排真实 collision gap 定中心，登记 `CITY-PW-20260825-07`。
- 硬骨架槽被通用近邻 gap 拒绝：关闭该重复门禁，登记 `CITY-PW-20260825-09`。
- full-width gateway spanning tree 失败：登记 `CITY-PW-20260825-08`。首个“所有同 Patch 父槽强制主路 edge-gap”修法在聚焦回归中破坏既有父阵列，已撤回，根因恢复待定位。

*调用/输入错误，不作为产品 Bug*：

- 首次 D4 HTTP 请求漏 `runId`；修正后发现路径重复，再改为顶层 runId。
- PowerShell `Group` 与 `Group-Object` 冲突，提交 groups 为空。
- CONNECTION 错传 `distancePreference=NEAR`，按校验改为 NONE。
- 删除测试 Group 后残留 `spatialGrounds.sourceGroupId`，校验正确拒绝；新 context 清理后通过。
- p.601 超出 City D3 硬边界，父槽正确拒绝。
- 多个 Blueprint 组合因 DENSE/extent/required Landscape/地块容量不匹配被正式校验或 D4 正确拒绝；这些只用于收敛测试范例，不登记产品 Bug。
- p.302→p.141 无非水非崖主路走廊，`MAIN_ROAD_NO_LEGAL_PATH` 属正确约束。

*环境状态*：旧 SurfacePrint v0.6 active registry 被 v0.7 parser 正确拒绝；旧 active 与 ledger 已移动到可恢复目录 `run/saves/RTF0810/geomantia_city_masks/stale_v06_backup_20260824_2201/`，不是产品 Bug。

*最后返回*：最近一次已接受 Blueprint 中建筑阵列与视觉门禁可推进至主路规划；主路返回 `CITY_BLUEPRINT_MAIN_ROAD_PARENT_GRAPH_DISCONNECTED`。首个父槽 gap 假设已证伪；下一步用独立 dry Patch 测试布局排除测试输入，并继续定位 connector/gateway。

*产物路径*：`run/realm_debug/rtf0810_medium_city_20260810_01/city_test_runs/city_realm_salt_kingdom_0_capital/steps/blueprint/`、`steps/d4/city_generation_compile_trace.json`、包根 `test_run_manifest.json`。

## 2026-08-25 客户端进程污染排除

*现象*：`runClient` 控制台被中断后，Gradle 包装进程与 Minecraft 子 JVM 仍存活；后续 D4 HTTP 有命中旧字节码风险。

*处理*：按命令行精确筛选 `E:\Mod_Dev\StructureBinder` 所属 Java 进程，停止 PID `23868`、`12120`、`28592`、`30904`；复查剩余数为 0。随后只启动一个最新构建客户端重测。

*归类*：测试环境/调用故障，不是 City 产品 Bug，不进入故障索引。

## 2026-08-25 最新字节码 D4→D6 复验

*目标*：用唯一新客户端复验 `CITY-PW-20260825-08/10`，继续 D5、D6、LandUse 正式链。

*实际调用*：`compile_d4_blueprint` -> `plan_d5` -> `plan_d6`；run=`rtf0810_medium_city_20260810_01`，city=`city_realm_salt_kingdom_0_capital`。

*返回摘要*：D4 `ok=true`，原 `PARENT_GRAPH_DISCONNECTED` 与 `CENTER_AXIS_STREET ROAD_OVERLAPS_STRUCTURE` 均消失；D5 `ok=true` 并生成 reservation/mask/wall/road access/build operation/预览/质量报告；D6 `ok=false, reasonCode=STRUCTURE_CHUNK_ALREADY_GENERATED`。

*调用错误*：脚本在 D6 返回业务 `ok=false` 后仍继续调用 `plan_land_use`，接口以 `CITY_BLUEPRINT_LAND_USE_D6_NOT_LOCKED` 正确拒绝。此项是 Agent 顺序错误，不登记产品 Bug。

*结论*：08/10 真实 D4 修复通过。D6 是 RTF0810 既有目标 chunk 已生成导致的正确原子门禁；未删除 region、未绕过门禁、未激活世界修改。退出 Agent Loop 后寻找安全新区块入口。

## 2026-08-25 三区 D4 预览重跑

*目标*：按案子测试范例提交行政中心、市场 LINEAR、市场住宅 GRID、农业 COMPACT 四个建筑 Group，并在农业区要求农田与林场，生成三区正式 D4 预览。

*实际调用*：启动正式客户端；多次 `prepare_d4_blueprint_context -> submit_d4_blueprint -> compile_d4_blueprint`。最终 Blueprint 校验通过，issues=0；四个 Group 使用 p.302、p.141、p.317，HIERARCHICAL 主路，农田和林场均为 required。

*输入修正*：农业共享空间 `WORK_YARD` 非法，按契约改 `FARMSTEAD`；GENERATE 要求每个 STRUCTURE Group 恰有一个 SpatialGround，补齐农业项；p.317 与 p.141 在 CONFORM 下容量不足，按返回改 ASSERTIVE。均为 Agent 输入/设计修正，不登记产品 Bug。

*最终结果*：D4 失败，`reasonCode=CITY_BLUEPRINT_REQUIRED_LANDSCAPE_LAYOUT_UNSATISFIED`。四组建筑已进入 compile trace，但 required 农田与林场无法同时取得合法景观容量；本轮未生成新的正式三区 PNG。现存 `structure_anchor_preview.png` 时间仍为 00:42:49，是前一次两区成功图，不能作为三区结果。

*产物路径*：`steps/blueprint/city_blueprint.json`、`steps/d4/city_generation_compile_trace.json`；案子离线三区示意位于文档仓库 `City主从路网与景观层-v0.1/assets/`。
