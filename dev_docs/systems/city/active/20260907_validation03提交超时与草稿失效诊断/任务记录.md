# validation03 提交超时与草稿失效诊断

*需求入口*：用户提供 RTF_beta_validation_03 存档及 23:10:43 停机截图，要求检查具体失败原因；本轮明确跳过需求案清理。

*修改内容*：仅诊断与留档，未修改实现或存档。证据来自实例 geomantia_mcp_log.jsonl 以及 provider_bd973b975f0bead3_r8192/city_test_runs/city_realm_qareth_capital/steps/blueprint 的拒绝报告、几何 trace、draft 与 failure_budget。

*验证结果*：23:08:18 居民组 required 候选耗尽；后续补丁将 groups/2/preferredPatchRefs 改为 p.77。截图显示 23:08:28 开始提交、23:09:13 返回超时；HTTP 日志记录同一补丁在 23:10:19 完成，ok=false，唯一 hardBlock 为 civic_core -> defense_watch: CITY_MAIN_ROAD_CONNECTION_UNAVAILABLE。主路 trace 显示三条已规划、一条 CITY_BLUEPRINT_MAIN_ROAD_NO_LEGAL_PATH；全部功能区、required structures/content 已满足。代码对应普通 terrain route 为空且 bridgeRoute 也不可用；尚不能据此确定哪个具体地形条件阻断。23:10:29 重发旧 baseDraftHash 832efa…，HTTP 400 仅返回 ok=false/error=CITY_BLUEPRINT_PATCH_BASE_STALE，当前 draft 为 d3a266…，未返回 revisionEvidence。截图 23:10:43 三轮无正式阶段进展停机；蓝图失败预算仍 0/5、retryAllowed=true。

*剩余问题*：超时后已落盘结果的恢复与 stale 响应反馈缺口确认，未修复。道路具体地形阻断点、首次编译耗时分布未定位。未运行游戏或测试。

## Agent Loop 与 baseline 反馈核对

用户质疑三轮限制并指出 E:\Mod_Dev\StructureBinder_designfirst_baseline 中逐错调参可收敛。只读核对该工作树 HEAD=b04219b、工作区干净；其 PlayerProviderAgentRunner 同样有 MAX_CONSECUTIVE_NO_PROGRESS=3，PreparedCityDesignTurn 同样仅开放候选查看/提交。不能声称 baseline 自动 Loop 没有限制；用户描述的逐错调参过程与当前自动会话边界是否一致尚未证实。

本次真实补丁序列：删除 arrayCompositions；civic_core 算法改 compact；civic_core Patch 改 p.77 后错误转移至 market_district；market Patch 改 p.74 后转移至 residential_quarter；residential Patch 改 p.77 后 required 全部满足，最终阻断为 civic_core -> defense_watch 主路不可用。这是有意义的逐步修订，不能称为多次原地失败。

反馈缺陷：CityDesignFailureFeedback.summarize 扫描 selections 中所有 no_legal_candidate，没有根据最终 compilationAcceptance hardBlocks 筛选本轮仍阻断的原因。23:10:19 以及日志中新出现的 23:54:26/23:56:59 响应，validationReport 唯一阻断为 civic_core -> defense_watch，道路错误 fieldPath 却为 $.groups[1]（market）；designFeedback.failures 列 market motel/laundry/flower/general_store 等候选失败及空 structureRef，不包含 defense_watch。parameterAdjustments 全部为空，并告知无已证明参数修正、需宿主诊断。这个摘要不能等同于已教会 GLM 如何修正当前道路阻断。

三轮计数只认正式 stage/semanticIdentity 变化，没有用草稿版本、已解决阻断或在途编译区分有效修订；建议将防空转与有效迭代分开，具体产品预算不在本轮擅自修改。程序错误及超时应走宿主恢复；设计侧应收到当前最终阻断、准确实体/字段、约束证据和可尝试的相关设计操作，不能把没有确定数字解误写成禁止一切合理局部试探。
