# 街区包络与组间缓冲真实游玩调用记录

*环境*：`runClient` 自动进入 `RTF0810`。目标为 `rtf0810_medium_city_20260810_01 / city_rtf0810_platform_terrain_21`；全过程未传送、未重新扫描 D3、未执行 D5/D6、未修改世界。

*调用*：使用 MCP `city_compile_d4_blueprint`，参数仅为上述 `runId` 与 `citySeedId`。

*过程*：首版对全部 Group 强制 12 格缓冲，约 2.29 秒返回 `CITY_BLUEPRINT_REQUIRED_STRUCTURE_NO_LEGAL_PLACEMENT`；直接关系豁免版约 250.07 秒返回 `CITY_BLUEPRINT_GROUP_MINIMUM_UNREACHABLE`，`residential_connector=5/6`。根据正式 trace 将显式 ADJACENCY 与父阵列整体关系纳入豁免后，最终调用约 254.98 秒成功。

*结果*：正式 D4 为 43 anchors、7 Groups，`group_extent_map.v0.9`；所有无直接构图关系 Group 的 collision 最近距离均大于等于 12 格。产物为 `steps/d4/structure_anchor_preview.png`、`structure_anchor_cluster_preview.png`、`group_extent_map.json`。
