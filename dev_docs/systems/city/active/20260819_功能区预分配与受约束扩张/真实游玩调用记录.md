# 真实游玩调用记录

*测试目标*：启动 `RTF0810`，复用既有最高有效 D3 上游，验证功能区预分配后正式 D4 编译是否能产出结构预览；不传送、不重扫、不执行 D5/D6。

*调用 1*：工具 `city_compile_d4_blueprint`；参数 `runId=rtf0810_medium_city_20260810_01`、`citySeedId=city_rtf0810_platform_terrain_21`。客户端已启动并报告 `Geomantia GIS API server started on 127.0.0.1:5000`。返回错误 `CITY_BLUEPRINT_GROUP_DISTRICT_CAPACITY_UNREACHABLE: admin_core reached 10 district cells but requires at least 11.`；MCP callId `60ebec3d-785e-4358-ad0e-d70a07e8516b`；耗时约 323 ms。参数与阶段引用有效，未做无变化重试。

*最终结果*：未生成本次 D4 预览；有效调用得到功能失败，已登记 `CITY-PW-20260819-01`。游戏随后发送 Ctrl+C 并正常退出。
