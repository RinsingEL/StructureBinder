# 父阵列槽位间距虚胖

*需求入口*：用户查看 `city_realm_salt_kingdom_0_capital` 最新 D4 产物后指出："CENTER_SYMMETRIC 父阵列……功能区之间隔很远"，并认为"出格是很正常的，因为 step 太大了，全是方格会很恶心，正确做法我觉得是真实落地的占地面积小于设计目标就得了吧"，同时提问"是不是一个城市阵列应该分主次，才能有那种明确的街区划分的感觉"。用户与 codex 讨论的"父阵列按固定 96 格排版、导致功能区间距虚胖"此前尚未登记。

*修改内容*：未改实现。在 `dev_docs/systems/city/故障索引/规划与工作流.md` 登记 `CITY-PW-20260820-02`，并完成产物数据与源码核对。

*验证结果*：
- 产物核对（`run/realm_debug/realm_w_mszypml0_fd99c6f2/city_test_runs/city_realm_salt_kingdom_0_capital/steps/d4/group_extent_map.json`）：capital_core / market_axis / residential_cluster 三个 SMALL 组 `slotBounds` 均为 96×96；对称对槽心距约 103.7（理论 104）；预分配功能区相邻间隔恰为一整行 32 格 cell（33 格）；组间初始 frontier gap 49/60，经连接后收敛到 0/15；capital_core 17 栋中 12 栋为连接建筑，`extentExpandedForConnectivity=true`，`collisionExtent` z -805..-644 越过自身槽位 z -768..-673 两侧共 66 格，并与 residential_cluster 预留区相贴（finalGap=0）。
- 源码核对：`CityBlueprintCompilerService.extentMaxSpan(SMALL)=96`（约 2659 行，extentClass 最大外展上限常量，另有 2251/2335 行作为上限校验使用）；`planArrayCompositions`（约 1049-1122 行）与 `slotBounds`（约 1245 行）把该上限直接当槽位占位边长；`CityBlueprintGroupLayoutPlanner.symmetricPairOptions`（171-174 行）对称半径 = `(centerSpan+memberSpan+1)/2 + targetEdgeGap`，由槽位跨度堆出。

*剩余问题*：修复方向已由需求案 `City阵列主从骨架与街带-v0.1`（文档仓库 `docs/systems/city/10_product/案子/`）冻结：城市阵列分主次、槽位与间距按组团真实需要推导、实际落地面积允许小于设计目标；功能区内主建筑定轴、次建筑沿街带排队，区内道路由阵列算法留出。容量预分配不基于真实模板 footprint 的既有缺口见 `CITY-PW-20260819-03`；连接建筑越过预分配功能区且功能区不重算的问题见 `20260820_D4功能区真实轮廓预览` 任务记录剩余问题。
