# 父阵列槽位间距虚胖：真实游玩调用记录

*测试目标*：真实启动 `RTF0810`，复用既有 `SLOPE-01 / p.302` 上游与已接受 Blueprint，只重编 D4，验证父阵列真实槽位跨度、功能区容量和新 preview；不 TP、不执行 D5/D6、不改世界。

| 序列 | 工具与关键参数 | 返回摘要 | 后续 |
| --- | --- | --- | --- |
| 1 | `gis_status` | 成功；玩家 `Dev` 位于 overworld `(-4189,79,-416)`。 | 世界与上游正确，继续 D4。 |
| 2 | `city_compile_d4_blueprint`；`runId=realm_w_mszypml0_fd99c6f2`、`citySeedId=city_realm_salt_kingdom_0_capital` | 失败；`CITY_BLUEPRINT_REQUIRED_STRUCTURE_NO_LEGAL_PLACEMENT`。`capital_core` 第一栋提交成功，第二栋候选为 0，过滤原因含 `D4_ARRAY_COUNT_UNSATISFIED` 与 `OCCUPIED_ENVELOPE_OVERLAP`。trace 已写出。 | 工具只有两个必填 ID，无可修正字段；取得正式业务失败，停止 D4 AgentLoop，登记故障。 |
| 3 | `gis_status` | 成功；玩家仍在 overworld `(-4189,79,-416)`。 | 确认未 TP、未改世界。 |

*槽位观察*：失败 trace 已真实输出 `plannedSpanBlocks`：`capital_core=66`、`market_axis=79`、`residential_cluster=72`，均为 `spanSource=TEMPLATE_ARRAY_DEMAND`；旧三个 96 格固定槽位已消失。

*产物判定*：`city_generation_compile_trace.json` 更新时间为 `2026-08-20 23:21:33`；`group_extent_map.json` 和两张 preview 仍为 `2026-08-20 11:07:15`，属于旧成功结果，本轮未生成新 preview，不得作为验收证据。

*首次结果*：真实 D4 回归失败。未执行 D5/D6，未 TP，未改世界。故障登记为 `CITY-PW-20260820-04`。

## 修复后复验

| 序列 | 工具与关键参数 | 返回摘要 | 后续 |
| --- | --- | --- | --- |
| 4 | `gis_status` | 成功；玩家 `Dev` 仍位于 overworld `(-4189,79,-416)`。 | 重跑同一 D4。 |
| 5 | `city_compile_d4_blueprint`；同一 `runId/citySeedId` | 成功；14 anchors，quality `passed=true/score=100`，`hardBlocks=[]`、`warnings=[]`。三组结构数为 5/5/4，最低数全达到；新 extent map 与两张 preview 均生成。 | 审查槽位、功能区、LINEAR trace 和正式 preview。 |
| 6 | `gis_status` | 成功；玩家仍位于 overworld `(-4189,79,-416)`。 | 确认未 TP、未改世界，结束 AgentLoop。 |

*修复原因*：66 格槽位与 32 格 D3 网格不对齐时，候选 legal region 曾要求整格完全位于槽内，四个已预留功能区格被二次裁成一个格。现有 `districtCapacity.cells` 时，真实预留格本身成为建筑硬合法区；父槽继续负责播种与父阵列编排，不再重复裁剪功能区扩张。

*正式结果*：`plannedSpanBlocks` 稳定为 66/79/72；CENTER_SYMMETRIC 成员中心保持精确对称，中心半径约 80.65 格，旧结果约 103.7 格。实际 collision gap 为市场到核心 0 格、核心到居民 7 格，局部 preview 呈连续街区。市场主建筑记录 `primaryEntranceDirection=WEST` 与 `streetAxisDerivedFromPrimaryEntrance=true`，后续结构记录 LEFT/RIGHT 两侧和 rank；受 32 格功能区地形吸附影响，实际街带呈折线，本轮不宣称笔直街道。

*最终结果*：真实 D4 复验通过。正式产物更新时间为 `2026-08-20 23:31:08`。未执行 D5/D6，未 TP，未改世界。
