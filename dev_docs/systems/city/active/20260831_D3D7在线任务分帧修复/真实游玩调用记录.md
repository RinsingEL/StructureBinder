# D3 D7 在线任务分帧修复：真实游玩调用记录

## 验收目标

- D3：正式 `city_plan_d3` 在 RTF 世界完成时返回 `executionMode=api_worker`、`serverThreadBlocked=false`，并记录耗时与产物。
- D7：只使用独立测试运行包；首次执行缺失 owner 时返回 bounded queue 状态，报告 `maxOwnerActionsPerTick=1`、`synchronousChunkLoads=false`，后续轮询可见进度。
- 不使用用户正在观察的大型城市做世界修改。

## 调用

| 序号 | 环境与调用 | 结果 | 结论 |
| --- | --- | --- | --- |
| 1 | `RTF0810`，独立 town 测试包执行正式 `city_plan_d3`，同时每 200 ms 查询 `/realm/status` | D3 `durationMs=22813.4149`、wall `22994 ms`；`executionMode=api_worker`、`serverThreadBlocked=false`；111/111 次状态查询成功，最大 70 ms | 当前世界没有 RTF GeneratorContext，因此正确回退 Minecraft prior；虽然总耗时较长，但主线程保持响应。 |
| 2 | `RTF08052`，`rtf0805_newsave_w32768_20260805_1840 / city_realm_salt_kingdom_0_border_fort` 正式 `city_plan_d3`，同时每 100 ms 查询 `/realm/status` | D3 `durationMs=1602.9417`、wall `1872 ms`；`step=16`、279 patches；`rtf_heightmap_preview_inferred_climate`、`fastPath=true`；16/16 次状态查询成功，最大 75 ms | RTF 快路径秒级完成，且完整 D3 不占服务器主线程；D3 实机验收通过。 |
| 3 | `RTF08052` 启动后检查独立 D7 前置状态 | City LandUse `activePlans=0`；现有旧测试城市主要为旧目录/旧契约，强行恢复会引入额外激活与世界修改 | 按用户决定停止 D7 实机验收，不使用其正在观察的大型城市代测。 |

## 验收结论

- D3：通过。
- D7：代码与自动回归完成；真实验收未执行，状态保留为“已开发完成、未验收”。
