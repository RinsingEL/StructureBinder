# validation03 城市人工验收失败

*需求入口*：用户原话“随后我验收了的城市，完全不及格”，截图坐标 1731.312 / 85 / -5455.914。沿用本轮 validation03 存档上下文；人工验收结论为不通过。

*修改内容*：只读诊断并留档，未改变代码或游戏存档。坐标落在 city_qareth_skywatch_post 的 planningBounds（1456,-5552 至 1967,-5041）内。

*验证结果*：steps/d4/quality_report.json 为 passed=true、score=100，同时 warnings 明示居民区 ORGANIC_ONE_TO_THREE_BLOCK_GAPS_DISCONNECTED、后勤区 COMPACT_ALLEY_MISSING / COMPACT_ALLEY_DISCONNECTED。city_generation_compile_trace.json 的 compilationAcceptance 为 passed=true，但 qualityFullySatisfied=false、arrayVisualGeometryPassed=false、allStreetEntrancesConnected=false；arrayVisualQuality 为 passed=false、safeToMaterialize=true。后勤区 alleySegmentCount=0。D6 报告 plannedCount=34、failureCount=0、passed=true，只能证明该执行报告无失败，不能作为人工城市品质验收通过的依据。

*剩余问题*：用户补充道路截断、两个功能区意义不明、大量大坑、整城高架大平面，认可 9 月 2 日分级台面层次；本轮要求查原因，尚未修改实现。未重新运行游戏。一个整区块台面漏执行原因、苔石写入的具体 feature 身份、功能区观感修复方案仍待定位。

## 2026-09-07 根因核对

### 高架大平面：已确认 9 月 7 日回归

- `eded826` 新增 CityFoundationElevationPlanner，以四邻接完整连通分量计算一个众数高程；同时 CityFoundationPlanner 从局部闭合/窄桥拆分改成凸包建设面。
- 本城冻结 surface plan 的 29,509 格 foundation 全部 targetY=84。实际建筑 datum 均为 85。截图脚底 Y=85 与之吻合。
- 同提交在 CityLandUseMicroGrader.platformModel 中对 frozen target 提前返回，绕过旧 localDominantHeight、platformTargets 及局部孔洞闭合。9 月 2 日 `455c30e` 仍为半径 3 的邻域高程、4 格台阶分级与局部平台合并。
- 新测试仅验证连通面统一高度、断开面可不同高度；没有验证同一连通城区的多级台面，因此会通过而丢失用户认可的层次。

### 五栋建筑漏建形成深坑：已确认旧缺陷在此城触发

- D4 计划 34，实际 `geomantia_city_masks/worldgen_placement_ledger.json` placedStructures=29，failures=0。之前用 D6 plannedCount=34/failureCount=0 推断落地完整是不充分的；D6 是计划/锁定结果，不能替代 worldgen ledger。
- 缺失：g_residential_required_003、g_residential_connectivity_009/010/014/015。分别共用起始区块 (111,-346)、(107,-343)、(108,-342)、(109,-340)，最后一个区块漏两栋。
- MinecraftCityWorldgenStructurePlacer.java:113-115 对统一 CITY_TEMPLATE_TERRAIN_STRUCTURE_ID 查询已有 StructureStart，已有有效 start 就直接 return；没有追加第二栋 piece，也没有登记 failure。该逻辑 blame 为 `dfdc33d02`（7 月 18 日），不能将它说成 9 月 7 日新引入。
- SurfacePrint 对建筑 footprint 做 exclusion，周围升至 Y84，漏建 footprint 保持低地。直接只读解析 region NBT：用户附近 (1741..1754,-5457..-5447) 的 154 格坑与 connectivity_010 的整个 footprint 一致，坑底大部分 Y70/71，最低读到 Y61；其余四栋同样有对应空坑。
- 台面 Y84 上共发现 903 格 air/cave_air；不能把全部都称为深坑，其中也有台阶降高。主要五个缺失建筑坑另有独立空间对应证据。
- (1696..1711,-5312..-5297) 还有 255 格 Y84 空缺，底部主要 Y82；对应完整区块 (106,-332)，该 owner 不在 appliedOwners，邻区块存在。此独立漏执行原因待定位。

### 道路：规划未接通与写后覆盖并存

- trace 已有居民区缝隙不连通、后勤巷道数量 0、入口 UNRESOLVED。
- 只读 region 与 worldgen_block_observations 双重确认 (1737,84,-5495) 原写入 deepslate_tile_slab，post_features 已变 mossy_cobblestone，chunk_save 仍为 mossy_cobblestone；最终 NBT 一致。不能只归咎道路算法没生成。
- 两条 feature 抑制入口仅以 vegetationLike 字符串和 origin 判断；覆盖源的具体 feature 未记录在该方块观测中，不断言已锁定是哪一种原版石块 feature。

### 放行退化与功能区

- `50d9c9c`（9 月 5 日）明确将 STREET_ENTRANCE_UNRESOLVED 从 hardBlocks 移至 warnings，并增加 SAFETY_AND_REQUIRED_CONTENT_V1 / qualityFullySatisfied；CityStructureAnchorPlanner.quality 无 hardBlock 就给 score=100。应区分“可继续施工”和“人工质量验收通过”，当前满分不能证明后者。
- 本城设计实际有哨戒、住宅、后勤三个 Group，计划分别 9/16/9 栋；34 栋中 24 栋来自 connectivity 扩张。记录用户“两个功能区意义不明”的现场判断，但没有城市全景，不把该统计冒充观感根因已全部解释。

*只读复核脚本*：[inspect_saved_surface.py](inspect_saved_surface.py)，依赖本机 Python 的 nbtlib；读取 .mca 与冻结计划，不写存档。

*复核输出*：[saved_surface_evidence.json](saved_surface_evidence.json)。未运行实现测试，因为本轮无实现改动；验证使用存档、代码历史及真实方块交叉核对。
