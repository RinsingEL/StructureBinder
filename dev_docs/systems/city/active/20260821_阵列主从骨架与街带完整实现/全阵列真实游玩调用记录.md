# 全阵列真实游玩调用记录

*目标*：真实启动 `RTF0810`，基于同一 D3 点位、同一模板池、同一密度与同一 generation seed，分别 prepare / submit / compile 六种正式建筑阵列：`COMPACT`、`GRID`、`LINEAR`、`COURTYARD`、`ORGANIC_COMPACT`、`CENTER_SYMMETRIC`。每种仅含一个结构群，避免群间连接补边干扰阵列形态。输出单图及六宫格对照图；不确认世界修改。

| 序列 | 工具与关键参数 | 返回摘要 | 后续 |
| --- | --- | --- | --- |

## 用户纠正

*用户原话*：`不是这个意思，我的意思是让他们在一个城市李同时出现，应该可以用一个父阵列同时阵列6种吧`

*修正后的测试目标*：此前六次独立 Blueprint 仅作为误解过程保留，不作为最终展示。最终必须在一个 Blueprint 内同时声明六个完整子 Group，各自使用一种正式建筑阵列；再由一个父 `GRID` 阵列把六个子 Group 编排到同一城市，输出同城正式 D4 全景图。

## 组合测试调用

| 序列 | 工具与关键参数 | 返回摘要 | 修正/结论 |
| --- | --- | --- | --- |
| 1 | `gis_status` | 成功；`Dev` 位于 overworld `(-4189,78,-416)`。 | 世界正确。 |
| 2 | 六种阵列分别 prepare / submit / compile | 六种独立 Blueprint 均可成功，quality 100；但不符合用户要求。 | 用户纠正后废弃为最终展示。 |
| 3 | 单 Blueprint：父 GRID + 六子 Group；`p.302` | prepare/submit 成功；父槽无法保留 `gallery_courtyard`。 | 改父 ORGANIC_COMPACT。 |
| 4 | 父 ORGANIC_COMPACT；`p.302` | 有限 required 组合耗尽。 | 改父 LINEAR。 |
| 5 | 父 LINEAR；`p.302` | 父槽无法保留第三个子 Group。 | 回父 GRID，调整子组密度与主从顺序。 |
| 6 | 父 GRID；子组 SPARSE / DENSE 多轮；`p.302` | 分别暴露 `gallery_organic reached 1, requires 5`、`gallery_linear reached 1, requires 2`、`gallery_courtyard reached 1, requires 3`。 | 确认不是单纯父算法或顺序问题。 |
| 7 | 父 GRID；统一 DENSE；最大陆地区 `p.1`（941 cells） | prepare/submit 成功；compile 返回 `gallery_courtyard reached 2 district cells but requires at least 3`。 | 合法大区仍失败，登记 `CITY-PW-20260821-04`，结束纯 MCP Loop。 |
| 8 | `gis_status` | 玩家仍为 `(-4189,78,-416)`。 | 未 TP、未确认世界修改。 |

## 修复后同城复验

| 序列 | 工具与关键参数 | 返回摘要 | 结论 |
| --- | --- | --- | --- |
| 9 | 启动 `RTF0805`；`gis_status` | 成功；玩家位于 overworld `(121629,98,118316)`。 | 使用已有真实平原 D3。 |
| 10 | prepare/submit；父 GRID + 六个子 Group；`r.22.-11:p.1` | Context 与 Blueprint accepted；平原 113 member cells、平均坡度 0.63。 | 进入 D4。 |
| 11 | 修复前多轮 `city_compile_d4_blueprint` | 完整 trace 依次暴露 CENTER 子槽跨度 53 小于真实 pair、pair 混入普通候选、四栋离散占位只预留三格。 | 登记并修复 `CITY-PW-20260821-04`。 |
| 12 | 修复后 prepare/submit/compile；父 GRID、六子组 DENSE、同 seed/模板池 | 成功；33 anchors，quality 100；0 hard block/warning/review。六组内部数量：COMPACT 5、GRID 5、LINEAR 7、COURTYARD 4、ORGANIC_COMPACT 6、CENTER_SYMMETRIC 5。 | 一个城市内六种阵列同时成立。 |
| 13 | `gis_status` | 玩家仍为 `(121629,98,118316)`。 | 未 TP、未确认世界修改。 |

*最终正式图*：`rtf0805_newsave_w32768_20260805_1840/array_gallery_combined_city_realm_salt_kingdom_8_capital/structure_anchor_cluster_preview.png`
