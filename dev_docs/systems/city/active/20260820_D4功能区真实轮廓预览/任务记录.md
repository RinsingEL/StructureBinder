# D4 功能区真实轮廓预览

*需求入口*：用户查看真实 D4 预览后指出功能区仍显示为一个个矩形框，要求先修改预览图；最新确认要求是展示扩张所得功能区范围。

*修改内容*：D4 总览和局部预览不再用 `districtEnvelope` 矩形作为功能区主边界；把 `districtCapacity.reservationSpans[]` 合并成单一屏幕区域，填充并绘制真实外轮廓，保留建筑主体、碰撞和 mask 诊断层。没有 reservation spans 时不绘制功能区。局部图 viewport 同时纳入可见功能区范围，避免扩张区域被裁掉。同步更新图例，并增加 L 形功能区缺口及局部取景的像素回归。

*验证结果*：`CityStructureLandingPreviewRendererTest` 定向测试通过；`./gradlew.bat test --no-daemon` 全量通过；`git diff --check` 通过。真实启动 `RTF0810`，玩家保持 overworld `(-4189,79,-416)`，复用 `realm_w_mszypml0_fd99c6f2 / city_realm_salt_kingdom_0_capital` 编译 D4 成功。新局部预览中行政区按 5 个 reservation cells 显示折形边界，居民区和市场区按各自 4 格显示，旧矩形外包框已消失；未执行 D5/D6，未 TP，未改世界。

*剩余问题*：连接阶段生成的建筑可越过预分配功能区，功能区未随连接结果重算；本次只修预览表达，不修改该规划行为。
