# D4 功能区真实轮廓预览：真实游玩调用记录

*测试目标*：真实启动 `RTF0810`，复用现有最高有效 D4 上游，只重编结构预览，确认功能区显示 `reservationSpans` 合并后的真实扩张轮廓；不 TP、不执行 D5/D6、不改世界。

| 序列 | 工具与关键参数 | 返回摘要 | 后续 |
| --- | --- | --- | --- |
| 1 | `gis_status` | 成功；玩家 `Dev` 位于 overworld `(-4189,79,-416)`。 | 坐标与区块状态未改变，继续复用现有 D4 上游。 |
| 2 | `city_compile_d4_blueprint`；`runId=realm_w_mszypml0_fd99c6f2`、`citySeedId=city_realm_salt_kingdom_0_capital` | 成功；重新生成 structure anchor plan/map、quality report、group extent map 与两张 D4 preview。 | 检查正式局部预览；行政区呈 5 格折形，矩形 envelope 不再绘制。 |

*最终结果*：真实 D4 预览复验通过。正式产物为 `steps/d4/structure_anchor_cluster_preview.png` 与 `steps/d4/structure_anchor_preview.png`。连接建筑越出预分配功能区仍真实显示，本次未掩盖也未修改该行为。
