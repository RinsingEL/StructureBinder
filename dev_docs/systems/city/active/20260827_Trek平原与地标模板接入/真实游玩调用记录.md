# 真实游玩调用记录

## 2026-08-27 固定模板运行时 metadata 回读

- 目标：启动 RTF0810 后，通过正式模板 metadata 接口回读新增喷泉、两座塔和矿业巨爪的运行时 hash/rawSize，作为 City 固定模板目录准入证据。
- 可复用上游：`run/saves/RTF0810/generated/geomantia/structures/city/trek/` 已由审核 manifest 离线写入 24 个固定 NBT；不重跑 D3/D4。
- 实际调用：启动 `runClient` 自动进入 RTF0810，向 `POST /realm/city/query_template_metadata` 提交四个正式 `geomantia:city/trek/...` templateRef；完成后正常停止客户端。
- 实际结果：四项均 `readable=true`、`failureCode=NONE`。
  - `plains_fountain_01`：`9x4x9`，`sha256:bbb39271f8b10368316feb4230e6e646a62fc5449f702f95e35f3f41a14873fd`。
  - `mangrove_watchtower_1`：`12x12x11`，`sha256:bd5c4682939f51d3d7db0829524d05a3fa239334087b99d841845b9cf1fff754`。
  - `mangrove_watchtower_2`：`12x23x11`，`sha256:dbe5b45b2c86144d62581d0d1c25c1970ae1c120148a50fdd9b8006aa1fb2820`。
  - `claw_pillager`：`15x15x3`，`sha256:861dd3da7f978ee63304b86b3dca5fa40965ef6639a25cc2c83a6afc350c08ed`。

## 2026-08-27 正式语义目录重启检索

- 目标：确认运行配置扩为 65 份画像后，City 正式目录能看到四个新增模板，而不是只有世界数据包存在文件。
- 实际调用：重启 `runClient` 并自动进入 RTF0810，向 `POST /realm/city/query_structure_catalog` 提交正式 `TerraSenseStructureProfileSource.official.json`，按 `function.行政|function.防御|function.矿业` 查询。
- 实际结果：`ok=true`，schema `city_structure_catalog_query.v0.5`，`matchedCount=7`；四个新增 semanticProfileId 全部命中且均为 `approved`，source quality 为 65，`warnings=[]`、`needsReview=[]`。完成后正常停止客户端。
