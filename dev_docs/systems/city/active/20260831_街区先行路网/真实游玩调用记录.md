# 街区先行路网真实游玩调用记录

测试环境：`runClient`，存档 `RTF0810`，run `realm_w_mte38ltl_f1964c5a`，城市 `city_realm_stone_march_1_capital`。

| 顺序 | MCP 调用 | 关键参数 | 结果与纠正 |
| --- | --- | --- | --- |
| 1 | `city_compile_d4_blueprint` | 同 run/city | 编译成功，但短巷 `segment_004/006` 与其服务建筑 actual footprint 重叠，`arrayVisualGeometryPassed=false`；停止 Agent Loop，修复完整横断面净空。 |
| 2 | `city_compile_d4_blueprint` | 同 run/city | 重叠归零，但 `town_core_fill_002` 入口 `UNRESOLVED`；停止 Agent Loop，扩展 COMPACT 先行骨架并增加入口验收字段。 |
| 3 | `city_compile_d4_blueprint` | 同 run/city | 原入口接入，布局变化后 `town_core_fill_007` 成为新孤门；停止 Agent Loop，令骨架按 formation span 缩放。 |
| 4 | `city_compile_d4_blueprint` | 同 run/city | 孤门迁移回 `town_core_fill_002`，证明固定形状/尺寸不稳健；停止 Agent Loop，新增同功能区确定性绕障共享支路。 |
| 5 | `city_compile_d4_blueprint` | 同 run/city | 通过：8 anchors；3 reserved skeleton、2 shared extension、7 short alley；8/8 入口接入；road/body overlap 0；视觉与 acceptance 通过，warnings 空。D4 产物位于 `steps/d4/`，局部视觉证据为 `structure_anchor_cluster_preview.png`。 |
| 6 | `city_run_workflow` | `skipExisting=true`、`confirmWorldMutation=false`、不规划/执行城墙 | D3/D4 复用，D5 规划成功；D6 返回 `STRUCTURE_CHUNK_ALREADY_GENERATED`。按规则停止，未激活、未传送、未执行 D7。 |

辅助调用：首次关闭旧客户端前尝试 `realm_debug_command save-all`，未带 `allowUnsafeCommand` 被安全门拒绝；补充确认后命令返回 `DEBUG_COMMAND_NO_SUCCESS/result=0`，没有据此判定保存成功。后续只关闭已精确核对为本仓库 `forgeclientuserdev` 的进程。

结论：真实游戏服务中的 D4 编译与预览验收通过；当前城市不具备新区块 worldgen 前提，D6 保护性拒绝有效，D7 现场验收待新 CitySeed/测试存档。

## 绿廷大型三功能区补测

测试城市：`city_realm_green_court_2_capital`（`large_city`；行政、市场、农业）。旧 D3/Blueprint/测试清单均来自 schema 字段迁移前；先在不改写验收原件、不放宽哈希校验的前提下加入只读兼容，并通过全量及聚焦回归，随后重启客户端重编 D4。

最终 MCP 结果：`ok=false`、`CITY_BLUEPRINT_COMPILED_ANCHOR_FINALIZATION_FAILED`。共 34 anchors（农业 29、市场 5、行政 0）与 19 street bands；市场 5 个入口全部接入，农业 29 个入口全部 `CITY_INTERNAL_STREET_ENTRANCE_NETWORK_EXTENSION_UNAVAILABLE`。显式农业→市场 `CONNECTION` 生成 1 条主路、10 段，但 source transition 与 `agriculture_large_required_001` 风车重叠。硬阻断为 `administration_small: FUNCTION_AREA_EMPTY`，`allStreetEntrancesConnected=false`、`arrayVisualGeometryPassed=false`。总览及三张分区预览均与指标一致；按失败规则停止，未进入 D5/D6、未修改世界。

预览：`steps/d4/structure_anchor_cluster_preview.png`；分区图为 `structure_anchor_group_agriculture_large.png`、`structure_anchor_group_administration_small.png`、`structure_anchor_group_market_small.png`。另记录接口序列化缺陷：响应内部包含 `Infinity`，不是合法 JSON 数值。
