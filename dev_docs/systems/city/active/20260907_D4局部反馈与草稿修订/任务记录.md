# D4 局部反馈与草稿修订

*需求入口*：用户要求参考原型，提交后给出明确的局部报错指引，避免整城盲改。

*修改内容*：正式submit直接提供group/structure/字段路径/候选过滤计数与位置样本；保存未接受canonical草稿，支持baseDraftHash+replace补丁。prepare与设计回合重启在failureCount=0时也能取得草稿反馈。补丁校验当前context/city/内容hash/accepted版本，接受后草稿失效。新增作者parcelCount与景观角色比例的实际值、上下限、局部调整说明。TS工具schema、Java Provider schema、HTTP转发和Hermes提示同步更新。未知布局求解失败不猜最低面积，不引导模型删除必需内容。

*验证结果*：最终 `gradlew test` 890 tests / 0 failures / 0 errors / 7 skipped；`country_designer_mcp npm test` 10/10通过，包含stdio桥接。`git diff --check`通过。新增测试覆盖无accepted时草稿局部修订、其他groups与seed保持、hash二选一与接受后过期、零失败计数恢复证据、局部搜索反馈、数值上下限反馈。上轮实际失败trace经新模块离线回放，准确返回g_market_district/flower_shop_01、$.groups[1]、144次尝试（输出本目录JSON）。未启动新的游戏或GLM回合，未宣称布局或后续编译已通过。

测试中先发现空初始context恢复兼容问题，已修复；另将未知候选失败的保守分类限制在新submit分支，避免意外改变旧post-D4分类，完整回归最终通过。离线jshell回放最初遇Windows编码/PowerShell参数问题，改为显式UTF-8并引用参数后通过；这些是测试工具操作问题，不是产品Bug。

*剩余问题*：原型主阵列宽深/开口参数、逐区几何确认与独立预览仍需接入；草稿局部修订保留输入，不承诺无关区域编译几何完全不变。花店无候选的求解根因尚未修复。本次不新增模型调用或改写用户存档，未提交。
