# GIS 故障索引

本文件记录 GIS 采样、指标与地貌真值中可复用的故障摘要；当前功能真值仍以文档仓库 `docs/systems/gis/` 为准。

| 编号 | 状态 | 现象 | 根因 | 修复方向 | 验证 |
| --- | --- | --- | --- | --- | --- |
| `GIS-PRIOR-20260804-01` | 已修复，待真实世界复扫 | 未生成区块的高海拔河流 / 湖泊被 D3 记录为 `water=false`，City `SURFACE` terrain gate 因而允许建筑落入水体。 | `MinecraftPriorAtlasSampler.isWaterColumn(...)` 只在 `height <= seaLevel` 时读取 `NoiseColumn` 流体；高于海平面的水体被无条件当成陆地。D3 `cellStepBlocks=32` 每 Cell 单柱采样会进一步放大岸线漏判，但不是本次整片高海拔水域误判的首要原因。 | 已继续使用 Minecraft `ChunkGenerator#getBaseColumn(...)` 的未生成区块标准接口，改为检查实际 WORLD_SURFACE 顶层流体，并以实际水面减 OCEAN_FLOOR 计算水深；未加入 Epic Terrain 专用分支。另行评估 City footprint 水体 gate 是否需要比 32 格 planning cell 更细的精确采样。 | 现场 run `trek_stubbs_currentworld_20260804_2032`，市政厅锚点 `-2960,96,5840`；以市政厅中心约 `-2937,5855` 向外 96 格、step=8 抽样 625 列，574 列存在 Y=88..99 水体，覆盖率 91.8%，对应 6 个 D3 Cell 却均为 `minecraft:river / water=false / waterDepth=0`。自动回归 `MinecraftPriorAtlasSamplerTest` 已覆盖原版海平面水、Y=100..104 高海拔湖泊与普通陆地；仍需对原现场新鲜 D3 复扫。 |
