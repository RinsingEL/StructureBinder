# 测试存档迁入开发端

*需求入口*：用户要求将 PCL 的 `RTF_beta_validation_02` 带到开发端运行测试。

*修改内容*：已复制 `D:/PCL2-2.8.12/.minecraft/versions/1.20.1-Forge_47.4.23/saves/RTF_beta_validation_02` 到 `E:/Mod_Dev/StructureBinder/run/saves/RTF_beta_validation_02`。只复制，原档不移动、不删除；开发目录不存在同名档，没有覆盖旧档。

*验证结果*：迁入前没有 Java 游戏进程，源日志 14:39:20 确认全部维度保存完成。源档 2215 文件、217273669 字节，无 reparse point；复制期间持有 source session.lock 只读共享句柄，完成后逐文件 SHA-256 核对，差异 0。开发 RTF 与源实例 RTF 哈希相同：`00390B4BEB053316E927FB4ECFFB998596506DE13165ACA4628CD7B8B5F2A3D9`。开发端没有 provider.json，默认自动 Provider 禁用，未复制源实例密钥。

*开发启动*：执行 `gradlew.bat runClient '-Pforge_version=47.4.23' '-PgeomantiaDevUseReTerraForged=true' '-PgeomantiaDevUseStructurePacks=true' '-PgeomantiaDevAutoLoadWorld=RTF_beta_validation_02' --console=plain`。仅本次启动覆盖 Forge 版本以匹配源实例，不修改项目默认值。第一次未引用 PowerShell 参数导致版本值截断为 47，已加引号后重试，构建日志确认解析 47.4.23；这是启动调用错误，不登记产品 Bug。目标是加载副本并用已有 run 的 T3 输入复现错误；不重扫 W，不改三国设计或大陆选择。

*可复用上游*：副本 `world_survey_manifest.json` 为 `status=sealed`、`durationMs=3948`，provider 为 `rtf_heightmap_preview_inferred_climate`、`fastPath=true`；已有 run 为 `provider_ce6a3c351fa2667b_r8192`。

*启动边界修正*：14:50:31 运行日志表明旧的 auto-load Base64 设置优先于新传入的世界名，误加载开发端既有 `RTF_beta_validation_01`。发现后未发出任何规划 API 调用，确认游戏 PID 41860 属于本轮 Gradle 启动后通过正常关闭窗口退出，进程已结束。误加载可能造成旧开发档常规 tick/save，不能宣称该档完全未变；PCL 原档与新复制的 02 未受影响。后续仅通过命令行显式覆盖 `geomantiaDevAutoLoadWorldBase64`，不修改全局旧设置。

*正式入口测试*：本会话未提供 Geomantia MCP connector，使用其正式 localhost HTTP 对应入口，未绕过业务 handler。14:52:42 日志确认自动加载 `RTF_beta_validation_02`，14:52:49 API 启动。

| 调用 | 请求 | 返回 | 修正与结论 |
| --- | --- | --- | --- |
| GET `/realm/status` | 无 | 200，`serviceReady=true`，debugRoot 为开发副本 `run/saves/RTF_beta_validation_02/realm_debug`，knownRuns 初始为空 | 确认当前世界边界；未调用 W。 |
| POST `/realm/t3/expand` | `{"runId":"provider_ce6a3c351fa2667b_r8192","qualityMode":"strict"}` | 400，`{"ok":false,"error":"No assignable land cells for normalizationGroup: primary_realms"}` | 使用契约允许的省略分组全局 T3 输入；成功恢复上游后复现原故障，无合法参数修正能同时保留当前分组与三个大陆。停止本轮测试，未更改国家设计。 |

*环境差异*：本轮启动还出现 LibraryFerret 缺依赖警告，涉及 26 个 block/item/mob_effect 注册项。自动载入绕过了缺项确认，可能改变试运行副本中的缺项内容。因此本轮仅证明既有规划数据能恢复并复现 T3，不是世界内容完整验收。发现后正常关闭本轮游戏进程 PID 49060，后续保留试运行副本并重新复制干净基线；PCL 原档不动。

*最终交付状态*：客户端已正常退出。试运行副本保存在 `run/test_import_snapshots/20260905_1452_RTF_beta_validation_02`，同目录 `development_latest.log` 保留本次运行日志，没有删除试运行资料。已从 PCL 原档重新复制干净的 `run/saves/RTF_beta_validation_02`，再次逐文件核对 2215 个 SHA-256，差异 0；该干净副本未再启动。缺失的源依赖为 `libraryferret-4.0.0.jar`，尚未修改开发依赖。

*剩余问题*：T3 分组与退出 W 取消两个已知缺陷尚未修复。还需补齐开发端 LibraryFerret 等运行内容依赖后再做完整世界验收；未调用模型、未重扫 W、未假造 T3 成功结果。
