# GLM 首城闭环验收

*需求入口*：用户确认由 Agent 自行开发、测试验收，授权复用 PCL 的 GLM 配置与 Key，在开发副本运行真实调用，目标为稳定且速度合理地生成城市。

*修改内容*：开始修复 T3 逻辑分组/多大陆扩张、W 生命周期取消，补齐开发依赖，准备隔离配置。原 PCL 存档、配置不动；不把密钥写入记录或提交。

*验证结果*：开发进行中。复用 `RTF_beta_validation_02` 已封存 W/T1/T2；不重新设计国家以绕过错误。

*剩余问题*：尚未真实首城通过；后续逐项记录回归、实际调用、耗时与视觉验收证据。

## 第一轮修复与隔离准备

- T3 按真实 `targetContinentId` 分大陆计算并合并；逻辑标签不再用于过滤地格。同大陆的不同标签国度共同参与，防止重复占地。计算失败保留旧状态/产物（不宣称多文件写入具备事务性）。
- W 取消信号传到粗采样、快速 feature/elevation 及 common-pool 微采样；世界关闭先发取消，再停止 Provider。取消不封存 survey，进度为 cancelled。
- 聚焦回归通过，新增双大陆/不同标签、两种扩张模型、失败保留旧产物、后台并行微采样停止验证。全量回归进行中。
- PCL Provider 配置和密钥仅复制至被 Git 忽略的 run/config/geomantia，暂时 enabled=false。作者 bundle 复制至独立 pcl_validation_02 目录，通过显式开发启动参数选择。PCL 原文件未改。
- 开发结构包依赖补入已有本地 LibraryFerret 4.0.0；游戏固定 Forge 47.4.23，启动显式指定 02 的 Base64，避免旧 01 自动加载参数覆盖。

## 实机轮次 1：T3

- `GET /realm/status` 确认 debugRoot 为开发副本 02。
- `POST /realm/t3/expand`，请求 `{"runId":"provider_ce6a3c351fa2667b_r8192","qualityMode":"strict"}`，2.294 秒，ok=true/status=completed，三国均单连通，原 primary_realms 错误消失。LibraryFerret 已加载，无之前缺失注册映射。
- 产物：开发副本 run 的 `t3_report.json` / `realm_territory_map.json` / `territory_preview.png`。报告同时显示输入 target=.4/.45/.35、max=.55/.6/.5，但 normalizedTargetAreaRatio 全为 1，实际占地约 99%。有效调用产生违反面积约束的结果，本轮结束，进入已授权修复。尚未启动付费 GLM。
- 全量 Java 测试与 runClient 重编译发生共享 build/classes 竞争，739 中 42 项 ClassNotFound；本次全量结果无效，将串行重新执行。聚焦测试、Node 10/10、Python 3/3 已通过。

## 面积约束修复后回归

- 归一化保留不足一块大陆的目标，超额时有界缩减；min 总和超额明确拒绝。Action frontier 增加最大面积停止条件；quota 不再强行吃满剩余土地。
- 新回归检查单大陆独立国家 target=.35/max=.5，两个模型不突破上限；同时补 owner interrupt 取消测试。
- 一次全量出现新增断言误用取整后的 cell ratio（已改为检查 normalizedScales 原比例），以及既有 Landscape 8 秒性能测试超时 157ms；专项复跑通过，未放宽性能限时。随后串行全量测试通过（1m53s）。
- 开发客户端 maxFps 从 260 限为 60，减少测试时渲染争用；只改被忽略的 run/options.txt。

## 实机轮次 2：面积复验通过 / GLM 准备

- 全量 JVM 740/740（106 suites）通过。
- 同一 `POST /realm/t3/expand` 请求，2.228 秒 completed；normalized target 保留 .45/.4/.35。action_budget 实际面积 .599947/.549850/.499621，均不超过原 max .6/.55/.5，三国单连通，明确记录 maximum_area_reached。仍保留现有行动力模型，实际面积不保证精确等于软目标。
- T3 本轮完成；接下来作为下一轮测试的环境准备，仅将开发副本 Provider enabled=true，使用已授权 GLM 真调用，不改国家/模板语义。

## 实机轮次 3：GLM 的上下文截断失败

- 15:46:52 启用开发 GLM；15:47:30 首次 API 请求。T4 15:51:59 完成 realm_sunmarch 四城。工具耗时 create=.05s、open=2.76s、show=.52s、select≈.3s；模型候选选择一次 164.8s/out=6558 tokens，其余约 7–21s。
- 15:52:17 进入最近的 city_realm_sunmarch_caravan，D3 后模型将 plain 误写为 plains，依据类型错误自行修正；这是调用错误，不登记产品 Bug。
- 有效 `city_prepare_d4_blueprint_context` 请求仅含 `runId=provider_ce6a3c351fa2667b_r8192,citySeedId=city_realm_sunmarch_caravan`；15:53:50 返回 709529 chars，程序 .46s，Hermes 明确 Inline-truncating。
- 运行 state.db 的消息 31/33 均为重复 prepare 返回，仅 1963 chars，末尾明确 `[Truncated: tool response was 709,529 chars. Full output could not be saved to sandbox.]`。正式 `city_blueprint_context.json` 为 2654002 bytes，目录为 run/…/city_test_runs/city_realm_sunmarch_caravan/steps/blueprint。
- 资料传递不完整且重复请求不能修正，本轮结束，回到已授权实现诊断；复用有效 W/T1/T2/T3/T4/D3，不重设计国度。

## D4 设计视图修复

- 新增 CityPlanningDecisionView：保留冻结 contextId、全部作者语义和可选池/算法/景观 profile；模板只展示真实尺寸/间隔，执行映射与重复 provenance URI 留在原冻结快照。地形首屏各类最大两个候选，声明非完整列表，通过原 Patch Explorer 分页访问剩余候选。
- 全部正式 Context/快照不删字段、不改内容；模型视图另有 schema。去掉旧 D3 功能区绘制提示，避免与 Blueprint 决策冲突。
- Java/Node 共用的宿主展示层以转义后的文本 envelope 检查 90000 chars 预算，超限为 PLANNING_PRESENTATION_TOO_LARGE 宿主阻塞，不允许成功截断和重复空转。Hermes 指令明确视图不是缺失数据。
- 真实 2.65MB Context fixture + 作者保留/分页/超限硬阻塞测试通过；捕获样本视图 82224 chars（测试目录下 preview 不可用的警告也计入），低于 Hermes 100000 chars 单工具阈值。

- 轮次 4 实机预检：同一 prepare 请求加 `X-Geomantia-Agent-View=true` 得到 500/PLANNING_PRESENTATION_TOO_LARGE；不加头返回完整 Context，证明完整 HTTP envelope 的额外预算/产物字段使边界超限。未开启 GLM。本轮结束，继续缩小首屏候选分页并增强完整 envelope 回归，保持作者目录完整。

- 首屏调整为每类最大一个候选，明确可翻页；回归额外加入 5600 字符 HTTP 余量后仍通过，含余量/不可用图片警告共 81983 chars。PlanningBoundary/HermesAgentClient/ProviderPlanningToolGateway 专项通过，恢复 GLM 原城市会话。

## 轮次 5：展示修复通过，恢复调度的性能结果不合格

- 同一 prepare + Agent-View 正式返回 ok=true，0.742s，76944 文本字符/83585 envelope 字符、3 张图、65 份作者语义、10 类首屏候选。contextId 保持 `sha256:65d3cf011ae184761f73211043ffb9a411c411bb60a0fc8f2ed4604d0663b39e`。
- 恢复请求 16:12:08→16:15:57，229.9s/out=7757 tokens，唯一工具为 city_design_queue_status（.04s）。尚未取得新版 Context；state.db 最新 user 输入仍是队列+作者概要，正确设计视图只在 prepare 工具后可得，旧截断工具历史仍在会话。
- 已获得明确阶段性能结果：近四分钟只读状态，不满足快速设计目标。本轮结束，按已授权 AI/程序分工修复调度输入：宿主先准备完整冻结设计视图和图片，设计阶段不再开放纯状态轮询/重复准备；不删除已有历史、不替模型设计。

- PreparedCityDesignTurn 在 CITY prepare/submit 阶段由宿主正式 prepare，直接注入当前冻结设计视图、原失败预算和三张当前世界内的实际地形图。新输入明确替代历史截断消息，不删会话。作者资料已在 Context 内，不再重复注入 authoringBrief。
- 本阶段只发布候选分页和 Blueprint 提交；两种执行器均校验当前白名单。准备失败先阻塞，不能唤醒模型猜资料。Runner/Boundary/Discovery 专项通过，恢复实机。

## 轮次 6：初始消息另有截断 / 串行视觉代理

- 16:23:49 开始新轮，state.db 消息 37 只有 65575 字符（65536 字符正文 + 三个 screenshot 占位），尾部在 landscape profile 中途断开，缺少当前 contextId 和宿主新指令。正式宿主 Context 完整，模型实际初始消息不完整，本轮失败并结束。
- 日志显示不是一次多图决策：Hermes 先进入 tools.vision_tools，逐图调用通用“Describe everything ... thorough detail”，第一张耗时约 38 秒、返回 3397 字符，随后第二张。需核对原生视觉能力路由，不能靠文字转述代替完整地形图。
- 继续定位初始消息长度限制与图片能力识别；原已封存资料/失败预算保持不变。

- Bootstrap 增加有界输入适配：262144 字符，与 Java 客户端一致，超限明确拒绝；10MB HTTP 请求限制不改。不编辑已安装 vendor。打包自带 Hermes 的真实 normalizer 验证 100k 文本及尾部 contextId、image_url 原样保留，专项 Java / Python 4/4 通过。
- 精确模型 `glm-5.3-flash` / `zai-org/glm-5.3-flash` 写入原生视觉能力补充，其他未知型号不推断。依据发布者模型卡 https://huggingface.co/zai-org/GLM-5.3-Flash 及当前 API 视觉辅助调用成功证据；准备恢复同一城市会话检验直传图像。

## 轮次 7：完整输入及原生视觉通过，实际编译失败

- 16:35:39 主请求开始；state.db 消息 38 长 78391、尾部指令与 contextId 完整，三图直接进入主请求，无 vision_tools 转述。
- 首次设计 298.5s/in=73366/out=14886，16:40:38 提交被 area share / relation enums / attached landscape 字段校验拒绝（.17s）；模型正常自行修订，16:41:39 第二次提交被正式接受。
- 后台编译约 27s，16:42:06 返回 CITY_BLUEPRINT_COMPILED_ANCHOR_FINALIZATION_FAILED，失败预算 1/5。d4/quality_report.json 与 city_generation_compile_trace.json 已生成。
- 16:42:09 宿主自动唤醒修订，但消息 44 仅有泛化失败预算 reasonCode，没有实际质量报告或具体局部冲突；本轮有效编译失败已成立，结束实机轮次，暂停 Provider，检查宿主错误分类/反馈完整性后再恢复。保留已接受 Blueprint 和原失败预算，不重新设计或清零。

## 修订输入与推理预算

- 当前求解/验收真值：quality_report 的必需建筑缺失、道路重叠、入口未连通均保留为失败，不降低门槛。本轮仅修复宿主证据交付，尚未宣称这些执行问题已修复。
- CityRevisionEvidence 由宿主读取当前城市正式产物，验证 accepted Blueprint hash、context/city/source refs；加入上一版完整 Blueprint、每组停止原因、质量 hardBlocks、实际失败预览。原 Context identity 和 1/5 失败预算不变，捕获实档专项通过。
- 完整设计输入采用按世界/run/city/context/failureCount 区分的会话，旧探索/截断会话保留在 DB，单轮参数修订仍在同一会话。减少旧历史负担，不丢业务状态。
- GLM-5.3-Flash 官方模型卡说明未传 reasoning_effort（或传不支持的 medium）默认 max；通过 Hermes 的精确 custom provider extra_body 配置 low，待实际 API 调用耗时/质量检验；不影响其他模型，也不改任何城市质量规则。
- 16:54 全量 Java 构建测试成功（107 suites；captured fixture 测试在全量无环境变量时按设计跳过，前次专项带真实 fixture 已通过）。打包 Hermes 实际 config 路由回归确认 native vision=true，custom provider extras 精确得到 reasoning_effort=low；Node 10/10、Python 4/4 通过。恢复开发实机轮次 8。

## 轮次 8：低预算与完整反馈通过，第二次编译仍失败

- 会话 geomantia-design-91ba3a97-d2fd-320c-a979-e3ec2a9096ee，history=0；消息 45 完整 101669 字符，上一版 Blueprint、quality hardBlocks、groupOutcomes、编译预览全部到达，预算保持 1/5。
- 16:56:03→16:57:22 首次修订请求 79.5s/in=40404/out=4308；随后两次提交 areaShare=.96 被拒，第三次改至 1.0 接受。此处为模型调用错误，不降低校验。
- 16:59:08 第二次实际编译失败，预算 2/5。仍有必需建筑缺失、道路两端 transition 与自身目标建筑碰撞、入口网络延伸失败、ORGANIC 缝隙图未连通。低推理/输入完整修复并未解决求解器质量。
- 17:00 暂停本轮，保留第二版正式 Blueprint 与失败报告；进入已授权求解层检查，不继续盲耗剩余 3 次编译预算。

## NBT / 入口契约核对：等待用户确认契约调整

- 开发游戏已正常保存退出，Provider enabled=false；PCL 原档/配置不动。
- 酒馆 accepted template hash=b0422e0bb5794e70a57c84a8fd4438c1766c6de01866d93a6432b213edb28bcd，rawSize=23×16×33；catalog 门标记 (12,18)/EAST 与实际门 (12,1,18)、blockstate facing=east 一致，但 EAST 射线在 x18 撞 barrel，x19 为实体外墙；WEST 从门经砾石地面到模板边界，双格净空完整。
- 15 个 Stubbs 模板只读 NBT 射线初筛：windmill_01、town_hall_01、tavern_01、household_01、household_02 当前方向堵塞而反向直通室外；不是所有模板反向，不能统一翻转。farm_storage_house/motel/workshop 等需处理台阶、内部通道或拐弯；horse_stall/guard_outpost 标记处不是普通门，不能套门朝向规则。
- 求解层 CityMainRoadPlanner.roadConnector 将门位偏移两格后延伸至建筑外，但 transitionClear 排除了 originObstacle，允许整段穿过来源建筑；质量门禁实际正确报错。不能通过放松 ROAD_OVERLAPS_STRUCTURE 掩盖数据/接路契约不匹配。
- 依据 bug-layer-debugger，在改变正式入口契约前请求用户确认：区分模板内部门位与外部接路出口，程序依据 NBT 几何验证可达通路，作者审核歧义；功能/风格仍只认作者标签。当前未改任何入口标记、未放宽验收、未清零 2/5 失败预算。首城仍未通过最终编译/世界生成。
