# 入口修正后首城复验

*需求入口*：用户要求在入口/旧素材池修正后验证一次实际城市落地流程。

*修改内容*：本轮只做开发存档 RTF_beta_validation_02 实机验收；复用有效 D3，不重扫 W/T，不清零已有编译失败 2/5；新准备上下文必须纳入已修正入口。不修改 PCL 原存档。

*验证结果*：本轮正式单次编译已完成，未通过 D4 终审，未进入 D5/D6/worldgen。新素材快照与 GLM 旧设计正式提交校验通过。编译 MCP 调用耗时 13178ms，响应展示层报 PLANNING_PRESENTATION_TOO_LARGE（4281231 chars > 90000），未重复执行；服务端 19:23:32 写出的 quality_report.passed=false，有 23 项 hardBlocks：3 必需建筑缺失、19 入口网络未接通、1 农贸区 ORGANIC 缝隙图不连通，ROAD_OVERLAPS_STRUCTURE=0。已读取本次正式报告、预算及整城预览，与新 contextId 对齐。

*剩余问题*：首城仍未落地。程序端必需建筑摆放/入口网络/区域连通问题与 MCP 大响应问题待诊断；本轮只要求验证一次，因此不修改求解器、不放宽验收、不继续消耗次数。正式新 Context 预算 1/5；跨 Context 实际累计失败 3 次（此前 2 次 + 本轮 1 次），未伪造/改写预算文件。未新调用 GLM，不能用 13 秒编译耗时宣传模型端速度。

## 正式调用记录

- `tools/list`：已构建 Node MCP server 返回 prepare/submit/compile/workflow/status schema。
- `realm_status {}`：serviceReady=true，debugRoot 明确为开发端 RTF_beta_validation_02。
- `city_post_d4_auto_compile_status {runId: provider_ce6a3c351fa2667b_r8192, citySeedId: city_realm_sunmarch_caravan}`：旧状态 needs_agent，attempt=2、failureCount=2/5；上次 D4 终审失败，返回正式产物与预览路径。
- `city_prepare_d4_blueprint_context`，同 runId/citySeedId：ok=true，新 contextId=`sha256:5d70407ac270a5da3499bb9380899c5591aebc7cfb7b4ca29fa6f3a59fef646c`，素材快照 hash=`sha256:f68317ccf193872fb23842f270791c96fee87218d3dc723def87a41f7e8c91bb`。复用 D3，无 W/T 重扫。
- 发现正式 prepare 因 contextId 改变自动创建 0/5 新预算（createdAt 2026-09-05T11:21:20.801013900Z）；不是测试者手工清零。保留旧状态返回的 2/5 证据，本轮只允许一次实际编译，累计口径继续计入此前两次失败。暂不进入源码或改预算文件。
- 为控制变量，拟复用原 GLM accepted Blueprint 的设计内容，仅将 catalogSnapshotRef 绑定到正式 prepare 新快照，通过 submit 正式校验；本轮不评价新的 GLM 推理耗时。
- 核验正式 snapshot 的酒馆接路点已为 `(0,18) WEST`；D3 Ref 与旧蓝图严格一致。旧蓝图文件 SHA-256=`6eb2bf5af97e47e5e632ad855d4e7fcd4555bb585af88be484150ae22e1405e7`。
- `city_submit_d4_blueprint`：同 run/city，新 contextId；cityBlueprint 完整复用旧设计、只替换 catalogSnapshotRef；`autoAdvanceAfterD4=false`，避免后台重复编译。返回 ok=true、valid=true、issues=[]、status=accepted、blueprintHash=`sha256:5d4e6f0781525b4b21f1ca215fcedee9afa949fc5224225955a2701c468b3f7e`，下一步 compile。
- `city_compile_d4_blueprint`：同 run/city，正式单次编译进行中；MCP timeout=600000ms，不自动重发。
- 最后返回：`Error: PLANNING_PRESENTATION_TOO_LARGE: decision envelope chars=4281231 exceeds 90000`，elapsedMs=13178；调用实际已执行，故不重发。通过先前正式响应返回的 artifact 路径读取本轮 quality、budget、test_run_manifest，得到明确功能失败，Agent Loop 结束。
- 产物位置：`run/saves/RTF_beta_validation_02/realm_debug/provider_ce6a3c351fa2667b_r8192/city_test_runs/city_realm_sunmarch_caravan/steps/d4/quality_report.json`、同目录 `structure_anchor_preview.png`、`city_generation_compile_trace.json`；预算位于 `steps/blueprint/city_blueprint_failure_budget.json`。Manifest 当前 `awaiting_city_blueprint_revision`，nextAction=city_submit_d4_blueprint。
- 具体缺失：general_store_01、farm_storage_house_01、horse_stall_02 各一；已修的 household_01/windmill_01/tavern_01 等仍报入口网络延伸不可用，说明门坐标修正不足以完成连通。这里只记录现象，不据错误码预断求解器根因。
- 环境收尾：`realm_debug_command stop`（confirmCommandExecution=true, allowUnsafeCommand=true, sourceMode=server）在单人模式返回 DEBUG_COMMAND_NO_SUCCESS，日志明确 unknown command；未视作已退出。随后 `help geomantia` + saveAfter=true 返回 executed/worldSaveRequested=true，19:26:50 日志证实 All dimensions are saved。保存后结束本轮启动的 Gradle 客户端会话。
