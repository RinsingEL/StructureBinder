# 大陆分区开放与城市保护

实现提交：`0ee0c62`；策划与契约提交：`5e9e494`。

*需求入口*：[大陆大洋分区开放与城市郊区隔离-v0.1](E:/Mod_Dev/designer_territoryMod/docs/systems/realm_planning/10_product/案子/大陆大洋分区开放与城市郊区隔离-v0.1/README.md)。用户要求开始开发，并确认近海归最近大陆、远海分成多个连续区域，分界距离可配置，地理区域不强制跟随政治边界。

*修改内容*：新增确定性大陆/附属近海/多个连续远海分区；T4 冻结实际 D3 预览设计范围和宽高 1.5 倍安全保护矩形，跨国度/会话/卫星城统一检查，finalize 再验合并名册；正式选址只读检查加载/磁盘区块，拒绝已加载地形。D3 验证冻结边界，D4 验证最终建筑完整占地和道路路幅。T4 名册封存使用 territory 文件 identity，结合城市队列和已激活结构/LandUse 方案决定整片大陆开放，远海分别依赖最近大陆。地图显示完整开放区域，移动/传送另留方形视距和生成依赖缓冲；ChunkMap 在 EMPTY 读取及后续调度前返回不可加载，开放后原版正常重试。T4 变更在服务器线程串行执行并失效开放缓存；登录/每 tick 开始检查位置，旧安全位置失效时回初始区。MCP 工具描述、数据/接口契约和测试入口同步更新。

*验证结果*：

- 当前工作树已快进到主实现仓的 `d0c3f57`，包含上一轮生成 mask 隔离。保留原有 D4 报错指引和道路高度修改；从主实现仓补齐其对应的 ChunkExecutor 阶梯测试。Hermes LFS 运行包由主仓本地副本补齐，SHA-256 与已跟踪指针及运行时常量一致，未修改包内容。
- 全量 JUnit 最近一次：923 项，912 通过、10 跳过、1 项失败。失败为既有 `CityLandscapeCapacityReservationPlannerTest.reusesInvariantCandidatesDuringMultiLandscapeJointSearch` 耗时断言，8 秒门槛超出 468 ms；没有放宽门槛或修改该搜索实现。
- 最终区域规划、D4 编译及上述耗时用例聚焦复验：125/125 通过；同次 `build` 成功并完成重混淆。日志 `build/region-targeted-build.log`。新增跨国度重叠测试确认失败不消耗选择凭证。
- MCP `npm test`：11/11 通过；bundle 构建成功。日志 `build/region-mcp-tests.log`。
- 独立 Forge 47.4.0 GameTest：1/1 通过。受限远处 FULL 请求立即返回不可加载、磁盘无区块；发布区域许可后，同一位置正常重试至 FULL，服务器正常保存退出。目录 `build/region-gametest-full`，日志 `build/region-gametest-full.log`。此前 EMPTY 调度同场景也通过。测试关闭 StructurePacks/RTF/VisualPreview 开发依赖；首次默认依赖启动被 Library Ferret 专用服务端错误阻断，未计作测试通过。隔离服务端 API 端口被运行中游戏占用不影响本次直接调度测试，未关闭游戏或占用它的 API。
- `git diff --check` 通过；输出 `build/libs/geomantia-0.1.0.jar`。本轮实现与配套文档已提交，未替换正在运行的游戏 Mod。

*剩余问题*：尚未进行用户整合包 Forge 47.4.23 中的新存档、完整城市规划和玩家移动验收。既有第三城的已生成区块不删除、不回滚、不补建；旧城不因这次升级自动恢复为未生成。全量测试存在上述既有耗时波动，聚焦复验通过；不宣称全量无失败。
