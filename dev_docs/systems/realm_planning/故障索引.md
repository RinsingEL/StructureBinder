# 国度规划系统故障索引

| 现象 | 根因 | 修复点 | 验证 | 记录 |
| --- | --- | --- | --- | --- |
| `cellStepBlocks=128` 的 W 预览 / 抽样中，山前、海岸或缓坡丘陵被大量标成 `cliff`。 | 低 step GIS 的 coarse `landform=cliff` 在高 step W 消费层被直接加入 `landformTags[]`，即使 micro `slopeP90/steepFrac` 不支持；128 block 邻格高差被当成局部悬崖语义。 | 高 step 且有 micro 指标时，正式 `cliff/steep` tag 改由局部 `slopeStats` 确认；coarse cliff 只输出 `cliff_candidate` / `micro_contradiction` 诊断 tag。新增 Tag Audit 抽样局部精扫报告。 | `./gradlew.bat test`、`npm run build`；`RealmPlanningServiceTest.worldSurveyRunnerSealsMultipleTilesAndCanResumeFromCache` 覆盖 micro cliff tag 判定和 `tag_audit_report.json`。 | 2026-06-13 v1.5 高步长局部指标封装。 |
| ReTerraForged 存档 `新的世界` 快速启动后停在加载 / 确认阶段，HTTP `127.0.0.1:5000` 不启动。 | 开发快速启动只调用 vanilla `WorldOpenFlows.loadLevel`；带 RTF 数据包的实验性世界会进入确认路径，且中文存档名通过 Gradle 普通 property 时可能被 ISO-8859-1 读取成乱码。 | `DevAutoLoadClient` 优先反射调用 Forge 带 `confirmExperimentalWarning` 的 `doLoadLevel` 重载；`gradle.properties` 固化 `geomantiaDevAutoLoadWorldBase64=5paw55qE5LiW55WM`，避免中文属性编码影响。 | `./gradlew.bat test --tests com.rinsing.geomantia.systems.realm_planning.RealmPlanningServiceTest`；`./gradlew.bat runClient -PgeomantiaDevUseReTerraForged=true` 进入 `新的世界`，`/realm/status` ready，4096 半径 W + Tag Audit 返回 `ok=true`。 | 2026-06-14 v1.6 ReTerraForged 快速启动验收。 |
| ReTerraForged 人工抽样中，小村庄旁的雪地小土坡被 W 和 Tag Audit 同时标成 `plateau`。 | 旧 `plateau` 规则只要求局部平坦和相对高度偏高；Tag Audit reference 又用同一 cell 内的高度分位自证“平顶”，缺少“连续面域”和“明显高出周边”的外圈参照。 | `plateau` 改为 local flat + regional high + 512b core flat support + 1536b outer-ring prominence；Tag Audit 的 `cellReferenceBaseLandform` 对 plateau 复用同一面域证据，不再用局部 p95 自证。 | `./gradlew.bat test --tests com.rinsing.geomantia.systems.realm_planning.RealmPlanningServiceTest.plateauRequiresBroadProminentFlatTop`；`./gradlew.bat test --tests com.rinsing.geomantia.systems.realm_planning.RealmPlanningServiceTest`。 | 2026-06-14 v1.6 plateau prominence 收紧。 |
