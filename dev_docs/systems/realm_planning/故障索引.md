# 国度规划系统故障索引

## 2026-09-06 国度数量与城市密度配置未接入正式调度

*现象*：用户检查 validation_03 时只看到一个国度/城市，询问最小国度数配置。

*首次偏离*：需求案《世界自动规划调度-v0.1》要求国度数量、城市密度允许配置，正式 Provider 构造却固定目标3国，仅通过提示要求数量。

*根因*：RealmProfileInput 只要求1–12份档案及提交的 realmCount 与数组长度一致，未绑定宿主数量；T4 finalize 只要求恰好一座可追溯首都，没有配置的城市数量/密度目标。

*修复*：未修改实现或存档，本轮仅检查。不能把该缺口当成本档只有一个国度的原因。

*验证*：validation_03/provider_e88e62d43d1875ba_r8192 的 t1_manifest 与 t2_report 均为3国，分别 Frostholt、Qadir、Veldmark；t4_report 仅登记 Qadir 首都。队列于19:45:09Z为 blocked_by_program/POST_D4_WORKFLOW_UNEXPECTED_STATUS，调度优先处理未完成城市，尚未进入其余两国T4。新阻塞根因未展开调查。

*记录*：2026-09-06 用户只读核查；未提交。

## 2026-09-06 多入口 frontage 缺口被误判为 AI 布局失败

*现象*：GLM 两次接受的港口蓝图在 civic fountain required 阶段立即失败；AI 被要求重写完整设计，failureCount 增至2。

*首次偏离*：OrientationSolver 返回 FRONTAGE_ENTRANCE_AMBIGUOUS，编译器根级压为 REQUIRED_STRUCTURE_NO_LEGAL_PLACEMENT，失败归责只查根级码。

*根因*：模板明确标有四向入口，但没有固定主入口/无正面语义；求解器要求显式 frontage 或唯一/front 入口。该能力/配置问题不由修改蓝图地块解决。

*修复*：失败路由检查真实 hardBlocks，保留 required 内容，进入 blocked_by_program，不再新增设计失败计数；旧队列迁移不减写历史预算。尚未擅自添加主入口或放宽朝向规则。

*验证*：嵌套 hardBlocks 分类回归和完整789项 JVM通过；实机重启确认 blocked_by_program、active=false、原蓝图哈希和历史计数2不变。未声称修复多入口语义或新城落地。

*记录*：[任务记录](archive/ada3aab_v0.1.0_GLM自动首城链路收敛/任务记录.md)，实现 ada3aab，契约 4efbedc。

## 2026-09-06 Hermes 将设计拒绝误报 MCP 不可达

*现象*：GLM 三次不同字段修正后，第四次提交未到达游戏，Hermes 报 MCP unreachable after 3 consecutive failures；下一模型轮重新灌入 Context，输入从34023升至71463 token。

*首次偏离*：ProviderToolBridge 对结构化 validationReport 的负结论也设置 isError=true；Hermes MCP 适配器将其转为 error 文本并触发服务级熔断。

*根因*：业务校验拒绝与传输失败混用错误通道。HermesAgentClient 续跑已有设计会话时忽略 message_count，无条件重复整份上下文。另查出 Node 原始 MCP schema 与 Provider 的复合选址 schema 分离，Hermes 未消费宿主决策工具定义。

*修复*：校验负结论作为成功执行校验的结构化结果返回，原 ok=false/报告完整保留，PlanningTurnControl 继续负责三次同错停止；认证桥提供当前工具定义，Node Provider 模式读取同一 schema；已有非空设计会话仅补续跑指令，不重复未变 Context/图像。

*验证*：桥接/重复拒绝停机/工具定义/续跑消息回归、MCP10项及完整789项JVM通过；实机同会话仅传续跑消息，33.5s提交成功，未再被原校验拒绝计数熔断。原上下文中已累积的历史没有删除。

*记录*：[任务记录](archive/ada3aab_v0.1.0_GLM自动首城链路收敛/任务记录.md)，实现 ada3aab，契约 4efbedc。

## 2026-09-06 AI 机械调用与程序失败误归责

*现象*：编译搜索预算耗尽、锚点终审失败被计入蓝图五次预算并要求 AI 完整重提；T2/T4 创建和候选引用转交、D3 扫描仍占模型调用。

*首次偏离*：编译 endpoint 对所有失败统一 recordFailure；Provider 只区分阶段白名单，没有将准备动作与决策提交拆开。

*根因*：求解器能力/资源边界被误当成设计可行性结论；机械字段和会话生命周期透传给模型。

*修复*：明确程序搜索/安全上限及终审故障保留预算，队列持久化 blocked_by_program；宿主准备候选、D3 和选址证据，代办引用提交。增加上下文字段绑定、显式权重模式和带哈希局部替换。

*验证*：最终完整JVM789项（0失败、2可选跳过）、MCP10项通过；实机 D3/候选准备无模型调用，GLM两份方案正式接受并自动编译。尚被多入口素材语义阻断，未宣称城市速度/落地验收通过。

*记录*：[任务记录](archive/ada3aab_v0.1.0_GLM自动首城链路收敛/任务记录.md)，实现 ada3aab，契约 4efbedc。

## 2026-09-05 入口修正复验：编译结果展示超限

*现象*：正式 city_compile_d4_blueprint 执行约 13.178s 后 MCP 返回 PLANNING_PRESENTATION_TOO_LARGE，decision envelope 为 4,281,231 字符，超过 90,000；服务端已完成本次编译并写出质量报告，不能据 MCP 错误重试整个编译。

*首次偏离*：编译执行后的模型响应展示阶段；本轮 D4 质量报告自身也未通过，是独立结果。

*根因*：编译接口重复内联已保存的完整 anchor geometry、Group extent 和编译搜索轨迹，PlanningToolPresentation 仅裁剪 terrain dense 字段，仍超过 envelope 上限。

*修复*：后续“落地底线与质量分层”任务已将有正式 artifact 的编译几何替换为摘要与完整文件引用，质量报告与作者选择不截断；D5/D6 同类结果也纳入，长度上限不变。

*验证*：通过既有正式返回的 artifact 路径读取 19:23:32 新报告与同 context 预算：passed=false、23 hardBlocks、当前 context failureCount=1；本轮未重复编译。

*记录*：[入口修正后首城复验](archive/50d9c9c_v0.1.0_入口修正后首城复验/任务记录.md)；[后续修复与验证](../city/archive/50d9c9c_v0.1.0_落地底线与质量分层/任务记录.md)，新 D4 两次实机正常返回，D5/D6 展示扩展待实机复验。

*后续未收敛项*：D7 有同类大型内联结果。加入 landUseOwnerCompletion 摘要后真实 envelope 仍有 391207 字符；该调用实际已入队，必须读正式结果，不盲目重执行。尚未定位剩余大字段，用户暂停后未继续修改，留待 GLM/Hermes 链路任务处理。

| 现象 | 根因 | 修复点 | 验证 | 记录 |
| --- | --- | --- | --- | --- |
| GLM 已提交蓝图，但 D4 道路端部穿入酒馆/风车，入口接路失败。 | NBT 门位不等于外部道路出口；至少 5 个 Stubbs 入口接路方向与室外通路相反。求解器还对来源建筑豁免整段 transition 避障。 | 待确认入口契约调整；不能放松碰撞验收或统一反转所有入口。 | 酒馆 NBT 东侧撞木桶/墙，西侧砾石通道直通外界；15 模板只读初筛。实机失败预算 2/5，已暂停。 | `archive/8fe6216_v0.1.0_GLM首城闭环验收/任务记录.md`；未提交。 |
| D4 失败后只得到“编译失败”，GLM 不知道具体冲突。 | 宿主重新唤醒仅复制 failureBudget，未包含 quality report、失败布局、上一版 Blueprint。 | 宿主在当前世界边界内验证 accepted hash/context 后装配局部修订证据；失败布局优先直传。 | 捕获实档回归保留全部 hardBlocks，待实机修订验证。 | `archive/8fe6216_v0.1.0_GLM首城闭环验收/任务记录.md`；未提交。 |
| GLM 在 D4 连续重复 prepare，初始消息/工具结果被截断；三张图串行转述。 | 正式 Context 2.65MB；工具展示 709529 chars 被 Hermes 截至 1963 chars，初始消息另有 65536 限制；运行时未识别 GLM 原生视觉。 | 完整紧凑设计视图、宿主前置准备、有限输入适配并超限拒绝、精确模型视觉声明；作者语义不删。 | 实机消息 38 完整 78391 字符，三图直传；16:41:39 正式接受 GLM 蓝图，后续编译未通过。 | `archive/8fe6216_v0.1.0_GLM首城闭环验收/任务记录.md`；未提交。 |
| 单大陆只有一国时，目标 .35/max .5 被归一化为 1，实机三国各占满约 99% 大陆。 | T3 首次偏离为 clamp 后再次除以总和；quota 强行分配整块大陆；action_budget 又没有最大面积停止条件。 | 目标总和不足 1 时保留绝对份额；超额时有界归一化；quota 不分配未申请土地，action_budget 加最大面积门禁。 | 实机正式报告确认，自动回归/实机复验进行中。 | `archive/8fe6216_v0.1.0_GLM首城闭环验收/任务记录.md`；未提交。 |
| 2026-09-05 新档 T2 全部完成后，T3 报 `No assignable land cells ... primary_realms`。 | T1 接受任意逻辑分组名，T3 却将其直接当大陆 ID；且默认只处理首个分组，不能覆盖多大陆。 | 分离逻辑标签与 targetContinentId，默认计算全部目标大陆，同大陆国度共同竞争。 | 双大陆两种模型回归通过；原实档调用 2.294 秒完成三国，发现独立面积上限问题继续修复。 | `archive/8fe6216_v0.1.0_GLM首城闭环验收/任务记录.md`；未提交。 |
| 2026-09-05 退出世界后 Java CPU 约 75%，数分钟后自行恢复。 | API executor 中断未取消 W parallel stream，旧世界微采样继续执行；达到末尾失败后 CPU 降至 0.7%。 | 世界关闭和 owner interrupt 传到粗采样及 common-pool 快速微采样；取消不封存，保留快速 elevation/feature 委托。 | 世界关闭后微采样停止、owner interrupt 两项回归通过；尚待真实慢扫描退出测试。 | `archive/8fe6216_v0.1.0_GLM首城闭环验收/任务记录.md`；未提交。 |
| 2026-09-05 首城长时间停在 D4 prepare，模型猜模板来源；开发 MCP 空 T1 产生固定国家。 | 首次偏离在 Agent 工具边界：Hermes 忽略宿主执行器，MCP 要求模型提供宿主目录且只回文本；T1 schema 不完整并允许固定草稿；调度等待所有 T4。 | 统一宿主资料/图片、严格正式 T1、逐国逐城顺序和两种循环的宿主终止；作者功能/风格前置校验；Hermes 断流同时中断后台 agent。 | build 与 737 JVM、10 MCP、3 Python 回归通过；实际 MCP 桥接及便携 bootstrap smoke 通过，尚未部署或实机首城验收。 | `archive/8fe6216_v0.1.0_AI场景策划与程序执行分工/任务记录.md`；未提交。 |
| Hermes Gateway 已连接，但 Agent 持续“进度推进失败”；日志把 `geomantia` / `mcp-geomantia` 报为未知 toolset。 | Windows 便携包已有 pywin32，但外挂 `site-packages` 不处理 `pywin32.pth`，`pywintypes` 导入失败；Hermes 将异常吞成 `_MCP_AVAILABLE=false`，因此 MCP 根本未注册，toolset 警告只是下游症状。 | 启动器的 `PYTHONPATH` 显式加入 `site-packages/win32`、`win32/lib` 和 `pywin32_system32`；真实运行时回归必须验证 `pywintypes`、MCP stdio 和 `_MCP_AVAILABLE`。 | 专项 JVM 测试通过；隔离 Gateway + 假 Provider + 假 Minecraft API 端到端 smoke 成功看到工具 schema、执行 `mcp__geomantia__realm_w_refresh`、收到 `/realm/w/refresh` 并完成会话。修复包已部署，SHA-256 `18BBC3C5EE98AD13F10BF64CC747ECC31AD65A161030F35A4A4C0C11A87C5F67`。 | `dev_docs/systems/realm_planning/active/20260904_HermesSidecar接入/任务记录.md`。 |
| Hermes 明明已由开发端安装，游戏却连续报 `CreateProcess error=2`；手工复制安装目录又卡死在 `cpython-*-none`。 | Hermes 被安装在 Codex AppContainer 的重定向 LocalAppData 中，游戏进程看不到同一逻辑路径；该安装还是指向容器源码的 editable 环境，并含 uv Python Junction，不具备可移植性。 | 改为 Mod 内置 Windows x64 便携运行时：CPython 3.12.10 + Hermes 0.18.2 非 editable 核心依赖 + Node.js 22.23.2；首次启用解压到游戏 `config/geomantia/runtime`，profile 也存于游戏目录，完全移除 AppData、系统 Python/Node 和 PATH 依赖。 | 运行时无 Junction；临时目录解压、哈希复用、内置 Python 在无系统 Python/Node PATH 条件下真实启动通过；99.5 MiB Mod 已部署，待游戏完整 Gateway/Agent 链路实验。 | `dev_docs/systems/realm_planning/active/20260904_HermesSidecar接入/任务记录.md`。 |
| GLM 在一次城市设计中运行三十多轮，反复质疑工具结果、重取 D4 Context，甚至声称缺少上下文；相同工作不像原 MCP Agent 那样稳定推进。 | 自研 Java tool loop 每次宿主唤醒都从一条新的 Provider 请求链开始，没有持久会话恢复、上下文压缩和成熟 harness；同时向城市阶段开放 D4 后台状态查询，模型可把宿主等待误当成自己应轮询的任务。 | 默认运行时改为 Hermes Agent 0.18.2 sidecar；按存档/run/城市生成稳定 session，使用真正恢复 SQLite 历史的 session chat stream API；按阶段生成 MCP 工具白名单并移除 D4 后台状态轮询，Mod 保留确定性调度与失败预算。旧 Java loop 仅保留为可切换兼容项。 | JVM 全量 716 项与 MCP 7 项通过；Hermes Gateway 真实启动、health/detailed 通过，API Server connected，打包 MCP 成功且只注册白名单中的 `realm_w_refresh`；新包已部署，待实机跑完整城市链。 | `dev_docs/systems/realm_planning/active/20260904_HermesSidecar接入/任务记录.md`。 |
| 自动规划运行约一小时仍没有首城；每约 189 秒重做一次 D4 Context，既不提交 Blueprint，也不在三次请求失败后停机。 | 完整 D4 Context 原样回灌 Provider，545 个 D3 Patch 的逐格几何使第二轮消息膨胀并触发 120 秒超时；Context 准备又刷新 queue 时间戳，而该时间戳和文件时间被错误纳入 Agent 语义身份，导致失败计数每轮清零。 | 正式 Context 不变，Provider 内部只交付已复核地形类型的 Patch 摘要并省略逐格/邻接/重复 facts，真实消息减少 86.7%；语义身份排除操作性时间戳；活动面板显示请求轮次、消息量、超时与耗时。 | Provider 聚焦及全量 713 项通过，已部署，待真实续跑。 | `dev_docs/systems/realm_planning/active/20260904_Provider超时无限重试/任务记录.md`。 |
| 换用新存档后，冒险者地图仍显示上一存档的 W 地图、国度和“国度已完成”数量。 | Realm 服务、HTTP、地图读取、开放区策略和 Provider discovery 都从游戏实例级 `serverDirectory/realm_debug` 读取，所有单机存档共享同一规划目录；当新 service 尚无 active run 时地图还会 fallback 到该目录最新旧 run。客户端地图同时缓存上一连接的静态 snapshot。 | 已统一改为当前 `LevelResource.ROOT/realm_debug`，Provider 显式接收存档根并清空跨存档活动，地图每次打开从空快照刷新。旧共享目录不删除、不猜测迁移。 | 现场路径证据闭环；`WorldScopedPlanningPathsTest`、地图/Provider 专项与全量 706 项通过；新包已安装且哈希一致，待重启后双存档实机复验。 | `dev_docs/systems/realm_planning/active/20260904_存档规划状态隔离/任务记录.md`。 |
| 冒险者地图的 W 底图把一小块地形在整个地图面板中重复平铺，玩家标记与眼前地貌看起来错位；缩放后也不是围绕玩家查看。 | 客户端把粗地图纹理放大时误用了 `GuiGraphics.blit(resource, x, y, u, v, width, height, textureWidth, textureHeight)` 重载；该重载同时把目标宽高当作源区域宽高，目标尺寸大于 66/96 像素纹理后 UV 超过 1 并重复采样。玩家 X/Z 与地图边界的数值换算本身一致，但背景 UV 已错位；另外现实现只有整图固定降采样和世界中心缩放，没有玩家视口与分级 LOD。 | 改用可分别指定目标尺寸和源区域尺寸的 `blit` 重载；地图请求携带缩放档位，服务端以量化后的玩家位置截取 W feature grid，远景聚合、近景恢复 W cell 粒度，只下发视口内节点。 | Forge 1.20.1 `GuiGraphics` 源码确认修复后的 UV 为完整纹理 0—1；新增玩家视口、LOD 粒度和节点裁剪回归，地图定向测试、完整 JVM 测试、MCP build 与 `reobfJar` 通过，待新包实机视觉复验。 | `dev_docs/workflows/Beta自动规划基础链/active/20260903_冒险者地图揭示与LandUse容量容差/任务记录.md`。 |
| 冒险者地图把 W 扫描矩形整片公开，未开放区域没有黑色蒙版，看起来像铺满 tiled 栅格。 | W 粗扫预览本身正常；地图快照只压缩 `world_feature_grid`，没有携带实际活动区/旅行走廊的开放判定，错误地把“已扫描”当成“已揭示”。 | 每个粗地图像素复用 `PlanningAreaAccessPolicy` 生成 reveal mask；客户端将未开放像素绘制为黑色，普通图层隐藏蒙版外城市，Debug 只允许显示节点而不揭开地形。 | `AdventurerMapStatusReaderTest` 定向通过；真实 W `world_biome_preview.png` 已确认海陆和群系轮廓正常，待新包实机视觉复验。 | `dev_docs/workflows/Beta自动规划基础链/active/20260903_冒险者地图揭示与LandUse容量容差/任务记录.md`。 |
| Provider 请求失败后不显示重试计数并可无限重试；D4 已产生有效 Blueprint 却反复返回“必需建筑无合法位置”。 | runner 在 `LoopResult.failure` 分支清空连续未推进计数；D4 真实失败证据只写入 compile trace，workflow 摘要丢弃 group/structure/filter/hard block，Agent 又被禁止读 raw artifact。实机样本的真实原因是 `civic_core` 喷泉四入口未冻结唯一 `frontageEntranceId`。 | 请求失败与成功但无产物统一按 semantic identity 计入 3 次边界，过程显示 `1/3`至 `3/3`并停止。workflow 从完整 trace 提取紧凑 `failureSummary`，直接回传失败 group、structure、filter reasons、hard blocks 和建议动作，不复制原始巨大 JSON。 | `PlayerProviderAgentRunnerTest`、`CityWorkflowStepRunnerTest`、`DeepSeekToolLoopClientTest`、`ProviderPlanningToolGatewayTest` 聚焦测试通过。 | `dev_docs/systems/realm_planning/active/20260903_冒险家地图Provider交互故障/任务记录.md`。 |
| Provider 到 D4 后反复调用 `city_submit_d4_blueprint`，依次补 `designIntent.cityIdentity/theme/functionalRoles` 等字段，长时间没有进入编译。 | Provider tool schema 只定义了 Blueprint 根字段，`designIntent`、group、outdoor 等嵌套对象均为空 object；正式 codec 又使用 closed-object 精确字段契约，Agent 只能读取一次一个的校验错误盲修 JSON。 | `city_submit_d4_blueprint` 工具 schema 对齐正式 codec，完整公开嵌套必填字段、可选字段和 enum；catalog ref 仍由 D4 context 冻结。 | 按用户要求不跑测试；Java 编译及 `reobfJar` 打包通过，待重启实机续跑既有队列。 | `dev_docs/systems/realm_planning/active/20260903_冒险家地图Provider交互故障/任务记录.md`。 |
| Provider 自动规划进入 T4 后连续创建会话，首都提交报 `PROVIDER_AGENT_TOOL_FAILED`，随后又报 `PROVIDER_AGENT_REQUEST_FAILED`；所有 T4 会话均停在 `awaiting_selection`。 | Provider 网关按已废弃的根级 `realm_t4_patch_planning_<id>.json` 校验会话，正式服务实际落盘为同名目录内的 `planning_session.json`，所以 T4 create 后的任何 select/finalize 都先被误判为会话不存在。Agent 同时把展示候选 ID `VALLEY-01` 当成了冻结凭证。 | 网关会话路径对齐正式 T4 artifact；select capital/add city 只接受 `psel_...`，错误候选 ID 返回 `PATCH_SELECTION_REF_REQUIRED`；工具描述明确要求先执行 `patch_explorer_select_candidate`。 | 真实 run 的三个会话目录证明正式 artifact 存在且均未产生 selection ref；新增网关目录解析和候选 ID 拒绝回归，Provider 两个聚焦测试类通过。 | `dev_docs/systems/realm_planning/active/20260903_冒险家地图Provider交互故障/任务记录.md`。 |
| 冒险家地图显示 W 未开始；Provider 页操作被轮询恢复；修复后自动化完成 W/T1，却在 T2 已冻结 Patch、未提交正式坐标时永久显示排队中。 | UI 每 40 tick 无条件重写表单且反馈被控件遮挡。自动化侧又把“模型停止工具调用”直接视为阶段成功并记录 `lastCompletedIdentity`，未验证 discovery / 正式 artifact 是否推进；相同 T2 identity 从此不再执行。 | 已修复：表单区分服务器刷新、本地 dirty 与 save-pending，反馈移到可见区。runner 在 Agent 返回后重读 discovery，只有非 actionable 或 semantic identity 变化才承认推进；相同状态 30 秒后重试，连续 3 次无推进后明确报错并停止，显式 retry 才解锁。 | 实机 W 与 T1 成功；T2 产生 `PLAIN-01` selection 但无 `realm_coordinate_selections.json`，01:16:35 后无写入，复现假排队。推进验收修复后 Provider 定向测试与客户端编译通过，待重打包实机续跑。 | `dev_docs/systems/realm_planning/active/20260903_冒险家地图Provider交互故障/任务记录.md`。 |
| Provider 自动 W 只扫描半径 1024，而 T2 首个国度核心门禁要求距原点至少 3072；Agent 反复翻页和冻结候选仍全部被拒。 | Provider 的 W tool schema 暴露 `planningRadiusBlocks`，宿主只校验 512—262144；模型自行选择 1024。正式 W 范围与 `planning_area_access` 没有前置一致性校验，且 discovery 会继续消费这个 sealed 小范围 run。 | 正式 W 范围改为只读 `config/geomantia/world_survey.json`，默认 8192、中心固定原点；请求和命令不能覆盖。半径小于首城最小距离时扫描前失败；discovery 按世界种子、原点和配置半径匹配 run，自动 runId 含半径后缀，旧不匹配 run 不再消费。 | 实机 1024 run 的全部 Patch 锚点距原点约 177—1414，均不可能通过 3072 门禁；定向回归更新为配置身份 fixture，待新包实机生成 8192 run。 | `dev_docs/systems/realm_planning/active/20260903_冒险家地图Provider交互故障/任务记录.md`。 |
| 已安装 RTF 且请求 generator-native fast path，T2 仍记录 `generator_context_unavailable` 并回退 Minecraft prior，首次同范围重采样耗时 268.905 秒；D3 也未接 provider selector。 | 当前反射桥只读取 `RTFRandomState.generatorContext()`，为 null 时直接判不可用，没有调用 RTF 公开的 `initialize(RegistryAccess)`；D3 还硬编码 `MinecraftPriorAtlasSampler`。 | 已修复：桥接层用当前世界 registry 主动初始化后重试；T2/D3 统一 provider selector 和整批回退语义；D3 固定 step 16。 | 聚焦、全量与 MCP 通过；`RTF08052` 冷 T2 1.681 秒、D3 1.865 秒，均为 `rtf_heightmap_preview_inferred_climate / generator_native`，D3 `fastPath=true`、step 16，群系 PNG 和正式 JSON 均存在。 | `dev_docs/systems/realm_planning/archive/821dabb_v0.1.0_RTF群系预览与快速采样/任务记录.md`。提交 `821dabb`。 |
| 同一次 T1 的三个国度共享完全相同的大陆候选范围，但分别调用 T2 `patch_explorer_open` 用时 295.43、267.58、260.46 秒，并各写约 108.5MB scope snapshot。 | 三份候选包均为 `profileDifferentiated=false`、相同 939 个 `allowedPatches`；`PatchExplorerService.loadRealmScope` 仍按 `scopeId` 分别调用 `TerrainScalePatchService.analyze`。后者为每个 source cell 扩一圈分析 region，并采样这些 region 的全部 32 格 cell；没有按“相同范围 + provider identity”共享 T 尺度结果，scope snapshot 又为每个国度重复展开全部 cells/candidates。 | 已修复并实机验证：以规范化搜索 cell、实际 provider / dimension / fingerprint / sampling semantics 和算法版本形成共享 refinement cache identity；相同范围只重采样一次，各 session 只冻结共享 artifact 路径和 SHA-256，恢复时校验。 | 原三次合计 823.47 秒；`RTF0810` 实机复验为冷启动 268.905 秒、两次热命中 1.563/1.311 秒，合计下降 67.0%。三国只保留一份 37.98 MiB refinement，session snapshot 为 962/961/961 bytes，show candidates 0.910 秒成功。冷启动实际因 RTF `generator_context_unavailable` 回退 Minecraft prior，首次耗时另行优化。 | `dev_docs/systems/realm_planning/archive/23bd12e_v0.1.0_T2重复地形重采样/任务记录.md`。提交 `23bd12e`。 |
| T2/T4 `patch_explorer_open` 在真实 32 格地形重采样超过 10 秒时，服务端最终已写完会话和预览图，但 MCP 先返回 `timeout of 10000ms exceeded`。 | MCP handler 把包含真实地形重采样的 `open` 错用为 quick 10 秒超时；同一工具的后续 show/select 已使用 refresh 600 秒。 | `patch_explorer_open` 改用 refresh 超时，允许正式重采样与总览渲染完成后返回；不改变服务端执行和缓存语义。 | 真实 T4 首次调用在 10 秒处复现，服务端随后写出 8.2MB scope snapshot 与两张 PNG；修复后待重启 MCP 客户端复验。 | `dev_docs/systems/realm_planning/active/20260815_T与D分尺度Patch重算/任务记录.md`。 |
| T2/T4 候选预览中的高亮 Patch 呈 W 阶段 128 格规则方块或方块并集，细图甚至可在“ridge”候选中显示大面积水体。 | Patch Explorer 只按 sealed W `patchId + landform` 生成候选；后续精细高程预览只重新采样底图，没有在 T 尺度重新分类和生成 Patch，因此高亮边界与语义仍是 W 粗尺度结果。 | 代码与自动回归已修复，待真实预览：W 只提供 T 搜索范围；T2/T4 open 使用当前地形 provider 在不大于 32 格的 T 尺度重新采样、分类并跨 region 合并 Patch，面积、边界、高亮与选择组件统一使用 T 结果。 | JVM 669 项与 MCP 7/7 通过；细尺度水体不再继承 W `ridge` 的回归通过。 | `dev_docs/systems/realm_planning/active/20260815_T与D分尺度Patch重算/任务记录.md`。 |
| 半径 32768 W 上配置 12 国后，T1/T2 数秒完成，但 T3 `quota_frontier` 运行 9 分钟仍无 checkpoint，Java private memory 约 11.9 GiB，客户端主线程冻结。 | `rebalanceAreaQuotas` 每移动一个 cell 都重新统计 ownership，并执行 `国度数 × 全大陆 cell` 扫描；连通性 BFS 进一步放大成本。旧 receiver 允许达到 quota 后继续接收至 120%，还会让边界 cell 往返振荡直至两次撞上 `landCellCount * 4` 上限。 | quota repair 改为跨国边界确定性优先队列、增量 realm count 和当前 cell/四邻局部更新；receiver 必须低于 quota，使每次移动严格减少总面积误差；保留 seed 与连通性门禁。 | 12 国/约 4096 cell JVM 两次限时确定性回归通过；真实 RTF run 的 10871 cell/12 国 T3 从 9 分钟无结果降至 8.034 秒完成，quota move 从 86968 降至 299，12 国全部单连通；T4 36 城兼容名册和 1.53 秒 RTF Patch Explorer 通过。 | `dev_docs/systems/realm_planning/active/20260805_T3多国重平衡性能/任务记录.md`。 |
| `cellStepBlocks=128` 的 W 预览 / 抽样中，山前、海岸或缓坡丘陵被大量标成 `cliff`。 | 低 step GIS 的 coarse `landform=cliff` 在高 step W 消费层被直接加入 `landformTags[]`，即使 micro `slopeP90/steepFrac` 不支持；128 block 邻格高差被当成局部悬崖语义。 | 高 step 且有 micro 指标时，正式 `cliff/steep` tag 改由局部 `slopeStats` 确认；coarse cliff 只输出 `cliff_candidate` / `micro_contradiction` 诊断 tag。新增 Tag Audit 抽样局部精扫报告。 | `./gradlew.bat test`、`npm run build`；`RealmPlanningServiceTest.worldSurveyRunnerSealsMultipleTilesAndCanResumeFromCache` 覆盖 micro cliff tag 判定和 `tag_audit_report.json`。 | 2026-06-13 v1.5 高步长局部指标封装。 |
| ReTerraForged 人工抽样中，小村庄旁的雪地小土坡被 W 和 Tag Audit 同时标成 `plateau`。 | 旧 `plateau` 规则只要求局部平坦和相对高度偏高；Tag Audit reference 又用同一 cell 内的高度分位自证“平顶”，缺少“连续面域”和“明显高出周边”的外圈参照。 | `plateau` 改为 local flat + regional high + 512b core flat support + 1536b outer-ring prominence；Tag Audit 的 `cellReferenceBaseLandform` 对 plateau 复用同一面域证据，不再用局部 p95 自证。 | `./gradlew.bat test --tests com.rinsing.geomantia.systems.realm_planning.RealmPlanningServiceTest.plateauRequiresBroadProminentFlatTop`；`./gradlew.bat test --tests com.rinsing.geomantia.systems.realm_planning.RealmPlanningServiceTest`。 | 2026-06-14 v1.6 plateau prominence 收紧。 |
| HTTP 同时访问 Realm status / T1-T4 时可能竞争，切换世界后新服务还可能看到旧 run；多次启停服务器还会遗留 `Geomantia-API` 线程。 | `RealmPlanningService.RUNS` 是跨实例静态 `LinkedHashMap`，部分 HTTP handler 直接在三线程 executor 上修改；HTTP executor 未保存和关闭。 | run map 改为 service 实例状态；platform `RealmPlanningServices` 按 `MinecraftServer` 复用并在关服释放，HTTP / Forge 共用实例，所有可变 W/T HTTP 操作回到 server thread；HTTP server 在正常停止和启动失败时关闭 executor。 | `RealmPlanningServiceTest.runStateIsScopedToServiceInstance`、完整 JVM 377 项和 MCP TypeScript build 通过。 | `dev_docs/workflows/City与Realm框架清理/active/20260715_LandUse前置框架清理/任务记录.md`。 |
| T4 Patch planning `finalize` 后，`city_seed_registry.json` 已按 realm 更新，但 `t4_report.json`、`city_seed_preview.png` 等派生产物仍展示旧自动 T4 名册。 | `RealmT4PatchPlanningService.finalizePlanning` 只写回正式注册表和 planning session，没有复用 T4 产物生成逻辑，也没有重建或显式失效旧报告、预览、acceptance / score 结果。D3 直接读取注册表，因此运行不阻断，但人工与自动验收会看到互相冲突的证据。 | 已修复：finalize 通过共享 `RealmPlanningService.synchronizeT4RegistryArtifacts` 原样保留 Patch 来源并重建 T4 report、城市预览、单国候选包和 score；旧 acceptance 写为 stale / passed=false，只有全链重跑才能重新通过。同步成功后才将 planning session 标为 finalized。 | `RealmT4PatchPlanningServiceTest` 与 `RealmPlanningServiceTest.synchronizeT4RegistryRebuildsDerivedArtifactsAndInvalidatesAcceptance` 覆盖合并、来源保留、派生计数和验收失效；完整回归见任务记录。 | `dev_docs/workflows/patch_explorer/active/20260723_Patch探索式选址/任务记录.md`。 |
| 在当前存档对另一世界种子的 run 调用 City D3 时，接口仍返回 `ok=true` 并生成局部产物；T4 的林地候选落到当前世界后可能变成整片深海。 | D3 的实时世界扫描只按 `runId + citySeedId` 读取磁盘注册表和坐标，没有比较 run `world_survey_context.worldSeed` 与当前 `ServerLevel` seed。跨存档旧 run 因此能绕过来源身份校验，直到局部地形结果才暴露冲突。 | 已修复：D3、structure profiling、terrain probe、D6、execute D5/D7、城墙规划/执行和 workflow 在读取或修改实时世界前统一调用 run world identity guard，严格比较 `world_survey_context.worldSeed/dimensionId` 与当前 `ServerLevel`；缺失、损坏和不一致分别返回稳定错误码。离线 D2/D4/D5/LandUse 规划不受影响。 | `CityPlanningEndpointHandlerTest.runWorldIdentity*` 覆盖匹配 seed + dimension 放行及 seed / dimension 不一致拒绝；完整 JVM 511 项、MCP `npm test` 通过。实机在世界种子 `1269623911921362524` 重放旧 run `-281932406572342714`，211 ms 返回 `CITY_RUN_WORLD_IDENTITY_MISMATCH`；旧 D3 目录 101 个文件，前后整体 SHA-256 同为 `DC4D0DE886253BFBD6CD382669FC65DA894A2D604801A1863E74749D257E247A`，最后写入时间未变。 | `dev_docs/workflows/patch_explorer/active/20260723_Patch探索式选址/任务记录.md`。 |
| 客户端重启后，磁盘 sealed W 仍完整，但直接调用 T1 返回 `Unknown realm runId`；`realm_status` 同时显示 `knownRuns=[]`。 | `RealmPlanningService` 的 run map 正确改成实例状态后，普通 T1-T4 的 `requireRun` 仍只查内存；已有 `WorldSurveyRunner.loadSealedResult` 只接给 Tag Audit，阶段 checkpoint 也没有反序列化恢复路径。 | 已修复：普通 T / Tag Audit 统一按明确 `runId` 懒恢复 sealed W，并顺序只读恢复完整 T1/T2/T3/T4 checkpoint；`status` 保持不主动扫盘。恢复时校验候选包、选择、国度种子、首都种子与 sealed W / 上游 checkpoint 的身份链；损坏或来源不一致的完整 checkpoint 返回 `REALM_CHECKPOINT_INVALID`。恢复不刷新 W、不加载 chunk、不重写上游 artifact。 | 黑盒实机两次 T1 分别 210 ms / 224 ms 稳定复现 400 且未写产物；新增跨实例回归通过，完整 JVM 512 项和 MCP 2 项测试通过。修复后重启客户端，`knownRuns=[]` 状态下原样重放三个文明请求，3.807 秒返回 T1 completed、生成 3 个候选包，五个 W 核心文件的哈希、长度和修改时间均未变化。 | `dev_docs/workflows/patch_explorer/active/20260723_Patch探索式选址/任务记录.md`。 |
| T1 成功后，三个同大陆国度得到完全相同的旧候选图，响应仍只提示 `realm_t2_select_coordinate`；无上下文 AI 可能绕过新 Patch Explorer。 | 旧 `RealmCandidateMapPackage` 本来只按目标大陆全部可分配 patch 染色，不体现国度差异；接入 Patch Explorer 时没有同步收口 T1 `nextActions`、图包角色字段和 MCP 描述。 | 已修复：旧图包明确标记为 `continent_scope_reference`、`profileDifferentiated=false` 和 `patch_explorer_primary`；T1 只推荐 `patch_explorer_open`，直接坐标单列 compatibility；MCP 与正式契约同步说明主链。 | `RealmPlanningServiceTest.t1RoutesSemanticSiteSelectionThroughPatchExplorer` 通过；完整 JVM 70 个套件、513 项与 MCP 2 项测试通过。待客户端重启后做无上下文 T1 -> Patch Explorer 实机复验。 | `dev_docs/workflows/patch_explorer/active/20260723_Patch探索式选址/任务记录.md`。 |
| 单国 T1 成功且候选包含数百个 `allowedPatches[]`，但按 `realmId` 调用 `patch_explorer_open(scopeType=realm_t2)` 返回 `PATCH_EXPLORER_SCOPE_EMPTY`。 | `RealmCandidateMapPackage` 正式契约用 `allowedPatches[]` 表达候选范围，`targetContinentId` 只存在于 `RealmProfile`；Patch Explorer 实现和测试却依赖候选包中的契约外 `targetContinentId`，缺失后误把 `realmId` 当 `continentId` 过滤全部 W cells。 | realm_t2 scope 改为直接按候选包 `allowedPatches[]` 裁剪；兼容 continent scope 从 `realm_profiles.json` 读取大陆映射。fixture 移除契约外字段，并让真实 T1 输出直接进入 Patch Explorer open。 | 真实 `new_save_w_20260722_01` 复现 494 patches / 4134 cells 被误判为空；修复后定向回归及完整 JVM 70 个套件、520 项测试通过。 | `dev_docs/workflows/patch_explorer/active/20260723_Patch探索式选址/任务记录.md`。 |
| T4 Patch finalize 正确保留其他国度旧城市后，`score_manifest` 报 `city seeds outside owned territory=1`；本轮两个新城市实际均在 owned territory。 | `CitySeedRegistry` 是按 realm 合并的全局名册，但 T4 评分把所有保留种子都与本轮 `RealmTerritoryMap` 比较；不在本轮 `RealmProfile` 中的旧城市必然没有本轮 ownership，因而被误判。 | T4 的重复、间距、首都和领土质量校验统一限定到本轮 `RealmProfile`；完整名册和其他国度城市仍原样保留。 | `RealmPlanningServiceTest.synchronizeT4RegistryRebuildsDerivedArtifactsAndInvalidatesAcceptance` 加入外部旧城回归，要求 `offTerritoryAnchorCount=0`、无 hard block 且 score passed。 | `dev_docs/workflows/patch_explorer/active/20260723_Patch探索式选址/任务记录.md`。 |


## 2026-09-08：T4 允许前哨站规模，D3 无法识别

*现象*：新存档首个国家已登记 4 城，队首前哨站卡在 city_plan_d3，无 D3/D4 产物。

*首次偏离*：T4 合法登记 theoreticalScale=outpost，D3 规模解析不支持该值。

*根因*：Java/MCP 工具枚举包含 outpost，CityScale.fromContractName 却不包含，CitySiteContextBuilder 因而抛出 Unknown scale label: outpost。

*修复*：CityScale 将 outpost 兼容映射为 HAMLET，保留种子角色、选址与原始规模，旧存档无需改写。

*验证*：2 个回归修复前失败、修复后通过，覆盖 T4 工具全部规模进入 D3 及真实前哨站参数；全量 test build 通过。原存档游戏内复验待换包后进行。

*记录*：[任务记录](archive/7e3d356_v0.1.0_前哨站规模跨阶段不兼容/任务记录.md)，未提交。


## 2026-09-09 城市预留与提前加载

*现象*：第三城 D4 通过，但首次生成预检发现 247 个 owner chunks 中 172 个已到 FEATURES；城市地图开放范围也不能覆盖完整游玩区域。

*首次偏离*：T4 只按规划半径/中心距离选址，未使用 D3 clamp 后的实际预览及外围保护；开放层仍按单城圆圈和走廊放行，没有区块调度门禁。

*根因*：城市设计范围与加载保护范围没有统一，玩家视距和生成依赖可提前请求邻近待建区块。具体是哪次玩家/票据请求加载了旧第三城区块，历史记录不足以确定。

*修复*：原预览及 1.5 倍保护矩形、跨城检查和选址时只读区块检查；大陆完整名册与全部生成方案共同控制开放；真实 ChunkMap 调度拒绝未开放区块，开放后正常重试。

*验证*：区域与选址单元测试；独立 Forge 47.4.0 GameTest 的 FULL 拒绝、无落盘、开放后 FULL 重试通过。完整整合包玩家验收尚未进行。

*记录*：[任务记录](active/20260909_大陆分区开放与城市保护/任务记录.md)，待提交。
