# GLM 自动首城链路收敛

*需求入口*：用户要求改到底，AI 只做设计决策，W 与 D4 后编译/激活/生成由程序自动执行；作者预标记功能风格，不回退随机城市；用 GLM+Hermes 的真实速度与落地验收，而非 Codex 直连推进代替。

*修改内容*：本轮按用户确认聚焦 AI/宿主边界：明确搜索/程序安全上限和锚点终审故障不消耗设计预算，队列保留 blocked_by_program；T2/T4 候选准备和引用提交由宿主代办，CITY D3 扫描、候选首屏程序推进，D3 复核保留 AI 明确决策；宿主补齐省略身份字段、显式相对权重归一化和 baseBlueprintHash + replace-only JSON Pointer 局部修订。所有完整蓝图仍走原作者白名单、引用、关系和安全校验。

*验证结果*：基线 50d9c9c/c8063ec。最终完整 JVM 789 tests、0 failures/errors、2 optional skips，MCP 10/10，双仓 diff --check 通过。真实 GLM 首稿 API 61.6s；修复 Hermes 适配后的原会话续稿33.5s通过提交，随后模型修订92.1s也通过提交。程序编译仍被喷泉 frontage 配置/能力缺口阻断，不宣称新城已落地或达到整体速度目标。最终实机验证该缺口为 blocked_by_program、active=false；Blueprint SHA-256 保持4db26a33083c4fcd3e6ff72b77523fddee0255f7f21b7e2d492fb81efc26cfec，历史 failureCount=2 未清零、未继续增加。开发游戏已正式保存后关闭，Provider disabled，PCL 测试端未覆盖。

*剩余问题*：多入口素材的主入口/无固定正面语义需明确，当前只正确阻塞、不擅自猜门或替换 required 内容；还未验收到一座新 GLM 城市完整落地。旧入口到台地缺失与 D7 问题保留为独立落地风险。所有本轮修改未提交。

## 2026-09-06 01:30 实机回合 1（已结束）

- 启动开发档 validation_02，Provider 保持 disabled，复用原 run/provider_ce6a3c351fa2667b_r8192；不修改蓝图或历史预算。
- MCP city_design_queue_status(runId) 与 city_post_d4_auto_compile_status(runId, city_realm_sunmarch_caravan) 确认旧终审失败正确迁移为 blocked_by_program + city_post_d4_auto_compile_retry；没有模型调用和自动重编译。
- 读取返回质量报告时误用 MCP 子目录为相对 cwd，文件读取失败，属于调用者路径错误；该错误没有改变状态。
- 有效 MCP city_post_d4_auto_compile_retry(同 run/city) 返回 CITY_DESIGN_QUEUE_CURRENT_NOT_ACCEPTING_D4: status=blocked_by_program；接口尚不能消费自己返回的合法恢复动作，无参数修正方向。结束游玩，进入本轮新增状态门禁修复。

## 2026-09-06 01:34 实机回合 2（已结束）

- 独立程序续跑门禁修复，定向测试通过；MCP help geomantia + saveAfter=true 确认保存世界后重启开发客户端。
- 正确绝对路径读取现有 D4 quality_report：passed=true，hardBlocks=[]，qualityFullySatisfied=false。用相同 run/city 重放 city_post_d4_auto_compile_retry，返回 queued attempt=3。
- 两次状态 MCP 确认约 405ms 后 waiting_for_generation、queue completedCount=1，下一城 city_realm_sunmarch_port / city_plan_d3；此次只复用既有程序产物，不计 GLM 成绩。
- 正式响应同时 workflowStatus=waiting_for_generation 与 ok=false，存在成功标志矛盾。结果已可判断，结束回合，修复成功枚举后再进行真实 GLM 下一城验证。

## 2026-09-06 01:38–01:44 GLM 实机回合 3（功能失败，已结束）

- 成功枚举与严格 ready 判定已修复、完整 JVM 回归通过。启用既有 glm-5.3-flash + Hermes 配置；不代写蓝图、不手工执行新城 D3/D4。
- 港口城 D3 约 01:38:59 完成，01:40:06 Context 冻结；宿主自动候选准备和图像注入。GLM 会话 geomantia-design-903929f7-5587-3ff9-8833-0995b5afb5e9，history=0，三张真实图。
- 第一轮 API 61.6s、in=34023/out=2885，直接调用 city_submit_d4_blueprint；拒绝 $.proportionMode 放错层级。后续 API 38.1s、36.1s 修正其他字段，第三次拒绝 groups[4].preferredPatchRefs 缺失。均为正式输入校验、failureCount=0。
- 第四次 API 37.9s 后 Hermes 未转交修正请求，报 MCP server geomantia unreachable after 3 consecutive failures；随后花 11.2s 生成解释。宿主下一轮再次注入全 Context，history=10，输入上涨到71463 token；这一行为不是模型设计必须的步骤。
- 有效设计工具调用被框架熔断阻止，判为功能失败并结束回合。需定位应用级拒绝到 MCP 传输错误的首次偏离；不会把这轮称为城市速度验收通过。请求/响应可追溯 Hermes 当前会话、logs/agent.log 与港口城 steps/blueprint/last_rejection_report/trace；尚未 accepted、未进入城市编译。

## 2026-09-06 01:53 GLM 实机回合 4（功能失败，已结束）

- 桥接负校验、宿主 schema 同源、续跑短消息均通过聚焦 JVM 与真实 Node stdio 回归；开启原档、原模型与原会话。
- 日志确认 history=16，续跑只传 Continue the same frozen city context，不追加完整 Context/图片。01:53:50 正式接受 GLM 自主提交，产生 city_blueprint.json（12977 bytes）；没有 Codex 代写。
- 自动 D4 编译 383ms 返回 REQUIRED_STRUCTURE_NO_LEGAL_PLACEMENT，failureSummary 明确指向 g_civic_core / plains_fountain_01，候选数0，INTERNAL_FRONTAGE_UNAVAILABLE / D4_ARRAY_LAYOUT_FRONTAGE_ENTRANCE_AMBIGUOUS requires frontageEntranceId。
- 此类作者配置/编译能力缺口仍被根级 NO_LEGAL_PLACEMENT 分类为 design，错误增加预算到1并唤醒新修订会话。结束回合，需按真实 trace 中的原因正确阻塞配置问题，保留历史计数，不通过替换喷泉伪造成功。
- 结束并保存游戏前，已启动的 GLM 修订轮 92.1s 再次自主提交成功，仍保留同一个喷泉，第二次编译返回同一 frontage 原因，计数到2；这部分如实计入本轮，不将它算成修复成功。
- 分层核对确认：冻结模板为9×4×9，四个入口 north/east/south/west 均有明确位置与门向；OrientationSolver 在无显式 frontageEntranceId、无精确名称 front 且入口数>1 时拒绝。未确认作者是否声明其无固定正面，未改入口坐标或猜主入口。
- 修复失败路由读取真实 hardBlocks，将此明确配置/能力缺口作为程序阻塞；恢复旧队列时也按既有 failureSummary 归类，保留历史 failureCount=2。错误摘要继续保留 group/ref/filter 原因。

## 2026-09-06 02:04–02:06 实机回合 5（阻塞分类验收通过，已结束）

- Provider disabled，按新代码重启原档；MCP city_design_queue_status(runId) 与 city_post_d4_auto_compile_status(runId, city_realm_sunmarch_port) 均确认 blocked_by_program，nextAction=city_post_d4_auto_compile_retry，active=false，failureOwner=program。
- 失败摘要明确保留 g_civic_core/plains_fountain_01、INTERNAL_FRONTAGE_UNAVAILABLE 和 FRONTAGE_ENTRANCE_AMBIGUOUS，建议修复配置而非更换建筑。未重编译、未发起 GLM 请求；原 accepted 蓝图哈希与历史预算2均未变化。
- 本轮附带运行日志读取再次误用相对子目录 cwd，未读取到文件；正式 MCP 状态调用与后续绝对路径保存验证均成功，未影响产品状态。
- MCP realm_debug_command(help geomantia, confirmCommandExecution=true, sourceMode=server, saveAfter=true) 返回 executed，日志确认 All dimensions are saved 后关闭本轮自启客户端。
