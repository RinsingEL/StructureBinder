# 前哨站规模跨阶段不兼容

*需求入口*：用户“我在新存档跑完了，报错了你看看吧”。

*修改内容*：用户已要求修复。将 outpost 映射到现有最小规模 HAMLET，保留城市角色与旧存档种子；增加工具规模枚举进入 D3 的回归，同步契约。未修改存档或 Provider 配置。

*验证结果*：新存档“新的世界”，runId=provider_7dcd2ed26663924e_r8192。19:36:39 首个国家 T4 finalize 成功，4 座城市入队；队首 city_sahra_valdoran_library_qasr_ilm 的 theoreticalScale=outpost，停在 city_plan_d3，未产生 D3/D4。Java 和 MCP 的 realm_t4_patch_planning_add_city schema 明确允许 outpost，T4 服务接受并持久化；D3 CitySiteContextBuilder.buildWithFixedGridStep 调用 CityScale.fromContractName，后者无 outpost 映射，必然抛出 Unknown scale label: outpost。运行中 JAR 同一枚举亦无 outpost 字面量。Hermes 的 19:36:45 连接关闭伴随阶段结束，不能作为本次根因。宿主活动仅驻留内存，尚未取得界面原始错误文字。

*剩余问题*：实现已修复；尚未替换游戏实例 JAR 或在原存档进行游戏内复验。其余两个国家尚未进入 T4。

*修复验证*：新增 2 个回归测试在修复前均失败，修复后通过。`gradlew test build` 成功；全量 910 项通过，10 项跳过，0 失败。新包 build/libs/geomantia-0.1.0.jar。
