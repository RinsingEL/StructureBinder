# 连通开放区域与玩家越界保护

*需求入口*：`E:\Mod_Dev\designer_territoryMod\docs\systems\realm_planning\10_product\案子\初始活动区与规划区域开放-v0.1\README.md`。

*修改内容*：把孤立城市圆式 TP 白名单升级为单一活动规划 run 下的连通开放区域。策略从初始区或既有开放城市为已就绪城市规划有界旅行走廊，按城市半径、走廊宽度和服务端视距扩张未就绪城市硬保留区；无法合理绕行的远城保持设计完成但不可进入。平台层缓存策略并统一处理 PlayerTick、载具、指令 TP、`spreadplayers`、末影珍珠、紫颂果和跨维度到达，越界时停止移动、返回同维度最后安全点、显示警告并施加短时黑暗反馈。

*验证结果*：`./gradlew.bat compileJava` 通过；`./gradlew.bat test --tests "com.rinsing.geomantia.systems.realm_planning.application.access.PlanningAreaAccessPolicyTest"` 通过，覆盖直连走廊、绕开未就绪城市、无路锁住远城和只认最近活动 run；`./gradlew.bat test` 全量 JVM 回归通过。

*剩余问题*：尚未在真实客户端中验证步行、鞘翅、载具、末影珍珠、跨维度返回时的体感、最后安全点和黑暗反馈；旅行走廊当前是访问拓扑，不负责生成实体道路。
