# Patch 地貌候选与高程高亮预览

*需求入口*：`designer_territoryMod/docs/systems/realm_planning/10_product/案子/T4城市候选分级精扫选址-v0.1/README.md`；最新替代方案为 `designer_territoryMod/docs/systems/realm_planning/10_product/案子/Patch候选高程高亮预览-v0.1/README.md`。

*修改内容*：T2/T4 候选从按主导群系连续区生成改为直接按 sealed W `patchId + landform` 生成，按地貌类型返回面积 Top N；City D4 继续使用 D3 地貌 Patch。旧三图已替换为随高度变色、叠加轻量阴影并直接高亮当前 Patch 的一张主图；候选比较按 Patch 范围加上下文自适应取景，选中后仍以固定城市尺度确认。外部字段改为单数 `terrainPreview`，不再返回 contour / hillshade / patch 独立图片。

*验证结果*：`PatchCandidateTerrainPreviewServiceTest` 与 `PatchExplorerServiceTest` 聚焦测试通过；全量 JVM 测试通过；MCP 7/7 通过。2026-08-15 真实 RTF 客户端通过 MCP 生成 9 个 shore/slope/cliff 候选、2 个单格 ridge 候选和 SHORE-01 城市尺度确认图；验证单格候选使用 8 格/512 格取景，较大候选使用 16 格/1024 格取景，图片非空且候选高亮直接叠在高程地形上。

*剩余问题*：本次只替换候选观察图，不改变 W Patch 分类真值；真实结果中两个 ridge 候选本身位于水体，是否属于 W 地貌分类问题需另案判断。
