# 冒险者地图基础 GUI

*需求入口*：`docs/systems/realm_planning/10_product/案子/冒险者地图-v0.1/README.md`，用户确认先使用基础 GUI 控件开发。

*修改内容*：新增默认 `M` 键打开的冒险者地图 Screen，提供基础地图面板、实际配置的初始活动区、城市节点、缩放、刷新和普通/Debug 图层切换；新增服务端只读快照网络同步及 W/T/City artifact 解析；补中英文界面文本、代码导览与测试入口。

*验证结果*：`AdventurerMapStatusReaderTest` 聚焦测试通过；`./gradlew.bat test` 全量 JVM 测试通过，客户端源码与语言资源完成编译/处理。状态读取测试覆盖空存档、W 实际 phase 进度、T4/City 当前节点与不改写队列文件。

*真实游玩验收*：已在 `RTF_beta_validation_01` 中打开基础界面，地图能够显示 W/T 阶段状态；随后通过正式接口完成半径 4096 的 W、三国 T1/T2 与单次统一 T3，地图刷新后的目标状态为 T3 已完成、T4 待处理。详见 `真实游玩调用记录.md`。

*剩余问题*：美术底图和节点资源后续再接入。
