# 真实游玩调用记录

## 测试环境

- 日期：2026-09-01（Asia/Shanghai）
- 存档：`RTF_beta_validation_01`
- 玩家：`Dev`
- 规则：真实游玩 Agent Loop；只使用正式 HTTP 契约、运行产物、运行日志和游戏画面判断。
- 目标：从冒险者地图显示的 `T1 排队中` 推进到统一 T3 完成，明确不进入 T4。

## 调用记录

| 序号 | 工具/入口 | 关键参数 | 返回摘要 | 修正理由 | 产物 |
| --- | --- | --- | --- | --- | --- |
| 1 | `realm_status` | `{}` | 服务与 GIS ready；启动时无内存 run | 无 | HTTP 返回 |
| 2 | `realm_t1_prepare` | run=`rtf_beta_validation_probe_20260901` | T1 完成，生成 3 个国度 | 沿用地图识别到的最高阶段运行 | `t1_manifest.json` |
| 3 | Patch Explorer + `realm_t2_select_coordinate` | 盐国 `PLAIN-01` | 候选冻结成功；T2 被 `T2_REALM_CORE_INSIDE_INITIAL_ACTIVITY_AREA` 拒绝，最小距离 3072 | 正式硬门禁证明该 W 范围不可继续 | Patch Explorer session/selection |
| 4 | W 上下文核对 | probe radius=512、范围 `[-512,511]` | 整个扫描范围都位于 3072 格禁入线内 | 排除“换候选即可继续”的错误路径 | `world_survey_context.json` |
| 5 | `realm_w_refresh` | run=`rtf_realm_t3_20260901`；中心 0,0；radius=4096；step=128；RTF native；`use_cache` | W 完成；4096 cells、8 continents、256/256 tiles、failed=0、sealed=true；RTF fast path | 当前世界种子没有覆盖范围足够大的既有 W，补做最低可行正式调查 | `run/realm_debug/rtf_realm_t3_20260901/` |
| 6 | `realm_t1_prepare` | 新 run | T1 完成；盐国、石境、绿庭统一绑定 `continent_0` | 无 | `realm_profiles.json`、`t1_manifest.json` |
| 7 | Patch Explorer | 盐国 shore/plain/terrace；石境 ridge/cliff/slope；绿庭 valley/plain | 复用同一 T-scale refinement，筛除距中心不足 3072 格的候选 | 按国度风格与硬距离共同选点 | 三个 Patch Explorer session |
| 8 | `realm_t2_select_coordinate` | 盐国 `SHORE-01`；石境 `CLIFF-04`；绿庭 `VALLEY-03` | 三国 T2 全部完成；种子分别为 `(-2688,-2176)`、`(-3584,896)`、`(-1152,-3712)` | 三处互不重合并分散于主大陆 | `realm_seeds.json`、`t2_report.json` |
| 9 | `realm_t3_expand` | group=`continent_0`；`strict`；`action_budget`；`allowUnclaimedLand=false` | 单次调用完成三国统一扩张；`ok=true`、无 warnings/errors | 保证不是逐国先来后到 | `realm_territory_map.json`、`territory_preview.png`、`t3_report.json` |
| 10 | `realm_status` + 产物核对 | 最新 run | 最新 run 为 `rtf_realm_t3_20260901`；T3 产物齐全；T4 registry 不存在 | 确认恰好停在 T3 结束 | HTTP 返回与文件存在性 |

## 最终结果

- T3 正式完成，下一动作是 T4；本轮没有调用 T4。
- 三国领土均为单一连通分量：`largestComponentRatio=1.0`、`detachedAreaRatio=0.0`、`holeAreaRatio=0.0`。
- 领土占比：盐国 48.10%（505 cells）、石境 27.52%（289 cells）、绿庭 19.62%（206 cells）；均位于各自允许的 16.67%–53.33% 范围。
- 地图刷新后的预期状态：T3 已完成、T4 待处理。
- `rtf_beta_validation_probe_20260901` 只能证明 RTF 快速采样可用，不是可进入 T2 的正式 W；其 512 格半径小于国度核心 3072 格最小距离。
