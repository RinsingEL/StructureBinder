# 存档规划状态隔离

*需求入口*：用户换用新存档后，冒险者地图仍显示上一存档的地图和“国度已完成”数量。

*修改内容*：新增 `WorldScopedPlanningPaths`，正式 Realm/GIS 服务、HTTP Controller、冒险者地图、规划区域开放策略与 Forge 命令统一把世界派生产物解析到当前 `LevelResource.ROOT/realm_debug|gis_debug`。Provider 自动规划继续从实例目录读取共享配置和内容目录，但 discovery 与 tool gateway 显式使用当前存档的 `realm_debug`；每次启动自动化清空上一存档的活动消息。客户端冒险者地图删除跨连接 `lastSnapshot`，每次打开先显示空快照并向当前服务器刷新。实例级旧 `realm_debug` 未删除、未自动迁移，避免猜测它属于哪个历史存档。

*验证结果*：现场当前存档为 `RTF_beta_validation_02`，其 world root 没有 `realm_debug`；运行中 `/realm/status` 却返回实例级 `D:\PCL2-2.8.12\.minecraft\versions\1.20.1-Forge_47.4.23\realm_debug`，且该目录同时存在 `provider_4c88d8fb9d1cfd78_r8192` 与 `provider_91deac17ecb49a24_r8192`，证明跨存档共享。新增 `WorldScopedPlanningPathsTest` 证明两个 world root 不会得到同一路径；地图、Provider 与全量 `gradlew test jar --no-daemon` 通过，共 706 项、0 failure/error。构建包 SHA-256 为 `321F5A4B378196E29776590DE5DA9E9F6CA221DBB74A25BFA9DC2799A573B485`。

*部署结果*：玩家正常退出游戏后确认 Forge 客户端进程已结束，将构建包安装到当前 `1.20.1-Forge_47.4.23/mods/geomantia-0.1.0.jar`，安装后 SHA-256 与构建产物一致，均为 `321F5A4B378196E29776590DE5DA9E9F6CA221DBB74A25BFA9DC2799A573B485`。原包备份为 `geomantia-0.1.0.pre-20260904-world-scope.jar.bak`。

*剩余问题*：待重启 `RTF_beta_validation_02` 实机确认 `/realm/status.debugRoot` 指向该存档目录，冒险者地图不再出现旧底图与旧国度完成数。历史共享产物仍完整保留，需要恢复某个旧存档时再显式迁移，不能自动猜测归属。
