# T2 重复地形重采样

*需求入口*：用户询问本次 Patch 阶段耗时很久是否异常。

*修改内容*：未修改实现。核对 MCP 完整调用时长、T1 候选包、Patch Explorer scope snapshot 与 T 尺度分析实现。

*验证结果*：真实 run `realm_w_mte38ltl_f1964c5a` 的 W refresh 为 393.61 秒；三个国度的 T2 `patch_explorer_open` 分别为 295.43、267.58、260.46 秒。三份 T1 候选包均声明 `profileDifferentiated=false`，并拥有完全相同的 939 个 `allowedPatches`，但服务仍按国度各自重新执行 32 格地形采样和分类，并各写约 108.5MB snapshot。Top Patch show 仅 0.80..0.92 秒，D4 city open/show 仅 0.119/0.087 秒。

*剩余问题*：实现相同候选范围的 T 尺度 refinement 共享缓存，并让各国 session 引用共享快照，避免重复采样、重复分类和三份百 MB JSON 展开。
