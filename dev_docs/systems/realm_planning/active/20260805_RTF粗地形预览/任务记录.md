# RTF 粗地形预览任务记录

## 目标

为 W 与 T4 Patch Explorer 增加可独立开关的生成器原生粗地形预览。RTF 使用其二维 Heightmap 快速路径；原版、其他生成器和显式关闭场景回退现有 Minecraft prior sampler。T4 粗览只负责城市候选范围与建议粗锚点，D3 仍是最终地形真值。

## 当前设计

- 通用 `TerrainPreviewProvider` SPI 隔离具体世界生成器。
- W/T4 请求分别使用默认开启的 `preferGeneratorNativeTerrain`；关闭时记录 `generator_native_disabled`，互不改变对方配置。
- W `prior` 通过 `TerrainPreviewAtlasSampler` 复用 provider；非 `prior` 强制使用 Minecraft sampler。provider 完整身份进入 manifest/context/cache hash，Tag Audit 保留 Minecraft sampler 独立复核。
- W 通过 `GisRefreshService.ArtifactMode.NONE` 复用 GIS 计算内核，不生成逐 tile progress/preview PNG 和调试 manifest；普通 GIS refresh 默认 `FULL`，调试能力不变。
- `realm_t4` 打开探索会话时 lazy ensure 粗地形证据；每个 owned W cell 中心只采样一次。
- 邻格高度差计算 slope proxy 与 local relief，不追加四方向高度查询。
- 输出 `realm_t4_coarse_terrain_evidence.v0.1` JSON 和高度/水体 PNG。
- RTF provider 反射兼容 0.0.5 / 0.0.6，读取当前 `RandomState` 的 `GeneratorContext`，使用 `Heightmap.apply` 与 `Levels.scale`；不依赖 runtime tile cache，不生成 Chunk。
- provider 初始化不可用时整体回退；采样执行中失败显式报错，不逐点混用来源。
- 产物固定声明 `advisoryOnly=true`、`requiredNextGate=city_d3_site_review`。

## 已知边界

- 当前提供的 RTF 源码版本为 0.0.6，开发运行 jar 为 0.0.5，运行时桥必须以实际 jar 签名验证。
- 半径 32768 的大世界仍会产生大量 Region tile JSON；原生采样消除了主要噪声计算成本，但文件数量、聚合和 PNG 渲染仍需真实验收。
- 首轮半径 32768 实测曾产生 216333 个 tile 文件，其中 149769 张逐 tile PNG；run 最终约 517 MiB，扫描 422021ms。用户确认后已删除正式 run 与 512 半径探针 run；删除不可恢复，共用时 93 秒。
- 粗览高度是生成器二维 estimated surface，不包含 D3 最终地表、结构、植被和完整 surface rule 结果。
- City 固定 NBT 结构已在 RTF 世界现场确认真实落地；地基、削坡和地形融合不属于本任务。

## 验证记录

- 通用 provider selector：5 个聚焦 JVM 测试通过。
- T4 owned-only 粗览服务：2 个聚焦 JVM 测试通过。
- RTF 反射桥：5 个聚焦 JVM 测试通过，覆盖 0.0.5 / 0.0.6、线程独立 Cell、点级失败和 factory 回退。
- Patch Explorer 集成：4 个聚焦 JVM 测试通过，覆盖 typed evidence、建议锚点、PNG 引用和 source stale。
- 全量 `gradlew test`：82 个 suite、553 个测试，0 failure / error / skipped。
- `country_designer_mcp npm run build`：通过。
- 使用 `javap` 核对实际 `reterraforged-0.0.5.jar`：`RTFRandomState.generatorContext`、公开 `GeneratorContext.generator/levels/preset`、三参数 `Heightmap.apply`、`Cell.reset/height/terrain/biome`、`Levels.scale/water`、`Terrain.getName` 与桥接契约一致。
- Forge GameTest：关闭外部运行时后启动成功并执行 5 项，4 项通过；既有 `CityLandUseGameTests.landUseWorldgenCrossChunkCropAndFence` 因跨 chunk 栅栏高度连接断言失败，与本任务无关。
- 带 RTF 0.0.5 的 Forge GameTest 在测试启动前被 RTF 自身 `MixinUtil.shutdownExecutors` refmap 注入失败阻断；该 target 不能作为 RTF 实机验收。
- 重启 `RTF0805` 客户端后运行半径 512、step 256、micro stride 64 的 W 实机探针：provider 为 `rtf_heightmap_preview_v0_0_5`，9 tile、0 failed，runner 从优化前 451ms 降到 91ms；`tiles/` 恰好 18 个 snapshot/meta 文件，0 PNG、0 `debug_*` 目录。探针产物验收后已删除。
- 同参数扩大到半径 32768 的正式 W 实测：实际覆盖 66048 x 66048 blocks，16641 tile、66564 cell、1065024 micro sample、0 failed 并 sealed；runner 38599ms，HTTP W 端到端 45512ms。优化前同范围 runner 为 422021ms，约提升 10.9 倍。最终 `tiles/` 为 33282 个 snapshot/meta 文件、0 PNG、0 `debug_*` 目录，占 75.58 MiB；完整 run 33290 个文件、352.10 MiB。
- `RTF08052` 上对首都 `city_realm_green_court_2_capital` 执行 D3：覆盖 1536 x 1536 blocks、step 32、48 x 48 cell；按首个 GIS region 到 review package 的产物时间窗约 18.94 秒，且目标 region `r.-3.-46.mca` 未生成。
- 同一首都完成正式 Blueprint D4、D5、D6 与 D7：D4 编译 19 个 Trek 固定模板，D6 使用当前 `StructureTemplateManager` 的真实 hash/尺寸锁定全部通过，D5 激活后 D7 为 19 个 `WAITING_FOR_WORLDGEN`、0 failure；目标 region 仍不存在，已达到安全加载交接点。
- 实机验收同时确认组合 catalog 不能直接作为 D6 真值：Stubbs 引用在当前快速启动实例中为 `TEMPLATE_NOT_FOUND`；Trek 离线清洗 catalog 的 hash 与当前模组原始 NBT 不同。最终验收通过只读 template metadata 查询冻结当前世界的真实 `templateRef`、hash 与尺寸，未绕过 D6 校验。
