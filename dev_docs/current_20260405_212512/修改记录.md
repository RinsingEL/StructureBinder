# 修改记录

## 2026-04-05 21:25:12

- 新建本轮 `dev_docs/current_20260405_212512/` 目录，并写入 `计划文档.md`、`需求文档.md`、`修改记录.md`。
- 本轮目标是新增 AI 可调用的独立 Jigsaw 求解器工具，并提供求解后复用现有 place 清理链的落地入口。
- 当前决定保持 `C8 submit` 与 `C9` 默认主链不切换，只新增独立工具与最小文档同步。
- 当前验证：工作文档目录已创建成功。

## 2026-04-05 21:47:00

- 在 `CityC8ArrangementEngine` 中新增单次 parent-child 求解入口，支持基于已选模板、连接器方向或连接器 ID 解出 child 的 `origin`、`rotation` 与 footprint。
- 新增 `SolvedPlacementExecutionService`，把已求解 placement 适配成临时 `BuildTask`，并直接复用现有 `BuildExecutionPipeline` 的 runtime 校验、地形清理与结构放置链。
- 在 `CityController`、`ModHttpServer` 与 MCP 工具层新增独立 `city_jigsaw_solve` 入口，支持纯求解与 `apply_now` 求解后直接落地两种模式。
- 同步更新文档仓库的 `02_jigsaw_solver.md`，明确本轮先交付独立工具，不切 `C8/C9` 默认主链。
- 当前验证：定向测试通过，命令为 `.\gradlew test --tests "com.user.terra_script.world.city.stage.c8.CityC8ArrangementEngineTest" --tests "com.user.terra_script.world.city.execution.SolvedPlacementExecutionServiceTest"`。
- 当前剩余问题：尚未把该工具正式接入 `C8 submit` 主链，这部分留待后续阶段切换。

## 2026-04-05 22:40:00

- 将独立 `city_jigsaw_solve` 工具从手工 connector 解算切换为 `CityVanillaJigsawAdapterService`，改为基于真实 parent connector、临时单模板池与 `JigsawPlacement.addPieces(...)` 生成单个 child piece。
- `solve_and_place` 改为继续复用现有 `BuildExecutionPipeline` 的 chunk gate、runtime validator 与 terrain preparation，但 placement 改为通过 vanilla piece 放置，并显式保留 child jigsaw 方块。
- 在 `CityController` 中收紧 `/city_jigsaw_solve` 入参校验，要求必须提供真实 `parent_connector_id`，并阻止 `descriptor` 缺失时回退到旧普通结构放置路径。
- 在 MCP 工具定义中把 `city_jigsaw_solve` 的契约改成“真实 connector first”，明确 `selected_rotation` 仅保留兼容、当前会被 vanilla adapter 忽略。
- 补充 `CityVanillaJigsawAdapterServiceTest` 的契约断言，并更新 `SolvedPlacementExecutionServiceTest`，覆盖 jigsaw path 保留 jigsaw block 的执行语义。
- 当前验证：
  - `.\gradlew compileJava compileTestJava --no-daemon`
  - `.\gradlew test --tests "com.user.terra_script.world.city.stage.c8.CityC8ArrangementEngineTest" --tests "com.user.terra_script.world.city.execution.SolvedPlacementExecutionServiceTest" --tests "com.user.terra_script.world.city.stage.c8.CityVanillaJigsawAdapterServiceTest" --no-daemon`
- 当前剩余问题：
  - 仍需在真机环境用 `g_market_02` 再跑一次 `/city_jigsaw_solve` 与 `apply_now`，确认结果来自 vanilla path。
  - 当前主模块 `C8 submit` / `C9` 默认主链仍未接入该 adapter，这仍是后续阶段工作。

## 2026-04-06 00:20:00

- 对运行中的 `g_market_02` 真机测试发现：
  - synthetic connector `next_start_g_market_02_south_1` 会被正确拒绝，返回 `parent_connector_must_be_real_jigsaw_id`
  - 真实 connector `jigsaw_south_4_0_8` 仍返回 `parent_connector_not_found_in_template`
- 进一步核对原版 `taiga_butcher_shop_1.nbt`，确认模板内 jigsaw 真实位置与 catalog 中的 `[4,0,8]`、`[7,0,6]` 一致，说明问题更可能出在 runtime 模板解析或 parent connector 定位细节，而不是 catalog 真值本身。
- 已将 parent/child jigsaw 列表切换为直接基于 `StructureTemplate.filterBlocks(..., Blocks.JIGSAW)` 获取，并为 `parent_connector_not_found_in_template` 增补接口级调试 warning，返回：
  - 期望的 parent connector 世界坐标与 facing
  - 运行时模板里实际扫到的 parent jigsaw 候选列表
- 当前验证：`.\gradlew compileJava --no-daemon` 通过。
- 当前剩余问题：需要重启运行中的游戏/开发服，让新增的调试 warning 生效后，再次重跑 `g_market_02` 的 `solve` / `apply_now`。

## 2026-04-06 01:05:00

- 将本轮 TerraSense 排障与修正过程整理为正式文档，新增到文档仓库：
  - `docs/10_product/helper_mods/README.md`
  - `docs/10_product/helper_mods/TerraSense/02_runtime_jigsaw_问题与修改记录.md`
- 文档中收口了：
  - `g_market_02` 真机复现过程
  - synthetic connector 与真实 connector 的返回差异
  - runtime 模板 jigsaw 与 catalog connector 的不一致证据
  - TerraSense 旧版 `orientation -> facing` 压扁造成真值漂移的根因
  - TerraSense 与 StructureBinder 两侧已做修改、当前未完成事项和下一步建议
- 当前验证：文档文件已落地，路径结构与“辅助 mod / TerraSense”新目录约定一致。

## 2026-04-06 17:56:20

- 在 `CityVanillaJigsawAdapterService` 中新增 runtime parent jigsaw 候选解析，基于当前 parent placement 的真实模板扫描结果反推出 `jigsaw_<front>_<x>_<y>_<z>` 形态的 runtime connector id。
- `city_jigsaw_solve` 现在会把 runtime 实际扫到的 parent jigsaw 候选以结构化 `runtime_parent_connectors` 返回；如果旧 catalog connector 已漂移，也允许直接使用这些 runtime-derived id 继续求解。
- child connector 映射在 catalog 对不上时，新增回退到 runtime jigsaw id 的逻辑，避免求解成功后 `incoming_child_connector_id` 继续丢失。
- 同步更新文档仓库 `docs/10_product/city/main_module/02_jigsaw_solver.md`，补充当前独立工具会回传并接受 runtime 派生 connector id 的契约说明。
- 当前验证：
  - `.\gradlew compileJava compileTestJava --no-daemon`
  - `.\gradlew test --tests "com.user.terra_script.world.city.stage.c8.CityVanillaJigsawAdapterServiceTest" --tests "com.user.terra_script.world.city.execution.SolvedPlacementExecutionServiceTest" --no-daemon`
- 当前剩余问题：
  - 仍需在真机环境重新调用 `g_market_02` 的 `/city_jigsaw_solve`，确认返回中已经带出 `runtime_parent_connectors`，并验证用 runtime-derived id 可以继续求解。
  - TerraSense 新版 `C3.5` catalog 仍需正式重导并回灌，届时再确认 catalog id 与 runtime id 是否重新对齐。

## 2026-04-06 19:48:01

- 使用运行中的本地 HTTP 服务在 `g_market_02 / ba_g_market_02_a001 / start_g_market_02` 上做真机回归，分别测试：
  - 旧 catalog connector `jigsaw_south_4_0_8`
  - runtime 派生 connector `jigsaw_north_7_1_0`
- 真机结果显示当前运行实例已经加载到本轮 runtime connector 改造：
  - 旧 catalog connector 请求会返回结构化 `runtime_parent_connectors`
  - warning 中已带出 runtime jigsaw 的 `id/local/world/front/name/target`
  - 旧 catalog connector 仍会因漂移被拒绝，返回 `parent_connector_not_found_in_template`
- 改用 runtime 派生 connector `jigsaw_north_7_1_0` 再测时：
  - warning 中出现 `parent_connector_resolved_from_runtime_template`
  - 说明新入口已经成功接受 runtime-derived id，而不是继续卡在 catalog 对位阶段
  - 当前结果进一步推进到 `no_valid_jigsaw_solution`，表明瓶颈已从“父 connector 无法定位”前移到“vanilla 深度 1 下没有生成有效 child piece”
- 当前验证：
  - `curl.exe` POST `http://127.0.0.1:5000/city_jigsaw_solve`
  - `group_id = g_market_02`
  - `build_area_id = ba_g_market_02_a001`
  - `parent_node_id = start_g_market_02`
  - `selected_template_id = minecraft:village/taiga/houses/taiga_butcher_shop_1`
- 当前剩余问题：
  - 还需判断 `jigsaw_north_7_1_0 / minecraft:building_entrance` 这组 runtime 真值下，失败是因为 child 模板池不匹配、target/name 不可接，还是当前建造区本身不支持生成可用 child。
  - 后续可继续在 `g_market_02` 上尝试其他 runtime candidate 或改用更合适的 child 模板，验证是否能从 `no_valid_jigsaw_solution` 再向前推进。

## 2026-04-06 20:59:36

- 为独立 `city_jigsaw_solve` 新增了正式调试会话：
  - 每次请求生成 `debug_run_id`
  - 响应新增 `debug_artifact_dir / debug_trace / debug_preview_steps`
  - 同时把同构 trace 落盘到 `groups/<group>/jigsaw_solver_debug/<debug_run_id>/trace.json`
- 新增 `CityJigsawSolverDebugTrace`，把关键步骤改成中文“双层解释”：
  - `goal_zh`
  - `action_summary_zh`
  - `implementation_zh`
  - `result_zh`
  - `evidence`
  - `preview_image`
- 新增 `CityJigsawSolverPreviewExporter`，默认输出逐步预览图序列：
  - `01_request_context`
  - `02_runtime_jigsaws`
  - `03_parent_connector_resolution`
  - `04_vanilla_piece_result`
  - `05_validation_result`
  - `06_apply_result`（仅 `apply_now`）
- 扩展 `CityStagePreviewUtil` 的通用能力，补充：
  - 点位 marker
  - 带标签 marker
  - 高亮矩形
  - 调试说明面板
  - 支持 group 子目录落盘的 preview 写出方法
- 在 `CityVanillaJigsawAdapterService` 中补充结构化 debug 细节，供 controller 组装中文 trace：
  - parent connector 来源
  - catalog 期望坐标
  - runtime 命中坐标
  - vanilla startPos / target / stable seed
  - piece 是否生成
  - 生成出的 origin / bounds
  - child connector 来源
- 同步更新文档仓：
  - `docs/10_product/city/main_module/02_jigsaw_solver.md`
  - `docs/40_tests/city/main_module/01_acceptance.md`
  - 补充 `debug_trace`、逐步预览图与中文实现机制日志的正式契约和验收重点
- 当前验证：
  - `.\gradlew compileJava compileTestJava --no-daemon`
  - `.\gradlew test --tests "com.user.terra_script.world.city.stage.c8.CityVanillaJigsawAdapterServiceTest" --tests "com.user.terra_script.world.city.execution.SolvedPlacementExecutionServiceTest" --tests "com.user.terra_script.world.city.stage.c8.CityJigsawSolverDebugTraceTest" --tests "com.user.terra_script.world.city.stage.c8.CityJigsawSolverPreviewExporterTest" --no-daemon`
- 当前剩余问题：
  - 运行中的真机实例需要重新加载最新代码后，才能直接看到新的 `debug_trace` 与逐步预览图输出。
  - 当前 `no_valid_jigsaw_solution` 仍需下一轮继续细分为更具体的 vanilla 失败原因，但本轮已经把“发生在哪一步、用了什么实现机制、看哪张图”打通。

## 2026-04-06 21:08:59

- 在已启动的游戏实例上重新执行 `g_market_02` 真机回归，确认最新 solver 调试链已经真正生效：
  - `parent_connector_id = jigsaw_south_4_0_8`
  - `parent_connector_id = jigsaw_north_7_1_0`
- 当前真机响应已经带出：
  - `debug_run_id`
  - `debug_artifact_dir`
  - `debug_trace`
  - `debug_preview_steps`
- `debug_trace` 现场确认：
  - 每一步都包含 `goal_zh / action_summary_zh / implementation_zh / result_zh / evidence / preview_image`
  - runtime 日志 `city.jsonl` 也同步写入了同一套中文步骤
- 当前抓到的两组关键真机结论：
  - 旧 catalog connector `jigsaw_south_4_0_8`：
    - trace 已明确写出 runtime jigsaw 扫描结果
    - 明确写出 connector 解析机制
    - 明确卡在 `parent_connector_not_found_in_template`
  - runtime-derived connector `jigsaw_north_7_1_0`：
    - trace 已明确写出“直接命中 runtime 模板中的真实 jigsaw”
    - 明确写出使用单模板 pool + `JigsawPlacement.addPieces(...)`
    - 当前进一步卡在 `no_valid_jigsaw_solution`
- 当前现场产物路径已生成到：
  - `groups/g_market_02/jigsaw_solver_debug/20260406_210800_606/`
  - `groups/g_market_02/jigsaw_solver_debug/20260406_210806_459/`
- 当前验证：
  - `curl.exe` POST `http://127.0.0.1:5000/city_jigsaw_solve`
  - 直接回读响应中的 `debug_trace / debug_preview_steps`
  - 直接回读 `run/saves/新的世界 (2)/terra_script/runtime_logs/city.jsonl`
- 当前剩余问题：
  - 还未进一步打开生成的 png 预览做人工视觉核查，但文件与路径已确认落地。
  - 下一轮可以继续围绕 `jigsaw_north_7_1_0` 深挖 vanilla 为什么没有生成 child piece，并把失败原因继续细化到 pool/target 层。
