# Vanilla Jigsaw Adapter 改造需求

## 用户本次需求摘要

用户要求把当前独立 `city_jigsaw_solve` 工具从手工 connector 解算改成真正调用 vanilla jigsaw 的深度 1 拼接适配器，并在需要时继续复用现有 place / 执行链中的清理与预处理逻辑完成落地。

## 用户特别强调的约束

- 本轮只改独立工具，不切换当前 `C8/C9` 默认主链。
- 求解成功后的落地必须复用现有 `place` 相关清理逻辑，但最终 placement 要切到 vanilla piece。
- 只接受真实 jigsaw connector id，不再兼容 `next_start_*` 这类旧 synthetic connector。
- `selected_rotation` 第一版不支持，按 vanilla jigsaw 实际结果返回。

## 不做的事情

- 不把 child 默认接入 `C8 submit` 主路径。
- 不重写 `C9` 的默认消费逻辑。
- 不把自动扩张或自动选模板逻辑并入本轮独立工具职责。
- 不为旧 synthetic connector 做翻译兼容。

## 当前决定的工作边界

- 主要修改实现仓库 `E:\Mod_Dev\StructureBinder`。
- 同步更新文档仓库 `E:\Mod_Dev\designer_territoryMod` 中 Jigsaw 求解器案子文档。
- `/city_jigsaw_solve` 的求解与落地都要走 vanilla jigsaw adapter 语义，不允许再悄悄回退到手工 origin 计算 + 普通模板放置。
- 如需兼容字段，只做最小适配，不推动主链切换。
