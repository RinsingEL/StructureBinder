# 用户本次需求摘要
用户希望先不写代码，而是按规范编写一份城市生成大重构文档。核心想法是利用 OpenAI 图像模型的指令遵从和一致性，让 AI 基于地形图生成基础城市设计草图，再由程序查错和结构化。

# 用户特别强调的约束
- 先不写代码。
- 按项目规范编写大重构文档。
- 需要了解 OpenAI 的 image2/图像能力。
- 用户认为图像模型可以承担基础城市设计草图。

# 不做的事情
- 不改 Java/MCP/运行时代码。
- 不提交 git。
- 不把新的主方案写回实现仓旧文档体系。

# 当前决定的工作边界
在文档仓新增或更新正式方案文档；实现仓只保留本轮过程记录。
