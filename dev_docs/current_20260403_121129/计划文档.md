# 本次任务标题

双仓库 AGENTS 规则收口与职责边界固化

## 最终计划摘要

在实现仓库与文档仓库分别补齐 `AGENTS.md` 规则，明确双仓库职责、文档真值来源、`dev_docs` 与历史归档的定位，以及后续提交与维护边界。

## 实施步骤

1. 在实现仓库创建本轮 `dev_docs/current_20260403_121129/` 记录目录。
2. 修改实现仓库 `AGENTS.md`，补充双仓库协作、`dev_docs` 保留、文档真值迁移和提交检查规则。
3. 在文档仓库新增 `AGENTS.md`，定义新文档目录、归档区和只读规则。
4. 核对两个仓库的最终规则是否一致且无明显冲突。

## 验收标准

- 实现仓库 `AGENTS.md` 已明确：
  - `StructureBinder` 与 `designer_territoryMod` 的职责分工
  - `dev_docs` 保留且继续使用
  - `workFlow` 不再作为当前真值来源
  - 提交前需区分实现仓库提交与文档仓库提交边界
- 文档仓库存在新的 `AGENTS.md`，并明确：
  - 新文档主目录体系
  - `90_archive` 只读归档定位
  - 当前真值只认新结构
- 本轮 `dev_docs` 三件套已写入并可用于后续验收说明。

## 关键假设

- 当前用户希望采用双仓库长期协作模式，而不是临时迁移。
- 文档仓库后续将持续维护，不再依赖旧 `workFlow` 作为主入口。
- 本轮先只处理规则文档，不顺带提交代码或其他实现改动。
