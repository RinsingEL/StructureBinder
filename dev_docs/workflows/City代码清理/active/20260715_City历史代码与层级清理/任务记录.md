# City 历史代码与层级清理任务记录

## 任务边界

- 删除已经退出 active endpoint、且无生产调用的旧功能区 / D6 / D7 / bounded jigsaw 子图及其历史测试。
- 清除 `CityPlanningEndpointHandler` 中遗留的旧 import、旧 artifact 读取器和无调用辅助方法。
- 把活动城墙的 Minecraft NBT 与文件写入职责从 application 下沉到 infrastructure。
- 继续抽取 HTTP 巨类中不依赖文件、世界和 HTTP 的 D4 分阶段纯规划逻辑。
- 同步文档仓库 City 数据契约、代码导览和测试入口；不改动正在开发的 LandUseAreaPlan 产品案。

## 已读取真值入口

- `docs/README.md`
- `docs/00_nav/repo_map.md`
- `docs/00_nav/function_map.md`
- `docs/00_nav/AI语义检索规则.md`
- `docs/00_nav/从0到1设计开发流程.md`
- `docs/00_nav/实现仓库结构规范.md`
- `docs/systems/city/README.md`
- `docs/systems/city/20_contracts/数据契约/城市规划数据契约.md`
- `docs/systems/city/20_contracts/数据契约/结构落地交接契约.md`
- `docs/systems/city/30_code_guide/代码导览.md`
- `docs/systems/city/40_tests/测试入口.md`

## 工作区保护

文档仓库开始时已有以下用户改动，本任务不得覆盖或纳入清理：

- `docs/systems/city/README.md`
- `docs/systems/city/10_product/案子/README.md`
- `docs/systems/city/10_product/案子/60_LandUse与城市基面/City建筑驱动LandUseAreaPlan-v0.1/`

## 验证计划

- 静态搜索确认已删除类、旧 artifact reader 和 application 反向依赖不再存在。
- 运行新增组件与 City HTTP / 城墙 / 结构落地主链定向测试。
- 运行实现仓库完整 `gradlew test --no-daemon`。

## 当前状态

已完成，尚未提交。

## 完成情况

- 删除 23 个无生产入口的历史 main 文件：旧 FunctionZone / BuildableArea / D6 / D7 / bounded jigsaw、专用 domain model、preview 和 Minecraft backend，共约 9176 行。
- 删除 10 个只覆盖上述历史实现的测试文件，共约 3143 行；保留并补强 active endpoint 对旧字段和旧 artifact 的 hard-fail 隔离测试。
- `CityPlanningEndpointHandler` 删除旧 import、旧 artifact reader、孤儿 record 和 D4 staged 私有算法副本，文件由约 4490 行降到约 4250 行。
- 新增 `CityD4StagedPlanCompiler`，承接 key-before-array 校验、array plan 默认化、最小 layout plan 和 anchor 合并；新增 7 项聚焦测试。
- 新增纯 application 的 `CityWallTemplateCatalog` 与 infrastructure 的 `MinecraftCityWallArtifactWriter`；`CityWallPlanner` 不再写文件或依赖 Minecraft。
- 删除 `CityStructureProfileCatalog.pretty()`；`CityStructureEnvelopeProfiler` 使用与原 `CityJson` 相同配置的私有 Gson，并用测试锁定缓存文件字节不变。
- application 包对 `systems.city.infrastructure`、`net.minecraft`、`platform` 的静态反向引用已归零。
- 文档仓库已同步 City 历史契约状态、D3 下游、代码导览、测试入口、城墙职责和 TerraSense 当前消费入口；历史开发计划保留原文。

## 验证结果

- 城墙 / envelope / City HTTP 定向测试：104 项通过。
- D4 staged compiler / City HTTP 定向测试：48 项通过。
- 完整 `gradlew test --no-daemon`：308 项通过，0 failure、0 error、0 skipped。
- `country_designer_mcp npm run build`：TypeScript 编译通过。
- `git diff --check`：实现仓库和文档仓库均无 whitespace error，仅有既有 LF/CRLF 转换提示。
- 静态搜索：已删除类在 `src/main` / `src/test` 无残余引用；application 层无 infrastructure / Minecraft / HTTP 反向引用。

## 剩余风险

- 本轮未执行 Minecraft 实机 worldgen、城墙放置或 NBT 现场验收；相关运行行为由现有 NBT 解析测试和完整 Java 回归保护。
- `CityPlanningEndpointHandler` 仍约 4250 行；本轮只抽了高内聚的 D4 staged 纯算法，文件 / endpoint 编排仍按文档约束留在 HTTP 层。
- 文档仓库的 LandUseAreaPlan 产品案和两个索引文件是任务开始前已有用户改动，本轮未修改、未覆盖、未纳入验收内容。
