# Agent 协作规则故障索引

| 现象 | 首次偏离 | 根因 | 修复 | 验证 | 记录 |
| --- | --- | --- | --- | --- | --- |
| 简单需求被扩写成难以审阅的长篇技术策划案，后续 Agent 偏离用户原意。 | 需求原话被转换成包含实现、契约、测试和非目标的通用方案。 | 从 0 到 1 流程强制生成多类产物，且没有要求 subagent 保留用户原话和图片。 | 需求案收口为原话、示意图和 `*xxxx*：xxxxx` 总结；移除角色化通用设计流程。 | 现行规则和导航只指向三份项目规则。 | `active/20260811_项目特化工作流瘦身/任务记录.md` |
| 真实游玩中 MCP 调用或参数错误被当成功能验收失败，Agent Loop 过早退出。 | Agent 未完成工具调用纠错便进入失败报告。 | 独立验收角色和首次失败停止规则压过了工具使用者的正常重试行为。 | 独立真实游玩 Agent Loop 规则允许根据新错误信息持续修正，调用错误不得记为功能失败。 | 旧验收角色和固定重试额度已从现行入口移除。 | `active/20260811_项目特化工作流瘦身/任务记录.md` |
| 快速迭代后旧案与最新案同时留在当前索引，开发过程中反复读入相反顺序并发生需求漂移。 | 当前案已替代旧流程，但旧案仍以“开发中”“部分覆盖”或另一个“最新需求案”留在工作树。 | 只有“最新原话优先”的原则，没有替代后的审计、迁移、删除、唯一入口和周期检查工作流。 | 新增需求案清理与真值收敛规则；session 到期先询问，启动后迁移有效内容、删除已提交且干净的旧案并收敛唯一入口。 | 待完成首次真实清理与配套 Skill 验证。 | `active/20260826_需求案清理与真值收敛/任务记录.md` |
