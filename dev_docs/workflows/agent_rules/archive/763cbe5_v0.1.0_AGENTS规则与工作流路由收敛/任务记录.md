# AGENTS 规则与工作流路由任务记录

## 目标与边界

- 目标：将实现仓库 `AGENTS.md` 收敛为轻量路由规则，避免启动阶段上下文过重。
- 目标：把从 0 到 1 开发、bug 修复、语义检索、`dev_docs` 组织方式沉淀到文档仓库规则文档。
- 边界：不修改已有 GIS 方案、契约、导览、测试内容；不处理文档仓库既有未提交 GIS 改动。

## 当前计划

1. 更新实现仓库 `AGENTS.md`，保留双仓库边界、语义触发和提交规则。
2. 在文档仓库 `docs/00_nav/` 增加流程和检索规则文档。
3. 更新文档仓库导航入口，保证后续 agent 能按索引查找。
4. 复查双仓库差异，确认未混入无关文件。

## 关键决策

- `AGENTS.md` 不维护大关键词表，只维护“先判断领域，再判断文档类型，再按索引检索”的算法。
- 从 0 到 1 的设计开发流程属于文档仓库当前协作流程真值。
- `dev_docs` 改为按 `systems/`、`tools/`、`workflows/` 组织，并增加系统 / 工具级故障索引。
- bug 修复不走完整从 0 到 1 流程，优先复现、定位、查测试入口、查故障索引、回归。
- Git 提交规则需要单独文档化：提交前必须看近期日志，标题和正文都必须中文，并沿用目标仓库既有版本号风格。

## 重要修改

- 实现仓库 `AGENTS.md` 更新为轻量路由规则：开局检索、语义触发、系统 / 工具 / workflow 版 `dev_docs` 路径、提交和验收要求。
- 文档仓库 `docs/00_nav/` 新增：
  - `从0到1设计开发流程.md`
  - `Bug修复流程.md`
  - `AI语义检索规则.md`
  - `dev_docs组织规则.md`
  - `Git提交规则.md`
- 文档仓库 `docs/README.md`、`docs/00_nav/function_map.md`、`docs/00_nav/repo_map.md`、`AGENTS.md` 增加上述规则入口。
- 实现仓库 `AGENTS.md` 的提交段落补充：提交前读 `git log --oneline -n 10`，提交标题和正文全中文，正文必须详细说明改动、影响、验证和归档。

## 验证结果

- 已检查实现仓库和文档仓库 `git status --short`，确认文档仓库存在既有 GIS 文档未提交改动。
- 本轮编辑范围限定在实现仓库 `AGENTS.md`、本任务记录，以及文档仓库 `00_nav` / 仓库级规则入口。
- 已复查实现仓库 diff：`AGENTS.md` 从长规则改为轻量路由，并新增本任务记录。
- 已复查文档仓库规则入口 diff：`AGENTS.md`、`docs/README.md`、`docs/00_nav/function_map.md`、`docs/00_nav/repo_map.md` 挂载新增流程文档。
- 已检查新增规则文档路径存在：`AI语义检索规则.md`、`从0到1设计开发流程.md`、`Bug修复流程.md`、`dev_docs组织规则.md`。
- 已根据两个仓库近期 `git log --oneline -n 12` 确认提交标题风格，并据此补充提交规则。
- 本轮未主动修改文档仓库既有 GIS 方案、契约、导览、测试文件。

## 未解决风险

- 文档仓库当前已有多份 GIS 文档未提交改动，本轮必须避免误触。
