# 项目特化工作流瘦身

*需求入口*：用户要求只保留需求案、真实游玩 Agent Loop、开发与 Bug 留档三类约束，并让流程从实际需求和游戏结果出发。

*修改内容*：文档仓库将七份通用角色流程收为三份项目规则，移除 AI 角色状态机；跨系统地貌主线移入产品说明；两仓 `AGENTS.md` 和导航改为按需读取。需求案只保留原话、示意图和总结，目的是方便人类 review 和方案确认；AI 总结改为“三级子功能标题 + 一段高密度完整说明”，核心对象、关系和禁令使用 Markdown 加粗，列表只用于真正离散的内容。MCP 调用错误允许持续纠正，目的是避免约 5 分钟的真实游玩流程拖成半小时；所有开发与 Bug 强制简短留档，目的是供以后遇到相似问题时快速参考。

*验证结果*：`docs/01_workflows/` 已只保留三份项目规则；旧角色、旧流程和旧留档规则在现行 AGENTS、导航与文档入口中均无残留引用；需求案新排版已在 City 景观扩张案中实际应用。两仓目标文件 `git diff --check` 通过，仅有既有 LF/CRLF 提示。

*剩余问题*：当前两仓已有大量 City 景观相关未提交改动，本任务未修改、未回滚这些文件；本轮只整理规则，尚未重写既有 City 产品案。
