# 需求案清理与真值收敛

*需求入口*：用户确认将旧需求清理建立为定期工作流；每次 session 做轻量时间戳检查，到期先询问是否启动；用户同意启动后，已提交且干净的旧文件删除无需逐项授权。

*修改内容*：新增并接入《需求案清理与真值收敛规则》、轻量到期脚本、事件标记脚本和单一项目导航入口；新增实现仓 `dev_docs/README.md` 并限制历史记录读取。首次实际清理锁定 City D4 蓝图职责链：把仍有效的城市效果原话迁入《City单次递归蓝图与自愈编译》，补入“建筑先落位、区域后形成”的最新原话与总结；删除与当前案混合冲突的《City意图驱动PCG城市生成》和顺序相反的《City功能区预分配与受约束扩张》；把街带 v0.1 的完整原话、标注和两张图片迁入 v0.2 后删除 v0.1；City 系统入口只指向职责索引和 D4 当前案，案子索引统一使用 `实现当前案|设计中案|能力参考`。

*验证结果*：首次清理前状态为 `DUE status_DUE_INITIAL_CHECK`，两仓工作区干净；清理后 City 所有仍含文件的产品案均进入职责索引，D4 蓝图编译链只有一份实现当前案；三份被删旧案名和路径在文档仓无残留引用；街带两张迁移图片与删除前 Git blob 哈希逐一一致；当前案保留“区域由建筑形成、优先建筑再管功能区面积”的最新原话及对应总结；两仓 `git diff --check` 通过。完整审计完成后状态更新为 `CLEAN`，下次检查时间为 2026-08-27 12:32:41 +08:00。

*剩余问题*：无。
