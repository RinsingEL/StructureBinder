# 旧 city / workFlow 归档恢复任务记录

## 目标与边界

- 从文档仓库历史提交中恢复被清理的旧 city 与旧 workFlow 设计资料。
- 恢复位置仅限文档仓库 `docs/90_archive/`，不恢复到当前真值路径。
- 本轮不修改 Java / MCP / TS 实现代码，不改变 GIS 当前方案、契约、导览或测试入口。

## 来源

- 文档仓库：`E:\Mod_Dev\designer_territoryMod`
- 来源提交：`b1e33c7`
- 参考清理提交：`76c3d82`
- 恢复目标：`docs/90_archive/StructureBinder_pre_gis_20260610/`

## 恢复内容

- 旧 city 四层文档：
  - `docs/10_product/city`
  - `docs/20_contracts/city`
  - `docs/30_code_guide/city`
  - `docs/40_tests/city`
- 旧工作流文档：
  - `docs/90_archive/StructureBinder/workFlow`

## 执行记录

- 先用 `git archive --format=tar` 恢复，发现 Windows `tar.exe` 对部分中文路径报 `Invalid empty pathname`，只恢复了 47 个文件。
- 改用 `git archive --format=zip` + PowerShell `Expand-Archive`，完整恢复 76 个历史文件。
- 删除并重建了不完整的目标目录，删除前校验目标路径位于 `docs/90_archive/StructureBinder_pre_gis_20260610`。
- 新增归档说明：
  - `docs/90_archive/README.md`
  - `docs/90_archive/StructureBinder_pre_gis_20260610/README.md`

## 当前结论

- 旧设计资产已恢复，可作为后续重建城市、国度、工作流阶段的参考。
- 当前真值仍以 `docs/systems/`、`docs/tools/` 和 `docs/00_nav/` 为准。
- 从归档迁回主线时，需要按地貌驱动 GIS-C 链重新评估，不直接沿用旧 AI image / 逐步 AI 求解 / 旧 C 链字段。
- 用户确认旧 C 阶段资料营养不足，后续不迁回主线；旧 W / T 可参考世界主题、国度扩张、粗扫和宏观约束。
- 已新增当前主线入口 `docs/00_nav/地貌驱动主线Workflow.md`，记录 W / T / C 的新阶段职责：
  - T：RealmProfile。
  - C1：TerrainPatchMap。
  - C2：CityGrammarPlan。
  - C3：FunctionZoneMap。
  - C4：BoundaryTreatment + RoadNetwork。
  - C5：AnchorPlan。
  - C6：GenerationConstraintField。
  - C7：PoolPlan。
  - C8/C9：Materialization。
- 已同步 `docs/README.md` 与 `docs/00_nav/function_map.md`，让后续涉及 W / T / C 主链职责的问题先读取新 workflow。
- 2026-06-11 继续整理 T 阶段：用户确认 T 阶段应继承旧 W/T 中“世界粗扫后生成国度配置”的有效部分，但只锁定国度、城市名册和城市生长种子，不进入城市边界和功能区规划。
- 已新增并迁移 `docs/systems/realm_planning/10_product/T阶段国度与城市种子方案.md`，明确 RealmProfile、CitySeedRegistry、T 到 C 的交接边界。
- 已修正 `docs/00_nav/地貌驱动主线Workflow.md` 中 C1 的口径：C1 只给地貌事实和预览，不做建筑可建性评分。
- 用户进一步确认：W 保留，T1 / T2 / T3 也保留，因为旧阶段思路整体可用；但旧代码迁移时要注意旧 bug。
- 已更新 T 阶段方案：T1 为国度蓝图与落脚候选，T2 为核心点与首都种子，T3 为国度扩张，T4 为新的城市名册与生成条件登记。
- 已明确旧 T3 结束后只定义首都城市，后续城市生成条件配置缺失；新的 T4 专门补这个缺口，且不迁回旧 T4 的城市 bootstrap 默认行为。
- 2026-06-11 继续确认 W / T 边界：W 阶段保留，并新增粗尺度 WorldPatchMap。该 patch 来自大 step 粗扫，只用于国度落脚、扩张背景、首都选点和宏观判断，不替代 C1 的城市局部精细 TerrainPatchMap。
- 已更新主线 workflow：W 产物新增粗尺度 patch 与带网格坐标的选点预览图；T 阶段显式负责城市名册、城市数量、理论规模和城市生成条件。
- 已更新 T 阶段方案：T2 读取 W 粗 patch 和带网格坐标候选图，让 AI 可直接选择首都候选坐标；程序负责校验坐标属于目标大陆、目标 patch 或候选范围，必要时 snap。
- 已更新 T4 口径：T4 决定每个国度未来拥有多少城市、各规模城市的大致数量，并写明城市存在所需的宏观地理或叙事条件。
- 用户询问 W 粗 patch 是否能复用 GIS 逻辑。已查看当前实现，确认 `GisSampleConfig.cellStepBlocks`、`AtlasRegion.cellStepBlocks`、`RadiusRefreshPlanner` 和 `PatchMerger` 已具备 step 参数化基础，但当前入口多走 `GisSampleConfig.defaults()`，缓存 key 也尚未按 profile 隔离。
- 已新增 GIS v1.1 开发计划 `docs/systems/gis/10_product/开发计划-v1.1-多分辨率Atlas与W粗Patch.md`：明确 W 粗 patch 是 GIS 的 `world_coarse` profile，不另建算法；后续需要补 profile 配置、缓存隔离、粗扫阈值、网格坐标预览和 W 调度接入。
- 已修正 GIS 开发计划版本边界：`docs/systems/gis/10_product/开发计划.md` 只保留 v1 已落地基线，v1.1 多分辨率扩展独立成文。
- 已新增国度规划系统入口 `docs/systems/realm_planning/README.md`，并把 T 阶段国度与城市种子方案从 `00_nav` 迁入该系统；`00_nav` 不再承载该功能方案。
- 用户确认 T 阶段方案应写明 GIS 使用方式。已在 `docs/systems/realm_planning/10_product/T阶段国度与城市种子方案.md` 新增 “GIS 使用口径”：T 阶段可消费 W 粗 WorldPatchMap、GIS v1.1 `world_coarse` 粗 Landform Patch、patch 摘要、自然群系和气候摘要；禁止消费 C1 `city_fine`、`verify_surface`、城市内部逐方块扫描和结构物化参数。

## 验证

- 已确认来源提交目标范围共有 76 个文件。
- 已确认归档目标中恢复 76 个历史文件，新增目录内 README 后目标目录文件数为 77。
- 已新增并导航当前主线 workflow 文档。
- 已新增并导航国度规划系统和 T 阶段国度与城市种子方案。
- 已检查 `地貌驱动主线Workflow.md` 中 W 粗 patch、带网格坐标预览图、T 阶段城市数量和理论规模的关键段落。
- 已检查 `docs/systems/realm_planning/10_product/T阶段国度与城市种子方案.md` 中粗 WorldPatchMap、T2 首都坐标选择、T4 城市数量 / 规模 / 生成条件的关键段落。
- 已检查国度规划系统中 “GIS 使用口径” 段落和 README 上下游说明。
- 已检查 GIS v1 / v1.1 开发计划拆分、GIS README 阅读顺序和 function_map 系统入口。
- 未运行代码测试；本轮为文档归档与 W / T 方案修订。
