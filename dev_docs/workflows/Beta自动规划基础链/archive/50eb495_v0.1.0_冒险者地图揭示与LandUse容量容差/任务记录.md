# 冒险者地图揭示与 LandUse 容量容差

*需求入口*：用户实机验收提出：冒险者地图未开放区域使用黑色蒙版；核对当前底图是否真的来自 W 粗扫地形；LandUse 以落地优先，required Landscape 实例实际形成容量达到申请容量的 90% 即可通过，低于 90% 才失败。

*问题定位*：本次 W 的 `world_biome_preview.png` 已包含完整且可辨认的群系轮廓，粗扫数据本身正常；冒险者地图当前只把整个 `world_feature_grid.json` 压成分类纹理，没有携带服务端开放区判定结果，因此规划范围被全部画出。LandUse 在 D6 后对每个 required Parcel 使用 `claimed < minimumExecutable` 的 100% 硬门禁，`850/864` 的轻微地形损失也会中止整城。

*拟修改*：地图快照按 `PlanningAreaAccessPolicy` 为每个粗栅格冻结 reveal mask，客户端将未开放像素绘制为黑色，并在普通图层隐藏蒙版外城市节点；底图仍只读 W feature grid，不加载区块。LandUse 按 required Landscape instance 汇总申请量与实现量，门槛固定为 `ceil(requested * 90%)`；90%—100% 记录可审计降级告警和容量字段后继续，低于 90% 明确失败。

*修改内容*：地图快照新增与 `PlanningAreaAccessPolicy` 同源的逐像素 reveal mask，mask identity 同时绑定 W/国境产物、开放区配置和队列/城市状态；客户端将未开放像素绘制为近黑色，普通图层不画蒙版外城市。LandUse required Landscape 改为按实例汇总容量，固定 90% 向上取整门槛；通过但未满额时写入 `CITY_LANDSCAPE_REQUIRED_CAPACITY_DEGRADED` warning，并在 quality 冻结 requested、realized、threshold、basis-points ratio、status 与 reasonCode。

*验证结果*：真实 run `provider_4c88d8fb9d1cfd78_r8192` 的 W 群系预览已确认正常，地图填满问题属于揭示层而非扫描层。`AdventurerMapStatusReaderTest` 与 `LandUsePlanningServiceTest` 定向通过，`reobfJar` 打包通过；850/864 可通过，864 的最低通过量为 778，777 仍失败。产物 `build/libs/geomantia-0.1.0.jar`，SHA-256 `F72ECDA5A289BCC3188301E991F662ADE73F2636B469CFB8F03FB11785D8E0A1`。待新包重启后实机复验地图视觉与原失败城市续跑。

## 2026-09-03 地图实机复验补充

*需求入口*：用户反馈地图仍把中间小块地形平铺到整个渲染面，且玩家位置与地貌看起来不一致；最新口径是直接复用 W 粗扫预览语义，显示粒度按缩放层级选择，并只处理玩家视口内的地图与节点 LOD。

*问题定位*：`AdventurerMapScreen.drawMap` 误用九参数 `GuiGraphics.blit` 重载。该重载把目标宽高同时当作源区域宽高；实际 8192 半径 W 样本经固定降采样后仅为 `66×66`，放大到 GUI 数百像素时 UV 超过 1，形成重复采样。玩家实时 X/Z 和粗图 block bounds 共用同一线性变换，数值链没有发现轴交换；视觉错位由错误底图 UV 直接造成。现有 `zoom` 始终围绕完整 W 范围中心，服务端固定生成整图最多 `96×96` 并发送全部城市节点，不是玩家视口驱动的分级 LOD。

*修改内容*：客户端改用源区域与目标区域可分别指定的纹理绘制重载，固定以完整 W 纹理的 0—1 UV 拉伸一次。缩放按钮改为请求新的玩家视口：服务端以玩家位置为中心、按 256 block 量化以复用缓存，缩放 `0.5—4.0` 对应半径 `8192—1024`；在该范围内从 W feature grid 重新选择聚合倍率，远景合并多个 W cell，近景恢复到 W 原始 cell 粒度。网络只下发视口内城市节点，实时玩家标记继续每帧读取本地 X/Z。网络协议随请求结构升级。需求案、代码导览和测试入口同步最新 LOD 口径。

*验证结果*：对照 Forge 1.20.1 `GuiGraphics` 源码确认新重载将目标尺寸与 `sourceWidth/sourceHeight` 分离，UV 固定覆盖完整纹理。新增 `cropsAndRefinesTheWGridForThePlayerViewportAndOmitsOffscreenNodes`，覆盖整图 256-block 聚合、玩家近景 128-block W 粒度及视口外节点剔除；`AdventurerMapStatusReaderTest`、完整 `gradlew test`、MCP `npm run build` 和 `gradlew reobfJar` 均通过。产物 `build/libs/geomantia-0.1.0.jar`，SHA-256 `44CD9E38871E11525AFADD5A1F5FC1A67ECE52ABE91BBBD69DF0683D97D7015F`。

*剩余问题*：待替换新包并重启客户端后实机确认底图无平铺、玩家位置与地貌对齐、缩放切换自然且持续移动无明显卡顿。

## 2026-09-04 原失败城市续跑补充

*实机操作*：在新包已加载且世界 API 正常的会话中，对 run `provider_4c88d8fb9d1cfd78_r8192`、城市 `city_realm_a_east_valley` 显式调用 `city_post_d4_auto_compile_retry`，生成 Post-D4 attempt 4。D3、D4、D5、D6 均按已有合法产物跳过，LandUse 重新执行。

*结果*：90% 实例汇总门禁已经生效，失败信息改为 `greenbelt_common::instance_01:realized=2226:requested=3456:requiredThreshold=3111:thresholdPercent=90`。D4 `city_landscape_capacity_reservation_plan.json` 同时确认该实例冻结四块互不重叠 Parcel，每块 864、并集 3456。由此否定“850/864 达到 90% 后原城即可续跑”的先前实机预期：850/864 只是旧逐 Parcel 门禁最先暴露的局部结果，完整实例实际仅兑现 64.41%。队列恢复 `needs_agent / POST_D4_WORKFLOW_UNEXPECTED_STATUS`，未再次 retry。

*根因闭环*：D4 `CityLandscapeCapacityReservationPlanner` 生成冻结域时只把 Anchor 的 `actualFootprint` 作为结构障碍，没有接收已经存在的 pre-fill street skeleton 与 main-road bands；LandUse Outdoor 编译则把最终 `streetBands.platformBounds` 加入 Landscape 的 `structureFootprints`，扩张器把它们视为硬障碍。真实冻结域与道路逐格相交 329 格；去除道路后，四个 Parcel 从各自冻结种子/父 Parcel 接力界面可达的连通分量依次为 850、721、149、506，合计精确等于运行时 2226。故障归属 D4/LandUse 跨阶段障碍口径，不在世界落块执行层；重复 retry 或放宽容量门槛都不能修复。

## 2026-09-04 产品口径终版

*决策*：该独立城市绿带已经被 building greenery policy 生成的建筑周边绿化替代，不再修正它的 D4 道路预留。当前游戏实例及开发运行目录的 reference catalog 同步删除 `greenbelt` rule、`surface:greenbelt`、`landscape:common_green` 与 `fill:relay_common_green`；`COMMON_GREEN` 类型能力本身保留，因为牧场等其他配置仍可复用，建筑周边绿化也不受影响。

*LandUse 准入口径*：所有其余 Landscape 不再设置 90%、`minAreaBlocks` 或填充阶段数面积门槛。required 只表示优先于 optional 抢占空间；任何非零逐格结果都合并落地，零格 Parcel 才记录 skipped。父 Parcel 接力界面被道路或地形耗尽时，保留此前已形成 Parcel，当前及依赖它的后继以零格退出，不回落到配置 seed，也不让整城失败。quality 继续记录 requested、realized 与比例，并用 `CITY_LANDSCAPE_CAPACITY_REDUCED` 表达降量，但不再包含 threshold 或硬失败原因。

*验证与部署*：两个 reference catalog 均通过 JSON 解析且已无四个独立绿带引用；新增回归覆盖“父界面耗尽仍保留前序 Parcel”和“optional 后继无 seed 仍保留前序 Parcel”。`gradlew test jar --no-daemon` 全量 705 项通过，0 failure/error。游戏未运行时已将新包替换到当前 Forge 实例，构建产物与安装包 SHA-256 均为 `6467E65735F6FECF3E8D38EC16C451D3F58136E8EBA52E4123E64821FB8E5DCE`；旧包备份为 `geomantia-0.1.0.pre-20260904-landscape-relax.jar.bak`。用户选择以新存档复验，不再继续旧东谷城。
