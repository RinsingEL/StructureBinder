# Beta 自动规划真实验收

*验收范围*：真实客户端与正式 MCP/HTTP 调试服务下，验证城市设计队列、D4 后自动推进、初始活动区/规划区域开放以及 W 一次完整扫描；优先复用身份匹配的最高有效上游。

*执行规则*：遵循 `真实游玩AgentLoop规则.md`，测试中不读取实现源码或 Git 历史，不边测边修改功能。每次正式调用、修正理由、返回摘要、产物路径与耗时记录在同目录 `真实游玩调用记录.md`。

*当前状态*：2026-08-31 已完成第一轮真机 Agent Loop。W、T1-T4 产物、全局径向城市队列、越序门禁和当前城市 D3 均已取得正式结果；D4 仍停在 `waiting_for_agent`，符合尚无玩家 Provider 时不自动跑到底的设计。初始活动区配置与首城距离已核对，TP 交互闸门仍需在客户端手动命令复验。固定 acceptance 的 T4 质量存在 1 个城市种子越出 owned territory，因此本次不记为完整产品验收通过。2026-09-01 新存档 `RTF_beta_validation_01` 的最小 W 真扫已确认 RTF 原生快速路径可用，快速启动目标已切换到该存档。随后以 `rtf_realm_t3_20260901` 验证三国九城 realm-grouped 连续流水线：9 城注册与队列成立，首城 D3、D4 锚点编译成立，但后台自动接力仍注入已删除的 D4 模式；手动续跑又因三功能区未显式声明连接而在最终验收失败。队列正确停为 `needs_agent`，其余 8 城未越序。本轮保留单次蓝图提交边界，未重投。两处主链漂移现已修复：Post-D4 请求不再携带旧模式；多 Group 且允许关系扩张的 Blueprint 必须在提交前声明关系，非法输入不消耗唯一提交次数。托管 TerraSense/template/reference 配置已迁移到当前无版本 `schema` 并通过在线 Context 探针。旧 run 保留故障现场，需新建三国九城 run 才能完成连续性复验。
