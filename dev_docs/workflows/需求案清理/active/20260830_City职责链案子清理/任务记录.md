# City 职责链案子清理

*需求入口*：Session 到期检查返回 `DUE`，用户同意执行需求案清理；随后继续整理大型农业 GRID 道路预留方案。

*修改内容*：审计 City 产品案职责索引及阵列排版、D4 蓝图编译相关职责链；确认各职责链当前案唯一。清理 9 个无文件、无 Git 跟踪内容、无文档引用的旧案空目录，不迁移案文，不修改当前需求真值。

*验证结果*：City 产品案索引仍只将 `City阵列主从骨架与街带-v0.2` 标记为阵列排版链实现当前案，将 `City单次递归蓝图与自愈编译-v0.1` 标记为 D4 蓝图编译链实现当前案；旧空目录名称在 City Markdown 中均无引用；文档仓审计前工作区干净。

*剩余问题*：无。
