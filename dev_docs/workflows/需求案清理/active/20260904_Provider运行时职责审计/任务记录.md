# Provider 运行时职责审计

*需求入口*：Session 到期检查返回 `DUE`，用户同意执行需求案清理；当前开发目标为以 Hermes sidecar 接管现有 User Provider Agent Loop。

*修改内容*：审计“世界自动规划调度 / User Provider 接管 Agent 决策”职责链。确认当前唯一实现依据为策划仓 `docs/systems/realm_planning/10_product/案子/世界自动规划调度-v0.1/README.md`；W、统一 T3、逐国逐城、D4 后自动推进以及 Provider 只替换 AI 决策位的现行结论完整。未发现竞争旧案或失效入口，因此未删除或迁移需求案；所有已有未提交文档均从清理范围排除。完成后更新需求案清理状态时间戳。

*验证结果*：核对 realm_planning 系统 README 的职责标记、该领域已跟踪案子列表、职责链关键词引用和两个仓库工作区状态；该职责链仅有一个 `实现当前案` 入口，未发现已删除路径或竞争案引用，未覆盖现有未提交改动。

*剩余问题*：无。
