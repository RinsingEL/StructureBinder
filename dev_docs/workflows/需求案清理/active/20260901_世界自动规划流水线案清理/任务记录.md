# 世界自动规划流水线案清理

*需求入口*：`世界自动规划调度-v0.1`、用户确认“流水线是一个自动的功能”，以及《需求案清理与真值收敛规则》。

*修改内容*：把城市排序、逐城推进、D4 后自动推进和失败停驻等有效内容收敛到世界自动规划总调度案；删除两个独立 City 队列需求案；更新 Realm 与 City 产品入口。保留工作区中既有的 City 整合包内容配置设计稿及其索引改动。

*验证结果*：`世界自动规划调度-v0.1` 已成为自动规划总调度链唯一实现当前案；两个旧队列路径均不存在，案名和路径无残留 Markdown 引用；迁移的五条独有用户原话均可检索；策划仓 `git diff --check` 通过。工作区中既有的 City 整合包内容配置设计稿、Beta 验收记录和 `gradle.properties` 改动均未被覆盖。

*剩余问题*：无。
