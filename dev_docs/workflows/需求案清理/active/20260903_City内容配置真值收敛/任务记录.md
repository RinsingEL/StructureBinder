# City 内容配置真值收敛

*需求入口*：Session 清理检查返回 `DUE_REQUIREMENT_CHANGED`，原因为 City 内容配置设计中案接管实现；用户确认执行清理。

*修改内容*：审计城市内容配置链与固定模板落地主线的职责重叠。确认 `City整合包内容配置-v0.1` 是城市内容配置链唯一实现当前案，`City固定模板唯一落地主线-v0.1` 独立负责固定 NBT 落地与幂等；没有竞争旧案需要迁移或删除。更新本地清理状态为 `CLEAN`。

*验证结果*：产品索引中两条职责链各自只有一个实现当前案；限定范围搜索未发现第二份整合包内容配置案或失效引用；清理前两个仓库工作区均干净。

*剩余问题*：无。
