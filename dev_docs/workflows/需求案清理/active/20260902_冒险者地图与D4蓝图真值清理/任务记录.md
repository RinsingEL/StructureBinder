# 冒险者地图与 D4 蓝图真值清理

*需求入口*：Session 轻量检查返回 `DUE_REQUIREMENT_CHANGED`，状态原因为“冒险者地图由设计中案提升为实现当前案”；用户同意执行需求案清理。本轮同时收敛刚完成实现的 D4 Blueprint 失败预算口径。

*修改内容*：审计冒险者地图职责链，确认 `冒险者地图-v0.1` 已是规划地图与进度展示链唯一实现当前案，未发现竞争旧案，因此不删除文件。D4 蓝图编译链原实现当前案原位改名为 `City递归蓝图与失败预算编译-v0.1`，迁入 Top Patch 失败可换已审查 Patch、提交校验不计数、真实编译失败最多 5 次、只按工具黑盒返回恢复及不得用长锁牺牲并发的用户确认；同步更新 City 根入口、产品案职责索引、数据/MCP 契约、代码导览、影响面和活跃任务入口。已从 MCP 暴露面删除的候选、array、design-loop、cluster 与 sequential 工具不再列为当前公开接口，仅在代码导览标明实现层遗留 HTTP/测试身份。

*验证结果*：冒险者地图链与 D4 蓝图编译链均只保留一个实现当前案入口；策划仓及 City 活跃任务记录中无旧 D4 案名或“一次性 Context / 只允许一次决策”现行表述。City MCP 契约表与 `tools.ts` 当前 26 个公开 City 工具集合一致，已删除 legacy 工具不再出现；City 与 realm_planning 全部 Markdown 相对链接存在。双仓库 `git diff --check` 无空白错误，仅有既有 LF/CRLF 提示；已有未提交实现、契约和冒险者地图真实游玩记录均未被覆盖。需求案清理状态已更新为 `CLEAN`，下次检查时间为 2026-09-03 00:09:52 +08:00。

*剩余问题*：D4 失败预算仍需重启游戏后执行真实 Agent Loop 的“编译失败—同 Context 修订重提—连续推进”验收；该项属于实现验收，不阻塞本次需求案清理。
