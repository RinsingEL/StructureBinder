# 工作流读取与阶段衔接修订

*需求入口*：用户审查现有工作流后要求“ok，那先改改前三个呗”：解除当前案身份规则的读取依赖冲突、分开只读核对和清理启动、补齐已授权修复任务从真实游玩返回诊断的路径。

*修改内容*：调整项目入口、任务路由、需求案规则、需求案清理规则和真实游玩 Agent Loop 规则；将当前案身份与定位规则移至普通需求规则，为规则审查开放读取入口，明确局部需求核对不自动启动清理，以及游玩结束后按原任务授权继续诊断和修复。

*验证结果*：修改前双仓库工作区干净；补丁应用检查通过，写入后逐份核对 5 份规则与预期修改一致；双仓库 `git diff --check` 通过。核对普通需求编辑、规则审查、索引不唯一、仅验收、已授权修复和工具层受阻的规则路径；原有循环读取依赖、索引不唯一即启动审计、有效失败一律不进入诊断的表述已移除。本次仅修改文档，未运行代码或游戏测试，未提交。

*剩余问题*：无。
