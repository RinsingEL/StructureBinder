# 修改记录

## 2026-04-03 11:44:42

- 本轮修改了什么
  - 新建 `dev_docs/current_20260403_114442/` 目录。
  - 创建 `计划文档.md`、`需求文档.md`、`修改记录.md` 作为本轮“C9 执行层方案初稿”工作的基础文档。
- 为什么改
  - 按仓库 AI 工作流文档规则，为新的方案整理任务建立独立工作记录。
- 改到了哪些模块或文档
  - `dev_docs/current_20260403_114442/计划文档.md`
  - `dev_docs/current_20260403_114442/需求文档.md`
  - `dev_docs/current_20260403_114442/修改记录.md`
- 做了什么验证
  - 已核对当前分支和已有 `dev_docs/` 状态，确认本轮为新的执行层方案整理任务。
- 当前还剩什么问题
  - 仍需补充执行层拆分与落地流水线初稿正文。

## 2026-04-05 19:20:00

- 本轮修改了什么
  - 将 `C9` 真实结构落地主链从 `CityBuildQueueExecutor -> StructureInjector` 的直连方式拆成新的执行层包：
    - `BuildExecutionPipeline`
    - `BuildChunkGate`
    - `BuildRuntimeValidator`
    - `BuildTerrainPreparationService`
    - `BuildPlacementService`
    - `StructurePlacementGateway`
  - 扩展 `StructureInjector`，让真实放置返回 `PlacementOutcome / PlacementBounds`，并把 `JIGSAW` 清理数量与样例坐标回传到执行层。
  - 调整 `CityBuildQueueExecutor`，保留队列索引与 tick 调度职责，把运行时校验、清空、放置和失败策略委托给新的执行流水线。
  - 为 `C9` 真落地补上结构化运行时日志，记录 `soft_obstacle_clear / embedded_excavate / post_cleanup / place_structure` 四个关键阶段。
  - 新增执行层测试与清空测试，并修正 `CityController.loadC8GenerationSelection(...)`，让 `C8` 生成优先读取组级 `c7_selection.json`，避免组级工头计划被城市级 `C7` 覆盖。
- 为什么改
  - 需要把当前真实落地链拆成可扩展、可测试的执行层，为后续把 connector / jigsaw 求解器接入主链预留边界。
  - 需要把清空逻辑和调试日志正式放到 `APPLY_NOW` 里，保证每次真机验收都能回溯世界改动。
  - 真机联调暴露出 `g_market_06` 的 `C8` 会错误读取城市级 `C7`，导致缺失 `start_node`；因此同步修复组级优先加载逻辑。
- 改到了哪些模块或文档
  - `src/main/java/com/user/terra_script/world/city/execution/`
  - `src/main/java/com/user/terra_script/world/StructureInjector.java`
  - `src/main/java/com/user/terra_script/world/city/CityBuildQueueExecutor.java`
  - `src/main/java/com/user/terra_script/server/mcp/CityController.java`
  - `src/test/java/com/user/terra_script/world/city/execution/`
  - `src/test/java/com/user/terra_script/server/mcp/CityControllerTest.java`
  - `dev_docs/current_20260403_114442/修改记录.md`
- 做了什么验证
  - 执行 `./gradlew.bat build`，确认执行层重构与清空日志改动可编译并通过现有测试。
  - 执行 `./gradlew.bat test --tests com.user.terra_script.server.mcp.CityControllerTest`，确认组级 `C7` 优先加载修复已通过单测。
  - 对 `run/saves/新的世界 (2) / city_3120_-3200` 进行了真机回归：
    - `g_market_04` / `g_market_05`：确认真实落地、清空和日志工作正常。
    - `g_market_06`：补齐 `C6 -> C7 -> C8 -> C9`，确认首节点 `start_g_market_06` 已真实落地，坐标为 `3048,65,-3243`。
  - 回读 `C9_BuildQueue.json`、`c9_placement.json`、`c9_apply_preview.legend.json` 与 `runtime_logs/city.jsonl`，确认首节点落地结果与日志一致。
- 当前还剩什么问题
  - `g_market_06` 的第二个节点 `start_g_market_06_child_1` 在 `C8` 提交可以通过，但 `C9` 运行时仍因为 `runtime_footprint_collision` 被拦下；当前 child 节点的落点仍主要依赖人工给定 origin，而不是按父/子 connector 自动解算。
  - 仓库里已有 `CityC8ArrangementEngine` 的 connector 求解能力，但它还没有接入当前 `C8 submit -> C9 apply_now` 主链。
  - 下一步应在保持 `start` 节点直接 `place` 能力不删的前提下，把 connector / jigsaw 求解器接成独立能力层，用于 child 节点 origin、rotation、真实 bounds 和落地前预判。这会开始实质支撑主模块工头施工系统，但仍符合执行层按职责拆分的方向。
