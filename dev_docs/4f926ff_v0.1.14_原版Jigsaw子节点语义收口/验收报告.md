# 验收报告

## 本轮范围

- group 级清表工具落地
- 子节点原版 Jigsaw 语义收口

## 已完成项

- `/city_group_surface_clear` 已实现并注册到 HTTP 服务
- `CityVanillaJigsawAdapterService` 已改为：
  - 从 parent runtime jigsaw 读取原始 `pool`
  - 在原始 pool 中筛出 AI 指定模板元素
  - 再调用原版 `JigsawPlacement.addPieces(...)`
- `/city_jigsaw_solve` 第 04 步调试证据已新增：
  - `runtime_parent_pool_id`
  - `runtime_parent_pool_source`
  - `vanilla_pool_source`
  - `runtime_parent_pool_entry_count`
  - `runtime_selected_pool_entry_count`

## 自动化验证

- `./gradlew.bat test --tests "com.user.terra_script.world.city.stage.c8.CityVanillaJigsawAdapterServiceTest"`
- `./gradlew.bat test --tests "com.user.terra_script.world.city.execution.GroupSurfaceClearServiceTest" --tests "com.user.terra_script.world.city.execution.BuildExecutionPipelineTest"`

## 真机验证

- `g_market_03` group 清表成功
- `g_market_03` 旧链路下的水平 child `solve -> apply_now` 曾成功落地
- 新版 pool 语义回归后：
  - `taiga_butcher_shop_1` 的 `building_entrance` 口在结构本体中确认为 `pool = minecraft:empty`
  - 因此 `selected_template_not_in_runtime_parent_pool` 为正确诊断

## 额外定位结果

- `plains_butcher_shop_1/2` 的入口口连接到 `minecraft:village/plains/streets`
- 临时换 `start` 后继续测试，新的阻塞点变为：
  - `missing_selected_template_meta`
- 这说明下一层问题不在 solver，而在 TerraSense / catalog 没导出 `village/plains/streets/*` 这类模板元数据

## 当前未解风险

- `apply_now` 快通道仍需后续继续核对 `jigsaw` 清理与 piece 落地观感
- 当前系统 catalog 仍无法覆盖部分原版 pool 中的 street 模板
