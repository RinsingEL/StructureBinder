# 修改记录

## 2026-04-02 01:52:08
- 本轮准备对 `C7/C8/C9` 做工头计划化重构，先创建当前任务文档目录并落地计划、需求、修改记录三份基础文档。
- 这样做是为了让后续代码改造、接口扩展、测试调整都有统一的计划和需求依据，满足仓库对 AI 工作流文档的要求。
- 当前已确认改造重点包括：`C7` 工头计划结构、`C8` 节点会话结构、`C9` 新队列行为、`CityController` 新接口以及中文自然语言值约束。
- 受影响模块预计包括：
  - `src/main/java/com/user/terra_script/world/city/stage/c7/`
  - `src/main/java/com/user/terra_script/world/city/stage/c8/`
  - `src/main/java/com/user/terra_script/world/city/stage/c9/`
  - `src/main/java/com/user/terra_script/server/mcp/CityController.java`
  - 对应 `src/test/java/.../c8/`、`src/test/java/.../c9/`
- 当前尚未开始正式代码改造，下一步将先收敛类型结构和控制器入口，再逐步调整执行链与测试。

## 2026-04-02 02:20:00
- 已完成 `C7` 工头计划主结构落地。
- 具体改动：
  - 在 `CityC7Stages.C7Selection` 中新增 `version`、`foreman_plan`、`foreman_plans`、`phase_list`。
  - 新增 `ForemanPlan`、`PhasePlan`、`StartNode`、`ConnectorTargetRule`、`TemplatePoolRef` 等结构，并在字段定义处补中文注释。
  - `generate/fromDecisionRequest` 现在会在保留旧 `arrangements/selections` 的同时，生成新的工头计划视图。
  - 阶段采用 `phase_key + phase_name` 双字段，其中 `phase_name`、目标说明、备注统一为中文自然语言。
- 这样改是为了让 `C8` 能直接消费新的工头计划，同时不立即打断旧兼容链路。
- 影响模块：
  - `src/main/java/com/user/terra_script/world/city/stage/c7/CityC7Stages.java`
  - `src/main/java/com/user/terra_script/world/city/stage/c7/CityC7Validation.java`
- 当前验证：
  - 定向测试已通过 `CityC7StagesTest`
  - `CityControllerTest` 仍能读取 `C7` 产物
- 当前剩余问题：
  - 旧 `arrangements/selections` 目前仍保留兼容字段，后续如果完全切换到新主结构，还需要进一步清理。

## 2026-04-02 02:26:00
- 已完成 `C8` 会话模型与节点提交入口的首版重构。
- 具体改动：
  - 在 `CityC8Stages.FoundationItem` 中新增 `session_state`、`queue_summary`、`active_node`、`node_queue`、`validated_nodes`、`failed_attempts`、`foundation_context`、`node_debug`。
  - 新增 `NodeTask`、`NodeDecision`、`NodeSubmitResult`、`FailedAttempt`、`TerrainRelaxProfile` 等结构，并在定义处补中文注释。
  - `CityC8Stages.generate` 不再默认调用旧的一次性整组排列求解，而是初始化单起点施工会话。
  - 新增 `submitNodeDecision` 与 `retryNode`，用于程序校验节点决策、记录失败回包、生成子节点、重新打开失败节点。
- 这样改是为了把 `C8` 从“完整 placements 求解器”改成“节点施工编排器”。
- 影响模块：
  - `src/main/java/com/user/terra_script/world/city/stage/c8/CityC8Stages.java`
- 当前验证：
  - 定向测试已通过 `CityC8ArrangementEngineTest`
  - 项目编译通过
- 当前剩余问题：
  - 当前节点校验仍是首版轻量实现，后续可继续把更多旧引擎中的精细连接器/模板规则迁回新节点流。

## 2026-04-02 02:31:00
- 已完成 `C9` 队列消费逻辑和控制器入口改造。
- 具体改动：
  - `CityC9BuildQueue` 现在优先从 `validated_nodes` 中提取可执行 placement，而不是只依赖旧 `placements`。
  - `CityC9Stages` 在输出 `PlacedStructure` 时补了 `runtime_error_code` 与 `runtime_error_details`，并把人可读 `reason` 改成中文。
  - `CityBuildQueueExecutor` 在失败时补写中文 `last_error_message`。
  - `CityController` 新增 `handleCityC8Submit`、`handleCityC8Retry`，并扩充 `C7/C8` 返回内容。
  - `ModHttpServer` 新增 `/city_c8_submit`、`/city_c8_retry` 路由。
- 这样改是为了让外部调用方可以真正按“工头计划 -> 节点提交 -> 程序校验 -> 已校验节点入队 -> C9 执行”的新流程推进。
- 影响模块：
  - `src/main/java/com/user/terra_script/world/city/stage/c9/CityC9BuildQueue.java`
  - `src/main/java/com/user/terra_script/world/city/stage/c9/CityC9Stages.java`
  - `src/main/java/com/user/terra_script/world/city/CityBuildQueueExecutor.java`
  - `src/main/java/com/user/terra_script/server/mcp/CityController.java`
  - `src/main/java/com/user/terra_script/server/ModHttpServer.java`
  - `src/test/java/com/user/terra_script/world/city/stage/c9/CityC9StagesTest.java`
- 当前验证：
  - 运行 `./gradlew test --tests com.user.terra_script.world.city.stage.c7.CityC7StagesTest --tests com.user.terra_script.world.city.stage.c8.CityC8ArrangementEngineTest --tests com.user.terra_script.world.city.stage.c9.CityC9StagesTest --tests com.user.terra_script.world.city.stage.c9.CityC9BuildQueueTest --tests com.user.terra_script.server.mcp.CityControllerTest`
  - 上述定向测试全部通过。
- 当前剩余问题：
  - 仓库工作区里存在一批与本任务无关的既有删除文件，本轮未触碰，也未纳入当前改造。

## 2026-04-02 02:45:00
- 已使用真实游戏内的本地 MCP/HTTP 接口做首轮联调验收，目标城市为 `city_3120_-3200`，重点测试 `g_market_03`。
- 实际执行与结果：
  - 调用 `/city_c7_generate`（group 级）成功，返回 `foreman_plan_count = 1`、`phase_count = 4`，说明 `C7` 新工头计划结构已在运行中的游戏代码里生效。
  - 第一次调用 `/city_c8_generate`（group 级）返回 `session_state = FAILED`，错误为 `missing_start_node`。
  - 继续调用 `/city_c7_generate`（city 级）后，再次调用 `/city_c8_generate`（group 级）成功，返回 `session_state = READY_FOR_AI`，且 `active_node` 已带上：
    - `phase_key = phase1`
    - `phase_name = 先修前庭`
    - 中文 `target_role / target_structure_kind`
  - 调用 `/city_c8_submit` 成功，起始节点已进入 `validated_nodes`，并自动生成两个子节点，当前 `queue_summary` 为：
    - `ready_for_ai_count = 2`
    - `validated_count = 1`
    - `total_count = 3`
- 这说明以下链路在真实运行中的游戏环境里已经成立：
  - `C7` 工头计划输出
  - `C8` 进入单节点施工会话
  - `C8` 节点提交后通过程序校验并写入 `validated_nodes`
- 同时也发现一个新的真实问题：
  - `/city_c8_data` 读取到的 group 级 `c8_foundation.json` 仍然是旧失败内容。
  - `/city_c9_generate` 在当前 group 下没有把刚提交成功的 `validated_nodes` 转成新队列任务，返回 `enqueued_tasks_count = 0`。
  - `/city_c9_queue_data` 读到的仍是该 group 历史遗留旧任务，而不是本轮新节点。
- 当前判断：
  - 新接口和内存态主流程已经跑通。
  - 但 group 级 `C8` 持久化结果与 `C9` 读取结果之间，仍存在“写入到哪里/读取优先级/旧产物覆盖”的真实联调问题，需要下一轮继续修。

## 2026-04-02 03:02:00
- 根据用户反馈，已将阶段自然语言从固定模板改为“按排列骨架 + 主体功能”动态生成。
- 具体改动：
  - `CityC7Stages.defaultPhasePlans(...)` 不再固定输出“先修前庭 / 修主厅 / 补两侧附属 / 修花园收尾”。
  - 现在会按 `arrangement_type` 生成更贴近结构骨架的阶段话术，例如：
    - `SPINE_BRANCH` -> `先修中轴线 / 再补两侧附属 / 再接远端延伸 / 最后做外围收尾`
    - `COURTYARD / RING` -> `先立入口与前场 / 再围主厅院心 / 再补两侧附属 / 最后收外围与花园`
    - `LINEAR_DOCK` -> `先定沿岸主线 / 再顺主线向前推进 / 再补侧向附属 / 最后收边缘与端头`
  - `CityC8Stages` 的子节点推进也改为优先读取 `ForemanPlan.phase_list` 中的阶段名称，不再回退到固定 `phaseName` 文案。
- 当前验证：
  - 定向测试重新运行通过。
- 当前限制：
  - 由于游戏进程中的类不会热更新，真实 MCP 运行态要看到新的 `phase_name`，仍需要用户重启当前这次游戏/服务后再做一轮接口验收。
