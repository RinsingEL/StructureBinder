# 本次需求文档

## 用户本次需求摘要
用户要求直接实现“C8/C9 工头计划化重构实施计划（阶段自然语言版）”，将现有 `C7/C8/C9` 流程改造为工头计划驱动的节点式施工流程，并同步把阶段、目标、备注、错误说明等自然语言内容统一改成中文表达。

## 用户特别强调的约束
- `C7` 要输出工头计划，`C8` 要负责节点施工编排与校验，`C9` 要负责已批准节点落地。
- 阶段字段采用 `phase_key + phase_name` 双字段。
- `phase_name` 要是自然语言，例如“先修前庭”“修花园收尾”。
- 参数中的自然语言类型值统一使用中文，枚举除外。
- 代码正式实现时，在定义处使用中文注释说明字段与职责。

## 不做的事情
- 不继续沿旧的 `C8` 一次性完整 placements 主流程扩展能力。
- 不保留旧的 `C9` 自动 terminalize 祖先继续执行作为默认行为。
- 不在本轮实现多 `start`、并发施工、多工区并发推进。

## 当前决定的工作边界
- 保留原有 `C7/C8/C9` 主文件名与 `CityController` 路由，不整体推翻 API 表面。
- 主体工作集中在数据结构、控制器语义、队列执行逻辑和必要测试改造。
- 新增说明性字段统一中文；协议稳定值继续保持英文。
