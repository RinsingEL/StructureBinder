# 用户本次需求摘要
用户希望直接运行新 survival C1，使用以前老的几个城市配置产出 C1 预览图，然后关闭 Minecraft。

# 用户特别强调的约束
- 可以直接运行。
- 产出一张 C1 预览图。
- 运行后关闭 MC。
- 优先使用已有 MCP/旧城市。

# 不做的事情
- 不提交 git。
- 不继续改实现代码，除非运行发现必须修复。

# 当前决定的工作边界
以本地运行和产物生成为主，必要时读取旧城市配置并调用 HTTP MCP 入口。
