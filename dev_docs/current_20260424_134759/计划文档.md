# 本次任务标题
运行 survival C1 并产出预览图

# 最终计划摘要
复用或启动 Minecraft dev client/MCP HTTP 服务，使用旧城市配置调用 /city_survival_c1_generate，产出 C1_boundary_preview.png，随后关闭 Minecraft 进程并汇总产物路径。

# 实施步骤
1. 创建本轮 AI 工作流过程文档。
2. 检查本地 MCP/HTTP 服务是否已运行。
3. 从旧城市或保存文件提取可用 CityConfig。
4. 调用 /city_survival_c1_generate 生成 C1 预览。
5. 确认产物存在并关闭 Minecraft/dev client。

# 验收标准
- 成功产出至少一张 C1_boundary_preview.png。
- 返回结果包含 C1_BoundaryPlan.json 和 validation report。
- 操作完成后关闭本轮启动或复用的 MC 进程。
- 若服务无法启动或缺少旧城市配置，明确说明阻塞原因。

# 关键假设
- MCP HTTP 服务端口沿用当前项目默认 127.0.0.1:5000。
- 旧城市可从 	erra_script_cities.json 或现有城市目录推断配置。

