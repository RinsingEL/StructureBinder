# 本次任务标题
删除 MC 自动启动 skill 并同步 TerraSense 新导出配置

# 最终计划摘要
移除最近新增的 MC 自动启动占位 skill，检查 TerraSense 最近提交与开发说明，确认 C3.5 输出结构变化后，将外部成功导出的 `config/terrasense` 结果同步到 StructureBinder 的 `template_output`，为后续 jigsaw solver 测试做准备。

# 实施步骤
1. 定位并删除最近新增的 MC 自动启动相关 skill。
2. 检查 `E:\Mod_Dev\TerraSense` 最近 git 提交与相关说明文档。
3. 确认 StructureBinder 当前消费 TerraSense 产物的目录与文件口径。
4. 将 `D:\PCL2-2.8.12\.minecraft\versions\1.20.1-mod开发测试\config\terrasense` 导出内容同步到目标目录。
5. 记录本轮修改、来源与验证结果。

# 验收标准
- MC 自动启动相关 skill 已从本地 skill 目录移除。
- 已确认 TerraSense 最近输出结构的主要变化点。
- 外部导出的 TerraSense 配置已同步到 StructureBinder 正在消费的目录。
- `dev_docs/current_20260409_005803` 下三份过程文档齐全且记录完整。

# 关键假设
- 用户提到的“最近写的 mc 自动启动 skill”即 `mc-restart-placeholder`。
- StructureBinder 当前仍通过仓库根目录 `template_output` 消费 C3.5 catalog 与 function enum。
- 外部导出的 `config/terrasense` 是本轮希望回灌到 StructureBinder 的最新有效结果。
