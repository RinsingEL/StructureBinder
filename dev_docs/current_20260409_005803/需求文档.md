# 用户本次需求摘要
1. 删除最近新增的 MC 自动启动 skill。
2. 去 `E:\Mod_Dev\TerraSense` 看最近 git 变化和可能存在的开发日志说明。
3. 把已经成功导出的 TerraSense 配置从 `D:\PCL2-2.8.12\.minecraft\versions\1.20.1-mod开发测试\config\terrasense` 运到当前测试链路需要的位置。
4. 准备好后继续测试 jigsaw solver。

# 用户特别强调的约束
- 看 TerraSense 先以 git 为主。
- 外部导出的那份配置已经成功，需要优先回灌。
- 当前目标是尽快进入 jigsaw solver 测试。

# 不做的事情
- 不扩写文档真值仓库。
- 不假装已有可用的 MC 自动启动自动化。
- 不在未确认需求前修改 solver 运行时逻辑本身。

# 当前决定的工作边界
- 在 StructureBinder 侧处理导入与过程文档。
- 在 TerraSense 侧仅做只读检查，不直接改代码。
- 在本地 Codex skill 目录删除指定 skill。
