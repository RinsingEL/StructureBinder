# 修改记录

## 2026-04-09 00:58:03
- 开始处理“删除 MC 自动启动 skill、检查 TerraSense 输出变化并回灌导出配置”任务。
- 原因：
  - 需要先确认 skill 的实际目录、TerraSense 最近是否有已提交的数据结构变更，以及 StructureBinder 当前真实消费路径。
- 涉及模块或文档：
  - `C:\Users\Rinsing\.codex\skills\mc-restart-placeholder`
  - `E:\Mod_Dev\TerraSense`
  - `E:\Mod_Dev\StructureBinder\template_output`
  - `dev_docs/current_20260409_005803/*`
- 已做验证：
  - 确认本地 skill 目录存在 `mc-restart-placeholder`
  - 确认 `E:\Mod_Dev\TerraSense` 工作区当前干净
  - 确认外部导出目录存在完整 `config/terrasense` 文件集
  - 确认 StructureBinder 通过 `template_output` 读取 `C3_5_StructureCatalog.preprocessed.json` 与 `C3_5_FunctionEnumTable.json`
- 当前还剩问题：
  - 待完成 skill 删除、TerraSense 提交回看、配置同步与最终差异复核。

## 2026-04-09 01:12:00
- 已删除本地占位 skill：`C:\Users\Rinsing\.codex\skills\mc-restart-placeholder`。
- 已回看 TerraSense 最近提交与说明：
  - `6c9f98e Add C3.5 structure labeling pipeline`
  - `2b7770b TerraSense-v0.1.0版本-补充拼图方块pool导出字段`
- 已确认 TerraSense 仓库当前新增了 C3.5 相关正式文档与实现入口：
  - `C35_SCHEMA_REFERENCE.md`
  - `C3_5_OUTPUT_RULES_V2.md`
  - `src/main/java/com/rin/terrasense/c35/C35Pipeline.java`
- 原因：
  - 需要先核实用户提到的“生成数据结构已改”到底是哪些提交与哪些字段方向，避免盲目把旧口径文件搬进来。
- 涉及模块或文档：
  - `E:\Mod_Dev\TerraSense\C35_SCHEMA_REFERENCE.md`
  - `E:\Mod_Dev\TerraSense\C3_5_OUTPUT_RULES_V2.md`
  - `E:\Mod_Dev\TerraSense\src\main\java\com\rin\terrasense\c35\C35Pipeline.java`
- 已做验证：
  - 确认 TerraSense 文档口径声明 `catalog_version = 2`
  - 确认 TerraSense 代码中的 `toCatalogStructure()` 与 `normalizeStructure()` 目标是输出完整的 `orientation / placement / constraints / connectors`
- 当前还剩问题：
  - 待核对外部导出的实际 catalog 是否真的包含这些字段，再决定如何回灌。

## 2026-04-09 01:20:00
- 已将外部导出的 TerraSense 文件同步到 `E:\Mod_Dev\StructureBinder\template_output`，但主 catalog 采用了“兼容合并”而不是整包覆盖。
- 本轮同步策略：
  - `C3_5_StructureLabelQueue.json`
  - `C3_5_StructureScanDebug.json`
  - `C3_5_PresetMatchReport.json`
  - `c3_5_preset_rules.json`
  - `c3_5_manual_overrides.json`
  - 上述文件直接从外部导出覆盖同步
  - `C3_5_StructureCatalog.preprocessed.json` 改为用外部导出做主、并对能匹配到旧条目的结构保留旧版 `orientation / piece_role / connector_types / connector_dirs / allowed_neighbors / placement / constraints / connectors / weight_profile`
  - `C3_5_FunctionEnumTable.json` 与 `C3_5_StructureCatalog.by_function/*.json` 依据合并后的正式 catalog 重建
- 原因：
  - 外部导出的正式 catalog 实际只有 `221` 条结构，而旧 `template_output` 中有 `238` 条。
  - 外部导出的结构对象缺少 `orientation / piece_role / placement / constraints / connectors` 等当前 solver 仍在依赖的字段，不能直接整包覆盖。
- 涉及模块或文档：
  - `template_output/C3_5_StructureCatalog.preprocessed.json`
  - `template_output/C3_5_FunctionEnumTable.json`
  - `template_output/C3_5_StructureCatalog.by_function/*.json`
  - `template_output/C3_5_StructureLabelQueue.json`
  - `template_output/C3_5_StructureScanDebug.json`
  - `template_output/C3_5_PresetMatchReport.json`
  - `template_output/c3_5_preset_rules.json`
- 已做验证：
  - 合并后正式 catalog 共 `243` 条结构
  - 其中 `22` 条为旧 catalog 独有条目并被保留
  - 其中 `5` 条为外部导出新增条目，但没有旧版可继承的 `connectors / placement / orientation`
  - 合并后有 `238` 条结构显式保留了 `connectors / placement / constraints / orientation`
  - 合并后有 `166` 条结构的 `connectors` 非空
- 当前还剩问题：
  - 新增的 `5` 条结构当前仍只有轻量字段，没有旧版 connector 空间字段可继承；如果后续需要把它们纳入 jigsaw solver 候选，仍需 TerraSense 侧重新导出完整字段。
