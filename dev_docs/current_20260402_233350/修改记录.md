# 修改记录

## 2026-04-02 23:33:50

- 本轮修改了什么
  - 新建 `dev_docs/current_20260402_233350/`。
  - 创建 `计划文档.md`、`需求文档.md`、`修改记录.md` 三份文档，作为本轮实际结构落地测试依据。
- 为什么改
  - 项目要求每轮正式开工前补齐 AI 工作流文档，并在验收前显式回读。
- 改到了哪些模块或文档
  - `dev_docs/current_20260402_233350/计划文档.md`
  - `dev_docs/current_20260402_233350/需求文档.md`
  - `dev_docs/current_20260402_233350/修改记录.md`
- 做了什么验证
  - 已确认用户目标是继续执行到 C9 实际结构落地。
- 当前还剩什么问题
  - 尚未确认 C9 实际落地入口与真实调用参数。

## 2026-04-02 23:45:00

- 本轮修改了什么
  - 未新增代码修改，完成了 `g_market_04` 的真实 C9 落地测试。
- 为什么改
  - 需要把上一轮 C8 联调继续推进到实际结构写入世界，而不是停留在队列生成或 dry run。
- 改到了哪些模块或文档
  - `dev_docs/current_20260402_233350/修改记录.md`
- 做了什么验证
  - 读取 `CityController.handleCityC9Generate` 与 `CityC9Stages.Mode`，确认只有传 `mode=APPLY_NOW` 才会立即执行真实落地；旧 `apply_blocks=true` 仅映射到 `ENQUEUE`，不会直接写入世界。
  - 对 `city_3120_-3200 / g_market_04` 调用 `POST /city_c9_generate`，参数为 `mode=APPLY_NOW`、`max_blocks=10`，服务端返回 `status=ok`，`mode=apply_now`。
  - 读取 `run/saves/新的世界 (2)/terra_script/cities/city_3120_-3200/C9_BuildQueue.json`，确认任务 `city_3120_-3200|ba_g_market_04_a010|start_g_market_04` 已写入队列。
  - 读取 `run/saves/新的世界 (2)/terra_script/cities/city_3120_-3200/groups/g_market_04/c9_apply_preview.legend.json`，确认 `start_g_market_04` 被标记为 `status=done` 且 `placed=true`，说明真实落地已发生。
  - 读取 `run/saves/新的世界 (2)/terra_script/cities/city_3120_-3200/groups/g_market_04/c9_placement.json`，确认本轮落地针对的是新版工头流节点 `start_g_market_04`，而不是旧版 `p1/p2` 节点。
- 当前还剩什么问题
  - 当前发现一处状态回写不一致：`c9_placement.json` 里仍显示 `placed=false`、`applied_tasks_count=0`，但 `C9_BuildQueue.json` 与 `c9_apply_preview.legend.json` 已显示任务完成且 `placed=true`。这更像是落地后的 placement 文件没有同步刷新，而不是落地本身失败。
  - 当前 MCP 侧 `city_c9_generate` 仍只暴露 `apply_blocks`，无法直接通过 MCP 传入 `mode=APPLY_NOW`；本轮真实落地是通过直接 HTTP 调用完成的。

## 2026-04-02 23:55:00

- 本轮修改了什么
  - 在 Java 侧新增 C9 placement 与最新队列状态的同步逻辑。
  - 在 MCP 侧为 `city_c9_generate` 补充 `mode` 参数透传，允许直接指定 `apply_now`。
  - 补充了 C9 队列同步测试。
- 为什么改
  - 修复真实落地后 `c9_placement.json` 仍停留在“已入队未落地”视图的问题。
  - 修复 MCP 无法直接触发 `APPLY_NOW` 落地模式的问题。
- 改到了哪些模块或文档
  - `src/main/java/com/user/terra_script/world/city/stage/c9/CityC9Stages.java`
  - `src/main/java/com/user/terra_script/server/mcp/CityController.java`
  - `country_designer_mcp/src/city/handlers.ts`
  - `country_designer_mcp/src/city/tools.ts`
  - `src/test/java/com/user/terra_script/world/city/stage/c9/CityC9StagesTest.java`
  - `dev_docs/current_20260402_233350/修改记录.md`
- 做了什么验证
  - 执行 `./gradlew test --tests "com.user.terra_script.world.city.stage.c9.CityC9StagesTest"`，结果通过。
  - 执行 `country_designer_mcp` 的 `npm run build`，结果通过。
  - 从 `dist/src/city/tools.js` 读取 `city_c9_generate` 定义，确认输入 schema 已包含 `mode` 参数和 `apply_now` 枚举值。
  - 从 `dist/src/city/handlers.js` 读取 `city_c9_generate`，确认 handler 已成功导出。
- 当前还剩什么问题
  - 对当前已运行的游戏实例再次回读 `city_c9_data` 时出现了 `502 Bad Gateway`，而磁盘中的 `c9_placement.json` 仍保持旧视图；结合 Java 单测已通过这一点，推断更可能是当前运行中的游戏进程尚未加载到最新 Java 修复，而不是源码修复本身无效。
  - 因此，`c9_placement.json`/`c9_data` 的真机修复验证仍需要在重启游戏或重新加载当前 Java 变更后再做一次回归确认。

## 2026-04-03 00:05:00

- 本轮修改了什么
  - 未新增代码修改，完成了重启游戏后的真机回归验证。
- 为什么改
  - 需要确认上一轮 C9 状态同步修复与 MCP `mode=apply_now` 透传修复已经在真实运行实例中生效。
- 改到了哪些模块或文档
  - `dev_docs/current_20260402_233350/修改记录.md`
- 做了什么验证
  - 重新探测 `http://127.0.0.1:5000/workflow/status`，确认当前游戏内 HTTP 服务已正常返回 `200`。
  - 读取 `city_3120_-3200 / g_market_04` 的 `POST /city_c9_data`，确认返回中的 `placement` 已显示 `placed=true`、`placed_structures=1`、`applied_tasks_count=1`，同步后的 `reason=placed_from_c9_queue`。
  - 直接读取 `run/saves/新的世界 (2)/terra_script/cities/city_3120_-3200/groups/g_market_04/c9_placement.json`，确认磁盘文件也已同步为 `placed=true` 与 `applied_tasks_count=1`，说明 placement 文件回写问题已修复。
  - 通过 MCP 侧 `city_c9_generate` handler 以 `mode=apply_now` 再次调用 `g_market_04`，服务端成功返回 `mode=apply_now`、`applied_tasks_count=1`，说明 MCP 已可直接透传真实落地模式。
- 当前还剩什么问题
  - 当前这两项修复已完成真机回归，未发现新的阻塞问题。

## 2026-04-03 00:25:00

- 本轮修改了什么
  - 未新增代码修改，继续把 `g_market_04` 的主模块施工从已落地的起始节点推进到 `phase2` 主线节点。
- 为什么改
  - 用户要求继续测试，尽量把这组主模块推进到“修完”为止，需要验证 `phase2` 主线节点能否真实落地。
- 改到了哪些模块或文档
  - `dev_docs/current_20260402_233350/修改记录.md`
- 做了什么验证
  - 通过 `city_c8_submit` 成功将 `start_g_market_04_child_1` 提交为 `VALIDATED`，坐标为 `3065,-3210`，随后又尝试把它调整为 `3070,-3226`。
  - 通过 `city_c8_submit` 成功将 `start_g_market_04_child_2` 提交为 `VALIDATED`，坐标为 `3045,-3210`；随后尝试调整到 `3039,-3226` 时被 C8 直接判定为 `out_of_area`。
  - 多次执行 `city_c9_generate(mode=APPLY_NOW)` 与 `city_c9_data` 回读，确认当前 `start_g_market_04` 已真实落地，但两个 `phase2` 主线节点 `start_g_market_04_child_1`、`start_g_market_04_child_2` 均在 C9 阶段进入 `runtime_footprint_collision`，队列状态为 `BLOCKED`。
  - 当前 `c9_apply_preview.legend.json` 与 `C9_BuildQueue.json` 已明确显示：主模块起始节点 `DONE`，两个主线子节点 `BLOCKED`。
- 当前还剩什么问题
  - 当前这组“主模块修完”测试没有完全成功，卡在 `phase2` 两个主线节点的运行时结构碰撞上，因此主模块还未整体修完。
  - 在对已校验节点执行 `city_c8_retry` 后再次提交时，`c8_data` 中会同时保留旧状态节点和新的 `VALIDATED` 节点，暴露出节点重试/去重的一致性问题；这会增加后续联调复杂度。

## 2026-04-03 00:40:00

- 本轮修改了什么
  - 调整 `CityC9PlacementPreviewExporter`，让 `c9_apply_preview` 与 legend 中的结构矩形优先使用 `StructureInjector.placementBounds(...)` 计算出的真实放置边界，而不是继续使用 C8 节点里近似的点状 footprint。
- 为什么改
  - 用户指出当前地图预览图难以理解，希望按 Minecraft 实际 `place` 规则绘制正确矩形，便于判断真实碰撞与占地区域。
- 改到了哪些模块或文档
  - `src/main/java/com/user/terra_script/world/city/stage/c9/CityC9PlacementPreviewExporter.java`
  - `dev_docs/current_20260402_233350/修改记录.md`
- 做了什么验证
  - 执行 `./gradlew compileJava`，结果通过。
- 当前还剩什么问题
  - 需要在重启后的游戏实例中重新导出一次 `c9_apply_preview`，确认新导出的 preview 与 legend 已切换到真实 MC 结构占地语义。

## 2026-04-03 00:50:00

- 本轮修改了什么
  - 在 `StructureInjector` 中为真实结构放置日志补充了实际占地的两个对角坐标打印。
- 为什么改
  - 用户希望能直接从日志里看到结构实际落地的矩形边界，便于自行对照世界验证。
- 改到了哪些模块或文档
  - `src/main/java/com/user/terra_script/world/StructureInjector.java`
  - `dev_docs/current_20260402_233350/修改记录.md`
- 做了什么验证
  - 执行 `./gradlew compileJava`，结果通过。
- 当前还剩什么问题
  - 日志打印代码已落地，但需要在重启后的运行实例中实际触发一次结构放置后，才能看到新的 bounds 日志输出。
