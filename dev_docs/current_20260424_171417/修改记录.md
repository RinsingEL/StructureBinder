# C1城市边界生成运行态测试修改记录

## 2026-04-24 17:14

- 修改了什么：创建本轮 AI 工作流记录目录与三份开工文档。
- 为什么改：满足项目开工前必须记录计划、需求与修改过程的要求。
- 改到了哪些模块或文档：`dev_docs/current_20260424_171417/计划文档.md`、`需求文档.md`、`修改记录.md`。
- 做了什么验证：待后续执行文档读取与运行态 client 测试。
- 当前还剩什么问题：需要定位 W/T 文档与 C1 client 测试入口。

## 2026-04-24 17:18

- 本轮修改了什么：读取文档仓库 W3/W4、T1/T2/T4、T3 单大陆入口和 C1 城市边界生成验收文档，确认 C1 前置要求。
- 为什么改：用户要求先看 W 步骤和 T 步骤文档，并据此跑一次 C1 运行态 client 测试。
- 改到了哪些模块或文档：仅读取 `E:\Mod_Dev\designer_territoryMod\docs\20_contracts\world\w3_w4.md`、`docs\20_contracts\territory\t1_t2_t4.md`、`docs\20_contracts\territory\t3_run_continent.md`、`docs\40_tests\city\main_module\C1城市边界生成验收.md`；实现仓库新增运行态证据文件 `mcp_runtime_client.out.log`、`mcp_runtime_client.err.log`、`c1_runtime_headers.txt`、`c1_runtime_response.json`。
- 做了什么验证：启动 `.\gradlew.bat --no-daemon runClient`，自动进入 `新的世界 (2)` 后 `GET /workflow/status` 返回 200；W3/W4/T1/T2 为 DONE，T3/T4 为 NOT_STARTED。
- 当前还剩什么问题：运行态目标大陆 c15 当前 T3 主权面积为空，T4 dat 缺失，尚不具备 C1 成功生成自然边界的前置条件。

## 2026-04-24 17:20

- 本轮修改了什么：按 W/T 文档前置检查运行态国度状态，并调用 C1 survival 边界生成接口。
- 为什么改：确认 C1 在空主权上下文下是否会拒绝继续生成，而不是误用旧 claimed chunks 或空 T3 结果。
- 改到了哪些模块或文档：未改业务代码；记录响应到 `dev_docs/current_20260424_171417/c1_runtime_headers.txt` 与 `c1_runtime_response.json`。
- 做了什么验证：`GET /territory/summary?territoryId=r15_test_01&continentId=15` 返回 `total_area=0`；正确格式的 `POST /territory/t4_window` 返回 404 `T4 dat not found`；`POST /t3_run_continent {"continent_id":15}` 返回 409，`claimed_total=0`、`wild_total=0`；`POST /city_survival_c1_generate` 返回 409，响应为 `status=fail_safe`、`blocking_errors=["no_owned_non_overlapping_chunks"]`、`warnings=["requested_center_outside_sovereignty"]`、`claimed_chunk_count=0`。
- 当前还剩什么问题：本轮完成的是运行态失败路径验收；若要验收 C1 成功生成 `C1_BoundaryPlan.json` 与 `C1_boundary_preview.png`，需要先修复或重建 W3/W4 -> T3/T4 使目标国度 `total_area > 0`。

## 2026-04-24 17:22

- 本轮修改了什么：关闭本轮启动的 dev client/Gradle Java 进程，并补跑 C1 planner 聚焦单测。
- 为什么改：避免遗留运行态进程，同时确认算法层测试仍通过。
- 改到了哪些模块或文档：未改业务代码。
- 做了什么验证：确认 `127.0.0.1:5000` 端口关闭；执行 `.\gradlew.bat test --tests "*CitySurvivalBoundaryPlannerTest"`，结果 `BUILD SUCCESSFUL`。
- 当前还剩什么问题：`mcp_runtime_client.err.log` 中有一次由于本轮手工 curl JSON 转义错误造成的 `MalformedJsonException` 记录，随后已用正确 JSON 重试并得到预期 404；该错误不代表 C1 运行态失败原因。

## 2026-04-24 17:35

- 本轮修改了什么：按用户提示检查 `run/saves/新的世界 (2)/terra_script/4304716556552747623/territory` 下的历史 T 阶段产物。
- 为什么改：用户指出该存档按理应已有数据，需要确认运行态 C1 判空是否是数据缺失还是读写对象不一致。
- 改到了哪些模块或文档：未改业务代码；补充记录本次数据排查结论。
- 做了什么验证：确认 `r15_test_01/T3/TerritorySummary.json` 是历史有效 T3，`claimed_count=2342`、`wild_count=4`；确认当前实例目录 `r15_test_01_c15/T3/TerritorySummary.json` 是空结果，`claimed_count=0`、`wild_count=0`，且在本轮 17:17 重跑 T3 时被写入；确认该 territory 根目录下没有任何 `T4` 目录；确认运行态注册配置来自 `run/config/terra_script_territories.json`，当前注册的是 `r15_test_01@c15`、`w4_smoke_01@c15`、`w4_smoke_02@c15`。
- 当前还剩什么问题：老的基础 ID `r15_test_01` T3 数据没有自动载入当前 `TerritoryManager.results`，当前 C1 查的是内存主权 `TerritoryManager.isChunkWithinSovereignty(...)`，因此仍看不到历史基础 ID 的 2342 个 chunk；若要复用老数据，需要做一次有边界的导入/迁移或让 T3 在当前 `@c15` 实例上重新产出非零面积，再跑 T4。

## 2026-04-24 17:45

- 本轮修改了什么：核对 T3/T4 触发入口、MCP 包装层和运行态数据加载路径。
- 为什么改：用户追问当前问题到底是代码加载问题，还是没有暴露 T3/T4 数据获取/触发接口。
- 改到了哪些模块或文档：未改业务代码；记录判断依据。
- 做了什么验证：确认 `/t3_run_continent` 已暴露，可按大陆触发 T3；确认 `/workflow/run` 可触发 T4，但 HTTP controller 只读取 `stageId`，文档仓库当前写的是 `stage_id`，MCP wrapper 会把 `stage_id` 转成 `stageId`，直接 HTTP 调用则会错跑默认 T1；确认 T4 选项代码读取 `t4_sample_stride/t4_max_chunks/t4_loaded_only`，而文档示例写的是 `sample_stride/max_chunks_per_territory/loaded_only`；确认 `TerritoryResultRepository` 只有 `readSummary/readT4Dat`，没有把 T3 `TerritoryResult.dat` 反序列化回 `TerritoryManager.results` 的加载入口；确认 `TerritoryManager.ensureLoaded()` 只加载 `run/config/terra_script_territories.json` 中的 territory config，不加载历史 T3 结果。
- 当前还剩什么问题：当前主要是代码的数据生命周期/接口契约问题：T3/T4 运行触发存在，但缺少“从磁盘 T3/T4 产物恢复运行态主权”的入口，同时 HTTP 文档字段与 controller 字段不一致，容易导致手动触发 T4 实际没有发生。
