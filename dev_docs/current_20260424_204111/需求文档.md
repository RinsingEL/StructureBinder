# T3/T4运行态恢复与触发接口修复需求

## 用户本次需求摘要

用户确认当前问题属于 bug，并要求按已确认计划实现修复：恢复 T3 磁盘结果到运行态、修正 T3/T4 触发接口兼容、增加显式 T3 导入入口、防止零面积覆盖有效产物，并同步文档。

## 用户特别强调的约束

- 必须先开计划并记录本轮过程。
- 默认采用“显式导入”策略，不自动兼容旧基础 ID 数据。
- 不提交代码，除非用户另行要求。
- 双仓库边界必须清楚：实现仓库改代码与过程记录，文档仓库改正式契约与验收入口。

## 不做的事情

- 不把文档仓库的正式方案回写到实现仓库旧文档体系。
- 不自动迁移所有历史 T3 产物。
- 不提交与本任务无关的既有工作区改动。

## 当前决定的工作边界

- 实现仓库：HTTP/MCP 接口、T3 dat 读写、运行态恢复、测试、`dev_docs` 记录。
- 文档仓库：T3/T4 契约、代码导览、测试验收说明。
