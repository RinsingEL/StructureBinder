# T3/T4运行态恢复与触发接口修复修改记录

## 2026-04-24 20:41

- 修改了什么：创建本轮 AI 工作流记录目录与三份开工文档。
- 为什么改：满足项目开工前必须记录计划、需求与修改过程的要求。
- 改到了哪些模块或文档：`dev_docs/current_20260424_204111/计划文档.md`、`需求文档.md`、`修改记录.md`。
- 做了什么验证：待后续执行代码与文档修改。
- 当前还剩什么问题：尚未实现接口兼容、T3 恢复、导入入口和文档同步。

## 2026-04-25 01:40

- 修改了什么：完成实现仓库代码修复，补齐 `/workflow/run` 的 `stage_id/stageId` 与 T4 参数别名兼容，新增 T3 dat 读取/解码、运行态 T3 恢复、`/territory/t3_import` 入口，以及零面积 T3 不覆盖已有非零 canonical 产物的保护。
- 为什么改：解决“磁盘已有 T3 数据但 C1 运行态看不到主权”的核心 bug，并避免后续再次被零面积 T3 覆盖有效结果。
- 改到了哪些模块或文档：`src/main/java/com/user/terra_script/server/mcp/WorkflowController.java`、`TerritoryController.java`、`CityController.java`、`src/main/java/com/user/terra_script/server/ModHttpServer.java`、`src/main/java/com/user/terra_script/territory/io/TerritoryResultRepository.java`、`src/main/java/com/user/terra_script/world/TerritoryManager.java`、`src/main/java/com/user/terra_script/domain/territory/stage/T4Stage.java`。
- 做了什么验证：`.\gradlew.bat test --tests "*WorkflowControllerTest" --tests "*TerritoryResultRepositoryTest" --tests "*TerritoryControllerTest" --tests "*CityControllerTest" --tests "*TerritoryWorkflowStagesTest"` 通过。
- 当前还剩什么问题：尚未完成文档仓库同步与真机运行态整链回归记录。

## 2026-04-25 01:41

- 修改了什么：同步文档仓库中的 T3/T4 契约、代码导览与验收入口，明确 `workflow/run` 字段兼容、`/territory/t3_import` 用法、T4 不再隐式重跑 T3、零面积 T3 保护与重启后的自动恢复验收方式。
- 为什么改：根据双仓库规则把本轮已经落地的正式行为变化同步到文档真值仓库。
- 改到了哪些模块或文档：`E:\Mod_Dev\designer_territoryMod\docs\20_contracts\territory\t1_t2_t4.md`、`t3_run_continent.md`、`docs\30_code_guide\territory\t1_t2_t4.md`、`docs\40_tests\territory\t1_t2_t4.md`。
- 做了什么验证：回读文档内容，确认与当前实现一致。
- 当前还剩什么问题：需要补最终运行态验收结论与残余风险。

## 2026-04-25 01:42

- 修改了什么：完成真实 `runClient` 回归，先通过 `/territory/t3_import` 把 `r15_test_01 -> r15_test_01@c15` 导入一次，随后重启 client 验证 `/territory_status` 自动恢复当前实例主权，再用文档字段格式调用 `/workflow/run {"stage_id":"T4","sample_stride":2,"max_chunks_per_territory":-1,"loaded_only":false}` 跑完 T4，并重跑 `/city_survival_c1_generate`。
- 为什么改：验证本轮修复不是停留在单测，而是能真正打通“磁盘 T3 -> 运行态 -> T4 -> C1”整链。
- 改到了哪些模块或文档：运行态存档 `run/saves/新的世界 (2)/terra_script/4304716556552747623/territory/r15_test_01_c15/T3`、`T4`，以及 `run/saves/新的世界 (2)/terra_script/cities/city_3120_-3200/C1_*` 产物。
- 做了什么验证：
  - `POST /territory/t3_import` dry-run 返回 `claimed_chunks=2342`、`wild_chunks=4`；真实导入返回 `runtime_restored=true`。
  - 重启后 `GET /territory_status` 中 `r15_test_01@c15 total_area=2342`，说明自动恢复生效。
  - `POST /workflow/run` 使用 `stage_id/sample_stride/max_chunks_per_territory/loaded_only` 成功跑完 T4，`GET /territory/summary?territoryId=r15_test_01&continentId=15` 返回 `step=T4`。
  - `POST /territory/t4_window {"territory_id":"r15_test_01@c15"}` 返回 `200`。
  - `POST /city_survival_c1_generate` 返回 `200`，`status=created`、`boundary_status=pass`、`fallback_used=false`、`reanchored=true`，并刷新 `C1_BoundaryPlan.json / C1_validation_report.json / C1_boundary_preview.png`。
- 当前还剩什么问题：`/territory/t4_window` 当前窗口返回 `matched_cells=0`，但 T4 summary 已成功写出且 C1 已进入成功路径；这说明窗口取样中心与 T4 网格之间可能仍有一层可继续优化的对齐问题，可作为后续单独排查项。

## 2026-04-25 02:00

- 修改了什么：继续排查 `/territory/t4_window matched_cells=0`，确认真实 `T4 dat` 坐标正常后，在 `TerritoryController` 中新增默认窗口中心的 reanchor 逻辑：未显式传 `center_x/z` 且默认 capital 不在 T4 覆盖区时，自动吸附到最近 T4 采样点，并在响应中回传 `warnings`、`window.reanchored`、`requested_center_x/z` 与 `nearest_distance_blocks`；同时补充 `TerritoryControllerTest`。
- 为什么改：根因不是 T4 dat 损坏，而是当前 `r15_test_01@c15` 的 capital 距最近 T4 采样点约 331.7 格，超出了默认 `radius_blocks=256`，导致默认窗口恰好为空。
- 改到了哪些模块或文档：`src/main/java/com/user/terra_script/server/mcp/TerritoryController.java`、`src/test/java/com/user/terra_script/server/mcp/TerritoryControllerTest.java`，以及文档仓库里的 `docs/20_contracts/territory/t1_t2_t4.md`、`docs/30_code_guide/territory/t1_t2_t4.md`、`docs/40_tests/territory/t1_t2_t4.md`。
- 做了什么验证：
  - 用真实 `T4 dat` 解码确认：`min_x=2896`、`max_x=4094`、`min_z=-4096`、`max_z=-3362`，最近采样点到 `3120,-3200` 的距离约 `331.693`。
  - `.\gradlew.bat test --tests "*TerritoryControllerTest" --tests "*WorkflowControllerTest"` 通过。
  - 重新启动 `runClient` 后，`POST /territory/t4_window {"territory_id":"r15_test_01@c15"}` 返回 `200`，`window.reanchored=true`、`center_x=3248`、`center_z=-3506`、`matched_cells=32577`。
- 当前还剩什么问题：无新的阻塞问题；如需更进一步，可单独评估是否要把显式传入但落在 coverage 外的 `center_x/z` 也做同样的可选 reanchor。
