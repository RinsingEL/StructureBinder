# 剩余归档与忽略规则收尾计划

## 本次任务标题

将 `logs/` 写入忽略规则，并把剩余未提交的归档重命名一起提交。

## 最终计划摘要

本轮只处理当前工作区中最后两类残留：一是 `logs/` 仍未忽略，二是上一次提交后产生的 `dev_docs/current_20260403_125130 -> 387afb6...` 归档重命名仍未入库。完成 `.gitignore` 更新后，将剩余可提交内容一次性收口。

## 实施步骤

1. 更新 `.gitignore`，忽略 `logs/` 运行产物目录。
2. 将 `dev_docs/current_20260403_125130` 的归档重命名纳入版本控制。
3. 核对工作区只剩预期忽略项后执行提交。

## 验收标准

- `logs/` 被正确写入忽略规则。
- 剩余 `dev_docs` 归档重命名被正确提交。
- 提交后工作区不再残留需要继续提交的文档变更。

## 关键假设

- 用户明确希望把剩余内容全部收口，因此本轮不保留新的未提交归档变更。
- `logs/` 为运行产物，应忽略而不是入库。
