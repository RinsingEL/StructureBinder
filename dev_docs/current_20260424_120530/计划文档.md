# 本次任务标题
C1 城市边界生成重构方案

# 最终计划摘要
先不改代码，单独编写 C1 城市边界生成方案，解决当前 createCity 阶段 4 方向 chunk 扩张导致边界方正、层级比例异常、过渡区被挤压的问题，并为后续 AI 总体设计草图提供更自然的城市允许范围。

# 实施步骤
1. 创建本轮 AI 工作流过程文档。
2. 复核当前边界生成现状与问题来源。
3. 在文档仓新增 C1 城市边界生成重构方案。
4. 补充对应非监督验收与 fallback 策略。
5. 回读文档并汇总完成情况。

# 验收标准
- 不修改实现代码。
- 新方案写入文档仓正式产品/测试入口。
- 明确 C1 边界和后续 C3 AI 草图之间的职责关系。
- 给出非监督自动审查、重试和 fallback 方案。

# 关键假设
- 当前城市方正边界主要来自 createCity/expandCity 的 chunk 级 4 方向扩张。
- C1 边界应提供自然、可校验的城市允许范围，而不是直接决定最终建筑布局。
