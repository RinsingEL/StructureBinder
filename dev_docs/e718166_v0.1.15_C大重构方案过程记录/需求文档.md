# C 大重构 2026-05-06 版需求文档

## 用户本次需求摘要

用户希望把本轮关于 C 部分大重构的讨论整理成 `2026-05-06 版` 计划。重点不是细代码，而是更新一版比旧方案更准确的框架文档，覆盖：

- C1-C9 的大框架和粗略推进步骤。
- C1 现在城市外形蠢的问题，改为 image2/CV 辅助生成城市意图图。
- C2 和后续功能区设计死板的问题，改为功能权重场和混合语义。
- 结构标记辅助 Mod 如何把现有 AI 标签升级为权重画像，用于结构池搜索和 AI 上下文压缩。

## 用户特别强调的约束

- 使用中文整理。
- 当前这版应视为比旧版更准确的更新方案。
- 主要整理大框架、C1、C2、结构标记辅助 Mod。
- 结构标记目前的目的包括：
  - 让 AI 知道结构是什么功能。
  - 避免一次把大量结构配置发给 AI 挤爆上下文。
- 新方案需要保留这个目的，并扩展为程序先搜索、AI 再精选的模式。
- C1 只硬保留 `territory_id` 与 `center_x / center_z`；城市规模使用 `city_scale_bucket`，不要再让旧边界大小控制新城市轮廓。
- `density` 不是大中小城市，而是同等规模下的建造紧凑程度。
- 旧 `targetChunkCount / bias / layers / selected_cluster_ref` 等字段应从正式契约中删除，过渡期只能在 adapter 内部读取。
- 本次可以恢复补充修改记录，并提交推送。

## 不做的事情

- 不修改 Java、MCP 或运行时逻辑。
- 不提交代码。
- 不把正式方案写回旧 `workFlow` 或旧归档文档。
- 不把 image2 输出定义成最终合法施工结果。
- 不把 AI 负责的语义权重和程序扫描得到的硬事实混在一起。
- 不把旧 C1 字段换成 `legacy / summary / hint` 名字后重新塞回 `UrbanIntentMap`。
- 不提交与本轮无关的旧 `dev_docs/current_20260409_114815`。

## 当前决定的工作边界

- 实现仓库只写本轮 `dev_docs/current_20260506_120709` 过程记录。
- 文档仓库新增 `docs/10_product/city/c_refactor` 作为本次 C 大重构当前方案入口。
- 本轮契约只写草案级字段和边界，后续实现前再细化为 MCP 输入输出。
- 重点落在方案可读性和阶段职责边界，不展开完整算法细节。
- 文档仓库承载当前功能真值；实现仓库只提交本轮 `dev_docs/current_20260506_120709` 过程记录。
