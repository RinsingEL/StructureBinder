# C 大重构 2026-05-06 版方案整理计划

## 本次任务标题

C1-C9 城市链路重构计划 2026-05-06 版整理。

## 最终计划摘要

本轮不修改运行时代码，只把用户本轮讨论出的最新方案整理为当前可继续推进的文档真值。实现仓库保留 `dev_docs/current_20260506_120709` 过程记录；正式方案、契约草案、代码导览和影响面写入文档仓库 `designer_territoryMod` 的当前文档目录。

## 实施步骤

1. 判断任务归属：本轮以方案和契约整理为主，优先更新文档仓库。
2. 回看当前实现中的结构池、C7 查询和 C1/C2/C6-C9 历史资料，确认本轮讨论与现状的映射。
3. 在文档仓库新增 `city/c_refactor` 系统目录，整理 C 大框架粗略步骤。
4. 单独整理 C1 城市画布与 image2/CV 意图图、C2 功能权重场、结构标记辅助 Mod 三个已讨论重点。
5. 补充轻量契约草案、代码导览、测试入口和影响面。
6. 回读本轮三份过程文档与新增正式文档，确认满足用户要求。
7. 根据后续讨论，收紧 C1 新契约：只保留 `territory_id`、`center_x / center_z`、`city_scale_bucket` 和规划软提示，删除旧 C1 字段回流口。
8. 分别检查文档仓库和实现仓库变更范围，按仓库边界提交并推送。

## 验收标准

- 有一份清晰标注为 `2026-05-06 版` 的 C 大重构系统概述。
- 文档中包含 C1-C9 粗略阶段职责和迁移步骤。
- 文档中明确 C1 的 image2/CV 城市意图图方向。
- 文档中明确 C2 的功能权重场方向。
- 文档中明确结构标记辅助 Mod 如何从标签升级为权重画像。
- 正式结论写入文档仓库，而不是只留在实现仓库 `dev_docs`。
- C1 新契约明确不接收或输出旧 `targetChunkCount / bias / layers` 系列字段。
- 本次提交不混入无关旧 `dev_docs/current_20260409_114815` 目录。

## 关键假设

- 本轮是方案整理，不要求马上实现代码和 MCP 接口。
- 当前实际文档仓库位置为 `E:\mc_dev\StructureBinder-rebuild\designer_territoryMod`。
- 旧 `E:\Mod_Dev\designer_territoryMod` 路径在当前机器上未发现，本轮以实际存在的工作区路径为准。
- `image2` 输出不直接作为施工真值，而是作为可识别、可校验的城市意图图来源。
