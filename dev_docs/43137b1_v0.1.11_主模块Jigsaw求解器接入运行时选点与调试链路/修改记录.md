# 修改记录

## 2026-04-07 00:32:35

- 新建本轮 `dev_docs/current_20260407_003235/` 目录，并写入 `计划文档.md`、`需求文档.md`、`修改记录.md`。
- 本轮目标是修复主链 start 选点/朝向问题，并将独立 Jigsaw Solver 正式分流为“水平求解 + 垂直占位”。
- 当前决定：
  - `up/down` 不再进入当前水平 solver
  - start 允许在 anchor 周围做局部挪位
  - 主链优先使用 runtime 模板评分，拿不到 runtime 时显式回退并写 warning
- 当前验证：已回读上一轮 `dev_docs/current_20260406_231347/` 文档，并确认 `handleCityC8Generate -> CityC8Stages.generate(...)` 是本轮主链入口。

## 2026-04-07 00:58:51

- 已完成 `CityC8Stages` 主链改造：
  - 为 `generate(...)` 增加 `ServerLevel` 入口，并沿 `buildFoundationItem -> initializeForemanSession -> createStartNode` 全链传递 runtime 上下文
  - start 节点新增 runtime 评分逻辑，围绕 anchor 做局部搜索，扫描 runtime 水平 jigsaw，按“水平口可用 -> 朝内 -> 第一跳 dry-run -> 更低风险/更近 anchor”排序
  - start 节点正式写出 `start_solver_kind`、`start_runtime_candidates`、`chosen_start_connector_id/front`、`chosen_start_rotation`、`chosen_start_reason_zh`、`runtime_fallback_used`、`runtime_fallback_reason`
- 已完成水平/垂直分流收口：
  - `CityVanillaJigsawAdapterService` 在 runtime 解析到 `up/down` 后直接路由到 `CityVerticalJigsawPlaceholderService`
  - `vertical_jigsaw_solver_pending` 已接入 `CityController` 中文结果文案与 preview 状态判断
- 已完成控制器接线：
  - `handleCityC8Generate(...)`
  - `handleCityC9Generate(...)` 中的 C8 懒重建路径
  - 两处都会把 `mcServer.overworld()` 传给 `CityC8Stages.generate(...)`
- 已补充测试：
  - 新增 `CityC8StagesTest`，覆盖“runtime 不可用时必须显式回退并留下证据”
  - 扩展 `CityVanillaJigsawAdapterServiceTest`，覆盖水平/垂直方向判定与 vertical placeholder 返回
- 已同步更新文档仓：
  - `docs/10_product/city/main_module/02_jigsaw_solver.md`
  - `docs/40_tests/city/main_module/01_acceptance.md`
  - 明确当前 solver 为“水平专用 + 垂直占位分流”，以及 `C8` start 为 runtime 评分优先、无 runtime 时显式回退
- 当前验证：
  - `.\gradlew compileJava --no-daemon`
  - `.\gradlew compileTestJava --no-daemon`
  - `.\gradlew test --tests "com.user.terra_script.world.city.stage.c8.CityC8StagesTest" --tests "com.user.terra_script.world.city.stage.c8.CityVanillaJigsawAdapterServiceTest" --no-daemon`
- 当前剩余问题：
  - 还未完成真机 `g_market_03` 的重新生成与 HTTP 回归，尚未实际核对 start 是否已经从北侧边缘挪开
  - vertical solver 目前仍是占位路由，只返回 pending，不做真实垂直拼接

## 2026-04-07 01:21:00

- 已临时关闭“无显式 terrain probe 点时，退回 bounds 四角/节点点位做通用采样”的 fallback：
  - `CityVanillaJigsawAdapterService.resolveProbePoints(...)`
  - `CityC8ArrangementEngine.resolveProbePoints(...)`
- 当前策略改为：
  - 模板若显式提供 `terrain_probe_points`，仍按显式 probe 点做采样
  - 模板若未提供显式 probe 点，则不再由通用角点/单点采样触发硬地形拒绝，先交给 foundation / terrain adaptation 体系处理
- 已补充回归测试，防止后续误把 generic fallback 加回主链：
  - `CityVanillaJigsawAdapterServiceTest.templatesWithoutExplicitProbePointsNoLongerUseBoundsCornerFallback`
  - `CityC8ArrangementEngineTest.templatesWithoutExplicitProbePointsNoLongerUseGenericArrangementFallback`
- 当前验证：
  - `.\gradlew compileJava compileTestJava --no-daemon`
  - `.\gradlew test --tests "com.user.terra_script.world.city.stage.c8.CityVanillaJigsawAdapterServiceTest" --tests "com.user.terra_script.world.city.stage.c8.CityC8ArrangementEngineTest" --no-daemon`
- 当前说明：
  - 这次关闭的是 generic fallback，不会影响像 `minecraft:village/taiga/houses/taiga_butcher_shop_1` 这种本来就带显式 `terrain_probe_points` 的模板；这类模板仍会继续按 probe 点执行地形约束

## 2026-04-07 01:34:00

- 已继续关闭“模板显式 terrain probe 点参与硬地形限制”的两条主链：
  - `CityVanillaJigsawAdapterService.terrainRejected(...)`
  - `CityC8ArrangementEngine.terrainPasses(...)`
- 当前策略改为：
  - 模板的 `terrain_probe_points` 暂时仍保留在 catalog 里，作为历史数据与后续可能恢复的依据
  - 但 solver / arrangement engine 不再使用这些 probe 点触发 `terrain_rejected` 或 `terrainPasses=false`
  - 当前真正的地形落地调整预期由 foundation / terrain adaptation 体系负责
- 已统一注释检索标记：
  - `TEMP-C35-TERRAIN-PROBE-HARD-CHECK-DISABLED`
  - 当前在 `CityVanillaJigsawAdapterService` 与 `CityC8ArrangementEngine` 中都能全局搜索到
- 已补充回归测试：
  - `CityVanillaJigsawAdapterServiceTest.explicitTerrainProbeChecksAreTemporarilyDisabled`
  - `CityC8ArrangementEngineTest.explicitTerrainProbeChecksAreTemporarilyDisabledInArrangementEngine`
- 当前验证：
  - `.\gradlew compileTestJava --no-daemon`
  - `.\gradlew test --tests "com.user.terra_script.world.city.stage.c8.CityVanillaJigsawAdapterServiceTest" --tests "com.user.terra_script.world.city.stage.c8.CityC8ArrangementEngineTest" --no-daemon`
