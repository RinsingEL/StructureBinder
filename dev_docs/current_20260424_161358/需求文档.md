# 用户本次需求摘要

用户要求按 `C:\Users\Rinsing\Downloads\PLAN.md` 实施“最小修复计划：T3 单独入口零面积门禁”。

# 用户特别强调的约束

- 修 `/t3_run_continent` 的假成功问题。
- 当 T3 扩张结果 `claimed_total + wild_total == 0` 时，不再返回 200。
- 不改 survival C1、不改旧 C1、不改 T3 扩张算法。
- 保持完整 `T3Stage.execute(...)` 原有零面积检查不变。
- 按项目 AGENTS 规则区分实现仓库和文档仓库。
- 本项目提交信息必须使用中文；本轮若提交，需按既有版本号标题风格和归档规则执行。

# 不做的事情

- 不修复 W3/W4 scan cache 缺失问题。
- 不修复 cluster map 缺失问题。
- 不自动恢复旧 T3 文件到运行态。
- 不改城市 survival C1 生成逻辑。
- 不提交与本任务无关的既有工作区改动。

# 当前决定的工作边界

实现仓库负责 HTTP 入口校验、返回汇总与测试；文档仓库仅在确认已有接口契约、代码导览或验收入口需要同步时更新对应正式文档。
