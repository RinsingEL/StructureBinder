# 修改记录

## 2026-04-24 14:00:00
- 创建本轮排查过程文档。
- 当前目标：查明 T3 后运行态 sovereignty 为空导致 survival C1 fail_safe 的原因。

## 2026-04-24 14:02:19
- 静态排查 TerritoryController.handleT3RunContinent(...)：该接口调用 TerritoryStageOrchestrator.runT3ForContinent(...) 后只返回 exported_count，没有检查 claimed_total / wild_total 是否为 0。
- 静态排查 TerritoryManager.runExpansionForContinent(...)：实际会先 unExpansion()，依赖 ScanResultHolder.lastScanData、lastClusterMap 与 region cache；若当前运行态缺少有效 W3/W4 扫描上下文，ecalculateAll() 可产生空 claimed 结果。
- 对照运行结果：/territory_status 显示三个 continent 15 国度 	otal_area = 0；磁盘新 T3 summary 也显示 15_test_01@c15 claimed_count=0；旧历史 summary 中 15_test_01 曾有 claimed_count=2342，但那不是当前运行态结果。
- 关键发现：完整 T3Stage.execute(...) 在全部 T3 后会检查 claimedTotal <= 0 && wildTotal <= 0 并抛错；但单独 /t3_run_continent 入口没有同等零面积门禁，因此会把空 T3 当成功导出，导致后续 survival C1 正确返回 
o_owned_non_overlapping_chunks。
- 当前判断：这是 T3 单独 HTTP 入口的验收/门禁 bug，且也暴露当前运行态 W3/W4 扫描缓存未准备好或当前 region_id 与扫描 cluster 不匹配。
