# 本次任务标题
排查 survival C1 运行态无国度主权数据

# 最终计划摘要
排查 /city_survival_c1_generate 返回 
o_owned_non_overlapping_chunks 的原因，重点确认 T3 执行后 TerritoryManager 内存态为何仍显示 	otal_area = 0，区分配置问题、T3 入参问题、结果写入问题或运行态加载 bug。

# 实施步骤
1. 创建本轮过程文档。
2. 检查 T3 接口、参数解析与结果写入逻辑。
3. 对照 	erritory_status 返回与磁盘 T3 summary/result 文件。
4. 定位 TerritoryManager.results 是否被正确更新。
5. 给出最小修复或下一步验证方案。

# 验收标准
- 明确当前失败不是 preview 问题，而是运行态 sovereignty 为空。
- 明确 T3 生成文件与内存状态是否一致。
- 明确下一步该修哪个层。

# 关键假设
- 当前 MC 已关闭，需要先做静态代码/文件排查，必要时再启动。
