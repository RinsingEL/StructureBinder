# 当前功能文档补充与路径修正计划

## 本次任务标题

修正 AGENTS 双仓路径，并补充当前 Jigsaw Solver / runtime start / terrain probe 行为的正式文档。

## 最终计划摘要

本轮先把实现仓 `AGENTS.md` 中已经过时的双仓库绝对路径改成当前实际工作路径，再同步更新文档仓当前功能文档，确保当前主模块 Jigsaw Solver 的运行口径可直接从文档读懂。重点补充 runtime start 评分、水平/垂直分流、`terrain_probe_points` 硬限制已临时关闭，以及当前 `g_market_03` 首阻塞已经前移为 `vanilla_empty_stub` 的现状。

## 实施步骤

1. 在实现仓修正 `AGENTS.md` 中实现仓和文档仓的绝对路径。
2. 回读文档仓当前主模块 Jigsaw Solver 与验收入口文档，确认需要补充的现状口径。
3. 更新文档仓产品文档，补充：
   - runtime start 评分如何工作
   - 水平/垂直分流现状
   - `terrain_probe_points` 来源于 TerraSense C3.5 预处理
   - 当前硬限制已临时关闭并交给 foundation / terrain adaptation 处理
   - 当前真机回归首阻塞为 `vanilla_empty_stub`
4. 更新文档仓验收文档，补充当前应重点检查的现象和真机样例。
5. 在实现仓 `修改记录.md` 记录本轮文档与路径调整，以及本轮验证。

## 验收标准

- `AGENTS.md` 中的双仓路径改为当前真实工作路径。
- 文档仓中能直接看懂当前 Jigsaw Solver 的主行为与临时策略，而不需要回看代码或历史对话。
- 文档明确写出 `terrain_probe_points` 当前不再作为 solver / arrangement engine 的硬地形拒绝依据。
- 文档明确写出当前 `g_market_03` 的真机状态：start 已绕开 terrain probe 阻塞，当前首阻塞为 `vanilla_empty_stub`。

## 关键假设

- 当前用户要补的是“当前功能现状文档”，不是再展开新的方案设计。
- 本轮文档真值以文档仓为准，实现仓只保留 `dev_docs/` 过程记录与 `AGENTS.md` 路径修正。
- 这轮不额外扩展新的功能实现，主要做路径修正与文档收口。
