# 本次任务标题
补充 C8 提交落地调试证据并排查 start 半截结构

# 最终计划摘要
本轮不改业务语义，重点补足 `city_c8_submit(apply_now=true)` 的调试证据链，让 start 落地也能像 jigsaw solve 一样留下可回放的 trace、预览与执行证据。

# 实施步骤
1. 为 `city_c8_submit` 增加专用 debug trace 落盘目录与步骤记录。
2. 补充 start/普通节点落地时的请求上下文、校验结果、执行结果与落地后世界快照统计。
3. 增加最小可用的 C8 submit debug 预览图，帮助从空间上确认 start 的期望 bounds 与实际落点。
4. 更新相关测试，验证 debug 产物和回包字段能正常生成。
5. 同步文档仓中与 `city_c8_submit` 调试证据相关的正式真值说明。

# 验收标准
- `city_c8_submit(apply_now=true)` 完成后，能在 group 目录下看到独立 debug 目录与 `trace.json`。
- trace 中至少包含：请求上下文、节点校验/期望 bounds、执行层结果、落地后世界快照统计。
- 回包能直接给出 debug 产物入口，便于后续定位“半截结构”。
- 定向编译/测试通过。

# 关键假设
- 当前“start 落地只有一半结构”优先按执行/落地链路问题处理，先补证据，不先改放置语义。
- 本轮只补 `city_c8_submit` 的专用 debug，不改 `city_jigsaw_solve` 既有调试格式。
