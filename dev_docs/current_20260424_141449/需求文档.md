# 用户本次需求摘要
用户要求实现 T3 单独入口零面积门禁最小修复计划。

# 用户特别强调的约束
- 最小修复。
- 不改 survival C1、不改旧 C1、不改 T3 扩张算法。
- 只让 /t3_run_continent 正确暴露零面积失败。

# 不做的事情
- 不提交 git。
- 不恢复旧 T3 文件到运行态。
- 不启动 MC 做手动回归，除非编译/测试需要。

# 当前决定的工作边界
修改实现仓 TerritoryController 与对应测试，更新本轮 dev_docs 过程记录。
