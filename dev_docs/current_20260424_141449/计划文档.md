# 本次任务标题
T3 单独入口零面积门禁最小修复

# 最终计划摘要
只修 /t3_run_continent 假成功问题：当 T3 扩张结果 claimed_total + wild_total == 0 时返回 409，并给出明确 error/hint 和 per-territory 面积摘要；不改 survival C1、旧 C1 或 T3 扩张算法。

# 实施步骤
1. 创建本轮 AI 工作流过程文档。
2. 在 TerritoryController 中抽取 T3 response builder。
3. 为 /t3_run_continent 增加 claimed/wild 汇总与零面积 409 门禁。
4. 添加轻量单元测试覆盖零面积失败与非零成功响应。
5. 运行 Territory 测试和编译验证。

# 验收标准
- /t3_run_continent 零面积时不再返回 200。
- 成功响应新增 claimed_total、wild_total、	erritories。
- 失败响应保留 exported_count 和 per-territory 摘要。
- 完整 T3Stage 原有逻辑不变。
- 测试与编译通过。

# 关键假设
- wild_total 计入有效主权面积。
- 本轮不修 W3/W4 scan cache 或 region_id 配置问题。
