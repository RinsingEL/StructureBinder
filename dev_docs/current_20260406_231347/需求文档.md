# Vanilla Child 失败诊断增强需求

## 用户本次需求摘要

用户要求把当前 `/city_jigsaw_solve` 中的 `no_valid_jigsaw_solution` 从黑盒状态拆成可读的失败机制，重点解释为什么 `JigsawPlacement.addPieces(...)` 没有生成 child piece，并把这些机制同步体现在中文日志、结构化 trace 和第 04 步预览图中。

## 用户特别强调的约束

- 本轮先不改 `start` 逻辑，不处理 `start_connector_dir` 和 seed 选点策略。
- 诊断必须以 runtime 模板为真值，不能再以 catalog 作为 child attach 主判断依据。
- 第 04 步日志要能直接说明失败卡在 `target/name`、`attach`、`bounds/area`、`terrain` 还是 vanilla 返回空 stub。
- 第 04 步预览图要画出 parent connector、startPos、child 候选矩形，并用颜色区分不同失败类型。
- 不改变当前 solver 行为，`JigsawPlacement.addPieces(...)` 仍是最终求解结果来源。

## 不做的事情

- 不修改 `start` 的选点、朝向预检和多边形朝内策略。
- 不改变 `selected_connector_dir` 在当前 solver 中的语义。
- 不把手工兼容分析结果反向用于替换 vanilla 求解成败判定。
- 不切换当前 `C8 submit` / `C9` 默认主链。

## 当前决定的工作边界

- 主要修改实现仓库 `E:\Mod_Dev\StructureBinder` 中的 solver、controller、预览器和测试。
- 同步更新文档仓库 `E:\Mod_Dev\designer_territoryMod` 中 Jigsaw Solver 的产品/验收文档。
- 调试输出契约的新增字段属于正式接口扩展，需要进入响应、落盘 trace 和 runtime 日志。
