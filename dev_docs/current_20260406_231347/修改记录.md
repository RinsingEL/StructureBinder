# 修改记录

## 2026-04-06 23:13:47

- 新建本轮 `dev_docs/current_20260406_231347/` 目录，并写入 `计划文档.md`、`需求文档.md`、`修改记录.md`。
- 本轮目标是把独立 `city_jigsaw_solve` 的 vanilla child 失败从黑盒状态拆成 runtime 真值驱动的机制级诊断，并同步增强第 04 步预览图。
- 当前决定本轮不改 `start` 选点和 `start_connector_dir` 策略，只聚焦于 `no_valid_jigsaw_solution` 的解释能力。
- 当前验证：工作文档目录已创建，且已回读上一轮相关 `dev_docs/current_20260405_212512/` 文档，准备沿用同一套记录口径继续推进。

## 2026-04-06 23:36:00

- 在 `CityVanillaJigsawAdapterService` 中新增 runtime child 手工兼容分析：
  - 枚举当前 child 模板在可用 rotation 下的 runtime jigsaw
  - 为每个候选输出 `rotation / local/world / front / name / target / matches_parent_target / can_attach_to_parent / candidate_origin / candidate_bounds / inside_area / footprint_collision / terrain_rejected / reject_stage / viable`
  - 新增 `manual_attach_summary / first_blocker_stage / vanilla_stub_generated`
- 在 `CityController` 的 `04_vanilla_piece_result` 调试组装中，新增 Step 04 关键证据顶层字段：
  - `parent_target`
  - `start_pos`
  - `pool_template_id`
  - `manual_child_connector_candidates`
  - `manual_attach_summary`
  - `first_blocker_stage`
  - `vanilla_stub_generated`
  - `piece_generated`
- 在 `CityJigsawSolverPreviewExporter` 中增强第 04 步预览：
  - 增加 parent connector、startPos、child 手工候选 marker
  - 增加每个 child 候选矩形
  - 用颜色区分 `name 不命中 / attach 失败 / 越界 / 矩形碰撞 / terrain / 理论可行`
  - 调试面板状态颜色改为跟随步骤状态，而不是固定 `ok`
- 同步更新文档仓：
  - `docs/10_product/city/main_module/02_jigsaw_solver.md`
  - `docs/40_tests/city/main_module/01_acceptance.md`
  - 补充 Step 04 失败机制拆解、runtime 真值优先和第 04 步预览图的正式验收要求
- 当前验证：
  - `.\gradlew compileJava compileTestJava --no-daemon`
  - `.\gradlew test --tests "com.user.terra_script.world.city.stage.c8.CityVanillaJigsawAdapterServiceTest" --tests "com.user.terra_script.world.city.stage.c8.CityJigsawSolverPreviewExporterTest" --tests "com.user.terra_script.world.city.stage.c8.CityJigsawSolverDebugTraceTest" --no-daemon`
- 当前剩余问题：
  - 当前本机 `127.0.0.1:5000` 没有监听，无法直接抓真机 HTTP 响应；需要重新启动游戏/开发服后，再对 `g_market_02` 或 `g_market_03` 做现场回归。
  - 本轮仍未改 `start` 选点和 `start_connector_dir` 的前置朝向/朝内余量判断，这部分按约定留待下一阶段。
