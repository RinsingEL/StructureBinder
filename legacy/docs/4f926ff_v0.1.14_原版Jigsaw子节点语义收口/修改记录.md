# 修改记录

## 2026-04-08 02:33:11

- 新建本轮 `dev_docs/current_20260408_023311/` 目录，并写入 `计划文档.md`、`需求文档.md`、`修改记录.md`。
- 本轮目标聚焦为：把当前子节点处理收口回“原版 jigsaw + AI 只替换随机选模板”。
- 已确认当前主要偏差点：
  - `CityVanillaJigsawAdapterService` 目前直接构造 `single(template)` 的临时 pool
  - 这会绕开 parent runtime jigsaw 自带的原始 pool 语义
- 当前正在回读实现并准备修改 child solve 主链。

## 2026-04-08 02:52:00

- 已完成 `CityVanillaJigsawAdapterService` 主链收口：
  - 不再直接构造 `single(template)` 的脱离式临时 pool
  - 改为优先读取 parent runtime jigsaw NBT 中的原始 `pool`
  - 从该原始 pool 的元素列表里筛出 AI 指定模板对应的元素
  - 保留原始 pool element / projection / fallback 语义后，再交给 `JigsawPlacement.addPieces(...)`
- 已补充新的 debug 证据字段：
  - `runtime_parent_pool_id`
  - `runtime_parent_pool_source`
  - `vanilla_pool_source`
  - `runtime_parent_pool_entry_count`
  - `runtime_selected_pool_entry_count`
- 已同步更新 `/city_jigsaw_solve` 第 04 步中文文案与 evidence：
  - 明确说明当前是“从 parent runtime 原始 pool 中收束到 AI 指定模板”
  - 不再把该步骤描述成简单的“单模板 pool”
- 已做验证：
  - `./gradlew.bat test --tests "com.user.terra_script.world.city.stage.c8.CityVanillaJigsawAdapterServiceTest"`
  - `./gradlew.bat test --tests "com.user.terra_script.world.city.execution.GroupSurfaceClearServiceTest" --tests "com.user.terra_script.world.city.execution.BuildExecutionPipelineTest"`
- 当前说明：
  - 这轮主要修的是 child solve 语义与调试契约
  - `apply_now` 快通道是否还保留 jigsaw、以及 piece 落地表现是否完全一致，仍需下一步真机验证

## 2026-04-08 03:02:09

- 已完成真机回归与结构资源核对：
  - 对 `g_market_03` 再次执行 `/city_group_surface_clear`
  - 新 trace 已确认第 04 步改为读取 parent runtime jigsaw 原始 pool，而不是自造 `single(template)` pool
- 真机结果：
  - `start_g_market_03` 当前入口口 `jigsaw_east_7_1_0` 在 runtime 中读取到 `pool = minecraft:empty`
  - 新 trace 明确返回：
    - `runtime_parent_pool_id = minecraft:empty`
    - `runtime_parent_pool_entry_count = 0`
    - `reject_reason = selected_template_not_in_runtime_parent_pool`
- 已直接解包并读取 `bettervillageforge` 中的 `taiga_butcher_shop_1.nbt`：
  - 结构本体里 `building_entrance` 口确实写的是 `pool = minecraft:empty`
  - 说明该问题来自结构资源真值本身，不是 solver 读取错误
- 已进一步比对其他 market 候选结构：
  - `plains_butcher_shop_1/2` 的 `building_entrance` 口连接的是 `minecraft:village/plains/streets`
  - `savanna_butchers_shop_1` 入口口同样是 `minecraft:empty`
- 已做临时测试切换：
  - 在 `group/g_market_03/c8_foundation.json` 测试态切换 `start_g_market_03` 为 `plains_butcher_shop_1`
  - 再调用 `/city_jigsaw_solve` 试图接 `minecraft:village/plains/streets/straight_03`
- 新发现：
  - 该请求未再卡在 runtime pool，而是卡在 `missing_selected_template_meta`
  - 说明原版 source 已知 `street` 模板，但我们系统 catalog / TerraSense 导出链尚未导出这类 street 模板 meta
- 当前结论：
  - child solve 主语义已收口正确
  - 下一层真实阻塞点是 TerraSense / catalog 未覆盖 `village/plains/streets/*` 这类模板
