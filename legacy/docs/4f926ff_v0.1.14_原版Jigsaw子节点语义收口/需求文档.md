# 子节点原版 Jigsaw 语义收口需求

## 用户本次需求摘要

用户要求当前子节点处理必须严格按既定设计实现：整体仍然是原版 jigsaw 逻辑，只把“原版从模板池随机选 child 模板”的步骤替换成 AI 决策指定模板。

## 用户特别强调的约束

- 必须按用户设计走，不能再擅自改变解释框架。
- 当前处理子节点时，本质上应当“基本都是原版逻辑”。
- AI 的职责只是替换随机模板选择，不是另造一套拼接系统。

## 不做的事情

- 本轮不扩展新的垂直 solver。
- 本轮不重做完整 C9 / apply_now 执行体系。
- 本轮不讨论新的产品方案，只修当前实现与设计的不一致。

## 当前决定的工作边界

- 只收口 `CityVanillaJigsawAdapterService` 的 child solve 主语义。
- 必要时同步补充调试字段与测试，但不扩大到无关模块。
- 继续在实现仓记录本轮过程，文档仓是否更新待本轮实现稳定后再判断。
