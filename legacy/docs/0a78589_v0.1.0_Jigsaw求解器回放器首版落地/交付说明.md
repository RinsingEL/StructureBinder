# 本次完成了什么

本轮完成了“代码过程浏览可视工具”的首版系统案子编写，并在文档仓库中为该系统建立了独立目录骨架。

## 改了哪些代码、接口、文档或配置

- 新增系统概述文档，明确系统目标、边界、部署方式和首版补充能力。
- 新增功能设计文档，明确过程审查工作台页面结构、审查流程和首版最小功能。
- 新增展示模型契约文档，定义前端展示所需的会话、节点、边、详情、表格、检查点等结构。
- 新增代码导览文档，给出 Go + Web 的首版分层建议。
- 新增测试入口和影响面文档。
- 更新文档导航文件 `function_map.md`。

## 对应更新了哪些系统文档、功能文档、影响面文档

- 系统概述：`designer_territoryMod/docs/10_product/devtools/code_process_viewer/系统概述.md`
- 功能设计：`designer_territoryMod/docs/10_product/devtools/code_process_viewer/功能设计/过程审查工作台.md`
- 影响面：`designer_territoryMod/docs/40_tests/devtools/code_process_viewer/影响面.md`

## 做了哪些验证

- 检查了新系统目录和文件是否创建成功。
- 回读系统概述和功能设计文档。
- 修正了跨目录相对链接错误。

## 还剩哪些风险、限制或待确认事项

- 当前仍需确定上游过程数据是实时采集还是离线导入。
- 当前仍需确定首版存储方案和审查多人协作边界。
- 当前仍未进入实现阶段，因此技术选型还没有经过真实代码验证。

## 本轮补充收口

- 首版节点生成方式已固定为：
  - 后端按检查点自动建节点
  - AI 补节点摘要
  - 用户做审查结论
- 首版节点类型已固定为 7 类：
  - `session_start`
  - `context_scan`
  - `plan`
  - `change_batch`
  - `verify`
  - `issue`
  - `delivery`
- 首版默认开发端口已定为 `6657`，并确认当前环境该端口空闲。

## 当前实现阶段补充

- 当前任务已从“只写案子”切换到“开始实现”。
- 首版样本已改为主模块 Jigsaw 求解器真实调试产物。
- Go 工具链已安装完成，后续交付说明将继续在本文件中追加实现结果与验证情况。

## 本轮实现交付补充

- 已完成独立子工程 `code_process_viewer/` 首版落地，运行时不依赖现有 Java / MCP 子模块。
- 已实现：
  - `jigsaw_solver_debug/<runId>` 导入
  - SQLite 会话持久化
  - `runtime/cache/<debug_run_id>.json` 导出
  - 单页只读工作台
  - 审查结论保存
  - Markdown 报告导出
- 已开放接口：
  - `GET /api/sessions`
  - `GET /api/sessions/{id}`
  - `GET /api/sessions/{id}/graph`
  - `GET /api/nodes/{id}`
  - `GET /api/nodes/{id}/tables`
  - `POST /api/nodes/{id}/review`
  - `GET /api/sessions/{id}/report`
  - `POST /api/import/jigsaw-debug`

## 本轮对应更新的系统文档

- 系统概述：
  - `E:\mc_dev\StructureBinder-rebuild\designer_territoryMod\docs\10_product\devtools\code_process_viewer\系统概述.md`
- 功能设计：
  - `E:\mc_dev\StructureBinder-rebuild\designer_territoryMod\docs\10_product\devtools\code_process_viewer\功能设计\Jigsaw求解器回放.md`
- 契约：
  - `E:\mc_dev\StructureBinder-rebuild\designer_territoryMod\docs\20_contracts\devtools\code_process_viewer\配置表\展示模型.md`
- 代码导览：
  - `E:\mc_dev\StructureBinder-rebuild\designer_territoryMod\docs\30_code_guide\devtools\code_process_viewer\代码导览.md`
- 测试入口：
  - `E:\mc_dev\StructureBinder-rebuild\designer_territoryMod\docs\40_tests\devtools\code_process_viewer\测试入口.md`
- 影响面：
  - `E:\mc_dev\StructureBinder-rebuild\designer_territoryMod\docs\40_tests\devtools\code_process_viewer\影响面.md`

## 本轮验证

- `go mod tidy`
- `go test ./...`
- 手工冒烟：
  - 启动服务监听 `6657`
  - 自动导入一个临时构造的 `jigsaw_solver_debug/<runId>`
  - 校验 `GET /api/sessions` 返回 `200`
  - 校验首页 `/` 返回 `200`

## 风险、限制与待确认

- 当前真实样本目录路径仍需启动时通过 `--artifact-root` 或页面导入表单提供。
- 当前前端是原生 JS 只读工作台，未包含会话对比和实时刷新。
- 当前首版只覆盖 Jigsaw 调试回放，不负责触发 `city_jigsaw_solve`。
