# 修改记录

## 2026-04-09 01:10:21
- 开始处理“TerraSense 新增 jigsaw pool 字段后主 mod 是否需要同步修改”任务。
- 原因：
  - 需要先隔离这是 TerraSense 的原始导出层问题、C3.5 元数据层问题，还是 StructureBinder 的消费层问题。
- 涉及模块或文档：
  - `E:\Mod_Dev\TerraSense\src\main\java\com\rin\terrasense\data\StructureMetadata.java`
  - `E:\Mod_Dev\TerraSense\src\main\java\com\rin\terrasense\server\StructurePlacer.java`
  - `E:\Mod_Dev\StructureBinder\src\main\java\com\user\terra_script\world\city\stage\CityC35CatalogIO.java`
  - `E:\Mod_Dev\StructureBinder\src\main\java\com\user\terra_script\world\city\stage\c8\CityVanillaJigsawAdapterService.java`
  - `dev_docs/current_20260409_011021/*`
- 已做验证：
  - 确认 TerraSense 在 `2b7770b` 中新增了 jigsaw `pool` 原始字段
  - 确认 StructureBinder 当前存在未提交的 `template_output` 与启动配置变更，需要避免误覆盖
- 当前还剩问题：
  - 待确认 `pool` 是否已进入正式 catalog，以及主 mod 当前逻辑是否已经覆盖该真值。

## 2026-04-09 01:28:00
- 已确认层级结论：
  - `L0/L1`：TerraSense 新增的是原始 jigsaw `pool` 真值，`C35Pipeline` 内部 `ConnectorSpec` 也已有 `pool`
  - `L2`：StructureBinder 的 runtime vanilla solver 已直接读取 runtime parent jigsaw NBT 里的 `pool`，所以运行时 child 生成链本身不缺这个字段
  - 真正缺的是 catalog 消费层的前向兼容：`CityC35CatalogIO.ConnectorSpec` 还不认 `pool`，`C6/C8` 也不会在 `connect_to_pools` 缺失时回退使用 `pool`
- 已做实现：
  - 在 `src/main/java/com/user/terra_script/world/city/stage/CityC35CatalogIO.java` 的 `ConnectorSpec` 中新增可选 `pool`
  - 新增 `connectorPoolRefs(...)`，优先返回 `connect_to_pools`，为空时回退到单元素 `pool`
  - 在 `src/main/java/com/user/terra_script/world/city/stage/c8/CityC8ArrangementEngine.java` 中让 connector 视图使用上述回退逻辑
  - 在 `src/main/java/com/user/terra_script/world/city/stage/c6/CityC6Stages.java` 中让 connector reserve 统计也使用同一回退逻辑
  - 新增两条测试覆盖：
    - `CityC6StagesTest.growthGuidanceFallsBackToConnectorPoolWhenConnectToPoolsMissing`
    - `CityC8ArrangementEngineTest.resolveConnectorViewsFallsBackToRawPoolWhenPoolListMissing`
- 原因：
  - 这样即使 TerraSense 后续先把 raw `pool` 写进正式 catalog，但 `connect_to_pools` 还没完全补齐，主 mod 也不会继续漏掉这类候选池信息。
- 已做验证：
  - 执行 `./gradlew.bat test --tests "com.user.terra_script.world.city.stage.c6.CityC6StagesTest" --tests "com.user.terra_script.world.city.stage.c8.CityC8ArrangementEngineTest"`
  - 测试通过
- 当前还剩问题：
  - 当前外部导出的 `C3_5_StructureCatalog.preprocessed.json` 仍未稳定带出完整 connector 字段，这部分仍是 TerraSense 导出链自身问题
  - 文档仓是否要把 `connector.pool` 提升为正式 C3.5 合同字段，仍需单独判断与同步

## 2026-04-09 19:35:00
- 继续处理 dev client 自动进档与测试启动问题。
- 已确认：
  - `ModHttpServer` 绑定的是 `ServerStartedEvent`，因此 `http://127.0.0.1:5000/workflow/status` 只有在真正进入单机存档后才会可用
  - 原先依赖 `--quickPlaySingleplayer` 的方案在当前 Forge dev 链路下没有稳定生效
  - 首版改成 `TitleScreen -> createWorldOpenFlows().loadLevel(...)` 后，中文世界名在 `cmd / gradlew / JVM 参数` 链路中被编码破坏，触发 `InvalidPathException`
- 已做修正：
  - 将 dev 自动进档世界名改为通过 Base64 系统属性传递，再在 `ClientSetup` 中按 UTF-8 解码
  - 保留 dev-only 自动进档逻辑，但移除对命令行 quick play 参数的依赖
- 涉及模块或文档：
  - `src/main/java/com/user/terra_script/client/ClientSetup.java`
  - `build.gradle`
  - `.idea/runConfigurations/runClient.xml`
- 已做验证：
  - `./gradlew.bat compileJava` 通过
- 当前还剩问题：
  - 待再次拉起 client，确认 Base64 传参后能否稳定自动进入 `新的世界 (2)`，并等待 `127.0.0.1:5000/workflow/status` 返回。

## 2026-04-09 19:38:44
- 已完成验证：
  - 使用 Base64 世界名传参后，dev client 能成功进入单机存档
  - `http://127.0.0.1:5000/workflow/status` 返回 `200`
- 结论：
  - 当前“完全进档”的稳定判断信号就是本地 HTTP/MCP 端口可访问
  - `ModHttpServer` 确实是在单机存档服务端启动后才启动，而不是打开游戏时就启动
- 涉及模块或文档：
  - `src/main/java/com/user/terra_script/server/ModHttpServer.java`
  - `src/main/java/com/user/terra_script/client/ClientSetup.java`
  - `build.gradle`
  - `.idea/runConfigurations/runClient.xml`
- 已做验证：
  - `GET /workflow/status` 成功返回当前阶段状态 JSON
- 当前还剩问题：
  - 无启动层阻塞，后续可直接进入 jigsaw solver 测试。

## 2026-04-09 20:05:10
- 继续收敛“是否真正纯自动进档”的启动验证。
- 已确认：
  - 仅靠 `@Mod.EventBusSubscriber` 时，`ClientSetup` 的自动进档钩子在当前 dev client 链路里不稳定，日志中长期看不到任何客户端侧自动进档事件
  - 当前阻塞界面并不是主菜单，而是 `net.minecraftforge.client.gui.LoadingErrorScreen`
  - `ReTerraForged` 在当前环境会抛出 `Missing data pack mod:reterraforged` / `Missing metadata in pack mod:reterraforged`，但这不是致命阻塞，只是让游戏停在 Forge 的 loading error 提示页
- 已做实现：
  - 将 `ClientSetup` 改为显式注册到 Forge event bus，不再依赖注解自动发现
  - 将 dev 自动进档逻辑改为在 client tick 中观察当前 `screen`，当同一界面稳定停留若干 tick 后，直接以该 `screen` 作为 parent 执行 `loadLevel(...)`
  - 新增启动诊断日志：
    - `ClientSetup registered on Forge event bus.`
    - `Dev auto load observing screen: ...`
    - `Dev auto load world from screen ...`
- 涉及模块或文档：
  - `src/main/java/com/user/terra_script/client/ClientSetup.java`
  - `src/main/java/com/user/terra_script/TerraScriptMod.java`
- 已做验证：
  - `./gradlew.bat compileJava` 通过
  - 新一轮 `runClient` 日志 `logs/codex_runClient_20260409_autotest5.log` 中确认：
    - `ClientSetup registered on Forge event bus.`
    - `Dev auto load observing screen: net.minecraftforge.client.gui.LoadingErrorScreen`
    - `Dev auto load world from screen net.minecraftforge.client.gui.LoadingErrorScreen: 新的世界 (2)`
    - `API Server started on port 5000`
    - `logged in with entity id`
  - `GET http://127.0.0.1:5000/workflow/status` 返回 `200`
- 当前还剩问题：
  - `ReTerraForged` 的缺包提示仍会出现，但现在已经不会阻断 dev 自动进入 `新的世界 (2)` 和后续 MCP/HTTP 测试链路

## 2026-04-09 20:23:20
- 开始在当前自动进档后的运行环境里实测 `g_market_03` 的 jigsaw solver。
- 已确认：
  - 当前运行中的存档为 `新的世界 (2)`，`GET /workflow/status` 返回 `200`
  - `g_market_03` 当前 group 级产物位于 `run/saves/新的世界 (2)/terra_script/cities/city_3120_-3200/groups/g_market_03`
  - 当前 `C8` 中唯一已校验 parent placement 为：
    - `node_id = start_g_market_03`
    - `template_id = minecraft:village/plains/houses/plains_butcher_shop_1`
    - `rotation = 180`
    - 当前真实 runtime horizontal connector 为 `jigsaw_east_0_1_7`
- 已做测试：
  - 调用 `/city_group_surface_clear`
    - `city_id = city_3120_-3200`
    - `group_id = g_market_03`
    - `build_area_id = ba_g_market_03_a002`
    - `preserve_existing_placements = true`
    - `clear_solids = true`
  - 结果：
    - 返回 `200`
    - 新产物 `group_surface_clear/1775737340165.json`
    - 因当前场地此前已经清过，且保留了现有 placement exclusion bounds，本轮实际清理方块数为 `0`
  - 调用 `/city_jigsaw_solve`
    - `parent_node_id = start_g_market_03`
    - `parent_connector_id = jigsaw_east_0_1_7`
    - `selected_template_id = minecraft:village/taiga/houses/taiga_butcher_shop_1`
    - `selected_connector_dir = east`
    - `apply_now = false`
  - 结果：
    - 返回 `422`
    - 新 debug run：`jigsaw_solver_debug/20260409_202248_033`
- 本轮关键结论：
  - solver 前三步已通过：
    - 成功恢复请求上下文
    - 成功扫到 parent runtime jigsaw
    - 成功命中请求的 runtime parent connector
  - 当前首阻塞不是地形、不是 bounds、也不是 runtime connector 解析，而是：
    - `reject_reason = selected_template_not_in_runtime_parent_pool`
  - debug 证据明确表明：
    - 当前 parent runtime pool 为 `minecraft:village/plains/streets`
    - 该 pool 在本轮运行时共有 `16` 个 entry
    - AI 指定模板 `minecraft:village/taiga/houses/taiga_butcher_shop_1` 在该 runtime pool 中匹配数为 `0`
  - 同时当前导入 catalog 中没有 `minecraft:village/plains/streets/*` 这类街道模板条目，因此当前 `g_market_03` 若继续沿 `plains_butcher_shop_1` 向外求解，主阻塞已经前移为“catalog 未覆盖当前 runtime pool 的 child 模板”
- 涉及模块或文档：
  - 运行接口：`/city_group_surface_clear`、`/city_jigsaw_solve`
  - 测试产物：
    - `run/saves/新的世界 (2)/terra_script/cities/city_3120_-3200/groups/g_market_03/group_surface_clear/1775737340165.json`
    - `run/saves/新的世界 (2)/terra_script/cities/city_3120_-3200/groups/g_market_03/jigsaw_solver_debug/20260409_202248_033/trace.json`
- 当前还剩问题：
  - 若要让 `g_market_03` 当前这版 `plains_butcher_shop_1` parent 真正产出 child，需要补齐 `minecraft:village/plains/streets` 对应 child 模板的 catalog 元数据，或切回一个 runtime pool 与现有 catalog 模板一致的 parent 样本继续测 apply_now

## 2026-04-09 20:39:40
- 继续向上游回溯 `TerraSense` 导出链，确认为什么 `pool` 里引用到的 child 模板没有进入当前 catalog / template_output。
- 已确认：
  - `TerraSense` 最新本地 HEAD 为 `05cdef9`，该提交只是 merge；真正与 jigsaw `pool` 有关的实现仍停留在 `2b7770b`
  - `2b7770b` 的提交说明明确写了“补充拼图方块 pool 导出字段”且“不引入递归展开或链式扫描逻辑”
  - 当前 `C35Pipeline.exportQueue / finalizeNextBatch / buildCatalogArtifacts` 只会处理：
    - 当前扫描到并被显式选中的 structure id
    - 或 queue 中 pending 的 structure id
  - `pool/target` 在当前 TerraSense 代码里只是写进 connector 元数据，并不会把被引用的结构补进 queue/catalog
  - 当前 PCL 导出目录 `D:\PCL2-2.8.12\.minecraft\versions\1.20.1-mod开发测试\config\terrasense` 中，`scan debug / queue / catalog` 都不存在：
    - `minecraft:village/plains/streets/*`
    - `minecraft:village/plains/houses/plains_butcher_shop_1`
    - `minecraft:village/taiga/houses/taiga_butcher_shop_1`
  - 说明 TerraSense 当前导出链对“关联引用结构一起入 catalog”这件事确实还没做
- 已做实现（TerraSense 仓库）：
  - 修改 `E:\Mod_Dev\TerraSense\src\main\java\com\rin\terrasense\c35\C35Pipeline.java`
  - 新增“选中结构导出时自动扩引用闭包”逻辑：
    - 顺着 jigsaw `pool` 读取 `worldgen/template_pool/*.json`，把 pool 内引用到的模板结构一起加进选集
    - 若 jigsaw `target` 本身长得像直接结构 id（`namespace:path/to/template`），也一并补进选集
    - 该扩展在 `exportQueue` 与 `finalizeNextBatch` 两条链路都生效
  - 保持策略克制：
    - 不会把 `building_entrance` 这类普通 jigsaw target 名字误当结构 id 扩爆
    - 仍以当前扫描到的模板集为边界，不凭空造 id
- 已做验证：
  - 在 `E:\Mod_Dev\TerraSense` 执行 `./gradlew.bat compileJava`
  - 编译通过
- 当前还剩问题：
  - 还需要在 TerraSense 实际运行环境里重新导出一轮，新的 `C3_5_*` 文件才会把这些 pool 引用模板真正落到 `config/terrasense`，随后再同步回 `StructureBinder/template_output`

## 2026-04-09 20:43:30
- 按用户要求，协助在 `E:\Mod_Dev\TerraSense` 执行完整构建，方便继续到 TerraSense 运行环境里重导导出物。
- 已做验证：
  - 在 `E:\Mod_Dev\TerraSense` 执行 `./gradlew.bat build`
  - 构建通过，`jar / reobfJar / assemble / build` 均成功完成
- 当前还剩问题：
  - 仍需用户在 TerraSense 实际运行环境中重新执行 `ts_c35_export / ts_c35_finalize_next`，生成新的 `config/terrasense/C3_5_*` 产物，再同步回 `StructureBinder`

## 2026-04-09 23:32:00
- 用户在 TerraSense 运行环境里误执行了不带选集的 `/ts_c35_export city_default`，导致 `C3_5_StructureLabelQueue.json` 被全量入队。
- 已确认：
  - 当前实现里 `ts_c35_export city_default` 会走 `C35Pipeline.exportQueue(server.overworld(), cityId, null)`，即空选集等价于“全量扫描后全部入队”
  - 误操作发生后 queue 状态为：
    - `total_items = 3132`
    - `pending_items = 2911`
    - `completed_items = 221`
- 已做修复：
  - 对外部运行环境 queue 文件先做时间戳备份：
    - `D:\PCL2-2.8.12\.minecraft\versions\1.20.1-mod开发测试\config\terrasense\C3_5_StructureLabelQueue.before_trim_20260409_233200.json`
  - 将当前 queue 裁回仅保留 `status = done` 的已完成项，清空误入队的 pending 项
- 修复后结果：
  - `D:\PCL2-2.8.12\.minecraft\versions\1.20.1-mod开发测试\config\terrasense\C3_5_StructureLabelQueue.json`
  - `total_items = 221`
  - `pending_items = 0`
  - `completed_items = 221`
- 当前还剩问题：
  - 用户仍需在 TerraSense 运行环境里按新的引用闭包逻辑重新执行正确选集的 `ts_c35_export / ts_c35_finalize_next`，生成新的 C3.5 导出物

## 2026-04-09 23:49:10
- 继续在 `g_market_03` 上验证“导入新 catalog 后是否能真正 solve_and_place”。
- 已确认：
  - 新导出的 `config/terrasense/C3_5_*` 已同步回 `StructureBinder/template_output`
  - `template_output/C3_5_StructureCatalog.preprocessed.json` 中已经包含：
    - `minecraft:village/plains/houses/plains_butcher_shop_1`
    - `minecraft:village/plains/streets/straight_03`
  - 重新起 game 后，dev client 已自动进入 `新的世界 (2)`，`GET /workflow/status` 返回 `200`
- 已做测试：
  - 先对当前 `g_market_03` 的 start placement 做小范围位置扫描
  - 结果发现原位置 `(3122,-3251)` 确实会导致 `straight_03` 落到 `out_of_area`
  - 首个可通过 solver 边界校验的位置为：
    - `x = 3119`
    - `z = -3239`
  - 随后将 `c8_foundation.json` 中 `start_g_market_03` 临时改到该位置，再次求解
- 已做验证：
  - `apply_now = false` 时已返回 `200`
  - `apply_now = true` 时已返回 `200`
  - 最新成功调试产物：
    - `run/saves/新的世界 (2)/terra_script/cities/city_3120_-3200/groups/g_market_03/jigsaw_solver_debug/20260409_234954_566/trace.json`
  - 关键结果：
    - child 模板：`minecraft:village/plains/streets/straight_03`
    - child placement：`(3112, 64, -3225), rotation = 270`
    - `05_validation_result = ok`
    - `06_apply_result = ok`
    - terrain preparation 共清理 `569` 个方块后成功落地
- 当前还剩问题：
  - 当前成功位置是直接改在 group 级 `c8_foundation.json` 上的临时有效位置，后续如果要保持长期稳定，需要判断是否应把 start runtime 选点策略或 group polygon 约束同步回正式 C8 生成链

## 2026-04-10 01:11:40
- 按用户要求，把普通 `C8` 节点也补成“可选立即落地”，并验证热重启 dev client 后新代码可直接生效。
- 已做实现（双仓）：
  - 实现仓：
    - `src/main/java/com/user/terra_script/server/mcp/CityController.java`
    - `city_c8_submit` 现在支持 `apply_now`
    - `apply_now=false` 时保持原行为：只做 `C8 validated`
    - `apply_now=true` 时：节点先写入 `C8`，再立即复用现有执行层管线尝试落地
    - 已验证节点若没有新的决策字段，也允许直接触发“仅执行已验证 placement”
  - 文档仓：
    - `docs/40_tests/city/main_module/测试入口.md`
    - `docs/30_code_guide/city/main_module/功能实现/C8节点会话与提交.md`
    - 同步补充 `city_c8_submit + apply_now` 的测试口径与接口说明
- 已做验证：
  - 在 `StructureBinder` 重新执行 `./gradlew.bat compileJava`
  - 由我执行“关旧 runClient -> 启新 runClient -> 自动进档”整链，确认改完代码后可以直接重启到新编译版本
  - 新起 client 后，调用：
    - `city_c8_submit`
    - `node_id = start_g_market_03`
    - `apply_now = true`
  - 返回 `200`
  - 回包中：
    - `status = ok`
    - `placement_execution.outcome = completed`
    - `task_status = DONE`
  - 说明 `start` 节点本身也已能按节点决定是否立即落地
- 当前还剩问题：
  - `g_market_03` 当前 start 成功落地仍依赖前面临时挪出来的有效位置 `(3119, -3239)`；若后续要完全自动稳定复现，还需要把该位置修正沉回 start 选点主链

## 2026-04-10 02:14:30
- 按用户确认的收口方案，统一地表清理、结构贴表/埋入语义与落地后 jigsaw 收口。
- 已做实现（实现仓）：
  - `src/main/java/com/user/terra_script/server/mcp/CityController.java`
    - `city_group_surface_clear` 默认 `min_y` 从 `foundation.base_y - 1` 开始；显式传入 `min_y / max_y` 时仍严格按请求值执行
  - `src/main/java/com/user/terra_script/world/city/stage/StructurePlacementContract.java`
    - 新增共享 placement 合同解析器
    - 统一把 `placement.origin_offset.y` 解释为默认落点高度偏移
    - 默认缺失或运行环境未就绪时退回 `0`
  - `src/main/java/com/user/terra_script/world/StructureInjector.java`
    - 旧的 `surfaceY - 1` 直接放置逻辑改为 `surfaceY + placement.origin_offset.y`
    - 新增共享 `finalizePlacedJigsaws(...)`
    - 所有落地后的 jigsaw 统一执行 `final_state -> air fallback`
  - `src/main/java/com/user/terra_script/world/city/execution/BuildExecutionPipeline.java`
    - `post_cleanup` 现在直接消费真实 jigsaw finalizer 统计，不再把所有收尾都压成单一 `jigsaw`
  - `src/main/java/com/user/terra_script/world/city/execution/SolvedPlacementExecutionService.java`
    - vanilla solved child 路径也接入共享 jigsaw finalizer
    - solved placement task 默认高度改为复用 `placement.origin_offset.y`
  - `src/main/java/com/user/terra_script/world/city/stage/c8/CityC8Stages.java`
    - start fallback / start runtime 搜索 / 节点 placement 写回统一按 `placement.origin_offset.y` 计算默认原点高度
    - 不再在校验阶段把 `placement.y` 强行重写回 `base_y`
  - `src/main/java/com/user/terra_script/world/city/stage/c9/CityC9BuildQueue.java`
    - `BuildTask.y` 与 placement signature 统一改为消费 `placement.origin_offset.y`
- 已做实现（文档仓）：
  - `docs/40_tests/city/main_module/测试入口.md`
  - `docs/20_contracts/city/main_module/配置表/C8节点会话.md`
  - `docs/20_contracts/city/main_module/配置表/C8子节点Jigsaw求解.md`
  - `docs/20_contracts/city/C9.md`
  - `docs/30_code_guide/city/c9_execution.md`
  - 已同步当前真值：
    - `group_surface_clear` 默认下探 1 格
    - `placement.origin_offset.y` 的贴表/埋入语义
    - 落地后 jigsaw 的 `final_state -> air fallback`
- 已补单测：
  - `src/test/java/com/user/terra_script/world/city/stage/StructurePlacementContractTest.java`
  - `src/test/java/com/user/terra_script/world/city/execution/BuildExecutionPipelineTest.java`
- 已做验证：
  - 编译：
    - `./gradlew.bat compileJava compileTestJava`
  - 定向测试：
    - `./gradlew.bat test --tests "com.user.terra_script.world.city.execution.BuildExecutionPipelineTest" --tests "com.user.terra_script.world.city.execution.GroupSurfaceClearServiceTest" --tests "com.user.terra_script.world.city.execution.SolvedPlacementExecutionServiceTest" --tests "com.user.terra_script.world.city.execution.BuildTerrainPreparationServiceTest" --tests "com.user.terra_script.world.city.stage.StructurePlacementContractTest" --tests "com.user.terra_script.world.city.stage.c6.CityC6StagesTest" --tests "com.user.terra_script.world.city.stage.c8.CityC8StagesTest" --tests "com.user.terra_script.world.city.stage.c8.CityC8ArrangementEngineTest"`
  - 真机回归：
    - 重启 `runClient` 并自动进入 `新的世界 (2)`
    - `GET /workflow/status` 返回 `200`
    - `city_group_surface_clear` 在 `g_market_03` 上默认返回 `min_y = 64`（对应 `base_y = 65`）
    - 显式传 `min_y = 65` 时回包保持 `65`
    - `city_c8_submit(apply_now=true)` 成功，`placement_execution.terrain_preparation.post_cleanup.reason_counts.jigsaw_final_state = 2`
    - `city_jigsaw_solve(apply_now=true)` 成功，`06_apply_result.terrain_preparation.post_cleanup.reason_counts.jigsaw_final_state = 12`
- 当前还剩问题：
  - 当前导出的 catalog 里 `origin_offset.y` 仍几乎全为 `0`，后续若要真正区分“贴表模板 / 下沉模板”，还需要在 TerraSense 的 override 或正式导出合同里补齐非零样本
  - `g_market_03` 的 start 位置仍是当前联调固定的 `(3119, -3239)`，尚未回沉到自动 start 选点主链

## 2026-04-10 10:24:00
- 切换到本轮主链 bug 修复任务，明确只处理两项：
  - `city_jigsaw_solve` 成功后 child 未沉回 `C8` 会话树
  - `start` 高度偏高，且当前真值应以运行时覆盖模板为准
- 已确认的分层结论：
  - `handleCityJigsawSolve(...)` 当前只做 solve / 可选 apply，不会把 `solveResult.placement` 写回 `validated_nodes / placements`
  - `g_market_03` 当前 `c8_foundation.json` 中仍保留旧的 placeholder：`start_g_market_03_child_1/2`
  - `start` 当前 `y=65`，而同链路 child 的 runtime origin 为 `y=64`
  - 当前运行时实际加载的 `minecraft:village/plains/houses/plains_butcher_shop_1` 与 `straight_03` 都已被 `bettervillageforge` 覆盖，不能继续只信静态 catalog 的 `origin_offset.y`
- 本轮计划收口：
  - 在 `CityC8Stages` 增加 solved child 入树 / placeholder 替换 / 下一层真实节点展开
  - 在 `CityC8Stages` 增加 start runtime 高度归一化，并在 controller 读档后自动修正现有存档中的 stale start y
  - 在 `CityController.handleCityJigsawSolve(...)` 接入保存与响应增强
  - 同步补测试与文档仓契约
- 当前还剩问题：
  - 代码尚未落地，接下来进入实现、编译、测试与真机回归阶段

## 2026-04-10 10:57:00
- 已完成本轮两项主链 bug 的实现收口。
- 已做实现（实现仓）：
  - `src/main/java/com/user/terra_script/world/city/stage/c8/CityC8Stages.java`
    - start runtime 评分改成按选中的 runtime connector 真值定高：`start_origin_y = foundation.base_y - connector.local_y`
    - 新增 start 高度归一化逻辑，可对已有 `c8_foundation.json` 中的 stale start `y / placement.y` 自动修正
    - 新增 solved child 入树逻辑：
      - 把 `jigsaw_solve` 成功结果写回 `validated_nodes / placements`
      - 删除同 parent 下旧的 placeholder child
      - 清理相关 `failed_attempts`
      - 按真实 child connector 的 pool refs 生成下一层 `READY_FOR_AI` 节点
  - `src/main/java/com/user/terra_script/server/mcp/CityController.java`
    - `loadC9GenerationPlan` 外围新增读档归一化包装，读取 group 级 `C8` 后会先自动修正 start 高度并回存
    - `handleCityJigsawSolve(...)` 现在会在 solve 成功后立即写回 `C8` 会话树并保存，再决定是否继续 `apply_now`
    - `city_jigsaw_solve` 成功回包追加：
      - `queue_summary`
      - `active_node`
      - `validated_nodes`
      - `failed_attempts`
  - `src/test/java/com/user/terra_script/world/city/stage/c8/CityC8StagesTest.java`
    - 新增 solved child 入树 / placeholder 替换 / start 高度归一化相关测试
- 已做实现（文档仓）：
  - `docs/20_contracts/city/main_module/配置表/C8子节点Jigsaw求解.md`
    - 明确 `city_jigsaw_solve` 成功后即入树，`apply_now` 只控制是否继续落地
    - 明确 placeholder child 会被真实 `vjigsaw_*` child 替换
  - `docs/20_contracts/city/main_module/配置表/C8节点会话.md`
    - 明确 start 节点在存在 runtime connector 真值时，优先按 `foundation.base_y - chosen_start_connector.local_y` 定高
    - 明确读取 `c8_foundation.json` 时会自动归一化这类 start 高度
  - `docs/40_tests/city/main_module/测试入口.md`
    - 补充 `city_jigsaw_solve` 入树验证项与 start 高度归一化验证项
- 已做验证：
  - 编译：
    - `./gradlew.bat compileJava compileTestJava`
  - 定向测试：
    - `./gradlew.bat test --tests "com.user.terra_script.world.city.stage.c8.CityC8StagesTest" --tests "com.user.terra_script.world.city.execution.BuildExecutionPipelineTest" --tests "com.user.terra_script.world.city.stage.c8.CityC8ArrangementEngineTest"`
  - 真机回归：
    - 重启 `runClient`，自动进入 `新的世界 (2)`，`GET /workflow/status` 返回 `200`
    - 调用 `city_jigsaw_solve(apply_now=false)` 后，`g_market_03` 当前 `c8_foundation.json` 已从：
      - `start.y = 65`
      - 旧 placeholder `start_g_market_03_child_1/2`
      变为：
      - `start.y = 64`
      - 真实 child `vjigsaw_start_g_market_03_child` 已进入 `validated_nodes`
      - `active_node = vjigsaw_start_g_market_03_child_child_1`
    - 随后重新执行：
      - `city_group_surface_clear(preserve_existing_placements=false)`
      - `city_c8_submit(node_id=start_g_market_03, apply_now=true)`
      - `city_jigsaw_solve(apply_now=true)`
    - 回包验证：
      - `start` 落地时 `y = 64`
      - `child` 落地时 `y = 63`
      - `city_jigsaw_solve(apply_now=true)` 返回 `200`
      - `placement_execution.outcome = completed`
- 当前还剩问题：
  - 本轮未处理“start 方向偶发不对 / 像被砍了一半”的独立视觉问题
  - `g_market_03` 的 start 位置仍是当前联调固定的 `(3119, -3239)`，尚未回沉到自动 start 选点主链

## 2026-04-10 17:35:00
- 继续修复 `child-child_*` 自动续链仍会漂到旧 catalog connector id 的残留问题。
- 已做实现（实现仓）：
  - `src/main/java/com/user/terra_script/world/city/stage/c8/CityVanillaJigsawAdapterService.java`
    - `solveResult.placement` 现在会额外写入 `outgoing_connectors`
    - 每个 outgoing connector 都按 child 当前真实 runtime jigsaw 扫描得到：
      - `connector_id`
      - `front`
      - `pool_refs`
    - 优先读取 runtime jigsaw NBT 的 `pool`，取不到时才回退 catalog 映射
  - `src/main/java/com/user/terra_script/world/city/stage/c8/CityC8Stages.java`
    - `enqueueRuntimeChildNodes(...)` 现在优先消费 `placement.outgoing_connectors`
    - 下一层节点的 `incoming_connector_id` 改为真实 runtime connector id
    - 下一层 `candidate_template_ids` 改为直接根据 runtime `pool_refs` 展开
    - 旧的 `outgoing_connector_ids` 仍保留，作为兼容字段与 fallback
  - `src/test/java/com/user/terra_script/world/city/stage/c8/CityC8StagesTest.java`
    - 更新 solved child 入树测试，覆盖 runtime connector refs 驱动的下一层节点展开
- 已做验证：
  - 编译：
    - `./gradlew.bat compileJava compileTestJava`
  - 定向测试：
    - `./gradlew.bat test --tests "com.user.terra_script.world.city.stage.c8.CityC8StagesTest" --tests "com.user.terra_script.world.city.stage.c8.CityC8ArrangementEngineTest" --tests "com.user.terra_script.world.city.execution.BuildExecutionPipelineTest"`
  - 真机观察：
    - 最新 `city_jigsaw_solve(start -> child)` 回包里，`solve_result.placement.outgoing_connectors` 已变为真实 runtime connector truth
    - 例如 `minecraft:village/plains/streets/straight_03` 当前会写出：
      - `jigsaw_south_17_1_20 -> minecraft:village/plains/houses`
      - `jigsaw_east_19_1_23 -> minecraft:village/plains/streets`
      - `jigsaw_north_29_1_10 -> minecraft:village/plains/houses`
- 当前还剩问题：
  - 还在做最后一段端到端验收，目标是证明“干净会话树上的自动第二跳”无需手动看 trace 挑 connector 也能直接续链

## 2026-04-10 17:50:00
- 继续做 `g_market_03` 的 `start -> child -> child` 真实落地验收，并按层隔离当前阻塞。
- 已确认的数据与验收前提：
  - 当前新鲜生成的 `g_market_03` 仍只给出 `minecraft:village/taiga/houses/taiga_butcher_shop_1` 作为 start，且它的第一跳 dry-run 仍落在 `selected_template_not_in_runtime_parent_pool / vanilla_empty_stub`；这属于上游规划/数据不足，不能直接作为本轮真实落地验收样本。
  - 为继续完成运行时验收，恢复并沿用受控 plains fixture，再把 start 明确挪回已知可行位置 `(3119, -3239)`。
- 已做验收动作：
  - 执行 `city_group_surface_clear(preserve_existing_placements=false)`，本次默认口径从 `base_y - 1 = 64` 起清，产物：
    - `run/saves/新的世界 (2)/terra_script/cities/city_3120_-3200/groups/g_market_03/group_surface_clear/1775814037627.json`
  - 执行 `city_c8_submit(apply_now=true)`，将 `start_g_market_03` 以：
    - `minecraft:village/plains/houses/plains_butcher_shop_1`
    - `rotation = 180`
    - `x = 3119`
    - `y = 64`
    - `z = -3239`
    成功落地。
  - 执行第一跳 `city_jigsaw_solve(apply_now=true)`：
    - `parent = start_g_market_03`
    - `parent_connector_id = jigsaw_east_0_1_7`
    - `child = minecraft:village/plains/streets/straight_03`
    - 成功落地，child placement：
      - `node_id = vjigsaw_start_g_market_03_child`
      - `x = 3112`
      - `y = 63`
      - `z = -3225`
      - `rotation = 270`
    - 调试产物：
      - `run/saves/新的世界 (2)/terra_script/cities/city_3120_-3200/groups/g_market_03/jigsaw_solver_debug/20260410_174103_756/trace.json`
  - 执行第二跳 `city_jigsaw_solve(apply_now=true)`：
    - `parent = vjigsaw_start_g_market_03_child`
    - `parent_connector_id = jigsaw_north_29_1_10`
    - `child = minecraft:village/plains/houses/plains_accessory_1`
    - 成功落地，grandchild placement：
      - `node_id = vjigsaw_vjigsaw_start_g_market_03_child_child`
      - `x = 3122`
      - `y = 63`
      - `z = -3255`
      - `rotation = 180`
    - 调试产物：
      - `run/saves/新的世界 (2)/terra_script/cities/city_3120_-3200/groups/g_market_03/jigsaw_solver_debug/20260410_174201_579/trace.json`
- 已确认的验收结论：
  - `start -> child -> child` 三段都已真实落地，且每一段都经过：
    - 建造区边界判断
    - runtime jigsaw 命中
    - vanilla piece 生成
    - terrain preparation
    - 执行层落地
  - 本轮选择的城市设计思路是：
    - start 先以 market 主厅站住前场
    - 第一跳接 streets 主线，保持主体骨架向内推进
    - 第二跳从 street 的 north 口挂一段 accessory house，作为主线旁的附属节点
  - 第一跳与第二跳的 `06_apply_result` 都为 `completed`。
- 本轮新暴露的问题：
  - `validated_nodes` 里出现了重复的 `start_g_market_03` 记录，说明 start 重提交流程还存在重复入树问题。
  - `child` 和 `grandchild` 虽然都成功入树并落地，但当前 `active_node = null`、`node_queue = null`，说明“成功 child 后自动展开下一层 ready 节点”这条会话主链仍未完全恢复。
  - `child/grandchild` 当前写回会话树的 `template_pool_id = village_misc`，`candidate_template_ids` 也落成了过宽的大集合，并没有收束到各自 connector 的精确 pool；这说明下一层推荐候选的系统元数据仍不够准。
- 当前还剩问题：
  - 真实三段落地验收已通过，但“自动连续决策主链”还未完全通过验收，下一步应继续修：
    - 重复 start 入树
    - child/grandchild 后的下一层 ready 节点自动展开
    - 过宽的 `template_pool_id / candidate_template_ids`

## 2026-04-10 18:40:00
- 继续收口 `C8` 会话续链主链与 `jigsaw_solver_debug` 预览标签问题。
- 已做实现（实现仓）：
  - `src/main/java/com/user/terra_script/world/city/stage/c8/CityC8Stages.java`
    - `submitNodeDecision(...)` 现在会先按 `node_id` 去重，避免重复 start / placement 继续累加
    - `validatePlacement(...)` 会跳过“与自身同 node_id 的旧 placement”碰撞
    - solved child 入树后，`template_pool_id` 改为优先使用 incoming runtime pool
    - 下一层 `candidate_template_ids` 改为以 runtime pool 为主、catalog 为兜底，并对 namespaced runtime pool 增加路径级收束
    - 若 runtime pool 无法展开成员或 runtime/catalog pool 冲突，会把问题显式记到 `node_debug`
  - `src/main/java/com/user/terra_script/world/city/stage/c8/CityVanillaJigsawAdapterService.java`
    - `solve_result.placement.incoming_pool_refs` 现在会记录本次 child 实际接入的 runtime parent pool
    - `solve_result.placement.outgoing_connectors` 现在会额外带：
      - `catalog_pool_refs`
      - `pool_truth_source`
      - `pool_mismatch`
      - `mismatch_detail`
    - 当 runtime pool 与 catalog pool 不一致时，继续按 runtime 走，但不再静默吞掉差异
  - `src/main/java/com/user/terra_script/world/city/stage/c8/CityJigsawSolverPreviewExporter.java`
    - `jigsaw_solver_debug` 预览矩形与关键 marker 标签改为外置错位、带引线、半透明底
    - `*.legend.json` 增加：
      - `label_layout = external_staggered`
      - `label_opacity = 176`
      - `label_scope = rectangles_and_key_markers`
  - `src/test/java/com/user/terra_script/world/city/stage/c8/CityC8StagesTest.java`
    - 新增/更新：
      - start 去重
      - runtime pool 优先
      - mismatch 留痕
  - `src/test/java/com/user/terra_script/world/city/stage/c8/CityJigsawSolverPreviewExporterTest.java`
    - 新增 legend 元数据断言
- 已做验证：
  - 编译：
    - `./gradlew.bat compileJava compileTestJava`
  - 定向测试：
    - `./gradlew.bat test --tests "com.user.terra_script.world.city.stage.c8.CityC8StagesTest" --tests "com.user.terra_script.world.city.stage.c8.CityJigsawSolverPreviewExporterTest"`
  - 真机回归（最新编译 client 重启后）：
    - 重新进入 `新的世界 (2)`，`GET /workflow/status` 返回 `200`
    - 以受控 plains fixture 重跑：
      - `city_group_surface_clear`
      - `city_c8_submit(apply_now=true)`
      - `city_jigsaw_solve(start -> straight_03, apply_now=true)`
      - `city_jigsaw_solve(straight_03 -> plains_accessory_1, apply_now=true)`
    - 当前已确认：
      - `start` 不再重复入树
      - `child` 会写回 `template_pool_id = minecraft:village/plains/streets`
      - `child` 的下一层 `houses / streets` 候选已按 runtime pool 收束，不再回成 `village_misc`
      - `grandchild` 的 `solve_result.placement.outgoing_connectors` 已出现 runtime-vs-catalog mismatch：
        - runtime: `minecraft:village/plains/streets`
        - catalog: `village_misc`
      - 最新 active node 已继续推进到：
        - `vjigsaw_vjigsaw_start_g_market_03_child_child_child_1`
      - 最新预览 legend 已包含：
        - `label_layout = external_staggered`
        - `label_opacity = 176`
        - `label_scope = rectangles_and_key_markers`
- 当前还剩问题：
  - `jigsaw_solver_debug` 预览虽然已经从“完全叠死”改成可读，但密集步骤顶部仍有局部拥挤，后续如果还要继续提可读性，可以再细化 marker 取舍或锚点策略
  - 当前新鲜自动规划的 `g_market_03` 仍偏向 `taiga` start 且首跳失败，这一项仍属于上游规划/数据不足，不在本轮继续处理
