# 用户本次需求摘要
当前 start 结构仍然出现“只落下一半”的现象，需要补足日志与调试证据，便于后续直接定位问题层。

# 用户特别强调的约束
- 希望先把日志/证据补全，再继续排查。
- 目标是能直接查清 start 半截结构，而不是先盲改逻辑。

# 不做的事情
- 本轮不直接修复 start 半截结构的根因。
- 不主动调整 start 放置语义、旋转规则或模板真值来源。
- 不扩到全部预览系统，只聚焦与 `city_c8_submit` 落地排查直接相关的证据。

# 当前决定的工作边界
- 实现仓补 `city_c8_submit` 的 debug trace、预览与世界快照统计。
- 文档仓只同步 `city_c8_submit` 调试入口和验收口径变化。
