# Start 选点修复与 Jigsaw 分流需求

## 用户本次需求摘要

用户要求开始修复 start 选点问题，并明确当前求解器只处理水平 jigsaw；垂直 jigsaw 要单独走另一种求解器，不再与当前 solver 混用。

## 用户特别强调的约束

- 本轮优先修主链 start 选点，不直接实现垂直 jigsaw solver。
- 当前 solver 只求解水平 jigsaw，`up/down` 需要单独分流。
- start 选点应围绕 connector 来决定，而不是继续只按当前 anchor 固化落点。
- runtime 模板真值优先；若主链一时拿不到 runtime 模板，要显式回退并保留调试证据。

## 不做的事情

- 不在本轮实现真正的垂直拼接求解逻辑。
- 不把 `up/down` 再混进当前水平 solver 的评分或求解链。
- 不做全区重选 start 的激进重构；本轮只允许 anchor 周围局部挪位。
- 不静默回退到旧 catalog heuristics。

## 当前决定的工作边界

- 主要修改实现仓库 `E:\Mod_Dev\StructureBinder` 中的 C8 主链、独立 solver 路由、测试与 `dev_docs`。
- 同步更新文档仓库 `E:\Mod_Dev\designer_territoryMod` 中与主模块 Jigsaw Solver、验收入口相关的说明。
- `C7` 继续负责模板选择与 seed hint，`C8` 接手 start 最终落点和朝向决定。
