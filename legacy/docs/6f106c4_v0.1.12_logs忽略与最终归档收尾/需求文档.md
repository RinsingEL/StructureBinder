# 剩余归档与忽略规则收尾需求

## 用户本次需求摘要

将 `logs/` 写入 `.gitignore`，并把剩余未提交内容一起提交。

## 用户特别强调的约束

- `logs/` 不再作为未跟踪目录悬挂在工作区。
- 剩余内容需要真正提交，而不是继续留在工作区。
- 继续遵守中文提交与 `dev_docs` 归档规则。

## 不做的事情

- 不提交 `logs/` 目录本身。
- 不再额外扩展业务代码改动。
- 不把无关运行产物带入提交。

## 当前决定的工作边界

- 聚焦 `.gitignore` 与 `dev_docs` 最后一组归档 rename。
- 本轮允许补充当前清理任务的三份文档。
